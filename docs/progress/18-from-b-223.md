# Build progress — part 18

_Detail entries, in build order. The index over every part is [`../PROGRESS.md`](../PROGRESS.md)._

## Continued — in build order

### B-223 — A report's month is complete when it is complete at every facility the figures come from

`a2b54f1`

**What it built.** D-109 made every report default to the last **complete** calendar month, and `reportRange` took a `timeZone` to reckon that in — but **no caller could supply one**. All eleven call sites (seven screens and four CSV routes) take an actor, not a site, so every one of them fell to the UTC default. For the hours between UTC midnight and facility-local midnight on the 1st, a report opened on a month that had not ended in Texas: up to five hours, more in Alaska and Hawaii.

`reportRange` now takes `timeZones` — the zones of every facility in scope — and reckons the month against the **westernmost**, which is simply the earliest local date among them. `reportRangeForActor(actor, params, options)` in `lib/admin/reports.ts` is the one function that knows what that list is, and all eleven call sites go through it.

**What it decided.**

- **Option (a) from the row: complete everywhere, not "pass a timezone".** For a multi-facility roll-up there is no single right zone, and picking the operator's own would make two staff reading the same URL see different months. The figures come from all of those sites, so the window closes when the last of them closes it. That is correct by construction rather than by choosing well.
- **No offset arithmetic.** "Westernmost" sounds like it needs UTC offsets and DST handling; it does not. `businessDateFor(now, zone)` already answers "what day is it there", and the **minimum** of those dates is the one that has advanced least — which is the westernmost, by definition, without a single offset computed. Ordering does not matter and DST is the zone database's problem, not this function's.
- **Option (b) was not taken and (c) was not needed.** Giving the report pages the facility switcher is a bigger change that would make the all-facilities case fall back to (a) anyway, so (a) is the part that has to exist either way. And with (a) built there is nothing left to disclaim on screen.
- **`timeZone: string` was replaced rather than kept alongside `timeZones`.** Two ways to say the same thing, one of which is wrong for the multi-facility case that is the whole point, is how the next page picks the wrong one.
- **The extra query was accepted.** `reportRangeForActor` reads `reportableFacilities` again — the report itself calls it too. A small indexed select against threading the list through every report signature to save it, which buys less than it complicates.

**One runnable check.** Two, and the second is the one that matters more.

`tests/report-range.test.ts` gains the multi-zone cases: at 00:32 UTC on 1 September a London/New York/Chicago/Honolulu portfolio reports **July**, because August has not ended at every site; an all-London portfolio reports August, because for it, it has; the answer does not depend on the order of the list; and mid-month every zone agrees, so the rule costs nothing for the other twenty-nine days.

And a **guard**: no file under `apps/web/app` may call `reportRange` directly. The defect here was never that the function reckoned badly — it was that no caller could tell it where the facilities were, so all eleven took the default and nobody noticed for a month. The failure mode is a NEW file, which no existing test would cover, so it is a grep over the app directory rather than a type. **Verified by pointing one page back at `reportRange` and watching it fail**, naming the file.

**What it left behind.**

- **`/admin/access` and `/admin/impersonation` changed too, and deliberately.** Both use the `rolling-30-days` window, which is anchored on the same `today`, so their window now also closes when it closes everywhere. That is consistent rather than incidental — but it is a behaviour change on two screens the row named only in passing.
- **No e2e assertion was pinned to a fake clock**, as the row instructed. Pinning is what let the original defect survive until a 19:32 run.
- **The management pack still does not go through here**, and should not: it has one facility by construction and reckons its own month in that facility's zone.
- **Nothing stops a report from being written that has no actor.** The guard covers `apps/web/app`; a range computed in a job or a script would not be caught, and none exists today.

---

### B-222 — The revenue report's figures sat in rows with no header, and the fix had to change what the report looks like

`0da0eff`

**What it built.** `RowPair` attached each facility's name with one `<th scope="rowgroup" rowSpan={2}>`. B-216 had already established that no screen reader implements `rowgroup`, and this instance was worse than the aging table's: `RowPair` had **no per-row header at all** to fold the name into, because each row's identity — "Billed", "Coll." — lived in a `<span>` *inside* its first money cell. A row describing itself from within one of its own data cells. So all fourteen of a facility's figures sat in rows with nothing naming them.

Each of the two rows now carries its own `<th scope="row">`. The facility name is printed on the first and visually hidden on the second, so the column still reads as one facility with two lines under it, and the half-labels moved **out of the money cells** and into the headers.

**What it decided.**

- **B-216's remedy was not transferable, and the alternatives were each wrong for a nameable reason.** `scope="row"` on the spanning cell rests on rowspan semantics nobody here has watched an AT resolve. Promoting a money `<td>` to a `<th>` makes a figure into a header. An sr-only `<th>` added to the second row breaks the column count the `rowSpan` balances. What was left was the design call the row named: give the label column real content, which means the report looks different.
- **`headers`/`id` across all fourteen cells was refused**, as the row instructed. It is the no-visual-change option, and it would have replaced the AT-behaviour claim B-216 just removed with a second one this repo equally cannot watch. Trading an unwatchable claim for another unwatchable claim is not progress.
- **The comment claims structure and nothing else.** Every figure sits in a row whose header names the facility and which half of the pair it is. **No announcement is asserted and none was observed** — the standard B-216 set, and the sentence B-222 said not to re-introduce.
- **`text-transform: uppercase` is a rendering choice, so the test does not read the rendered string.** The first version of the assertion used `allInnerTexts()` and failed against correct markup, because `innerText` returns what is painted — "BILLED" — while the DOM and the accessible name carry "Billed". An assertion on the painted string is a check on a CSS declaration. It reads `allTextContents()` now.

**One runnable check.** `e2e/admin-reports.spec.ts`, scoped to this table by its caption because the page carries other tables with row headers: every `Billed` row header has a `Coll.` row header naming the same facility, which is the association `rowgroup` was failing to make. **Verified by replacing the second row's `<th>` with a `<td>` and watching it fail** (`a Coll. row header for every Billed one`), then restoring. The route also gained an axe assertion; it had none of its own before, only the route-loop scan.

**What it left behind.**

- **`scope="rowgroup"` is now gone from the whole codebase**, not just from one of its two users. The accessibility page's B-216 entry named this instance as deliberately unfixed; its B-222 entry now closes that loop, so the two are read together.
- **No screen reader has been run**, here as everywhere else in this repo. What changed is that the markup no longer depends on a scope value nothing implements.
- **`LAST_REVIEWED` does not move.** `/admin/reports/revenue` is behind `reports:financial`, no public or portal route is touched, and no claim on the accessibility page was re-checked against the build. The structurally frozen review date is **B-250**'s row.
- **The visual change is not reviewed by anyone.** "Billed" and "Coll." now sit under the facility name in the label column instead of beside the first figure of each row. That is the design call B-222 authorised, but nobody has looked at the result on a real report.

---

### B-253 — The ordinary way to add a migration connected to shared cloud infrastructure and offered to drop it

`e16bac9`

**What it built.** `db:migrate` was `dotenv -e .env.local -- prisma migrate dev`, and `.env.local` is the **Neon dev branch**. So the documented way to author a migration connected to shared cloud infrastructure — and because that branch has **none of the 107 migrations applied**, `migrate dev` did not offer to add one. It announced *"We need to reset the `public` schema at ep-holy-block-axpgn0o0.c-4.us-east-2.aws.neon.tech"* and offered `migrate reset`. One keystroke, at a prompt raised by a command whose stated purpose is "add a migration".

Four script changes. `db:migrate` now runs `dotenv -e .env.test -e .env.local`, so it authors against local Postgres — the **resettable** thing, and the one `db:migrate:e2e` already keeps in sync. `db:migrate:cloud` is new and uses `migrate deploy`, which only applies pending migrations and cannot drop anything. `db:migrate:all` runs test → e2e → cloud. `db:status` reports **both** databases and **exits non-zero if either is behind**.

**What it decided.**

- **The scripts were built rather than the claim corrected.** The global `CLAUDE.md` says "`db:migrate:all` and `db:status` exist so that cannot go unnoticed" — and neither was in `package.json`. Two ways to make that true: delete the sentence, or build what it describes. The sentence describes the right safety net, and a session reading it would reasonably stop looking for the hazard, so the net is what was missing.
- **`db:status` deliberately exits 1 when either database is behind, and does not stop at the first one.** `cmd-a; cmd-b` would have masked the exit code, which is the specific failure the conventions call out. It captures both and re-exits, so it reports the full picture *and* fails.
- **Nothing was applied to the Neon branch.** `db:migrate:cloud` would bring it up to date in one command and `migrate deploy` cannot drop anything — but it is remote infrastructure and running it is the owner's call, not a side effect of a backlog item. It is one command when they want it.

**One runnable check.** Both new scripts were run. `db:status` prints `storage_test … Database schema is up to date!` then `neondb … Following migrations have not yet been applied` and exits **1**. `db:migrate --create-only` now reports `Datasource "db": … "storage_test" … at "localhost:5432"` and creates the migration instead of proposing a reset — verified, and the probe migration deleted.

**What it left behind.**

- **The Neon dev branch is still empty.** `npm run dev`, `db:seed`, `db:seed:demo` and `db:studio` all point at it, so any of them against it today will fail or find nothing. `npm run db:migrate:cloud` fixes it and was deliberately not run.
- **`db:generate` still reads `.env.local`.** Harmless — `prisma generate` needs the schema file, not a reachable database — but it means the one remaining unqualified `.env.local` in the migrate path is the one that cannot do damage.
- **No guard stops a future script from pointing a destructive command at the cloud.** The convention is now written down in the project `CLAUDE.md` and enforced by nothing.

---

### B-252 — Two unit tests that failed permanently from 12:00 CDT today, and only one of them was a defect

`1bf0b50`

**What it built.** `tests/portal-dashboard.test.ts:190` hardcoded `const expiresAt = new Date(Date.UTC(2026, 8, 1, 17, 0))` and handed it to a `reservation.create`. `reservation` carries a `reservation_expires_after_creation` check constraint, and that compares `expiresAt` against the row's own `createdAt` — the **database's** clock. The literal was in the future when it was written and stopped being so at 12:00 CDT on 2026-09-01, at which point every row the test wrote violated the constraint. Not intermittently: permanently, from that instant on.

`expiresAt` is now `Date.now() + 14 days`, and the per-test teardown moved from the end of the test body into an `afterEach`.

**What it decided.**

- **The clock was not pinned harder.** The test is already careful about time — it passes an explicit `new Date(Date.UTC(2026, 7, 1))` as "now" to `portalDashboardForTenant` — and that is exactly what hid this: the assertion's clock is pinned and the constraint's is not, so the two agreed right up until the wall clock walked past the literal. A fixture row that must be live when it is written has to be dated from the clock that will judge it. The pinned "now" in the assertion stays, because the *assertion* is about the dashboard's reckoning and is correct.
- **The second failure was not a second defect.** Every test in this file reuses the one `unitId` from `beforeAll`, and `Lease.unitId` is unique, so each test has to leave the unit free for the next. That was happening at the end of each test *body*, so when the transfer-hold test threw at `:191` it never deleted its lease, and the next test failed `Unique constraint failed on the fields: (unitId)` at its own `lease.create`. One defect was being reported as two, and the second report pointed at innocent code. Teardown that only runs when the body succeeds is teardown that abandons its post exactly when it is needed.
- **`afterEach` deletes by `facilityId`, not by captured id.** A test that dies before it can name what it made still gets cleaned up.

**One runnable check.** The suite itself: 9 passed, run twice, which is this repo's rule for anything touching shared state. The failing state is trivially reproducible by restoring the literal.

**What it left behind.**

- **No sweep for other date literals in fixtures.** This one was found because it detonated. `grep` for `Date.UTC(2026` across `tests/` returns many, and most are assertions about reckoning — which are correct and must stay literal. The dangerous subset is narrow: a literal handed to a WRITE whose table has a constraint against the real clock. Nothing enumerates that subset today.
- **B-253 is still open** — `db:migrate` still points at the Neon dev branch and still offers to reset it.

---

### B-219 — The document store keeps its "newest first" promise for two documents written in the same instant

`32d9e30`

**What it built.** `documentsFor` orders a subject's documents `createdAt desc`, and `Document.createdAt` was `@default(now())` — which Prisma fills **client-side, at millisecond precision**, and sends with the INSERT. The column's `DEFAULT` had been in the migration since B-023 and had never once been used. Two documents written back to back therefore shared an instant and the sort between them was a coin flip.

`Document.createdAt` is now `@default(dbgenerated("clock_timestamp()"))`, so Prisma omits the column and Postgres fills it. Same change on **`Notice.createdAt`** and **`Message.createdAt`**, which the row asked to be checked rather than assumed: a lease's notices are ordered newest-first and that list is the lien file, and the tenant profile's communication history is ordered the same way and capped at 20, so a tie there could reorder the page *and* change which row falls off the end of it. Two migrations, because the first was already applied by the time the other two were found and editing an applied migration desyncs its checksum.

**What it decided.**

- **`clock_timestamp()`, not the `CURRENT_TIMESTAMP` the row prescribed.** `CURRENT_TIMESTAMP` is transaction-START time and is identical for every row written inside one `$transaction`, so it would have narrowed the tie rather than removed it — and `storeGeneratedDocument` takes a transaction client precisely so callers can write inside one. `clock_timestamp()` reads the real clock per statement at microsecond resolution, which is what makes ties vanish rather than needing a tie-break. The row's stated *intent* — "makes ties vanish rather than needing a tie-break" — is met by `clock_timestamp()` and not by its own suggestion. No caller writes two documents in one transaction today (notices and auctions write one each; the lease loop runs outside a transaction over distinct subjects), so this is the difference between a fix that is correct and one that happens to be correct.
- **No cuid tie-break**, as the row instructed. cuid v2 is deliberately unsortable and relying on v1's timestamp prefix is the kind of cleverness this repo decodes at 3am.
- **`AuditLog` was checked and deliberately left alone.** `findAuditEntries` already orders `[{ occurredAt: 'desc' }, { id: 'desc' }]`, so a tie resolves deterministically rather than at random. Chronology within one instant is still unknowable there, but nothing reads differently between two runs — and non-determinism is the defect this row is about. The other ~57 `@default(now())` columns were not swept: the three named here are the ones that are both sorted on and where a tie costs something.

**One runnable check.** `tests/documents-db.test.ts`, "orders documents written back to back, unstamped": five documents stored in a loop with nothing stamped, asserting both the order and that the five stored instants are **distinct** — read back as `::text` through `$queryRaw`, because the distinction lives in the microseconds and a JS `Date` truncates to milliseconds, so `createdAt.getTime()` would report them identical even when the stored values differ. **Verified against a real failing run:** reverting the schema to `@default(now())` and regenerating makes it fail 3 runs out of 3; restoring makes it pass twice in a row. The pre-existing hour-apart case was kept and its comment corrected — it asserts the ordinary promise, this one asserts the back-to-back case.

**What it left behind.**

- **Two pre-existing unit failures, both in `tests/portal-dashboard.test.ts`, neither reachable from this diff** — reproduced identically with the diff stashed. The first hardcodes `expiresAt = Date.UTC(2026, 8, 1, 17, 0)` and every row it writes now violates `reservation_expires_after_creation`, because that instant passed at 12:00 CDT today; it will fail permanently from here. The second fails `Unique constraint failed on (unitId)` at `lease.create`. **Raised as B-252.** Full unit suite otherwise green: 3919 passed, 8 skipped, 2 failed, reconciled to 3929.
- **`db:migrate` points at the Neon cloud dev branch and proposes to RESET it.** Running it to author this migration produced "We need to reset the `public` schema at ep-holy-block…neon.tech". Nothing was reset and nothing was created; both migrations here were written by hand and applied with `db:migrate:test` and `db:migrate:e2e`. `CLAUDE.md` says `db:migrate:all` and `db:status` "exist so that cannot go unnoticed" — **neither script is in `package.json`**. Raised as B-253.
- **The accessibility statement was not re-read and `LAST_REVIEWED` does not move.** Nothing here is customer-facing: this is a database default, and `documentsFor` still has no production caller. It becomes customer-visible when a lease's document list is rendered.
- **Nothing sweeps the remaining `@default(now())` columns.** If a future screen sorts on one of them and ties matter, this is the fix and this is the test to copy.

---

### B-216 — The aging report attached its facility name with `scope="rowgroup"`, which no screen reader implements

`74bc47e`

**What it built.** `ArAgingSplitTable` gave every facility's three rows — being chased, halted, total — one `<th scope="rowgroup" rowSpan={3}>` carrying the facility name. `rowgroup` is in the HTML spec and is implemented by none of NVDA, JAWS or VoiceOver, so each row of figures had no header naming whose money it was.

The facility now rides in the row header each row already had. `SplitCells` takes a `group` prop and renders it visually hidden ahead of the label, so the `<th scope="row">` reads "Cedar Park, Halted" rather than "Halted"; the spanning cell is a plain `<td>`, there to be seen. Same for the `All facilities` footer. Rendered on `/admin/reports` and `/admin/reports/delinquency` — one component, so both screens are fixed together, which is the point B-207 made when it merged them.

**What it decided.**

- **The comment that asserted an announcement was deleted, not corrected.** It claimed the halted row announces as "Cedar Park, Halted, 61–90 days". Nobody had heard that, and with `scope="rowgroup"` it was not structurally true either. What replaces it claims structure and no more: every figure has a row header, in its own row, naming the facility and which half of the split it is. **No announcement is asserted and none was observed.** The same correction went into `e2e/admin.spec.ts`, whose comment described the old markup, and the accessibility page, whose B-207 entry recorded the defect as still open.
- **Visually hidden rather than folded into the visible label.** The name is already on screen once, in the spanning cell beside it; printing it three more times in the "Collections" column would be a worse report to read for the sake of the same association.
- **No claim about rowspan semantics was substituted for the one removed.** `scope="row"` on a spanning cell, and `headers`/`id` across the cells, both rest on AT behaviour nobody here has watched. Putting the name in a header the row genuinely has needs no such claim, which is why it is the fix.

**One runnable check.** `e2e/admin.spec.ts`, beside the existing assertion that the split's two halves tie out to the total: the halted row's `rowheader` is named for the facility, not just "Halted". Reverting the fix is a one-line deletion whose only visible effect is markup nobody looks at. **Verified by deleting that line and watching the assertion fail**, then restoring it.

**A real bug found along the way — raised as B-222, not fixed here.** `/admin/reports/revenue` uses the same `scope="rowgroup"` construct, and **B-216's fix does not transfer**: the aging table already had a per-row `<th scope="row">` to fold the facility into, and `RowPair` has none — each row's identity ("Billed", "Coll.") lives in a `<span>` inside its first money cell, so all fourteen of a facility's figures sit in rows with no header. Every cheap-looking fix is wrong for a nameable reason (rowspan semantics nobody watched; promoting a money cell to a header; an sr-only `<th>` that breaks the column count the `rowSpan` balances), so what is left is a design call — a visible row-label column — which is B-217's shape, not this row's.

**A process defect of my own, corrected in the same session.** While building B-215 I ran `npx prettier --write` on two files. **This repo has no prettier** — no config, no dependency, no format script — so npx fetched one and reformatted both files to its defaults (semicolons, double quotes) against a codebase that uses neither. Lint and typecheck both passed, so nothing caught it. `00c2129` went out with 206 lines of churn in `accessibility/page.tsx` and ~100 in `a11y-own-spec-routes.spec.ts`. Both files were rebuilt from their pre-B-215 versions with only the intended edits re-applied, and that correction is part of this commit. `git diff 00c2129^` on those two paths is now the B-215 change and nothing else. **Do not run prettier in this repo.**

**What it left behind.**

- **B-222**, above. `/admin/reports/revenue` still carries the defect this row removed from the aging table.
- **No screen reader has been run.** That is the honest state and the reason the comments now say so. What changed is that the markup no longer depends on a scope value nothing implements; whether the resulting announcement reads well is unmeasured, here as everywhere else in this repo.
- **`LAST_REVIEWED` does not move** on the accessibility page: both surfaces are behind `reports:financial`, no customer route is touched, and no claim there was re-checked against the build.
- **One pre-existing e2e failure in the verifying sweep, and it is B-220.** `admin-reports.spec.ts:196` ("promo ROI separates the discount given from the discount still owed") failed. Today is the 1st, which is precisely the condition B-220 records: `reportRange` defaults to the current calendar month, so `/admin/reports/promotions` opens on an empty month-to-date window and reports $0.00 against a campaign that discounted all month. **Reproduced on a clean tree with this diff stashed**, so it is not reachable from anything here. Otherwise: `admin.spec.ts`, `admin-reports.spec.ts` and `a11y-own-spec-routes.spec.ts` ran 312 passed / 1 failed, and the unit suite was green end to end — 3916 passed, 8 skipped, reconciled to 3924.

---

### B-220 — Two month-turn defects in the reports section, and a third the fix uncovered

`79aaca9`

**What it built.** Three things, from one root question: whose calendar is a report's default window reckoned in, and is that window one anything happened in?

1. **The management pack no longer offers a month that has not ended.** `previousMonth(new Date())` read `getUTCMonth()`; `periodsFor` lists only periods that have `.ended`, which is facility-local. At `2026-09-01T00:32Z` the default was August, but in `America/Chicago` it was 19:32 on 31 August and August had not ended — so the page rendered a pack for a still-running month under a name claiming all of it (which the comment beside `months` forbids in as many words), and the month being shown appeared in neither the nav nor `aria-current`. It now takes `businessDateFor(new Date(), facility.timezone)`, so the UTC getters read the LOCAL month.
2. **Every report defaults to the last complete calendar month (D-109).** It was the current calendar month, so on the 1st every report opened on a window nothing had happened in yet.
3. **The two activity logs deliberately do not** — see below.

`SwitcherFacility` gained `timezone`, which is what made (1) a one-line change at the call site rather than a new query: a page learns which facility it is on from the switcher, and every month-boundary default on an admin screen is a facility-local question.

**What it decided.**

- **D-109: last complete calendar month, chosen by the owner.** Rejected: rolling 30 days (never aligns to a billing period, so a figure here would not tie out against the pack or the close); keeping the current month and only fixing the empty state (stops the screen *looking* broken and leaves it empty); splitting financial from operational reports (an operator comparing two reports would silently compare two windows).
- **The decision is about REPORTS, and that scope was settled by a failing test rather than assumed.** `/admin/access` ("Gate activity") and `/admin/impersonation` ("Support sessions") share the parser and are not reports. Applying the report default to them was **a worse defect than the one D-109 fixed**: an owner opening the support-session log to see who is in a tenant's account *right now* was shown a window that ended before the session started. `e2e/impersonation.spec.ts:148` failed on exactly that. Both now default to a rolling 30 days ending today. This is not the split that was rejected — that objection was about two reports being compared, and a gate log is never read beside a revenue report nor required to tie out to a billing period.
- **`reportRange` took an options object rather than a fourth positional argument.** `(params, now, timeZone, window)` is four positionals, two of them stringly-typed, which is what somebody decodes at 3am. Only the test file passed the second argument, so the change cost nothing at the eleven call sites.
- **The demo redemption is dated, not left to `now()`.** `promoRoiReport` filters on `PromoRedemption.createdAt`, and the seed never set it — so the fixture was stamped *whenever the seed last ran*, and `createdAt:` appears exactly once in that ~1200-line file. Under D-109 a seed-time stamp lands in the CURRENT month and falls outside the window every report opens on, in CI as much as locally. It is now the 15th of the last complete month.
- **Neither e2e assertion was pinned to a fake clock**, per the row's own instruction. Pinning hides both defects, which is how they survived until a 19:32 run.

**One runnable check.** `tests/report-range.test.ts` gains four: the new default; that the 1st gets a whole month rather than a zero-day window; that the month is reckoned in the timezone given (UTC and `America/Chicago` return August and July for the same instant, which is defect 1's shape one level down); and that an activity log's window ends today.

**A real bug found along the way — raised as B-223.** `reportRange` now takes a `timeZone`, and **no report page can supply one**: all seven call `getAdminActor()` and span every facility the actor may read; none calls `resolveSelectedFacility`. So the default is UTC, and for the hours between UTC midnight and facility-local midnight on the 1st a report opens on a month still running in Texas. Strictly smaller than the defect it replaces — a window zero days wide, every 1st, all day — and the same shape as defect 1, which is why it is a row rather than a shrug. The row names three candidate fixes and recommends against simply "passing a timezone", since a genuine roll-up has no single right one.

**What it left behind.**

- **B-223**, above.
- **The seed fixture cannot follow a moving window.** It is pinned to the last complete month *at seed time*, so a database seeded in September and run against in October is out of range again — the same stale-seed class as the comms catalog already documented in CLAUDE.md, and it fails the same way, on the `Discount given` column header, because a report with no rows renders no table at all. Re-stamping it in `e2e/global-setup.ts` is the upgrade and was not built: that file is for genuinely disposable state, and this has not bitten twice yet.
- **B-220's row records the symptom slightly wrong** and the correction is worth keeping: it says `/admin/reports/promotions` showed **$0.00**. It did not — with no rows in range the page renders "No promotions were redeemed in this range" and no table, so the assertion that failed is the `Discount given` **column header** at `admin-reports.spec.ts:203`, not the money comparison below it. Same cause, different tell.
- **`LAST_REVIEWED` does not move** on the accessibility page: every file touched is under `/admin`, and no customer-facing surface uses `reportRange` — a portal statement goes through `monthBounds` on the lease's facility. Verified by grep, not assumed.
- **Verification.** Unit suite green end to end: 3920 passed, 8 skipped, reconciled to 3928. **Full `desktop-chrome` e2e sweep green: 653 passed, 5 skipped, reconciled to the 658 announced.** That includes `admin-reports.spec.ts:628` (the pack) and `:196` (promo ROI), the two assertions this row exists to make pass, neither of them clock-pinned. Lint, typecheck and the schema-drift check clean — no model changed, only a `select`.

---

### B-217 — The leases a counter staffer opens on a phone are cards now, and every scroll region that stays says it scrolls

`67c2c9c`

**What it built.** Two things, which are the two halves of the call B-199 explicitly left open ("whether the answer is a scroll wrapper or a stacked layout on narrow viewports is a UX call, not a mechanical one").

1. **The tenant profile's leases table renders as a card list below `sm`.** Facility and unit as an `<h3>`, status / total balance / rate / started as a `<dl>` of labelled pairs, the notice-given form, and the four actions as 44px tap targets. Above `sm` the table, its `overflow-x-auto` wrapper and its `min-w-2xl` floor are byte-for-byte what B-199 shipped. This is the one table the row said matters — what counter staff open away from a desk — and on a 375px phone it was a 672px table scrolled sideways to reach "Move out".
2. **Every `tabIndex={0}` `overflow-x-auto` region in the product draws a visible scrollbar.** One rule in `globals.css`. macOS and iOS use overlay scrollbars that are invisible until something scrolls, so forty scroll regions gave no sign there was anything off to the right; styling `::-webkit-scrollbar` (plus `scrollbar-width`/`scrollbar-color` for Firefox) is what makes the classic always-visible one render, and it still appears only when the content actually overflows.

The reports were deliberately not touched, per the row: eight columns of figures stacked into a card is worse than a scroll region on a desk.

**What it decided.**

- **The affordance is a scrollbar, not a scroll shadow, and that was a correctness choice rather than a taste one.** The usual answer is a `linear-gradient`/`radial-gradient` pair on the wrapper with `background-attachment: local`. axe cannot resolve text contrast over a background image, so a gradient on a wrapper makes **every cell inside it** a `color-contrast` incomplete — across forty regions, enough hand-check noise to bury the short exception list this repo's accessibility statement depends on being readable. The native scrollbar is also the thing a scroll region is *supposed* to have. Thumb `--muted-foreground` on `--muted` track clears 3:1 in both themes (1.4.11).
- **The rule is global rather than per-wrapper**, the same argument the `prefers-reduced-motion` block above it makes: there are forty of these and a forty-first will be added without remembering. Keyed on `[tabindex="0"].overflow-x-auto`, which is this codebase's own established pairing for a keyboard-reachable scroll region and nothing else.
- **Both renderings call the same two components.** `LeaseActions` and `NoticeGiven` are defined once and rendered in the table and in the card. Two copies of a four-link action set is exactly how one of them acquires a fifth link the other never gets — and this table has already grown from two actions to four. The links carry no styling of their own; the caller passes the container and link classes, because inline text link vs. tap target is the only thing that differs.
- **`min-w-` stays load-bearing wherever a wrapper stays**, per the row's closing instruction, and the desk-side test now pins it directly rather than by inference: the table's measured width must be ≥ 672px. A `w-full` table with no floor sizes to exactly the wrapper and crushes seven columns instead of scrolling them, which passes every reachability test and leaves the screen unusable.

**Duplicated forms, checked rather than assumed.** Rendering `NoticeGiven` twice means two `<form>` landmarks with the same `aria-label` in the DOM at once. axe's `landmark-unique` matcher requires `isVisibleToScreenReaders`, and the inactive rendering is `display: none`, so only one is ever a landmark — confirmed by the axe pass on the profile going green on **both** viewports, which is where a duplicate landmark would have shown up. Ids are `useId()`-generated per instance, so nothing collides there either.

**One runnable check — B-199's spec rewritten, not narrowed.** `the leases table on a phone` used to assert that the wrapper was a real scroll region at 375px; that assertion is now false *because the row fixed it*, so it asserts the replacement: the visible Ledger link has no `overflow-x-auto` ancestor, its box is ≥44px tall (2.5.5), the document still does not scroll sideways, and the click still navigates. A second describe, `the leases table on a desk`, keeps the half that is still true at 1280px — wrapper present, floor intact.

**Accessibility.** The page's own entry says it: no sentence changes, but unlike B-199 this row **does** reach two customer surfaces — the `globals.css` rule is not scoped to `/admin`, and `/portal/refer` and `/portal/notifications` both have scroll regions that now show a scrollbar. That is an improvement to an affordance, not a new claim, and the page has no sentence about scrollbars to keep true. `LAST_REVIEWED` does not move.

**Test verification.** Typecheck (including `tsconfig.tests.json`) clean; lint clean (7 pre-existing warnings, unrelated); `prisma migrate diff --exit-code` reports **no difference** — no schema change. Full unit suite **3,920 passed, 8 skipped of 3,928** (231 files passed, 1 skipped), exit 0. `e2e/admin-tenants.spec.ts`: **54 passed**, exit 0, on both projects, including every axe pass on the profile at 375px and 1280px and both new leases-table tests. `e2e/a11y-own-spec-routes.spec.ts` + `e2e/admin.spec.ts` + `e2e/portal.spec.ts`: **668 passed**, exit 0 — the reflow, 200%-zoom and text-spacing loops over every admin route and every own-spec route, which is what checks that a global CSS rule did not move a layout somewhere nobody looked.

**What it left behind.**

- **The other six B-199 tables keep the scroll wrapper, by design.** The row named the tenant profile as the one that matters and the reports as desk work; nothing here re-opened that. If a second table turns out to be phone work, the card pattern is now written twice in this repo (`/admin/units` from B-116, and here) and is a copy rather than a design.
- **No test asserts the scrollbar is visible.** Playwright can measure a scroll region's overflow but not that Chromium painted a classic scrollbar rather than an overlay one, and a screenshot comparison for this would cost more than it protects. The CSS is four rules and its failure mode is cosmetic.
- **The card list is not in `SCANNED_STATES`.** It does not need to be — it is not a state, it is the `mobile-chrome` rendering of a route already scanned on both projects, which is how it got its axe pass here.
- **`profile.tenantId` vs the route's `tenantId`** were used interchangeably in the old cell (ledger and notices used one, move-out and transfer the other). `LeaseActions` uses the route param throughout. They are the same value — the profile is fetched by it — but the inconsistency is gone rather than preserved.

---

### B-229 — A failed nightly job now wakes somebody, and the screen that shows it stopped speaking in cuids

`49b6d76`

**What it built.** The alarm first, then the screen the alarm points at.

1. **A failed or partial `JobRun` raises a high-priority `job_failed` task at that facility.** New catalog type (`packages/core/tasks/catalog.ts`), `entityType: 'JobRun'`, `entityId` the run's id — so `createTask`'s existing (type, entityId, businessDate) uniqueness is exactly one task per run per day, and a re-run from the screen or a catch-up tick does not make a second. `detail` is one sentence in the operator's words: *"Generate invoices did not finish: connection terminated"*, or *"Charge autopay finished, but 2 items did not go through."*
2. **`apps/web/lib/jobs/run.ts` is the single funnel every scheduled job goes through.** `runJob` stays in `packages/core` and stays free of the task queue; `runScheduledJob` wraps it and is what the cron tick (`app/api/cron/route.ts`) and the manual re-run (`lib/admin/billing-runs.ts`) both call. Both callers previously called `runJob` directly and duplicated the handler-adapter closure; there is now one copy of it.
3. **A money job that never runs at all raises its own task.** `raiseStaleMoneyJobTasks`, called at the end of each cron tick: no successful run of `billing.generate-invoices`, `billing.assess-late-fees`, `billing.autopay` or `delinquency.timeline` in 48 hours at a facility. This is the half the failure alarm structurally cannot cover — a job that never starts writes no row, so there is nothing to notice.
4. **The "Across your facilities" roll-up carries the count**, on `/admin/tasks` and on the dashboard, and only when it is not zero: *"41 of 60 vacant · $12,480 owed · 2 nightly job failures"*.
5. **Every `SCHEDULED_JOBS` entry has a `label`**, and `/admin/billing` renders it. Twenty-six labels; the table said `billing.assess-late-fees` and now says *Assess late fees*.
6. **Failed items name their subject.** `recordItem` is handed a lease, tenant, invoice or payment-plan id with no type attached, so the resolver both task screens already use (`resolveTaskSubjects`) is offered every candidate type for each id and the first hit wins — *"Failed · Unit B-114 — Ada Renter — card declined"*, linked to the profile. `PaymentPlan` was added to that resolver, since `billing.autopay` records installments by plan id and it was the one money identifier nothing could name.
7. **"OK 412 / Failed 3" links to the three.** The Failed count is an anchor to the run's item list, the list is opened by default when there are failures, and failures sort first inside it.
8. **`app/admin/[section]/page.tsx` is deleted**, along with `navItemForSection`, which only it used.

**What it decided.**

- **A GLOBAL run raises no task, deliberately.** `Task.facilityId` is required, so a run with no facility has nowhere to go. Fanning one portfolio-wide failure out to every site is a wall of identical cards nobody at any of those sites can act on, and picking one arbitrary facility to tell would be picking the wrong one. Four jobs are global — `access.drain-commands`, `checkout.expire` and the two marketing sweeps — and of those only the gate-command drain is urgent, and it is portfolio infrastructure rather than one site's work. They stay visible on `/admin/billing`, which now reads in plain words. A portfolio-level alert channel is the right home and there is no entity for one yet. An early draft fanned out with a composite `${runId}#${facilityId}` key and was removed: it turned one failure into N tasks and required every reader of a `JobRun` subject to know the key's shape.
- **48 hours, measured from the last success or from `facility.createdAt`.** Long enough that a single missed nightly hour — which the B-043 catch-up drains on the next tick — raises nothing. The `createdAt` fallback is what makes a site that has NEVER had a successful run alarm rather than stay silent forever, which is the misconfigured-new-facility case; a site created an hour ago is correctly quiet.
- **`partial` alarms as well as `failed`.** 799 leases billed and one not is still one tenant's unbilled rent, and the row that says `partial` is the only place it is written down.
- **Only failed items get their subject resolved.** A nightly biller records one OK item per lease; resolving all of them would put every lease at every facility into an `IN` clause to label rows nobody opens. OK items keep their raw id inside a collapsed disclosure; the failures — the three somebody has to go and look at — are what gets a name.
- **The `[section]` placeholder is deleted, reversing B-116's recorded decision to keep it.** B-116 kept the route and `navItemForSection` on the ground that the mechanism is what the next nav item needs before its screen ships. B-229's row instructs otherwise ("delete the route if it is dead, reword it if it is not") and the route was dead in a stronger sense than B-116 knew: every remaining `NAV_ITEMS` href now has its own static folder, and Next.js resolves a static segment before a dynamic one, so the placeholder had been unreachable rather than merely unused. A nav item added without a screen now 404s, which is the honest failure, and `tests/admin-nav.test.ts` asserts every href has a `page.tsx` so that failure is caught in the suite rather than by a staff member.
- **`scheduledJobLabel` falls back to the raw job name** for a `JobRun` row whose job has since been unregistered. That history is still worth reading and there is nothing better left to call it.

**Accessibility.** Admin-only, and the public statement makes no claim about `/admin` — `LAST_REVIEWED` does not move. The row's two criteria are met and checked by the existing axe pass on `/admin/billing`: the failure count and its link are text (1.4.1) — "Failed", the number, and an `sr-only` sentence naming what the anchor goes to, never a colour-coded badge — and the runs table now has headers on **both** axes (1.3.1), the job cell having become a `<th scope="row">` where it was a `<td>`.

**Test verification.** Typecheck (including `tsconfig.tests.json`) clean; lint clean (7 pre-existing warnings, unrelated); `prisma migrate diff --exit-code` against the local test database reports **no difference** — no schema change, since `job_failed` is a catalog entry rather than an enum. Full unit suite **3,987 passed, 8 skipped of 3,995** (238 files passed, 1 skipped), exit 0, including the new `tests/job-failure-alarm-db.test.ts` (12 tests). `e2e/admin-billing-runs.spec.ts` + `e2e/admin-tasks.spec.ts`: **22 passed, 2 skipped**, exit 0 on both projects, including both axe passes.

**One real bug found on the way.** `tests/a11y-scan-coverage.test.ts` failed the moment the `[section]` route was deleted — its exception list still claimed a route that no longer existed. That check exists for exactly the opposite direction (a route with no scan), and it caught the stale exception too. Fixed in the same commit.

**What it left behind.**

- **FR-1's notification bell is still unbuilt.** This row raises a task; it does not build the bell that FR-1 promises. The task queue, the roll-up count and the dashboard line are the surfaces, and they need somebody to open a screen — a smaller ask than "pick that facility and read a status column", but not a push.
- **Global job failures reach no task queue** (above). `/admin/billing` is their only surface.
- **A stale money job raises one task a day until it is fixed.** Deliberate — the alarm should not go quiet while the biller is still stopped — but it means a site left broken for a week accumulates seven cards. Completing them is a note, so this is noise rather than a block.
- **`access.evaluate-suspensions` is not in `MONEY_JOBS`.** It gates gate access on money owed rather than moving money, so its silence is a tenant getting in for longer than they should rather than revenue not billed. Add it if that turns out to be the wrong call.
- **An item id that is not a lease, tenant, invoice or payment plan prints "Unknown record".** Honest, and the fallback the row asked for, but it means a future job recording some other entity gets no name until its type joins `CANDIDATE_TYPES`.

---

### B-230 — The counter takes a card, a walk-in move-in takes cash, and a counter rental stops reporting as a web rental

`8221ee5`

Three defects on one code path, and the fix is the tender step each of them was missing.

**What it built.**

1. **Card at the counter — `/admin/pos/card`.** One screen, two controls, because they are the two things that happen when somebody at the desk says "card": they hand one over, or they say "use the one you have". `startCounterCardPayment` raises a PaymentIntent through the existing `createChargeIntent` and the tenant presents the card in Stripe's own Payment Element; `chargeCardOnFile` charges `Tenant.stripeDefaultPaymentMethodId` off-session. Reached from the POS payment form (choosing **Card** now carries the typed amount into the query string and redirects) and from every lease on the tenant profile that owes something — that screen carried no payment control at all before this.
2. **Cash and check on a walk-in move-in** — `takeCounterMoveInPayment` (`apps/web/lib/checkout/counter-tender.ts`), rendered as a staff-only section on the checkout payment step. `startWalkInMoveInAction` has always handed staff into the same public checkout the website uses, whose only tender was the Payment Element, so there was **no cash or check option on a move-in at all** — US-32's own AC since it was written, and B-039 closed without it.
3. **A counter-started checkout stamps `acquisitionSource: 'walk_in'`.** `StartInput` carries it, `startCheckout` writes it into the session's `data`, and `provisionMoveIn` reads it as `reservationSource ?? sessionSource ?? 'web'`.
4. **`payment.card_on_file_charged`** is a new audit action, distinct from `payment.recorded`.
5. **A real 1.4.3 failure in the shared payment button**, found by the new screen's own axe scan — see *Accessibility*.

**What it decided.**

- **The counter move-in provisions FIRST and takes the money second, and that is the opposite of the card path on purpose.** A ledger entry needs a lease, and at the moment the notes are handed over there is not one — `provisionMoveIn` is what creates it along with the opening charge the payment settles. That ordering is only safe because every refusal is decided before anything is written, which is why `counterTenderRefusal` was split out of `recordCounterPayment`: provisioning and *then* discovering the cash is over this staffer's approval limit is a tenant moved in for free. What is left if the payment record fails after a successful provision is an ordinary balance the POS screen settles in one payment; the other order's failure — money recorded against no lease — is the one nothing can reconcile. The card path stays webhook-first because its money is confirmed asynchronously; cash is settled the moment it is taken.
- **`recordCounterPayment` still refuses a hand-typed `card`, and that refusal is not the bug.** It would post a ledger entry with no money behind it. What was wrong was the product having no other answer; the copy no longer sends anybody to email. US-601's SAQ-A boundary is not negotiable for a staff-operated screen — it is the surface where a written-down number is most likely to end up on a sticky note — so there is no path anywhere in this item by which a PAN reaches this codebase.
- **The tender step is gated on the ACTOR, not on a session flag.** `/checkout` is a public route. "This checkout started at the counter" is a fact about where it began, not authority to settle it with money nobody can see, so the section renders only for a signed-in staffer with `payments:take` at that facility and the server action re-checks both regardless of what was rendered.
- **The amount is not a field on the tender form.** It is `amountDueToday`'s figure — the same one the screen printed and the card path charges. A typed figure would be a move-in settled for whatever a staffer keyed, against a total the renter had just read.
- **A new `counter` payment surface, and `methodsFor`'s ACH branch is now written as a rule about `checkout` rather than as `else`.** No bank debit at a desk: somebody wanting their gate to reopen now is not served by money that clears in four business days, and they would leave believing they had paid. The `else if (achAtCheckoutEnabled)` shape was a rule about one surface expressed as a rule about every surface that is not portal or setup, so adding `counter` to it would have silently handed it bank debit at every facility with ACH at checkout switched on. Both polarities are asserted.
- **`phone` is not a case the POS screen has, and does not need to be.** The backlog row names it. A rental taken over the phone starts from `/admin/leads`, where `holdForLead` already creates a reservation carrying `lead.source ?? 'phone'` — and a reservation's source still wins over the session's stamp in `provisionMoveIn`, which is US-43's own "source carries through reservation → move-in". A source select on the POS screen would have been a second way to answer a question already answered.
- **A second tender on an already-provisioned checkout is refused, by name.** A second press, or a card that cleared while the staffer was counting notes. The message says "do not take payment again" rather than reading like a failure, because a refusal that reads as "nothing happened" is how somebody gets charged twice.
- **`counter-actions.ts` is a separate module from `checkout/actions.ts` for a mechanical reason.** `requireStaffActor` reaches next-auth, whose `env.js` does a bare `next/server` import Vitest cannot resolve — putting the action in `actions.ts` broke `checkout-consent-db.test.ts`, which imports the renter-facing actions to exercise the real consent writes.
- **`Lease.acquisitionChannel` (D-57's marketing axis) is untouched.** A walk-in has no marketing channel to credit, and the schema's own comment on the two columns is what this row applies rather than reopens. `MOVE_SOURCES` membership is validated on the way out of the session's JSON: a source `normalizeSource` does not recognise would report as `unknown` for the life of the lease, which is worse than the honest `web`.
- **Change calculation, gapless receipt numbering (D-22), drawer sessions (B-078), the non-overridable staff attribution on cash (US-32) and the configurable cash ceiling (RBAC-2) are all built and unchanged.** The tender step routes through `recordCounterPayment` precisely so that it cannot become a way around any of them, and the ceiling test asserts a manager takes the same money the counter staffer was refused.

**A real bug found, filed as B-255 and deliberately not fixed here.** **A web move-in's payment never reaches the ledger.** `openingLedger`'s docstring promises both entries — *"what was owed today, and the payment that cleared it"* — and it writes only the charge. The payment side is `postPaymentToLedger`, which runs inside `applyStripeEvent`'s `payment_intent.succeeded` transaction while `provisionMoveIn` runs *after* it commits, so at the moment it looks for a lease there is none and it returns; a redelivery short-circuits on `status === 'succeeded'` before reaching the post, so the at-least-once design gives no second chance either. Every card move-in therefore reads as owing its whole first month, forever. It was invisible until this item because B-230's cash path nets to zero on the same screen — provision, then record — and the card one does not. Not folded in: it needs a backfill across every lease a card move-in ever created, and the arrears reports and the delinquency ladder all read that number.

**Accessibility.** WCAG 2.1 AA. The tender step is a labelled form whose refusals reach the field that caused them (3.3.1 A / 3.3.3 AA) — tendered, check number, or the method select — and the change-due figure is announced from `AdminForm`'s pre-mounted `role="status"` region rather than only re-rendered (4.1.3 AA). What is being charged is stated before the control that charges it on both new screens (3.3.4).

**The item found a genuine 1.4.3 Contrast failure in a customer-facing component.** `/admin/pos/card` reuses `PortalPayment`, so that component got its first axe scan in a state the portal's and checkout's existing scans never reach: `aria-busy` true, while the Stripe script is still loading. The pay button carried `aria-busy:opacity-60`, and dimmed it measures **3.34:1** (#dadada on #747474). The identical button is in `components/checkout/payment-element.tsx`; both existing scans miss it by racing the script, so by the time they run `aria-busy` is false and the button is at full opacity. 1.4.3's exemption for inactive components does not apply, because B-110 made these buttons `aria-busy` rather than `disabled` precisely so they stay focusable and activatable. The opacity is removed from both; nothing is lost, since the busy state is still announced by the pre-mounted "Taking payment" region and still shown by the label changing. `/admin/pos/card` is registered in `SCANNED_BY_OWN_SPEC` with a click-through in `e2e/admin-pos.spec.ts` and a `REACH` entry in `e2e/a11y-own-spec-routes.spec.ts`, so it gets the 320px, 200%-zoom and text-spacing passes too (B-201); `/admin/pos/card/done` joins `ADMIN_SCAN_ROUTES` in the same posture as `/portal/pay/done`. The public statement's log records the finding and `LAST_REVIEWED` does not move — the item's own screens are all staff ones.

**What it left behind.**

- **B-255**, above. The counter tender is correct; the card move-in is not, and always was not.
- **The three outcome states of `/admin/pos/card/done`** (taken, still confirming, declined) are unscanned, the same STATE gap `/portal/pay/done` has and for the same reason — they need a real card payment.
- **No receipt is printed or emailed for a counter card payment.** The done screen is the receipt; the emailed one is B-050's lifecycle notices, which already cover a succeeded `Payment` row however it was raised.
- **`chargeCardOnFile` charges the tenant's default method only.** A tenant with two cards cannot be asked "which one" from the counter — the screen names the one it will charge so nobody is surprised, and choosing between them is B-036's method management.
- **The counter card screen still shows no itemisation of what the balance is made of** — that is B-231's row, which owns "the counter takes payments on a screen that never shows what the tenant owes".

**Test verification.** Typecheck (including `tsconfig.tests.json`) clean; lint clean (7 pre-existing warnings, unrelated); `prisma migrate diff --exit-code` reports **no difference** — no schema change, since the source lives in the checkout session's existing `data` JSON and the audit action is a catalog entry. Full unit suite **4,012 passed, 8 skipped of 4,020** (240 files passed, 1 skipped), exit 0, **run twice** with the same result, including the new `tests/counter-move-in-db.test.ts` (7 tests) and `tests/counter-card-db.test.ts` (10 tests). e2e on `desktop-chrome`: `admin-pos` + `a11y-own-spec-routes` + `portal` **95 passed**, and `admin-pos` + `checkout` + `a11y` **135 passed**, both exit 0 — including the new axe pass on `/admin/pos/card` and the two contrast-affected suites. The full sweep is CI's.

---

### B-231 — The counter can see what the tenant owes, and a former tenant can finally hand over cash

`6751931`

D-110 was answered first, because this row was blocked on a permission policy and not on code.

**What it built.**

1. **`counterPayableLeases`** (`apps/web/lib/admin/pos.ts`) — what this tenant can be given a receipt for at this facility, with each unit's balance, its `daysPastDue`, and whether the lease has ended. Oldest debt first. Gated on `tenants:view` per **D-110(A)**.
2. **`/admin/pos`'s payment form is now `CounterPaymentForm`** (`apps/web/components/admin/counter-payment-form.tsx`). The unit `<option>` labels carry the money (`10x10-006 — $312.40 due`); the aging is stated in words beside them (`$312.40, 41 days past due`); and a **Pay in full — $312.40** button fills the Amount field and leaves it editable, which is `PaymentPlanBuilder`'s even-split control applied to the same problem.
3. **Ended leases with a balance appear in the picker**, visually labelled as a former tenant. This is the half the row existed for: the picker scoped itself to `OCCUPYING_LEASE_STATUSES`, so the AR that `/admin/tenants/former` lists under its own header — *"A read, not a queue"* — was 100% uncollectable inside the product, while `recordCounterPayment` has no status filter and would have taken the money any day.
4. **`/admin/tenants/former` gains a "Take payment" link per row**, landing on POS with the tenant *and* the unit already chosen (`?tenant=…&lease=…`).

**What it decided.**

- **D-110(A): the balance on one account in front of the counter is not a financial report.** It reads under `tenants:view` — the same permission that already showed this same person `lease.balanceCents` on the tenant profile one screen away (`page.tsx:505`). `reports:financial` keeps gating aggregate and portfolio figures. The omission on the payment screen was an accident rather than a policy, and `formerTenantDebts` had already reached the same conclusion in code (it accepts either permission) before the decision existed. What (A) accepts is written on the D-110 row.
- **The aging uses the SAME `daysPastDue` the access gate suspends on** — oldest unpaid **rent** invoice, `kind: 'rent'`, matching `delinquency-gate.ts`. The row asked for the figure so the person taking the cash knows whether to mention the overlock; a second definition that disagreed with the gate would have answered that question wrongly.
- **"Pay in full" is a prefill and never a second way of deciding the amount.** Part payment at a counter is the normal case. The button writes into the field; `takePaymentAction` still reads whatever is in the box, so no branch here can post a figure the staffer did not see.
- **An ended lease at $0.00 stays out of the picker.** A closed lease in the unit list with nothing to pay is a way to misapply a payment, not a feature.
- **A former tenant cannot pay by CARD at the counter, and the option is withheld rather than offered.** `chargeableLease` scopes the card screen to leases that have not ended, so a `card` selection on an ended lease would have redirected to a dead end — the exact failure mode this row was raised about. The method select drops `Card` for a former tenant and the hint says why. Widening the card path to closed leases is a change to the money path, which this row says twice it is not, and it belongs with **D-112**.
- **`recordCounterPayment` is unchanged.** This was a screen fix throughout: the service would have taken every one of these payments before the row was written.

**A pre-existing e2e failure found and fixed.** `admin-move-out.spec.ts:110` ("the notice-date field is named by its label and described by its hint") was **already failing on `main`, on both projects** — verified by stashing this item's changes and re-running it. `page.locator('input[name="noticeGivenAt"]')` is page-wide, and the tenant profile has one of those fields per lease; during the App Router's client transition off the profile both trees are briefly mounted, so the locator resolved to two elements and the test died as a strict-mode violation before asserting anything. Scoped to `form[aria-label="Record the notice date"]`, which only the move-out screen has. Nothing in the product was wrong.

**Accessibility.** WCAG 2.1 AA. The balance and its aging are text beside the control, not colour or position; the "Pay in full" button is a real `<button type="button">` at the 44px target size; the option labels carry the same figures the summary line does, so the picker is usable without reading the paragraph. `/admin/pos` keeps its existing axe pass. **`LAST_REVIEWED` on the public accessibility statement does not move and the statement is unedited — every screen this item touched is a staff one**, and re-read before the commit as the repo requires.

**What it left behind.**

- **No itemisation of what the balance is made of.** The counter sees `$312.40, 41 days past due`, not rent-plus-late-fee-plus-tax. That is the same gap B-230's entry noted, and the portal half of it is **B-232**.
- **The card path for a former tenant**, above — D-112's neighbourhood.
- **The search-result line still reads "no active unit" for a former tenant.** It is true, and it is now followed by a screen that can take their money, so it understates rather than misleads.

**Test verification.** Typecheck (including `tsconfig.tests.json`) clean; lint clean (7 pre-existing warnings, unrelated); no schema change, so no migration and no drift. Full unit suite **4,015 passed, 8 skipped of 4,023** (241 files passed, 1 skipped), exit 0, including the new `tests/counter-payable-leases-db.test.ts` (3 tests). e2e `admin-pos` + `admin-move-out` on `desktop-chrome` and `mobile-chrome`: **40 passed**, exit 0, **run twice** with the same result — the second run against the database the first one had already taken payments in. The full sweep is CI's.

---

### B-232 — The portal says what the balance is for, and what paying it buys

`2f7d524`

The screen that asks a past-due tenant for money was "Balance $487.50 / Paying today $487.50" and nothing else, and the two sentences about gate access on either side of it were both wrong.

**What it built.**

1. **`openItems`** (`packages/core/billing/ledger.ts`) — the entries behind the balance a tenant owes right now: everything after the last point the account was square. Pure, and the lines **sum to the balance by construction** rather than by a second addition that could drift.
2. **`balanceBreakdownFor`** (`apps/web/lib/portal/balance-breakdown.ts`) — those entries as a tenant reads them. Each open charge is expanded into its **invoice line items** (rent for the period, protection premium, tax); a late fee is rewritten from `Late fee (step 1) — 10+ days past due` to **"Late fee, assessed 11 August"**; a payment is "Payment received", not "Payment INV-0007".
3. **`/portal/pay` itemises** in a real `<table>` with column headers, `Balance` and `Paying today` as `<th scope="row">` in the `<tfoot>`, and the office's number on any line a tenant might argue with.
4. **`restoreShortfallCents` / `wouldRestoreAccess`** (`packages/core/access/suspension.ts`) — what still has to be paid before the gate reopens, and whether a given amount does it.
5. **The dashboard banner, the pay screen and the suspension notice all read it.** `/portal/pay` states it beside the amount, and "Pay a different amount" says live what the typed figure does — *"$100.00 will not reopen your gate. $437.50 will"* — from a `role="status"` region that exists before the message.
6. **`/portal/statements` rows carry their closing balance** — "$487.50 owed at month end", "Settled", or "$40.00 in credit at month end" — from one ledger read per tenant and a prefix sum, not an aggregate per month.
7. **The demo seed's unpaid invoices now carry line items**, so the demo shows the itemisation the product actually produces rather than the one-line fallback.

**What it decided.**

- **The open-item window is "since the account was last square", and that is the whole design.** The balance is a running total over an append-only ledger, so there is no stored set of "what you owe" to read — and any itemisation assembled a second way is a second number that can disagree with the total printed above it. Taking the tail after the last zero crossing makes the lines add up **by construction**. The page checks it anyway (`reconciles`) and drops the itemisation rather than showing a bill that does not add up on the screen that then asks for the money.
- **A settled month never appears.** A tenant who paid July and owes August is not shown July: reprinting it is how somebody concludes they are being billed twice. A tenant who has never been square gets their whole history, which is correct — all of it is still owed.
- **The amount that reopens the gate is the tenant's balance across every unit AT THAT FACILITY, less that facility's threshold.** The row named half of this (the threshold); the other half is worse and was not named. An `AccessGrant` is one per tenant × facility and cannot be partially suspended, so `gateDecision` sums every occupying lease (`tenantStates`) — and the banner read one lease's balance. A two-unit tenant was told paying unit A would reopen a gate unit B keeps shut. **This does not re-open D-16**: it makes three surfaces read the rule D-16 already wrote instead of restating its default.
- **What the screen and the message share is the FIGURE, not the string.** The suspension notice is a seeded `MessageTemplate` a facility may edit; a literally shared sentence would mean freezing the template or generating copy from code. Both read `restoreShortfallCents` — the screen directly, the notice through a new **`access.restore_amount`** merge field, registered in `packages/core/comms/merge-fields.ts` so the publishability gate can see it. `balance.total` stays available to template authors and is simply no longer what the shipped copy says.
- **The itemisation comes from invoice LINE ITEMS, not from the ledger's own descriptions.** The ledger row for a rent invoice reads `Invoice INV-0007` — which is the bare total this row was raised about, one indirection lower down. Only charges are expanded: a payment carries the invoice it was allocated to, and expanding one would print the charges again as negatives.
- **`formatCents`, not `formatRate`, on the pay screen's table.** This is a bill, and a column where one row reads "$129" and the next "$20.00" is harder to check than one where every row carries its cents. The prose and the dashboard are untouched.
- **The amount form is still `method="GET"`.** §6.2 wants the portal usable with JavaScript off up to the Payment Element itself. `PayAmountForm` adds a sentence to a form that already worked rather than replacing it with a server action; with JavaScript off the note renders for the server's amount and the form submits exactly as it did.
- **One clause of copy from a finding that stays declined.** The past-due banner now offers "or to ask about splitting it into payments", which describes what the office can already do with D-98's plan builder. **The tenant-initiated request feature remains declined** exactly as in the B-187–B-196 block, and PRD 01 §9 still carries it as an open gap.

**Accessibility.** WCAG 2.1 AA, and three of this row's acceptance criteria were accessibility ones: the itemisation is a real table with column headers (1.3.1); the live consequence region pre-exists its message and carries B-248's 700ms settle so one field edit is one announcement (4.1.3); and a disputable charge's phone number is a link on that charge's line (2.4.4). The new e2e asserts the headers and the status region **by role**, so a regression to `<div>`s fails the suite rather than only the scan. `/portal/pay`, `/portal` and `/portal/statements` keep their existing axe, 320px, 200%-zoom and text-spacing passes. The public statement is re-read and logged; **`LAST_REVIEWED` does not move** — three screens re-checked and one new pattern scanned is not a re-review of the page's claims, and no screen-reader pass was recorded.

**What it left behind.**

- **The tax line is only as itemised as the invoice is.** `generateInvoiceForPeriod` writes one line per tax jurisdiction, so that is what shows; an invoice written without line items (the pre-B-232 demo seed, and anything else that writes the ledger directly) falls back to the ledger's own description for that charge. It reconciles either way.
- **`/portal/statements` still lists the CURRENT month alongside closed ones**, and its closing balance for that month is "as of now" rather than a closed figure. The row's complaint — that statements do not answer "what do I owe today" — is answered on `/portal/pay`, which is where the question is asked.
- **`overlock.required` still says `balance.total`.** It is the same shape of sentence about a physical lock, whose removal is a staff action rather than an automatic threshold — so the gate's restore figure would be the wrong number to quote there. Untouched deliberately, not overlooked.
- **Nothing here disputes a fee.** The line carries the number to call; a tenant-initiated dispute record is not raised by this row.

**Test verification.** Typecheck (including `tsconfig.tests.json`) clean; lint clean (7 pre-existing warnings, unrelated); no schema change, so no migration and no drift. Full unit suite **4,030 passed, 8 skipped of 4,038** (242 files passed, 1 skipped), exit 0 — including the new `tests/portal-balance-breakdown-db.test.ts` (4 tests) and the new `openItems` / `restoreShortfallCents` / `wouldRestoreAccess` cases in `tests/ledger.test.ts` and `tests/access-suspension.test.ts`. **`npm run db:migrate:test` and `db:migrate:e2e` were both re-run** — the comms catalog is seeded state, so a template edit leaves the suite disagreeing with the branch until it is reseeded (B-206). e2e `portal` **142 passed**, then `portal` + `impersonation` **146 passed** against the same already-run database, both exit 0. The full sweep is CI's.

**A real defect the merge-field gate caught, worth recording because it worked.** Adding `access.restore_amount` to the template failed `tests/merge-fields.test.ts` — *"access_suspended uses fields its event cannot supply"* — before anything was sent, because `packages/core/comms/merge-fields.ts` had not been told the event supplies it. That is the guard doing exactly its job: without it the first suspension notice after this change would have thrown in `renderEmail` and been recorded `failed`, which is B-206's failure mode arriving from the other direction.

---

### B-233 — A task can be taken, given back, and filtered to yours

`ab0ef59`

`assignTask` shipped with B-095, permission-checked and unit-tested, and **its only callers were those tests**. Both queues rendered `Assigned to X` / `Unassigned` as static prose for a column nothing in the product could write, so US-41's "my day" was the facility's day: two staff on a Saturday work one undifferentiated list and either cut the same lock twice or both skip it — on the queue that drives the lien clock.

**What it built.**

1. **`TaskAssignment`** (`apps/web/components/admin/task-assignment.tsx`) — the assignee sentence and its one control, as a single component. Unassigned gets **Take this**; a task you hold reads **Assigned to you** and gets **Give back**; a task somebody else holds gets their name and no button. Rendered on `/admin/tasks` and `/admin/delinquency`, replacing the duplicated static line on both.
2. **`assignTaskAction`** (`apps/web/app/admin/tasks/actions.ts`) — one action for both buttons and both queues, the way `completeTaskAction` already is. Revalidates all five task-queue paths from one list, which `completeTaskAction` now shares.
3. **`assignTask` enforces who may hold what** (`apps/web/lib/admin/tasks.ts`) — claim only what nobody holds, give back only what you hold, and never assign to anyone but yourself. The check is the `where` clause of an `updateMany`, not a read followed by a write.
4. **A three-state filter on `/admin/tasks`** — Mine / Unassigned / Everyone as links, `aria-current` on the active one, in a `<nav>` with an accessible name. The heading carries the counts the reader is responsible for: **"Tasks — Cedar Park · 4 yours, 11 unassigned"**.
5. **`TaskRow.assigneeStaffId`** — `assigneeName` says who to a reader; this is what the card compares against the viewer and what the filter groups on. `delinquencyQueue` reads `facilityTasks`, so it inherited it.

**What it decided.**

- **The claim race is settled by the database, not by a read.** Two staff pressing Take this on the same overlock within the same second is the exact failure this row exists to stop, and read-then-update lets both succeed — the loser walks out to cut a lock a colleague is already cutting, which is the outcome the row describes as the bug. `updateMany({ where: { id, assigneeStaffId: expected } })` makes it one winner or none. **The refusal names the holder** — "Cal Counter has this one — ask them before starting it" — because "could not be assigned" sends a person looking for a bug that is really a colleague.
- **The rule lives in `assignTask`, not in the action or the card.** Every caller routes through it, and it is reachable with any task id; a guard in the screen is a guard one POST can walk around.
- **`assignTask` returns a result instead of `void`, and now refuses a non-staff actor** — it had no `actor.kind` narrowing at all, because it took the assignee as a parameter and never read the actor's own id.
- **Everyone is the DEFAULT view, not Mine.** The row's fix text orders the filter "mine first and unassigned second, which is the order a person works" — that is the order of the controls. Defaulting the *view* to Mine would open the lien-clock queue with eleven unassigned notices hidden, which is a worse first screen than fifteen visible ones. The counts in the heading are what make the other two views findable, and they are computed over the unfiltered list for that reason: a count of yours taken from a list already narrowed to yours is just the list's length.
- **Filtered in memory, not in the query.** One facility's open tasks is already the page's working set, and the heading has to name what the reader is *not* looking at.
- **Manager reassignment to a named third person stays out**, as the row says. A card somebody else holds gets no button rather than a button that silently overwrites them.
- **The empty state is view-aware.** "Nothing open right now" was a lie under a filter — a site with eleven unassigned notices said it to anyone who picked Mine. It now says what is empty and links to everyone's.
- **An error summary with no field errors no longer renders an empty `<ul>`** (`components/admin/form.tsx`). A refusal about who holds a task carries its whole meaning in `message`, and the empty list under it was announced as "list, 0 items".

**Accessibility.** WCAG 2.1 AA, per US-41's existing 2.4.6/4.1.3 criterion. Each button's accessible name carries its own card's subject — `Take this: Overlock to apply, Ada Renter — unit 104` — rather than the same bare verb once per card; the claim outcome announces from the page-level `AnnounceRegion` B-170 built, which survives the card it reports on and takes the focus (4.1.3); the filter is a named group with `aria-current` rather than colour alone (1.4.1); and the list's new length is stated in words under it ("Showing 4 tasks assigned to you."), because following one of those links re-renders the list under a heading that did not change. The new e2e runs axe on the claimed state.

**What it left behind.**

- **`/admin/access/queue` still shows its assignee inline (`· Name`) with no control.** It renders a different row shape, not `TaskRow`; the two queues the row named are done.
- **No claim from `/admin/walkthrough` or `/admin/overlocks`** — neither prints an assignee today, so neither had the defect this row is about.
- **Nothing notifies the person whose task was taken**, and nothing shows a facility-wide "who has what" view. Claiming is the 90% case; a roster is a different screen.

**Test verification.** Typecheck (including `tsconfig.tests.json`) clean; lint clean (7 pre-existing warnings, unrelated); no schema change, so no migration and no drift. Full unit suite **4,033 passed, 8 skipped of 4,041** (242 files passed, 1 skipped), exit 0 — including three new `assignTask` cases in `tests/tasks-db.test.ts`: the losing claim is refused and names the winner, giving back what you do not hold changes nothing, assigning to somebody else throws, and a view-only actor is refused. e2e `admin-tasks` **11 passed** then **10 passed, 1 skipped** on an immediate re-run against the same database (the skip is the returned-mail test's own same-day idempotency guard, as designed), and `admin` **265 passed**, all exit 0. The new claim spec reuses B-221's per-worker fixture task and follows B-120 discipline (2): it gives the task back before taking it, so a run that died holding the claim self-heals, and it puts the task back at the end.

---

### B-234 — A held auction surplus gets an alarm, not a screen

`93ea899`

`surplusObligation` has computed both dated obligations a surplus carries since B-062 — the former tenant not yet notified, and the holding period run out — and the only thing reading it was `outstandingSurpluses`, rendered on `/admin/auctions` for the one facility the switcher happened to be on. Nobody opens that screen at a site with no live cases, so a surplus set to `sale + 365 days` sat correctly computed and unread for the whole year of its hold. US-28's own words are that a surplus is a liability with a statutory life, and that quietly retaining one is how a routine auction becomes a class-action-shaped problem. **What was missing was the alarm, not the arithmetic.**

**What it built.**

1. **`auctions.surplus-alarms`** (`apps/web/lib/auctions/surplus-alarms.ts`), per facility at 8am facility-local. The same hour as `leads.follow-up` and for the same reason: this raises off-system work — post a notice, write a cheque, file with the comptroller — and a card created at 2am is one that sat six hours before anybody could act on it.
2. **Two task types** in the catalog. `surplus_notice_due` while `surplusTenantNotifiedAt` is null, raised within days of the sale rather than a year later. `surplus_disposition_due` from `surplusNoticeLeadDays` before `surplusHoldUntil`, at `normal`, **escalated to `high` on the row already standing** once the deadline passes.
3. **`Facility.surplusNoticeLeadDays`** (default 30) with its settings field in the same item, beside the hold it counts back from.
4. **`overdueSurplusCount`** on `taskRollup`, rendered on the `/admin` dashboard roll-up and the `/admin/tasks` roll-up, across facilities, on the same "only when it is not zero" rule B-229 established for job failures.
5. **A held-surplus section in the management pack** (`packages/core/accounting/pack.ts`) — unit, former tenant, amount, deadline, and whether they have been told — appended only when there is something in it.
6. **An `AuctionCase` subject resolver**, so a card reads `Unit 104 — Ada Renter` and links to the tenant record.

**What it decided.**

- **Two task types, not three.** The row asked for a notice alarm, a deadline alarm and an escalating one. The third is not a separate type: the action does not change when a hold runs out — record the disposition — only how long it has been true. That is a priority, and it goes on the row that is already there rather than beside it as a duplicate.
- **Guarded on an open task, not on `createTask`'s per-business-date key.** The idempotency key is per business day, so the `leads.follow-up` shape would raise a fresh card every night for the entire year a hold runs. The `insurance_proof_lapsed` pattern — `findFirst` on an open task of the same type for the same entity — is what stops a queue nobody opens. Escalation is an `update` on that row.
- **Escalation is one-way.** A card a person deliberately set to `high` is not something a later quiet night demotes.
- **Both types are `resolvedByAction`.** A note cannot close either: the surplus stays un-notified, or stays held, whatever anybody types, and a completed card would mean the queue had forgotten a liability rather than that anyone had discharged it. `recordSurplusNotified` and `recordSurplusDisposition` cancel them, and dispositioning cancels the notice card too — a surplus that has been paid out is not one anybody still has to be told about.
- **The pack's surplus section is labelled as read at build time, not as at the month.** Every other figure in that pack is month-scoped and the provenance sentence is the most important line in it. A liability read for a closed month would be wrong on the day the pack is opened, so the section says so in its own words rather than borrowing the pack's.
- **`overdueSurplusCount` counts the escalated priority, not the type.** A `surplus_disposition_due` at `normal` is ordinary work inside the lead window; the roll-up figure is for the ones past the deadline.
- **The durations stay configuration and stay labelled as example configuration** (US-29, D-10). This alarms on whatever the facility is set to and asserts nothing about what any state requires. The lead has a floor of 1 — a lead of zero is the deadline itself, which is the state the row exists to stop arriving unannounced.

**A real bug found on the way.** **`main` was red.** B-233 added an `a11y-state: /admin/tasks | task claimed` comment to `e2e/admin-tasks.spec.ts` without the matching `SCANNED_STATES` entry, so `tests/a11y-scan-coverage.test.ts` failed on a clean checkout (`expected [ '/admin/tasks | task claimed' ] to deeply equal []`). Confirmed pre-existing by stashing this branch. The entry is added here.

**What it left behind.**

- **Nothing sends the surplus notice.** The task says a person must; the letter itself is off-system, as US-28 describes it. Wiring it to the comms catalog is not this row.
- **A surplus with no recorded deadline gets the notice card but never a disposition card.** `surplusDispositionDue` returns false on a null `surplusHoldUntil`, deliberately: there is no date to count back from, and inventing one would be the alarm asserting a deadline nobody set. Such a row still appears in `outstandingSurpluses` and in the pack, where the deadline column reads "Not recorded".
- **The management pack section is per facility**, because the pack is. The cross-facility view of overdue surpluses is the dashboard roll-up.
- **`db:status` exits non-zero against the Neon dev branch** (`P3005`, the schema is not empty and has no migration history). Pre-existing and unrelated to this item — that branch has never been baselined, and baselining shared cloud infrastructure is a deliberate decision, not a side effect of a backlog item. Local and `storage_test` are both up to date and the CI-shaped drift check reports no difference.

**Test verification.** Typecheck (including `tsconfig.tests.json`) clean; lint clean (7 pre-existing warnings, unrelated); `prisma migrate diff` against the local `public` schema — the CI check's shape — reports no difference. Two new pure cases in `tests/auction-readiness.test.ts` for the lead window (it opens on the lead day, overdue still counts as due, a longer lead opens it earlier, a null deadline is never due) and five new database cases in `tests/auctions-db.test.ts`: the notice card is raised the day after the sale carrying the amount and the tenant, a second night raises nothing, the disposition card appears inside the lead window and the same row escalates to `high` past the deadline, dispositioning closes both, and a sale that raised no surplus raises nothing. `tests/auctions-db.test.ts` **55 passed**; `management-pack`, `tasks-db`, `golden-path-2` (which drives the real registry) and `facility-settings-db` **55 passed** together. Full unit suite **4,040 passed, 8 skipped of 4,048** (242 files passed, 1 skipped), exit 0 — against **4,039 passed, 8 skipped, 1 failed** before the `SCANNED_STATES` fix, the one failure being B-233's orphaned state above.

---
