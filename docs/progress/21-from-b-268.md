# Build progress — part 21

_Detail entries, in build order. The index over every part is [`../PROGRESS.md`](../PROGRESS.md)._

## Continued — in build order

### B-268 — the page a Spanish reservation ends on is Spanish, date included (2026-09-07, `5f60cab`)

B-267 translated the reservation form and stopped at its redirect. `/reservations` is where that redirect goes, and it is also where the cancel link in the confirmation email lands — so it is the page that says a unit is held, the page that shows when the hold ends, and the page that releases it. It imported nothing from `@/lib/i18n`.

**What it built.** Twenty-four `res.*` keys and four `err.reservation*`, across the page and its two server actions. `generateMetadata` replaces the static `metadata` export and translates the title — unlike the marketing pages B-262 deliberately left English, because this route already carries `robots: { index: false }` and there is no crawler here to keep on English.

**The row's own warning was the smaller of the two things wrong with this page.**

- **The date, which the row named.** `formatWhen` and `formatDay` hardcoded `'en-US'`, so the hold's expiry rendered "Saturday, September 12 at 5:00 PM" underneath a Spanish label — the one fact on the page with a deadline attached, in the language the reader had just said they could not read. Both now take the locale. `timeZone` was already explicit and stays that way: it is the facility's, not the server's, which is what keeps B-228's class out of this even as the tag changes.
- **The status sentence, which it did not.** `<p>This reservation is <strong>{reservation.status}</strong>. …</p>` interpolated a `ReservationStatus` enum value straight into prose. Two defects in one line: an English word inside `<html lang="es">` with no `lang` of its own (3.1.2), and — for `converted` — a sentence that was **false in both languages**. `provision.ts` sets that status when a move-in completes, `reservationByToken` returns the row regardless of status, so a renter who had just signed a lease and paid, revisiting the link in their own confirmation email, was told the unit was *"back available for anyone to take"*. One key per ended status fixes both at once, which is why the fix is three keys rather than one with a placeholder.

**A false comment is what kept that branch unread.** `reservationByToken`'s doc comment said it returns null "for one whose reservation is no longer live". It never has — the page branches on `status` on the line after it calls it, which is the whole reason the `!live` paragraph exists. The comment has said the opposite of the code since B-018, and a reader trusting it would conclude the converted branch was unreachable. Corrected in place, and it now says why the filter is deliberately absent.

**`LOCALE_TAG` moved from `lib/comms/prose.ts` to `lib/i18n`.** B-261 wrote it beside the send path's Spanish prose, which is where it was first needed and not where it belongs: every surface that formats a date, a number or a list needs `en` | `es` widened to a BCP-47 tag, and a public page reaching into the comms module to get `'es-US'` is the wrong dependency for two strings. Values are unchanged, so all thirty-five existing callers in `comms/service.ts` render character-for-character what they did.

**What it reused rather than duplicated**, which is B-267's rule and the reason it has one: `reserve.moveInDate`, `facility.footBy` and `card.perMonth` are the same words for the same things, and a second entry per label is how two surfaces drift.

**No fourth `keyedFieldError` caller**, which the handoff asked for explicitly. The only input on this page is a hidden token, so none of the five action messages hangs on a field; all five go back as the form's summary through `messages()`. `keyedFieldError` counts fields to pick its heading and would have had nothing to count.

**`res.cancelled` joins `MUST_ALSO_DIFFER`** in `tests/i18n.test.ts` — B-267's rule, third use. It is a `status: 'success'`, so the `err.` prefix that earns the untranslated-paste guard would style it red and tell somebody who succeeded that they failed; it still confirms something irreversible, which is exactly the weight the guard exists for.

**The e2e spec takes B-120's first discipline, and it is the first spec in `i18n.spec.ts` that needs any of them.** The page cannot be reached without a real hold, so this one reserves and cancels in the same test — scoped to `demo-e2e`'s `10x10 Test`, seeded at 250 units precisely so holds need not be cleaned up, and the same fixture and the same reserve-then-cancel cycle the English specs in `smoke.spec.ts` already run. The file header said "nothing here mutates shared demo state" and has been corrected rather than left one merge behind. The expiry is asserted on the Spanish weekday rather than a literal date, because the date moves with the run: no English tag produces any of those seven words and `es-US` always produces exactly one.

**`/accessibility` re-read.** No claim changed in either direction. No route is added (`/reservations?token=not-a-real-token` has been in `PUBLIC_SCAN_ROUTES` since B-090 part 1 and still names the state it actually reaches), no new axe scan is added that could make a statement understate, and the `/` | Spanish row in `STATE_EXCEPTIONS` already covers "the rest of the public site" — this page included. `LAST_REVIEWED` not bumped (D-115): no manual screen-reader pass was performed.

**What it left behind.** The **live confirmation state has never been axe-scanned**, in either language, and is not in `STATE_EXCEPTIONS`. It needs a real hold, so no fixed URL in `PUBLIC_SCAN_ROUTES` can reach it; the new spec now builds that state, so closing it is one `assertNoAxeViolations` call plus a `SCANNED_STATES` entry — but an honest entry needs `layout`, and `'reached'` means a `STATE_REACH` key built in `e2e/a11y-own-spec-routes.spec.ts`, which is a second reserve-and-cancel cycle in a second file. That is the whole of the deferral and it is not this row's shape. **B-269** (`{terms}` still English inside every Spanish sentence that quotes it) and **B-265** (the six `sendDirectEmail` callers) are unchanged and still open. B-265 is the closer sibling now: the reservation confirmation email this page's own token arrives in is one of its six.

**Test verification.** Typecheck clean including `tsconfig.tests.json`. Lint clean — 6 warnings, all pre-existing `_prev`/`_formData`, unchanged in count from B-266. Schema drift clean against `.env.test`; no migration in this item, and the differences `.env.local` reports are the Neon dev branch's documented lag, not this item's doing. `npm run build` green — checked deliberately, because `LOCALE_TAG` moved into `lib/i18n/index.ts`, which client components import, and that is exactly the server/client seam B-090f shipped a module across and which fails only after typecheck passes. Unit suite **4,378 passed, 8 skipped across 256 files passed and 1 skipped — reconciled to 4,386**; unchanged in count from B-266, because this row extended an existing assertion's list rather than adding a test. e2e `i18n.spec.ts` on both projects: **72 passed** (36 each). `smoke.spec.ts --grep "reserv"` on `desktop-chrome`: **5 passed**, run because four of its specs read this page's English copy by text (`'Your unit is reserved'`, `'We hold it until'`, `'Nothing has been charged'`, `'Cancel this reservation'`, `'back available'`, `"isn't good any more"`, `/^Call/`) — every English string this item moved into the dictionary is character-for-character what it replaced, which is what those assertions confirm.


### B-269 — a promotion's own terms, in the reader's language (2026-09-07, `e8e27dd`)

B-266 translated the sentence around a promo code's outcome and stopped at the terms it quotes, which left «Código aplicado: 50% off the first month» — a 3.1.2 Language of Parts failure inside a sentence that had just stopped being one. This is that half, and the row understated it: five surfaces read those terms, not three.

**What it built.** `describeTerms`, `withMinStay` and the package's own `formatCents` are **deleted** from `@storage/core/promotions`. `offerTerms` replaces all three and returns an `OfferTerms` union — `operator` | `free_months` | `percent_off` | `amount_off` — and `offerTermsText` / `OfferTermsText` in `apps/web/lib/promotions/terms.tsx` write the words against a dictionary. Eight `promo.terms.*` keys per locale. This is `judgeStartDate`'s shape (B-263) and `codeOutcomeMessage`'s (B-266), arrived at a third time: a pure package has no request to read a locale from, so it returns the discriminant and the numbers.

**The five surfaces, and the row named three.** Every unit-card badge on a translated facility page (the biggest — one per size, on a page that may show six), the facility page's "what you'd pay" discount line, the checkout price summary, the applied-code confirmation, and the checkout's **"currently applied" line**, which nothing had listed. The templated send path came free: `lead.promo_line` and `checkout.promo_line` already resolved `recipient.locale` for every other merge field beside them, so those are translated now too. The six `sendDirectEmail` callers compose English in code and are still B-265's.

**What it decided.**

- **D-129 — the operator's own wording is rendered as typed, not given a column per language.** `Promotion.termsText` is free text with no key to return for it. The alternative was a migration plus a second admin field (this repo's rule: a new column that configures behaviour gets its control in the same item) plus a blank-fallback rule plus another column per future language. Instead the operator's words are shown verbatim and marked `lang="en"` where they stand alone, which is what 3.1.2 asks for — identify the language of a part, not translate it. The assumption (overrides are typed into an admin surface D-122 keeps English, by staff at a Texas facility) is stated in the decision rather than hidden in the code.
- **The minimum stay moved from a second function call onto every variant of the type.** `withMinStay` had to be *remembered* beside `describeTerms` at each call site; B-144's comment says why that matters — an operator override wins over the generated sentence, and a condition the renter is held to (B-145 charges money on it) must not be droppable. It is now impossible to drop by forgetting a call, which is a stronger guarantee than the one it replaces.
- **Two English surfaces are deliberate and now say so in code rather than by omission.** The admin screens resolve against `dictionaryFor('en')` explicitly (D-122), and so does `amountDueToday` — its line labels are written by `provisionMoveIn` onto invoice lines and the receipt, which is the business's own record of a charge, not a display string. Both were English by accident of the package before; they are English by argument now.
- **The English is character-for-character unchanged.** `formatRate` is reused rather than a new formatter, because `smoke.spec.ts` locates by these words. The `$` it hardcodes is untouched and still B-228's class — in `es-US` the figure is identical anyway, so it is a currency question rather than a language one.

**The snapshot changed shape, and carries a legacy branch.** `checkoutSession.data.promoTerms` stored a finished English sentence; it now stores the `OfferTerms` object. `readTerms` reads a string back as an operator override with `minStayMonths: 0` — the minimum is already inside that stored text and would otherwise be stated twice. Marked `ponytail:` with the condition for deleting it (no resumable session predating the deploy).

**What it left behind.**

- **Three surfaces take the terms as a plain string and lose the `lang` marking** — the applied-code confirmation and the "currently applied" line, because `FormState.message` and `fieldErrors` are string-typed end to end, and the move-in cost `<dt>`, because `costLineLabel` returns text. Turning those into nodes is a change to the form machinery every admin screen shares. Named on `/accessibility` and in D-129 rather than left implied.
- **The generated half has no e2e path**, and that is a fixture fact rather than a gap in the work: both demo promotions carry a `termsText`, deliberately (an automatic promo would move prices the smoke suite asserts totals against). So `i18n.spec.ts` pins the operator branch in both directions — the Spanish frame and the English words inside it — and the four generated variants are covered in both locales by unit test.
- **B-265** is unchanged and open. **B-268's** un-scanned live confirmation state is untouched.

**A real thing found on the way.** `promo.codeApplied` renders `{terms}.` and the demo promo's `termsText` ends in a full stop, so the confirmation reads "…one per customer.." Cosmetic, pre-existing, and not fixed here — it belongs to whoever settles whether operator terms are a sentence or a phrase.

**`/accessibility` re-read.** The B-266 block filed this gap by name; it now records what closed, and states plainly that a promo with an operator override read in Spanish still meets unmarked English in three places. No bullet in "Where we fall short" changed — the three are JavaScript, the staff screens and the maps, all still true and unchanged in scope. The one markup change is a `<span lang>`, which is the fix rather than a claim. `LAST_REVIEWED` not bumped (D-115): no manual screen-reader pass was performed.

**One doc comment corrected rather than left one item behind**, which is B-268's lesson applied on the way out: `cost-line-copy.ts` said the promo label is "NOT translated … text an operator typed". Half of that stopped being true here — the generated half now arrives translated through the same parameter, and only a `termsText` override is still the operator's own words.

**Test verification.** Typecheck clean including `tsconfig.tests.json`. Lint clean — 6 warnings, all pre-existing `_prev`/`_formData`, unchanged in count from B-268. No migration in this item, so no schema drift is possible from it; `prisma/` is untouched in the diff and local `migrate status` is up to date (the differences `.env.local` reports are the Neon dev branch's documented lag). `npm run build` green. Unit suite **4,380 passed, 8 skipped across 256 files passed and 1 skipped — reconciled to 4,388**; +2 on B-268, the Spanish-terms and operator-half assertions. e2e `i18n.spec.ts` + `smoke.spec.ts` on `desktop-chrome`: **107 passed**, `smoke.spec.ts` run in full because five of its promo specs locate the terms by their English text — every English string is character-for-character what `describeTerms` produced, which is what those assertions confirm.

**A correction to B-268's handoff while it is fresh.** "The tell is the absence of any `[e2e setup]` line" is over-broad: those lines print only when something stale was actually released, so a run against a freshly reseeded database prints none and is not evidence that `DATABASE_URL` was unset. The reliable check is that the command went through `test:e2e` (whose `dotenv -e .env.test -e .env.local` is echoed by npm at the top of the log), not the presence of the release lines.

### B-265 — the emails composed in code, in the reader's language (2026-09-07, `0e8f611`)

B-261 translated the rule-driven send path: an event resolves a recipient, `effectiveTemplate` picks their language, `deliverForRule` sends it. The sends that carry a secret minted in the moment — a resume token, a reservation token, a sign-in link — bypass all of it and compose their own subject and body at the call site. This is those.

**The row said six and there were nine.** The two it missed are the **waitlist** availability mail and the **broadcast**. B-269's handoff said to count the surfaces rather than believe the row, and the reason a first pass agreed with the row is worth recording: `grep` classifies a source file containing an em dash as binary under `LC_ALL=C` and silently skips it, reporting nothing. `grep -a` found two more call sites in files the first sweep had passed over. `file` calls those same files "data".

**What it built.** `DirectEmailInput.locale` is a REQUIRED field. `sendDirectEmail` wraps the HTML part in `<div lang="…">` and resolves the appended unsubscribe label through `proseFor`. Twenty-eight sentences moved into a `DirectProse` block in `lib/comms/prose.ts`, in both languages, grouped by the message they belong to rather than flattened, so "is every sentence of the reservation confirmation translated?" is answerable by looking. Four sends are now Spanish for a Spanish reader: the checkout resume link, the reservation confirmation, the sign-in and password-reset links, and both halves of the email-change flow, plus the waitlist availability mail.

**What it decided.**

- **D-130 — the locale rule is three steps and lives in one function.** `writingLocale` in `lib/i18n/server.ts`: what the recipient told us (`Tenant.preferredLocale`, or the waitlist entry's own column), then the language of the request that caused the send, then English. The middle step is the answer to the question the row existed to ask. A renter mid-checkout is anonymous (D-7) at exactly the moment the FIRST email is sent, and English "because there is no stored preference" fails precisely the person it matters to — four Spanish screens are the only statement of language they have made. D-128's "null means never told us" is what lets the blank fall through instead of asserting English.
- **The fix went into `sendDirectEmail`, not into the nine callers.** `locale` is required rather than defaulted, on the same argument the file already makes about the marketing footer: a caller that can forget it is a caller that will, and this failure is silent. It also localizes the unsubscribe label, which was hardcoded English here while `deliverForRule` had read `proseFor(recipient.locale)` since B-261.
- **`WaitlistEntry.preferredLocale` is a new nullable column, and it is the only one this item needed.** Every other direct send happens inside the request that caused it, where the cookie is still readable. The waitlist mail is sent by the hourly sweep, hours or days later, to somebody with no `Tenant` row — so a language not captured at join time is not recoverable at all. Null means "never told us", the same fact the tenant column carries; the entries already on the lists must not be recorded as having chosen English.
- **Three sends stay English by decision and now say so in code.** The platform alert and the scheduled report are staff (D-122). The **broadcast is the interesting one**: it has `recipient.locale` available and deliberately does not use it, because its body is `input.message`, free text a staffer typed — D-129's operator half. Selecting the seeded Spanish template would wrap English words in `lang="es"`, which is the exact 3.1.2 failure the new wrapper exists to prevent.
- **Staff are refused the request's cookie outright.** `requestPasswordReset` serves both audiences, so `linkLocale` returns English for a staff subject rather than resolving the browser they happen to be sitting at — otherwise an admin whose browser was left in Spanish gets a Spanish reset link.

**Two real defects found on the way, both in the email-change flow.** The tenant's own first name was interpolated raw into the HTML part of both messages, so a name containing a tag reached the inbox as a tag; every interpolated value is escaped now, through the same `escapeHtml` the render path uses. And the plaintext half of the security notice carried a SHORTER sentence than its HTML half — it dropped "it only takes effect when the link we sent to that address is opened", which is the half that tells somebody reading an alert about their own account that they still have time. One sentence renders both parts now, with the new address arriving bold in the HTML and plain in the text (`MergeValue`'s device).

**`/accessibility` re-read, and this one closes a gap rather than disclaiming one.** B-261 fixed `renderEmail`'s hardcoded `<div lang="en">` on the templated path. All nine direct sends had **no language declaration at all** — survivable only while every one of them was English, and 3.1.2 the moment four of them became Spanish. No claim on the page needed correcting in either direction: it has said nothing about email since B-198 settled that it says nothing either way, and that sentence held. The "Where we fall short" list is unchanged in scope. `LAST_REVIEWED` not bumped (D-115): no manual screen-reader pass was performed.

**What it left behind.**

- **The waitlist form's own screen copy is still English** — `joinWaitlistAction`'s two `FormState` messages and `joinWaitlist`'s three refusals. That is B-263/B-264's class (a form inside a translated facility page), one surface neither of them reached, and no row is open for it. It is named here rather than left implied because the mail that form produces is Spanish now, which makes the screen the odd one out.
- **B-268's** live confirmation state of `/reservations` is still un-axe-scanned in either language and not in `STATE_EXCEPTIONS`; **B-269's** three string-typed surfaces still lose the `lang` marking. Neither is touched.
- **The broadcast double-wraps its `lang`** — `renderEmail`'s `<div lang="en">` inside `sendDirectEmail`'s. Both are `en` and nested identical declarations are inert, so it is noted rather than fixed.

**Test verification.** Typecheck clean including `tsconfig.tests.json`. Lint clean — 6 warnings, all pre-existing `_prev`/`_formData`, unchanged in count from B-269. Schema drift clean against `.env.test` after the migration (`20260907195407_b265_waitlist_preferred_locale`, one nullable column); `db:status` reports the local schema up to date and the Neon dev branch missing all 115, which is that branch's documented empty state (CLAUDE.md, B-253) and not this item's doing. `npm run build` green. Unit suite **4,384 passed, 8 skipped across 256 files passed and 1 skipped — reconciled to 4,392**; +4 on B-269 — the `lang` wrapper in both locales, the Spanish reservation confirmation, and the Spanish waitlist mail. e2e `i18n.spec.ts` + `smoke.spec.ts` on `desktop-chrome`: **107 passed, 3 skipped**, run through `test:e2e` (npm echoed its `dotenv -e .env.test -e .env.local`, which is the reliable tell — see B-269's correction). `smoke.spec.ts` in full rather than grepped, because it asserts the reservation and waitlist pages by their English text and every English string this item moved into `prose.ts` is character-for-character what it replaced, which is what those assertions confirm.

### B-270 — the waitlist form, in the reader's language (2026-09-09, `5cfe382`)

**What it built.** The notify-me form on a sold-out size now speaks the language of the page around it. Ten strings: five in `waitlist-form.tsx` (the `<summary>` disclosure, the email label, the one-email-only scope sentence, the submit button), three `FormState` messages in `joinWaitlistAction`, two refusals in `joinWaitlist`. Copy comes from `useT()`, the provider already mounted in `app/(public)/layout.tsx` — B-264's shape, one component over.

**The row undercounted itself, the same way B-265's did.** B-265's handoff named "`joinWaitlistAction`'s two `FormState` messages and `joinWaitlist`'s three refusals". The actual shape is three messages and two refusals — and, more to the point, `en.ts` had **no `waitlist.` key at all**, so it was never only the action: the whole form was English. The sequence a Spanish visitor met was a facility page translated since B-090f, an English box asking for their email address, and — since B-265 — a Spanish availability email.

**What it decided.**

- **`JoinResult.problem` is a `FieldMessage`, not a sentence.** That is `captureLead`'s shape adopted for `captureLead`'s reason: `joinWaitlist` is also reached from the sweep and from the admin screen, neither of which is the reader, so the service returns a KEY and the layer holding the request resolves it. The action resolves both halves of the refusal to the SAME sentence rather than reaching for `keyedFieldError`, which the lead form beside it uses — see the defect below, which is the one thing this item got wrong and had to back out.
- **Three success messages became one dictionary key.** The action's own comment already said the "already on the list" and "just joined" answers get the same words on purpose. They did not: one ended in an em dash, the other in a full stop, and the honeypot's silent-discard answer was a third, shorter sentence. One key makes the stated intent true and removes the way two halves drift apart — in two languages at once, from here on.
- **The staff caller pins English rather than reading the cookie.** `joinWaitlistForLeadAction` resolves the same key against `dictionaryFor('en')`, because D-122 keeps the admin surface English whatever a staffer's own browser is set to. That is the shape `offerTermsText`'s three admin call sites already use. `err.waitlistNoFacility` is reached only from there and still carries Spanish, deliberately — a key with no translation is a key the next renter-facing caller pastes English out of.
- **`waitlist.joined` is named in `MUST_ALSO_DIFFER`**, on B-268's `res.cancelled` argument: it is the `status: 'success'` a visitor reads to know a stranger's address was actually recorded, and the `err.` prefix that earns the untranslated-paste guard would style it red.
- **The honeypot's `<label>Company</label>` stays English**, as the lead form's does. It is `aria-hidden` and `display:none`; its only reader is a bot.

**`/accessibility` re-read.** Customer-facing, and it closes a 3.1.2 gap on a control the page already lists and already scans. No claim needed correcting in either direction: `/storage/...` gains no state and no exception, because the form is the same disclosure, the same single input and the same live region in different words; the `/` | Spanish state exception still says exactly what it said, since the scan loops carry no locale cookie and this form's Spanish rendering is unscanned in the same way the rest of the public site's is. The "Where we fall short" list was re-read and all three entries are unchanged in scope. `LAST_REVIEWED` not bumped (D-115): no manual screen-reader pass was performed.

**What it left behind.**

- **The waitlist mail's own body is not this item's.** B-265 already made it Spanish; nothing here touched `sweepWaitlists`.
- **B-268's** live confirmation state of `/reservations` is still un-axe-scanned in either language and not in `STATE_EXCEPTIONS`; **B-269's** three string-typed surfaces still lose their `lang` marking. Neither is touched, and no row owns either.
- **The Spanish rendering of this form has not been axe-scanned**, for the reason the `/` | Spanish state exception already states — the scan loops carry no locale cookie. That gap is named where it always was rather than grown a row.

**A real regression, caught by e2e and not by anything else.** The first cut used `keyedFieldError`, on the reasoning that it translates the summary heading as well as the field — which is true, and wrong here. That helper counts the fields to build its summary, so a one-field form's `message` becomes "There is a problem with one field." and the actual sentence survives only in `fieldErrors`. This form's `role="status"` region announces `message`, so a screen-reader user submitting a bad address would have heard a problem **identified** (3.3.1) with no **suggestion** (3.3.3) — strictly worse than the English it replaced. `smoke.spec.ts:1664` is the only thing in the repo that would have caught it: typecheck, lint, build and 4,392 unit tests were all green with the regression in place. The fix is the shape `applyPromoAction` already documents twenty lines from the same helper (`checkout/actions.ts:497`) — resolve the key once and give `message` and `fieldErrors` the same words — and the comment now says why this one call site refuses the helper, so the next reader does not re-derive it from a red spec the way this session did.

**One spec added, in `e2e/i18n.spec.ts`, following every i18n row before it.** `tests/i18n.test.ts` proves the Spanish EXISTS; only a rendered page proves `useT()` reaches this component and that the server's refusal came back in the reader's language. It locates the card by `5 pies por 15 pies` — which is itself an assertion, since `sizeLabel` is `t('facility.footBy')` — opens the disclosure, and asserts ONE thing on the live region rather than the lead form's two, with a comment saying that the single assertion IS the row's decision. It mutates nothing: the refusal happens before any `WaitlistEntry` is written, so it needs none of B-120's three disciplines.

**Test verification.** Typecheck clean including `tsconfig.tests.json` — it caught a second slip on the way: `joinWaitlistForLead` and `holdForLead` open with a byte-identical `if (!lead.facilityId) return ...` line, and the first edit keyed the wrong one, which typechecks as two errors rather than shipping the wrong sentence on a staff screen. Lint clean — 6 warnings, all pre-existing `_prev`/`_formData`, unchanged in count from B-265. Unit suite **4,384 passed, 8 skipped across 256 files passed and 1 skipped — reconciled to 4,392**, unchanged from B-265: no test was added, because `tests/i18n.test.ts` already asserts the whole class (keys present in both dictionaries, placeholders preserved in both directions, `{size}` included, no untranslated paste under `err.` or in `MUST_ALSO_DIFFER`, no blank value) and the new keys are enrolled in it. `npm run build` green. Schema drift clean against `.env.test`; no migration. `db:status` reports the local schema up to date at 115 and the Neon dev branch still missing all 115, which is that branch's documented empty state (CLAUDE.md, B-253) and not this item's doing — it is why a bare `migrate diff` against `.env.local` reports two phantom `preferredLocale` columns.

### B-238 — a renter with no email address can be leased (2026-09-09, `017d77f`)

**What it built.** `Tenant.email` is nullable and non-unique. It was `String @unique`, and that one constraint turned three ordinary renters away at the counter: the contractor with no email, the elderly tenant whose daughter handles the account, and the husband and wife who each want their own account on one household inbox. The workaround was the harmful part — staff typed `nobody@example.com`, and then the receipt, the dunning ladder, the lien-notice supplement and the pay link all went into a hole, on the account most likely to end up in a lien file.

Migration `20260909152814_tenant_email_optional`: drop `tenant_email_key`, drop `NOT NULL`, add a plain `tenant_email_idx` — the unique constraint carried the only index on the column, and every sign-in, magic link and checkout looks a tenant up by address.

**What it decided.** **D-111 is answered (A), and the answer is recorded in `07-decisions.md`.** Four choices sit under it that the row left open, and each is commented where it lands rather than only here.

- **A shared address resolves to NOBODY at sign-in.** `findAccount` counts (`take: 2`) instead of looking up, and returns null for two matches as well as for zero. There is no fact available at a sign-in box that says which of two people sharing an inbox is typing, and guessing signs somebody into their spouse's ledger, unit and card on file — worse than an address that cannot sign in. Both people keep the lease, the notices and the mail; a shared inbox costs the portal and nothing else. Nothing became enumerable: an ambiguous address returns exactly what an unknown one returns.
- **A blank address links to nobody and always creates.** `upsertTenantForCheckout` short-circuits before its lookup. The reflex fix — reuse the "existing" tenant whose email matches — matches every other no-email renter at every facility, because they all match each other, so it would collapse the contractor, the elderly tenant and the next twenty walk-ins into ONE tenant holding all of their leases, ledgers and gate codes. It would also pass a test that only counted rows, which is why the test asserts two distinct ids and both emails `null` rather than a count.
- **Email stays REQUIRED on the public checkout.** FR-5.1 makes it the identifier because online it is how the renter receives the lease they just signed, the gate code and the receipt; an optional field there invites every self-serve renter to skip it. It is optional only where the session carries B-230's `walk_in` stamp — `emailOptionalFor`, ONE definition read by both the step that renders the field and the action that validates it, so the control and the rule cannot disagree. The action reads it from the SESSION, never from the form: a hidden field saying "email optional" is a hidden field anyone can set.
- **Two refusals became POLICY rather than schema, and both are commented as such.** The portal's email-change screen still refuses an address another tenant holds — allowing it would take a working portal login from BOTH tenants as a side effect of a screen that says it is updating an address — and `existingTenantByEmail` refuses an ambiguous one rather than attaching the wrong half of a married couple to a business account. Dropping the constraint removed the enforcement, not the requirement, at those two places.

**The comms layer gained a defined behaviour, and it is not silence.** The whole argument for allowing a blank address was that `nobody@example.com` sends into a hole *without telling anyone*; a `failed` row nobody reads is that hole with better bookkeeping. `noReachableEmail` writes the message `failed` as before — "we did not reach this person" is the answer a lien file asks for — and opens the existing `no_reachable_channel` task beside it, the same type the bounce path raises, the same one CN-19's failure queue counts and `/admin/reports/deliverability` already links to. **Both dead-ends route through it**, not just the one D-111 widened: the SMS-fallback path had its own copy of the write, and a guard added only to the caller the row named would have left its sibling silent.

**`/accessibility` re-read.** Customer-facing — it changes one field on checkout step 1. The row's own criterion is met: where the field stops being required it carries a `hint` naming what the renter loses (no emailed receipt, no pay link, no late-payment or scheduled-sale notice), which `Field` wires into `aria-describedby` — **SC 3.3.2 (A)** satisfied programmatically. The entry states plainly that this state is **not axe-scanned**, because the optional rendering appears only for a session carrying `acquisitionSource: 'walk_in'` and no URL reaches it: B-184's route-versus-state gap, the same shape the B-149 and B-172 entries argued about. No route, no new exception, no claim corrected in either direction. `LAST_REVIEWED` not bumped (D-115).

**What it left behind.**

- **No backfill.** Existing `nobody@example.com`-style placeholders are left exactly as typed — they record what staff entered, and rewriting them from a migration would destroy that without anyone deciding to. Clearing one is a staff action on the tenant screen. No row owns a sweep for them and none is proposed; the point of this item is that the next one is never created.
- **Phone and postal are the alternative contact of record in the DECISION, but nothing routes to them.** A tenant with no email gets a task, not a letter. Promoting phone or post to a channel the comms layer will actually send on is unbuilt and unowned.
- **The counter does not warn when it creates a SECOND tenant on an address already in use.** The consequence — neither can then sign in — is real, is documented in `findAccount`, and is stated in the decision, but the form says nothing at the moment it happens. It is named here rather than left implied.
- **`/portal/contact` renders `tenant.email` unguarded**, which is safe by construction (`loadSubject` refuses a session to a subject with no address) and is commented as such rather than defended with a `?? ''` that would render "Your email is .".

**Test verification.** Typecheck clean including `tsconfig.tests.json` — and the typechecker is what **enumerated the blast radius the row required be enumerated before sizing**: 19 errors across auth, checkout, comms, broadcast, billing accounts, referrals, Stripe customers, portal transfer, rate increases, the admin tenant types and the demo seed, then 23 more in `tests/` that the repo-root coverage caught. Lint clean — 6 warnings, all pre-existing `_prev`/`_formData`, unchanged in count from B-270. Schema drift clean against `.env.test` after the migration. `npm run build` green. Unit suite **4,392 passed, 8 skipped across 256 files passed and 1 skipped — reconciled to 4,400**; +8 on B-270. The four touched suites were run **twice** (B-120), because the comms test nulls a shared tenant's address and creates a task: it restores the address in a `finally` and deletes the tasks it made, and the second run confirms it. e2e `smoke.spec.ts` + `checkout-unit-lost.spec.ts` on `desktop-chrome` after `db:migrate:e2e`: **75 passed, 3 skipped**, run through `test:e2e` (the `[e2e setup]` and `[e2e teardown]` lines are the reliable tell that `dotenv` loaded — B-130). Those two are the specs that walk checkout step 1 end to end, which is the surface this item changed; the public path is unchanged by design, and they confirm it.

### B-271 — the counter says so before it puts a walk-in on somebody else's account (2026-09-09, `c31f85c`)

**What it built.** `otherTenantOnEmail` in `lib/checkout/details.ts`, a confirm step in `submitDetailsAction`, and a `separateAccount` option on `upsertTenantForCheckout`. Four dictionary strings in both languages. No migration.

**What it fixed, and it is not the gap B-238 wrote down.** B-238's entry left "the counter does not warn when it creates a SECOND tenant on an address already in use". Traced through the code, that state was **unreachable**: `upsertTenantForCheckout` is the only `tenant.create` in the whole app outside the demo seed, and it links to the oldest tenant holding the address before it would ever create. So D-111's sentence — "a husband and wife may each hold an account on one household inbox", and "the counter says so at the moment it creates the second account" — was unkeepable in **both** directions simultaneously. The second account could not be created at all, and the walk-in whose spouse already held the address had their lease, ledger and gate code attached to the spouse's account **silently**. Dropping the unique constraint removed the enforcement and built nothing that used the freedom.

**What it decided.**

- **The NAME is the discriminator, and it is the only fact on this form that can be.** A returning renter types the address and the name already on the record; that links, with no interruption, because that is FR-5.3 working and a confirm on every repeat move-in is a confirm that gets clicked through. A *different* name on a known address is the case D-111 is about, and it is the one that was attaching a lease to the wrong person. Comparison is trimmed and case-insensitive on both halves, because that is how somebody retypes their own name.
- **Two people of the same name on one inbox still link.** That is the status quo, not a regression, and nothing on this form distinguishes them. Named rather than left implied.
- **It is gated on `emailOptionalFor` — the SESSION's `walk_in` stamp — for B-238's reason and a second one.** The echo NAMES an existing tenant. On the unauthenticated public form that would turn a checkout into an answer to "who holds this address?", which is exactly the enumeration D-111 was careful to avoid at sign-in. Staff have the person in front of them; a stranger does not. `confirmed=yes` is read only when the session already permits it, so a hidden field cannot reach the create path.
- **`separateAccount` is the ONE thing that makes a known address create instead of link**, and it is reachable only from a session that came back through the confirm step. The reflex alternative — creating whenever the name differs — would fire on every typo of a returning renter's own name and split their account in half.
- **No new UI.** It is `AdminForm`'s shipped confirm-and-echo branch: the pre-mounted live region, the focused summary, the deliberately withheld `role="alert"`, B-124's field restore. **SC 3.3.4 Error Prevention (Legal, Financial, Data), AA**, satisfied by *confirmed*, on the screen immediately before a lease and a payment.

**`/accessibility` re-read.** Customer-facing, so it was, and an entry was appended. The confirm state is **NOT axe-scanned** and the entry says so: it renders only for a `walk_in` session whose typed address is held by a differently-named tenant — a post-interaction, data-dependent state no URL reaches. That is one more instance of **B-184**'s route-versus-state gap, not a new exception; `/checkout` keeps its existing coverage. No claim changed in either direction. `LAST_REVIEWED` not bumped (D-115).

**What it left behind.**

- **Nothing routes to phone or post yet.** B-238's second left-behind is untouched: the second account on a shared inbox now at least reaches that inbox, but a tenant with a genuinely blank address still gets a task and not a letter. Unbuilt and unowned.
- **No sweep for existing `nobody@example.com` placeholders.** Unchanged from B-238, deliberately.
- **Two same-named people on one inbox.** Above. No row owns it and none is proposed — the form has no field that would settle it.
- **The confirm is counter-only by design**, so a returning renter on the public site who mistypes their name still links silently. That is FR-5.3 and is correct; it is stated because it is the asymmetry somebody will otherwise read as an oversight.

**Test verification.** Typecheck clean including `tsconfig.tests.json`. Lint clean — 6 warnings, all pre-existing `_prev`/`_formData`, unchanged in count from B-238. Schema drift clean against `.env.test`; no migration. `npm run build` green. Unit suite **4,397 passed, 8 skipped across 256 files passed and 1 skipped — reconciled to 4,405**, exactly +5 on B-238's 4,400, which is the five tests added. `tests/checkout-details-db.test.ts` was run **twice** (B-120), because it creates tenants sharing one address and its `afterAll` reclaims them by email suffix. **The two assertions the row turns on are the ones a row-counting test would pass**: two DISTINCT ids holding the SAME address after a confirm, and a link (not a create) with the existing first name **unchanged** when staff did not confirm — the second is the additive-only property, which an unauthenticated form must not be able to break by typing a different name against a known address.

### B-272 — the two carried gaps nobody owned (2026-09-09, `e624977`)

`NEXT.md` had named two gaps for four items running, under the heading "no row owns either": B-268's live confirmation state of `/reservations` had never been axe-scanned in either language, and B-269's three string-typed surfaces still lost their `lang` marking. Both were still open exactly as described, and one of the two descriptions turned out to be wrong.

**What it built.** `translateSegments` and `segmentsText` in `lib/i18n`, and a `MessageSegments` component in `components/`. A message var may now be RUNS rather than a scalar; `translate` flattens them and `translateSegments` keeps them, from the same `vars` object. On the scan half, two `SCANNED_STATES` entries and two `assertNoAxeViolations` calls at live states the existing specs already reached.

**B-269 said three surfaces and there were four.** It listed the checkout's applied-code confirmation, its "currently applied" line and the move-in cost `<dt>`, and missed `components/promo-code-entry.tsx` — the facility page's own code box, which renders `codeOutcomeMessage` too. A Spanish reader who typed a promo code on a facility page met exactly the same unannounced English as one who typed it at checkout, and no list said so. This is B-271's lesson arriving a second time in three days: **a "left behind" note names what its author could see from where they were standing.** B-271's was unreachable; this one was undercounted. Grep the call sites before believing the number — `grep -a`, because a source file with an em dash is binary under `LC_ALL=C` and is skipped silently.

**All four lost the marking at the same step, which is why one helper closes them.** Not "three different surfaces with three different problems": every one of them took terms that `offerTermsSegments` had already marked and interpolated them into a translated template as a STRING. `translate` does a `.replace` on `{name}`, and a string has nowhere to carry a language. `translateSegments` splits the template on its own placeholders instead and substitutes runs.

**What it decided.**

- **`FormState.message` stays a string, and this is the decision the row turns on.** B-269 sized this as "turning `FormState` into nodes is a change to the form machinery every admin screen shares", and that framing is what kept it unbuilt. It is not what was needed. `message` is what `announceOutside` hands to `AnnounceRegion` as plain text, and it crosses the server-action boundary; widening it would have put React elements in an action's return value to fix a `<span>`. Instead the success variant gained an OPTIONAL `messageParts: MessageSegment[]`, `AdminForm` renders it when present and falls through to `message` when it is not, and **every other action in the app is untouched by construction.**
- **The invariant is that joining the parts reproduces `message` exactly**, pinned by a unit test rather than by a comment. A live region announcing one sentence while the page displays another is a worse defect than the unmarked English it would be fixing, and it is one nothing else in this codebase would catch.
- **`costLineLabel` returns runs rather than a string**, so the `<dt>` case needed no special-casing at its three call sites. Two of them pass promo terms and one does not; all three render through `MessageSegments`, which emits a bare text node for a single unmarked run — so every non-promo line produces the same DOM it did before.
- **`translate` flattens a segmented var rather than refusing one.** That is what lets one `vars` object feed both functions, which is what makes the invariant above true by construction instead of by discipline.
- **Two `SCANNED_STATES` entries for `/reservations`, not one.** What differs between the English and Spanish confirmation is not markup: it is a `lang` on the document, ~20% longer strings, and a held-until date from a different formatter — precisely the defect B-268 shipped to fix. Scanning the English state proves nothing about any of it.
- **Both scan entries are `layout: 'excepted'` with a reason a person can disagree with**, which is what B-246 requires. `/reservations?token=not-a-real-token` is in `PUBLIC_SCAN_ROUTES`, so all four public loops already measure that container at 320px, at 200% zoom and under forced text spacing; the confirmation adds a date sentence and two buttons inside the same single column. `layout: 'reached'` would have meant a second reserve-and-cancel cycle in a third file, which B-268 correctly called out as not worth it.

**A coverage claim that was true by accident.** `/reservations` has been in the scanned set since B-090 — as `?token=not-a-real-token`, the dead link. So for four months the route counted as covered while the page a renter actually ends a reservation on, with its expiry date, its move-in button and its cancel form, had never been given to axe at all. The comment beside that entry says as much ("the live states need a real hold") and nothing had ever closed it. Both live states pass axe with no violations, which is the outcome worth recording: the gap was in the coverage claim, not in the markup.

**What it left behind.**

- **`keyedFieldError` is still wrong for a one-field form** (`form-state.ts:85`). It counts entries to pick its summary, so a single field error makes `message` "There is a problem with one field." and the sentence saying what to DO survives only in `fieldErrors` — a 3.3.3 trade for a count. Three call sites already route around it with comments explaining why, which is the tell. Not touched here: it is a different helper on a different criterion, and it deserves its own row rather than a ride on this one.
- **`promo.codeApplied` renders `{terms}.` and the demo promo's `termsText` ends in a full stop**, so the confirmation still reads "…one per customer.." — B-269 found it, called it cosmetic and pre-existing, and it stays that way. It belongs to whoever settles whether operator terms are a sentence or a phrase.
- **B-269's own `{terms}` note in `/accessibility` is corrected in place rather than deleted.** The paragraph is what the page said for two days; the correction sits under it and the new entry records what replaced it.

**`/accessibility` re-read.** The D-129 residual paragraph was FALSE in the understating direction — it disclaimed three surfaces as unmarked that are now marked, and the number was wrong besides. Corrected in place, with a B-272 entry recording what closed and that there were four. Two rows are added to the rendered `SCANNED_STATES` list, which is a rendered change to a public claim rather than a comment one. "Where we fall short" was re-read against this build — the no-JavaScript hold countdown, the staff screens, the embedded maps — and all three are still true and unchanged in scope. `LAST_REVIEWED` is not bumped, per D-115: no manual screen-reader pass was performed, and this item performed none. **B-254 is still the only thing that can move that date.**

**Test verification.** Typecheck clean including `tsconfig.tests.json`. Lint clean — 6 warnings, all pre-existing `_prev`/`_formData`, unchanged in count from B-271. No migration in this item; `prisma/` is untouched in the diff, `prisma migrate diff` against `.env.test` reports "No difference detected", and local `migrate status` is up to date (the 116 the `.env.local` half reports as unapplied are the Neon dev branch's documented empty state, CLAUDE.md and B-253). `npm run build` green. Unit suite **4,401 passed, 8 skipped across 256 files passed and 1 skipped — reconciled to 4,409**; +4 on B-271, all four the new `translateSegments` tests — the join-agrees-with-`translate` invariant, the marked/unmarked split, run merging, and the unknown-placeholder fallback. e2e `i18n.spec.ts` + `smoke.spec.ts` on `desktop-chrome` after `db:migrate:e2e`: **108 passed, 3 skipped — reconciled to 111**, which is exactly what `--list` collects for those two files. Run through `test:e2e` (npm echoed its `dotenv -e .env.test -e .env.local`, and `[e2e teardown] released 19 checkout lock(s)` confirms the env reached Playwright's own process — B-269's correction to the `[e2e setup]` tell). `smoke.spec.ts` in full rather than grepped, because five of its promo specs locate the terms by their English text, and this item moved every one of those strings through a new code path that must reproduce them character for character.

### B-273 — a one-field refusal announces the refusal, not a count of fields (2026-09-09, `53d425f`)

`NEXT.md` carried this as the one buildable thing on a page of six owner-blocked rows, and it was still open exactly as B-272 described it: `keyedFieldError` picked its summary by counting entries, so a single field error made `message` read "There is a problem with one field." and the sentence saying what to DO survived only in `fieldErrors`.

**What it built.** One branch in one helper. `keyedFieldError` resolves the field messages first and, when there is exactly one, uses that resolved sentence as the summary; two or more still get `err.someFields` with the count. `err.oneField` is deleted from both dictionaries because nothing renders it any more. The two actions that had spelled the helper out by hand call it again — `waitlist-actions.ts` and checkout's promo refusal.

**The count was a 3.3.3 trade, and it cost most where it was announced alone.** `AdminForm` renders the field list INSIDE the same `role="alert"` as the summary, so checkout and the reservation form read the suggestion out either way — the count was a wasted heading there, not a loss. `FormResult`, which the lead and waitlist forms use, announces `message` and nothing else and moves focus to it. A visitor refused by the lead form heard a problem identified and no suggestion; the sentence naming what to fix sat beside the input, reachable only by swiping back to it. `lead-actions.ts` has passed this helper exactly one field since B-264, so that form got the count on every refusal it has ever made, in both languages.

**Two callers already routed around it with comments explaining why, which is what made it a defect rather than a style.** B-270's first cut of the waitlist form used the helper, `smoke.spec.ts:1664` failed, and the row wrote the shape out by hand with a paragraph saying the helper would trade 3.3.3 for a count. B-266 did the same at checkout's promo refusal, independently, for the same reason. Both paragraphs described the defect accurately and neither fixed it — the fix is a smaller diff than either workaround.

**What it decided.**

- **`fieldError` is deliberately NOT changed, and the asymmetry is the decision rather than an oversight.** Every one of its callers is an `AdminForm` staff screen D-122 keeps English, where the suggestion is already inside the announced box. Changing it would rewrite admin assertions for no reader. Both helpers now carry a comment saying which one they are and why they differ.
- **The summary is the resolved sentence, not the key.** `vars` are interpolated before the branch picks, so the reservation form's only refusal ("…more than {days} days ahead") announces the number rather than a template with a hole in it. A unit test pins that specifically.
- **`err.oneField` is deleted rather than left in place.** A dictionary entry nothing renders is a translation someone will maintain for a string that never ships; both languages lose it in the same commit, so key parity holds.

**What it left behind.**

- **Nothing new.** The two workarounds this removes are gone rather than deprecated, and the helper is the only place the branch exists.
- **`promo.codeApplied` still renders `{terms}.` against a `termsText` that ends in a full stop** — B-269 found it, B-272 recorded it as cosmetic and pre-existing, and it stays that way. It belongs to whoever settles whether operator terms are a sentence or a phrase.

**`/accessibility` re-read.** No claim changed and none needed correcting in either direction. "When a form rejects something you typed, the message is tied to the field itself" describes the field message, not the summary above it, so it was not overstated before this row and is not understated after it. No route, state or exception changes. A B-273 entry is appended recording that this page's own B-270 note described the defect and left it in the helper — that note said routing the waitlist refusal through `keyedFieldError` "replaced the announced sentence with that helper's field COUNT", and did not say the count was still there for every other caller. "Where we fall short" was re-read against this build — the no-JavaScript hold countdown, the staff screens, the embedded maps — all three still true and unchanged in scope. `LAST_REVIEWED` is not bumped, per D-115: no manual screen-reader pass was performed, and this item performed none. **B-254 is still the only thing that can move that date.**

**Test verification.** Typecheck clean including `tsconfig.tests.json`. Lint clean — 6 warnings, all pre-existing `_prev`/`_formData`, unchanged in count from B-271 and B-272. Unit suite green: 4404 passed + 8 skipped = 4412, reconciled against the expected total. Three new tests in `tests/form-state.test.ts` pin the summary's provenance — that one error becomes its own summary, that two still count, and that `vars` reach the summary. No migration in this item; `prisma/` is untouched in the diff and `prisma migrate diff` against `.env.test` reports "No difference detected". `e2e/i18n.spec.ts`, `smoke.spec.ts` and `checkout-unit-lost.spec.ts` ran green after the change — 220 passed + 4 skipped = 224. The i18n spec's three assertions on "Hay un problema con un campo." are now assertions on the Spanish sentence the reader actually needs, which is the row's whole claim and is not checkable by a unit test.

### B-274 — nothing in the product said where a unit would be sold (2026-09-09, `2822230`)

`NEXT.md` carried this as the only buildable row, and it did not come from a review or from building something else: **D-131 answered master PRD §8 OQ-9 at a desk and the answer exposed a gap the open question had been covering.** `EXAMPLE_SALE_STATEMENTS` hedged the entire consequence to *"the property may be advertised and sold to satisfy the lien… governed by state law"*, and no field anywhere in the schema carried a sale venue. Correct about the law, and useless to the person it is sent to: a tenant who wants to attend, bid, or send a relative to buy their own property back could not learn from anything this product mailed them whether the sale was at the facility on a Saturday or on a website.

**What it built.** `Facility.auctionSaleManner` — a two-value enum (`online`, `live_onsite`) defaulting to `online` per D-131 — and `auctionSaleVenue`, with both controls on `/admin/settings/delinquency` beside the terms and the time, saving through the same action and landing in the same `facility.settings_changed` audit row. `EXAMPLE_SALE_STATEMENTS` became `saleStatement(type, facilitySale)`: an online sale names the site, a live one names the facility address the notice already carries. The lot sheet gains a `Where the sale is held` column, and `auctionReadiness` gains `no_sale_venue`.

**What it decided.**

- **An `online` facility with no venue renders the pre-B-274 sentence word for word, and does not refuse to generate a notice.** The row said a null venue is "a refusal, not a blank" and named `auctionReadiness` and `/admin/auctions` as the surfaces; it did not say what the notice does meanwhile, and the answer is: nothing new. The refusal belongs on the SALE, not on a pre-lien notice for a facility somebody has not finished configuring — and because readiness blocks, the hedge can never be the last thing a tenant is told before a sale actually happens.
- **The venue is a refusal where the terms and the time are a blank column, and the asymmetry is the point.** A sale on unstated terms is still a lawful sale; "somewhere on the internet" is not a place. `/admin/auctions` says so once, above the refusal list, because the fix is one field on one facility and the per-lot refusals would otherwise repeat it once per unit without ever saying it was one setting.
- **`live_onsite` is a first-class value, not a legacy branch.** It renders a facility-address sale statement, needs no venue at all, and clears readiness — a single-site operator running their own sale is a supported install, and D-131 chose that deliberately.
- **The lot sheet gets one column holding a whole phrase, not a code beside a blank.** "Live, at the facility address above" / "Online, at StorageTreasures.com". That file is read down a phone to a classifieds clerk; `online` next to an empty cell is the exact ambiguity this row exists to remove. It can never be blank, because an online facility with no venue has no lots on the sheet at all.
- **A posted manner that is neither legal value falls back to `online`, the seeded default.** The column is an enum and the write would 500 rather than set it, which is a stack trace where a setting should be.
- **No marketplace driver, and D-63 is untouched.** B-129 stays open on the partner agreement — the harder of its two blockers and the one no decision at a desk can clear. Advertising is still `AuctionAdvertisement` rows a person types, whichever channel a facility is set to.

**What it left behind.**

- **The demo seed sets no venue**, so demo facilities are `online` with no site — deliberately, matching how the seed already leaves the terms and the time unset. The auctions screen says so. Nothing in the demo data is scheduled, so no lot is refused by it today; the first demo scheduled sale will be, correctly.
- **B-129 is unchanged and still blocked.** This row is what D-131 made buildable; the listing driver is not it.
- **Every word of the new statement is draft legal text under D-10** and renders under `NOTICE_DISCLAIMER` in the template body. Naming a venue makes the sentence more specific, which raises the cost of getting it wrong — the argument for the readiness check, not against the row.

**`/accessibility` re-read.** No public route, page or rendered customer screen changed, so no claim moved in either direction. The two screens this touches are staff-only and are the "staff screens" the "where we fall short" list already names. A B-274 entry is appended anyway, because the row does change something a customer receives — the sale statement inside a lien notice, which is a generated document and outside every claim this page makes. Both new controls go through the shipped `Field`, one a native `<select>`, and the venue's hint states in text what leaving it empty costs. All three "where we fall short" entries re-read and still true. `LAST_REVIEWED` is not bumped, per D-115: no manual screen-reader pass was performed, and this item performed none. **B-254 is still the only thing that can move that date.**

**Test verification.** Typecheck clean including `tsconfig.tests.json` — making `saleManner`/`saleVenue` REQUIRED on `ReadinessInput` rather than optional is what makes that a gate: a future caller cannot fail open into the silence this row closes, and the compiler named the one fixture that needed updating. Lint clean — 6 warnings, all pre-existing `_prev`/`_formData`, unchanged in count from B-273. `npm run build` green. Migration `20260909224550_auction_sale_manner`; `prisma migrate diff` against `.env.test` reports "No difference detected". **`db:migrate:cloud` refuses with P3005 — "the database schema is not empty" — and that is the Neon dev branch's pre-existing state, not this migration.** That branch has a schema and no `_prisma_migrations` baseline, which is the same condition B-272 recorded as "the 116 the `.env.local` half reports as unapplied"; it now presents as an error rather than a count because `migrate deploy` will not write a first row into a populated schema. Baselining it is a deliberate act against shared cloud infrastructure and is exactly what B-253 exists to stop happening as a side effect of an ordinary item, so **nothing here touched it** — it needs an owner decision and a row of its own. Local and e2e schemas both carry the migration. Unit suite **4,415 passed + 8 skipped across 256 files passed and 1 skipped — reconciled to 4,423**; +11 on B-273, split between `tests/auction-readiness.test.ts` (the blocker, whitespace as no venue, and a live sale needing none), `tests/notice-templates.test.ts` (the venue named, the live branch, the hedge fallback, and the existing "state law" assertion widened to run over all three) and `tests/auctions-db.test.ts` (a real lot dropping when the venue is cleared and coming back when the facility is set to sell on site, plus the settings round-trip including the illegal-manner fallback). `e2e/admin-auctions.spec.ts` on `desktop-chrome` after `db:migrate:e2e`, through `test:e2e` so `global-setup` actually ran: **5 passed, 0 skipped — reconciled to the 5 `--list` collects (3 specs plus the 2 auth setup projects)**, exit 0. The CSV header assertion is the one that had to move: the sheet gains a column, and that spec asserts the header row character for character. No `[e2e setup]` line appeared, and here that is expected rather than the B-130 tell — `db:migrate:e2e` had just rebuilt the schema, so there were no stale checkout locks or holds to release and `global-setup` logs only when it releases something.

### B-276 — the auction lot could be sold at a venue the served notice never named (2026-09-10, `f43a561`)

`NEXT.md` led the B-252–B-274 review block with this row on the wrongful-sale ground B-224, B-202 and B-137 led theirs. B-274 built `Facility.auctionSaleManner`/`auctionSaleVenue` and not their drift check: `auctionCase` handed `auctionReadiness` the facility setting **as it stands today**, and `Notice` snapshotted the rendered address and the hash but no sale statement. Serve a notice saying StorageTreasures.com, switch the facility to another site three weeks later, and readiness stayed green.

**What it built.** Two nullable columns on `Notice` — `renderedSaleManner` (the existing `AuctionSaleManner` enum) and `renderedSaleVenue` — written by `generateNotice` from the same facility values `saleStatement` renders, carried on `NoticeContext.sale` so the preview and the generation cannot read two different settings. `auctionCase` selects them off the served lien notice and passes them to `auctionReadiness` as `noticeSaleManner`/`noticeSaleVenue`, which gains a `notice_names_another_venue` blocker worded like `notice_names_another_unit`: it names what the notice said, what the facility says now, and both fixes (restore the setting, or re-serve). Because `scheduleSale` and `auctionLotSheet` both decide on `auctionCase`'s readiness, the one rule stops the booking and drops the lot into the sheet's refusal list with no further wiring. Migration `20260910163701_notice_sale_snapshot`.

**What it decided.**

- **An absent snapshot is not a mismatch.** Every notice generated before this row has null columns and does not block, exactly as the row's acceptance requires. Blocking them would stop sales at facilities that never changed anything and give a manager no way to clear it short of re-serving.
- **The rule runs only once a notice is served.** Before that `no_lien_notice_served` is already blocking and there is nothing to compare against, so the two never appear together.
- **Venues compare case- and whitespace-insensitively, and a `live_onsite` sale reads no venue at all.** The venue is free text read down a phone; re-typing "storagetreasures.com" is not a change of place. The facility address on the notice is the place for a live sale, so a stale venue string left behind on a `live_onsite` facility is not drift.
- **An `online` snapshot with no venue, against a facility that now names one, IS a mismatch.** The notice rendered B-274's hedge with no place in it, so a sale at the site set afterwards is precisely a sale at a venue the notice never named. This is reachable only from a notice served while the facility was mid-configuration.
- **The snapshot is the setting behind the sentence, not the sentence.** Storing the rendered text would make the comparison depend on `saleStatement`'s wording, which is draft legal text under D-10 and will be edited. The document and its hash already hold the words.
- **No marketplace driver, D-63 untouched, D-131 not reopened** — the three things the row said not to do.

**What it left behind.**

- **Nothing new.** There is no control to add: the columns are evidence written at generation, not configuration, so the "a new column gets its control" rule does not apply.
- **`db:migrate:cloud` was not run.** The Neon dev branch still has no migration baseline and refuses `migrate deploy` with P3005, which is **B-275**'s job under D-132 — not something an ordinary item does to shared cloud infrastructure as a side effect.

**`/accessibility` re-read not required.** Nothing a customer touches changed: the notice document's wording is untouched, and the new blocker message renders only on the staff auctions screens, which are already on the "where we fall short" list.

**Test verification.** Typecheck clean including `tsconfig.tests.json`. Lint clean — 6 warnings, all pre-existing `_prev`/`_formData`, unchanged in count from B-274. `prisma migrate diff` against `.env.test` reports "No difference detected". Migration applied to `storage_test` (`db:migrate:test`) and to `public` (`db:migrate:e2e`, which reseeded the demo). Unit suite **4,424 passed + 8 skipped across 256 files passed and 1 skipped — reconciled to 4,432**, +9 on B-274: seven in `tests/auction-readiness.test.ts` (a changed site blocks and names both, a manner switch in either direction blocks, an unchanged sale is silent, a live sale ignores a venue string, case and whitespace are not a change, a pre-B-276 null snapshot is silent, and no served notice means no comparison), one in `tests/notices-db.test.ts` (generation writes both columns and the document carries the same site), and one in `tests/auctions-db.test.ts` (a scheduled lot on a real case goes un-ready, drops off the lot sheet into its refusals naming the served site, and comes back when the setting is restored). No e2e spec was touched and no admin screen changed shape, so none was run locally; CI's e2e lane owns the sweep.

### B-292 — every lease that had paid an invoice failed reconciliation, and the notice gate refused it (2026-09-10, `3830ead`)

Found while starting B-277, and **verified on real rows before any code changed.** B-277's remedy was "reuse `reconcile()`", so the first step was to run that arithmetic in SQL over `storage_test`'s 722 leases. **25 leases had an invoice paid through the real payment path, and all 25 failed. None passed.**

**The defect.** `postPaymentLedger` (B-257) posts a payment's ledger entry per lease with **no `invoiceId`**; which invoices the money settled lives in `PaymentAllocation`. Both callers of `reconcile()` (the ledger screen and `claimForLease`) passed *every* entry without an invoice as expected uninvoiced money. The invoice side already counts the settled amount, so it was counted twice. A paid $129 invoice read **+$129, "something was charged to the ledger without an invoice behind it"**, and `claimForNotice` refused a lien notice with `ledger_does_not_reconcile` for any tenant who had ever paid an invoice through the product. The fixtures hid it: `reports-financial-db.test.ts` writes payment entries *with* an invoice id, which is what B-049 assumed and what no production writer does.

**What it built.**

1. **`reconciliationInputs(scope)`** (`apps/web/lib/admin/ledger.ts`). The one loader for `reconcile()`'s three terms, per lease, scoped by lease ids or facility ids. It uses five queries whatever the lease count. `leaseLedger` and `claimForLease` both call it now, and their two hand-summed copies are gone.
2. **`SETTLING_STATUSES`** exported from `apps/web/lib/billing/allocation.ts`, so the add-back counts exactly the payments `recomputeInvoices` counts.

**What it decided.**

- **Allocations come back out of the uninvoiced term only when the payment has an uninvoiced `payment` entry on the SAME lease.** Adding back every allocation of the payment would read a pre-B-257 multi-unit payment (whole amount on lease P, invoices on P and Q both paid) as Q reconciled. Scoped this way, Q shows its +$100 gap. Lease P still reads as reconciled, the same shape as a legitimate prepayment; Q is the lease that surfaces the pair.
- **An entry that already names its invoice is left on the invoice side**, so the fixture shape still reconciles, and `storage_test` goes from **25 failing leases to 0** with the fixture rows included.
- **`claimForNotice` and `reconcile()` are unchanged.** What the gate refuses is still "the two sources disagree". Only the inputs were wrong.

**What it left behind.**

- **A partially paid invoice moved by a transfer (B-086) reads as a gap of the paid part on the old lease.** The payment's entry stays on the old lease while the invoice moves. This is rare (a partial payment followed by a transfer) and shows up on B-277's list rather than hiding. Nothing owns it yet.
- **Production has been refusing lien notices on this ground since B-061.** No data needs repairing, but any notice staff were refused should be retried. That is recorded in `NEXT.md` as an owner question.

### B-277 — nothing swept for leases whose ledger and invoices disagree (2026-09-10, `3830ead`)

Built on B-292, in the same commit. Without B-292 the list would have named every lease that had paid an invoice.

**What it built.**

1. **`ledgerExceptions(facilityIds)` and `ledgerExceptionsFor(actor)`** (`apps/web/lib/admin/ledger.ts`). Every lease that fails `reconcile()`, with facility, tenant, unit, ledger balance, invoices outstanding and the signed difference, largest gap first within each facility. The actor form scopes through `financialFacilities`, as every other money report does.
2. **`/admin/reports/ledger-exceptions`**, linked from the reports index's Money group beside Plans & holds. Each tenant name links to that lease's ledger, the table sits in `ScrollRegion` with a caption, and the page is registered in `ADMIN_SCAN_ROUTES`.
3. **`raiseLedgerExceptionTasks(now, facilityIds)`**, called at the end of every cron tick. The cron response gains `ledgerExceptions` (the count), and one high-priority `ledger_does_not_reconcile` task is raised per facility per business day while any lease fails. The new task type sits in `packages/core/tasks/catalog.ts` with a required note and `sensitive`, like `job_failed`.

**What it decided.**

- **Every tick, not once a business day.** A phantom balance keeps a paid-up tenant on the ladder, and `createTask`'s (type, entity, business date) key already stops a second task. The sweep runs after the job queue, so a lease tonight's billing just fixed is not counted.
- **The task's subject is the facility and its detail is the count at the time it was raised.** Later ticks find today's task and leave it alone, so the sentence says "when this was raised" instead of claiming to be current. The list is the report.
- **All leases, ended ones included.** A skew on a moved-out lease is still wrong former-tenant money.
- **`claimForNotice` is untouched**, as the row required.

**What it left behind.**

- **The production dry run of `db:backfill:move-in-payments` was not done.** `.env.prod-ops` has `DATABASE_URL`, `DIRECT_URL` and `EXPECTED_DEV_DB_HOST` all empty, so there was nothing to connect with. It is an owner action in `NEXT.md`.
- **The sweep does not find B-255's phantom move-in balances, whatever the row assumed.** An uninvoiced move-in charge reconciles by design (`tests/ledger-db.test.ts`, "reconciles a move-in charge that never became an invoice"), so an unposted move-in payment is not a ledger/invoice skew. Only `planMoveInBackfill` detects those leases. An alarm for them would be a new row, not a change to `reconcile()`.

**`/accessibility` re-read not required.** Nothing a customer touches changed. The new page is staff-only.

**Test verification (both items).** Typecheck clean, including `tsconfig.tests.json`. Lint 0 errors, 6 warnings, all pre-existing and unchanged in count from B-276. No schema change, so there is nothing for the drift check to see. **New `tests/ledger-exceptions-db.test.ts` (7 cases)**, driven through `recordCounterPayment` rather than hand-written payment rows, because a hand-written fixture is what hid B-292. **Mutation-checked:** with the allocation add-back switched off, 4 of the 7 fail. They are the paid-lease reconciliation, the notice not being refused, and the paid lease staying off both the list and the hourly count. The other three guard the opposite direction (over-broad add-back, the gate still refusing, and scoping). The seven files the loader touches passed 105/105 together. **Full unit suite, second run: 4,433 passed + 8 skipped = 4,441** (257 files passed, 1 skipped — the live-integration file), exit 0, totals reconcile. **The first full run was stopped at its first alarm:** four `marketplace-db.test.ts` feed tests failed with *"Timed out fetching a new connection from the connection pool"*. That is B-185's recorded `storage_test` accumulation (2,842 facilities), in a file this diff does not touch. Stopping it left **nine orphaned vitest workers holding 49 connections to `storage_test`**, because `pkill -f "$PWD.*vitest"` matches the `dotenv` wrapper only: the workers retitle themselves `node (vitest N)` and carry no path. They were killed by working directory (`lsof -a -d cwd`), which leaves other projects' workers alone. After `npm run db:reset-test`, the second run was green. **e2e:** `npm run test:e2e -- e2e/admin.spec.ts -g ledger-exceptions` ran the page through `ADMIN_SCAN_ROUTES`' four checks (axe WCAG 2.1 AA, 320px reflow, 200% zoom, forced text spacing) against the production build, on desktop and mobile Chrome. **10 passed** (the 8 checks plus 2 auth setups), exit 0. No `[e2e setup]` line appeared in that output. These checks only read the page and mutate nothing, so a stale hold or lock could not have changed them, but the run is not evidence that global setup ran. **What that scan did NOT cover: the table.** The demo `public` schema has no lease that fails reconciliation, so the page rendered its empty state. The table's markup follows `plans-holds`' scanned table (caption, `scope`, `ScrollRegion`), but no axe pass has seen it with rows in it.

### B-282 — three customer money-path tables were scroll wrappers a keyboard could not focus (2026-09-10, `041c9d5`)

**What it built.**

1. **The three bare `overflow-x-auto` wrappers are `<ScrollRegion>`**: the account card's units table on `/portal`, the bill on `/portal/pay`, and the consolidated statement. Each name comes from the table's existing caption key (`acct.tableCaption`, `paypg.captionAccount` / `paypg.captionUnit`, `astmt.caption`), so no new keys were added. They now carry `role="region"`, `tabindex="0"` and B-217's visible scrollbar.
2. **`offscreenOffenders` (`e2e/a11y-helpers.ts`) waives overflow only when a keyboard can reach it.** Its scroll-ancestor walk now also asks whether the scroller is focusable or the content is. `e2e/a11y.spec.ts` gains a fixture test with three cases: a bare wrapper of text fails, a `tabindex="0"` wrapper passes, a bare wrapper of links passes.
3. **One axe scan of `/portal/pay?account=` in Spanish** in `e2e/portal-billing-account.spec.ts`, as the payer, claimed in `SCANNED_STATES` as `business account, Spanish`. The portal Spanish `STATE_EXCEPTIONS` reason now names that screen as scanned.
4. **The accessibility statement names the gap it closed.** A visible, dated "Where we fall short" entry in both dictionaries (`a11y.short.statementTable.*`), plus a re-read log entry (D-135).

**What it decided.**

- **The overflow was measured, not assumed.** A throwaway probe, signed in as the demo payer on both projects at 320px, 320px with text spacing, 200% zoom and default viewport with text spacing, found the dashboard and pay-screen tables never overflowed. **The consolidated statement's table did: 301px in a 270px box at 320px, 345px under text spacing.** From B-256 (2026-09-04) to this commit, a keyboard could not reach its right-hand columns at that width. Nothing overflowed at 200% zoom.
- **The waiver is wider than the row's "require `tabindex="0"`", and deliberately.** The admin nav's mobile strip (`components/admin/side-nav.tsx`) is a bare `overflow-x-auto` whose content is all links, and every link is a focus stop that scrolls itself into view. The literal condition would fail it on every admin 320px test for something a keyboard can reach. The rule used is axe's `scrollable-region-focusable` rule: focusable scroller, or focusable content.
- **`nothingOverlaps`'s copy of the walk is NOT changed.** It decides contrast, which a cell has however it is reached. Its comment now says the two copies differ on purpose.
- **D-135 (owner): the closed gap is named on the page, dated and marked fixed.** The recommendation was log-only, on the grounds that the list describes the current state; the owner chose the visible entry. When it comes off is left to B-254.
- **`a11y.true.keyboard` is unchanged and `LAST_REVIEWED` is not bumped.** The claim is scoped to the public site and was not false; D-115 still owns the date.

**Two bugs found by its own tests, both fixed here.**

- **The region's name broke two locators.** "Units billed to Acme Contracting" made `getByRole('region', { name: DEMO_BUSINESS_ACCOUNT_NAME })` match two elements. Both card locators now pass `exact: true`.
- **The first fixture passed on Pixel 7 when it should have failed.** `page.setContent` with no viewport meta lays out at 980px, so the 900px table fitted. The fixture now carries `width=device-width`.

**Verification.** Typecheck and lint are clean (the 6 warnings were already there). `scroll-regions`, `a11y-scan-coverage`, `accessibility-statement` and `i18n` unit suites: 40 passed. e2e was run file by file rather than as one sweep, because runs were stopped on alarms and re-run:

- **Mobile:** `a11y-own-spec-routes` (21), `a11y.spec` (123) and `admin.spec` (283) all passed; the two tests named below were excluded. `portal.spec` 144 passed, 0 failed.
- **Desktop:** `a11y-own-spec-routes` 21 passed. `a11y.spec` plus `admin.spec` 409 passed, 0 failed.
- **Both projects:** `portal-billing-account` 10 passed, the Spanish scan included. `/accessibility` (axe, 320px, 200%, text spacing) and `/admin/access` axe: 12 passed.
- **`i18n.spec` on both projects:** 72 passed. That includes the Spanish portal dashboard's axe scan over the new region and the Spanish statement page.

**What it left behind.**

- **The full sweep was not run locally**; CI owns it. `e2e/admin-tenants.spec.ts:536` fails on `main` at `3830ead` and was not investigated. `NEXT.md` puts it first.
- **Two local-only failures were environment, not code.** (1) `/reserve`'s layout test failed after `db:migrate:e2e`, because `.next/cache/fetch-cache` survives builds and served the pre-reseed unit-type ids. It passed on both projects once the cache was cleared. (2) `/admin/access` failed its axe test, which is B-293.
- **The nav's "Pay $161" points at `?lease=`** for the payer. That was seen in the probe snapshot and is already B-278.

### B-293 — the gate log's rows had six cells under seven headers (2026-09-10, `041c9d5`)

Found while verifying B-282 and fixed in the same commit, by owner choice.

**What it built.** The Unit `<td>` on `/admin/access`, restored from `row.unitNumber`, which `accessEventLog` already returned, with "—" when there is none.

**The defect.** `68a1cc8` (B-086 part 2) dropped the cell and kept the `<th>`, so How read under Unit, Result under How, Flags under Result, and Flags had no data cells (SC 1.3.1, Level A). axe reports it as `th-has-data-cells` *incomplete*, and only when the range holds gate attempts: an empty range renders a `colSpan={7}` row and passes. That is why it failed in some runs and not others, and it is one of the three failures on `main`'s e2e lane at `3830ead`.

**What it decided.** Only the cell. The query and the column set are untouched.

**What it left behind.** Nothing owned. `/admin/access` passes axe on both projects with gate attempts in the range (12 in the last 30 days in the local e2e database).

### B-278 — the receipt named one arbitrary unit for a payment that settled several, and the nav sent a payer to somebody else's bill (2026-09-10, `ff797e3`)

**What it built.**

1. **`paymentCredits(paymentId)`** (`lib/billing/allocation.ts`, next to `postPaymentLedger`). It groups a payment's `type: 'payment'` ledger entries by lease and returns one row per lease, sorted by unit number (lease id breaks ties). It also returns the balance across exactly those leases. The receipt screen, the pay-link receipt and the emailed receipt all read it.
2. **`paymentReceipt`** drops `ledgerEntries … take: 1` (which had no `orderBy`) and returns `credits` plus a `balanceCents` summed over them. **`/portal/pay/done` and `/pay/[token]/done`** keep the old Unit row when one lease was credited. When several were, they render a `<table>` inside `ScrollRegion`, with a caption, `th scope="row"` per unit and a Total row. Two new keys in both dictionaries: `rcpt.creditsCaption` and `rcpt.total`. The pay-link page is English-only, as the rest of it already was.
3. **The emailed receipt** (`payment.succeeded` extender in `lib/comms/service.ts`) overrides `unit.number` with an `Intl.ListFormat` list of the credited units, and `balance.total` with the balance across them. It falls back to the recipient's lease only while nothing has posted.
4. **The portal nav** (`app/portal/layout.tsx`): when exactly one lease owes and it sits on an account the viewer pays (`portalAccountsFor`, `payable`), Pay opens `/portal/pay?account=`. Its label carries the account's net balance, and the link disappears when that balance is ≤ 0.
5. **Tests.** `portal-payment.test.ts`: three credited leases written out of unit order come back as three rows in unit order, summing to the payment, with the three-lease balance, and a second read gives the same result. `comms-billing-db.test.ts`: a split payment's email names "C-7 and C-8" and the balance across both, where the old code named C-7 with C-7's $0.00. `portal-billing-account.spec.ts`: no Pay link on the payer's page carries `lease=`.

**What it decided.**

- **The emailed half was traced, and the defect was real.** The row marked it "needs confirmation at build time". Tracing found that the recipient's lease is the earliest allocation's invoice, falling back to the newest occupying lease. So a split payment was receipted as one unit with that lease's balance, and a payer holding no unit was receipted with an employee's unit. It was fixed here rather than raised as a row, because it is the same payment and the same entry set.
- **Only `type: 'payment'` entries count.** A returned payment's `adjustment` carries the same `paymentId` (`reversals.ts`). The old `take: 1` could have landed on it.
- **The nav's label follows its destination.** The demo account has $161 owed on one unit and $75 credit on the other. Pointing at the account while still saying "Pay $161" would have opened a screen asking for $86. The label now says what the screen asks for.
- **Allocation is untouched** (D-28), and so is `payableLeaseWhere`.

**Verification.** Typecheck clean; lint clean (the 6 warnings were already there, in `mfa/actions.ts`, `portal/notifications/actions.ts` and `portal/refer/actions.ts`). `portal-payment` and `comms-billing-db`: 46 passed. `i18n`, `accessibility-statement`, `a11y-scan-coverage` and `scroll-regions`: 40 passed. `portal-billing-account.spec.ts` against a production build, both projects: 12 passed, 0 failed, 12 listed.

**What it left behind.**

- **The email's wording.** The seeded template still says "for unit {{unit.number}}", so a split payment reads "for unit C-7 and C-8" (Spanish: "por la unidad C-7 y C-8"). Pluralising it is a template edit in both languages, which is seeded state (B-206), so it did not go in here. No row owns it yet.
- **The multi-unit receipt table is not axe-scanned.** `/portal/pay/done`'s `succeeded` state is a stated exception in `scan-coverage.ts`, because the demo seed makes no payments. The statement's log entry says so.
- **The nav with SEVERAL owing leases still sums the positive balances and opens Overview.** For a payer whose account nets a credit, that figure can be higher than the account card's. It is pre-existing, and outside this row.
