# Decision Log — Self-Storage Business Application

**Status:** All 11 flagged gaps/conflicts from `06-backlog.md` resolved by the owner on 2026-07-30. D-12 (superuser) and D-13a–e (support impersonation) were raised and resolved later the same day, during the build.
**Precedence:** This document amends the PRDs. Where a PRD conflicts with a decision below, this document wins. When building from the backlog, treat each decision as settled — do not re-open unless the owner amends this log.

| # | Flag (from 06-backlog.md) | Decision | What it means for the build |
|---|---------------------------|----------|------------------------------|
| D-1 | Delinquency/lien + POS/reports MVP scope (PRD 02 vs master) | **Master's leaner MVP wins.** | MVP = online move-in, billing, reminders, basic late-fee schedule, occupancy & rent-roll reports. Full lien/auction pipeline, cash-drawer sessions, remaining reports, and field ops stay in Phase 2 (Milestones 5/7). Backlog phasing is correct as written. |
| D-2 | Marketing Phase 1 scope (PRD 04 vs master) | **Master wins.** | MVP marketing = SEO location pages, GBP checklist, basic lead capture. Reviews, drips, abandoned-reservation follow-ups, promo codes in checkout = Phase 2 labels, built together in Milestone 6 per backlog ordering. Promotions engine stays MVP (also required by PRD 02 US-10 / B-070). |
| D-3 | Kiosk mode (master P2 vs PRD 03 P3) | **PRD 03 wins: Phase 3, default no.** | B-085 remains an evaluation item only. No kiosk work before Phase 3. |
| D-4 | First real gate-vendor driver (master P2 vs PRD 03 stubs) | **PRD 03 wins: stubs only.** | Learning build ships the simulated adapter + manual adapter + stub vendor adapters (B-080). A real vendor driver (B-086) is Phase 3 and contingent on a partner agreement. |
| D-5 | Scheduled tenant rate increases (PRD 02 MVP vs master P2) | **Master wins: Phase 2.** | Stays at B-076. MVP has street-rate management only; existing-tenant increases with notice letters come in Phase 2. |
| D-6 | Stripe Billing vs ledger-driven charges | **Ledger-driven PaymentIntents.** | Our invoice ledger is the source of truth; it creates Stripe PaymentIntents on due dates and autopay runs. Do NOT use Stripe Billing subscriptions. PRD 05's CN-6 "Stripe Billing retry config" reference is void — retry schedule (+1/+3/+5) is implemented in our billing engine per PRD 02. Unblocks B-043–B-046. |
| D-7 | Reservation deposit policy | **Free hold, no card.** | Reservations require no card and no deposit; holds expire on a per-facility configurable window. PRD 02 US-14's "optional deposit" is dropped from scope; master OQ-3 closed. B-018 builds free-hold only. **Corrected 2026-08-15 by B-126 (see D-50):** this row said "default 7 days" and was wrong on both halves. PRD 01 US-401 has always specified "end of day after scheduled move-in date; configurable", and that is what `holdExpiryFor` has implemented since B-018 — so the "7 days" figure contradicted the feature PRD and the shipped code, not just one of them. The window is anchored to the renter's chosen MOVE-IN DATE, never the booking date, and B-126 built the configurable half US-401 already promised (`Facility.reservationHoldGraceDays`, default 1). Building D-7 literally would have been the defect: US-401 lets a renter pick a move-in up to 14 days out, so a 7-day-from-booking window would have taken the unit back a week before the day they reserved it for. |
| D-8 | Consent store ownership | **Shared schema package owns it.** | One `consent` table/package in the core schema (built at B-002, extended B-032/B-072). PRD 04 (marketing consent) and PRD 05 (SMS/transactional consent) both read/write through it; neither module owns the table. |
| D-9 | PRD cross-reference numbering | **Fixed editorially.** | Master §3 amended to five modules including `05-communications-prd.md`; PRD 02 sibling references corrected to actual filenames. No build impact. |
| D-10 | Operating state for legal defaults | **Texas, built configurable.** | Compliance config is per-state by design; Texas (Property Code Ch. 59 self-storage lien statute) is the seeded default for lien timelines, notice templates, late-fee behavior, lease clauses, and SMS disclosure copy. All legal artifacts are drafts requiring attorney review before any real-world use — this is a learning project, not legal advice. Unblocks Milestone 5 (B-056, B-061) and B-074. |
| D-11a | Autopay pre-charge reminder default | **On by default.** | Autopay tenants get a pre-charge notice (e.g., "card ending 4242 will be charged $X on {date}") a few days before the charge, plus receipts. Per-tenant opt-out. Applies to B-050 templates; closes PRD 05 Q1. |
| D-11b | Minor phasing splits | **Backlog's treatment stands.** | Spanish language: Phase 3 (bundled B-090). City landing pages: Phase 2 (B-082); generated size-in-city pages: Phase 3 (B-089). |
| D-12 | Superuser account (raised during B-004, 2026-07-30) | **No permission bypass. The `owner` role with an all-facilities assignment is the only super user.** | Never add an `isSuperuser` flag or any short-circuit in `can()` / `facilityScope()` / `checkMonetaryAuthority()`. Unrestricted access must always be an ordinary `StaffFacilityAssignment` row with `roleId = owner` and `facilityId = null` — grantable, revocable, and visible to the audit log (B-005) and the lien evidence chain. Bootstrapping the first such account is a `db:create-owner` script, scoped to **B-007**, which needs a staff login to demo role-gated routes anyway. **Not contradicted by D-13** — see PRD 09 §3 for why impersonation is not a bypass. |
| D-13a | Tenant notification on staff impersonation (PRD 09 OQ-1, 2026-07-30) | **Do not notify.** | No email, SMS, or in-portal alert fires when staff view a tenant's account. The `ImpersonationSession` row and dual-attributed audit entries are still written in full — this governs what the tenant is *pushed*, never what is *kept* (PRD 09 FR-16/FR-17). Consequence: oversight reporting (B-092) becomes the only channel where misuse surfaces, which is why D-13b is deliberately tight. Re-open if operating outside Texas — several state privacy regimes treat access disclosure differently (D-10). |
| D-13b | Default roles for impersonation permissions (PRD 09 OQ-2) | **Owner only, all four permissions.** | `impersonation:tenant`, `:staff`, `:write`, `:oversee` all seed to `owner` alone — tighter than the drafted `owner + regional`, because D-13a removed the tenant-facing signal. Widening to `regional`/`manager` is a seed change, not a migration; do it once B-092's reporting is live and usage is observable. |
| D-13c | `ImpersonationSession` retention (PRD 09 OQ-3) | **≥7 years, matching the audit log.** | Rows are a few hundred bytes and carry the accountability trail given D-13a. A shorter window would strand audit entries that reference them. Explicitly excluded from any shorter former-tenant PII purge; the rows hold no tenant PII beyond an id reference. |
| D-13d | Impersonated portal render target (PRD 09 OQ-6) | **The real portal at real URLs.** | Not embedded in the admin shell. Goal G1 is reproducing the user's exact view, and layout/responsive bugs are what people call about. URL segregation is therefore **not** a security control here — the banner, the service-layer write blocks, and the escalation guard are (PRD 09 FR-22/FR-23). |
| D-13e | Tenant-initiated support access (PRD 09 OQ-4) | **Deferred, not rejected.** | A portal-side "grant support access for 1 hour" button implies tenant awareness, which sits oddly with D-13a. If D-13a is ever reconsidered, reconsider this at the same time — they are a pair. |
| D-14 | Geocoding provider (PRD 01 OQ-6, raised during B-015, 2026-07-31) | **No vendor. A bundled US zip/city dataset (`zipcodes`, BSD).** | Follows D-4's precedent for undecided vendors: ship a real local implementation, slot a driver in later. Both OQ-6 candidates (Google Maps Platform, Mapbox) need a billed API key for what is public data that changes about once a year, and a network call on the hot search path. Offline and deterministic, so tests assert real coordinates instead of mocking a provider. **OQ-6 is not closed — it narrows to maps rendering (B-016, FR-1.3) and address autocomplete**, which do still need a vendor; typeahead at B-015 is served from our own facility list, not a gazetteer. Revisit if search must handle street addresses or non-US locations, neither of which a zip/city dataset covers. |
| D-15 | Customer-facing lexicon (raised by the UX review, 2026-07-31) | **One word per concept on every customer surface.** Recorded in full as PRD 01 §6.10. | **size** (not "unit type"), **unit**, **online price** / **in-store price** (not "web rate" / "street rate"), **gate hours**, **office hours**. The industry's words — unit type, street rate, web rate, lease, delinquent — stay on admin screens. No internal identifier (backlog ID, entity name, status enum) may render on a customer-reachable route, `/login` included, and a test asserts it. One money formatter for every customer-facing price (`formatRate()`, shipped in B-016): `$129/mo`, no trailing zeros. Binds every public-site and portal item from B-017 onward; the already-shipped copy is corrected in B-093. This is a product-owner call, not an owner escalation — recorded here because three surfaces had already drifted apart and PRD prose alone would not bind later items. |
| D-16 | Gate access suspension for non-payment, and what payment restores it (operator review, 2026-07-31) | **Accepted into MVP in full.** A single per-facility threshold suspends gate access at N days past due (Texas practice ≈ day 6, configurable), and access restores automatically within ~2 minutes of a **qualifying payment**, which is defined as **the balance going to zero** — rent, late fees, everything — stored as a per-facility setting so it can be relaxed later without a migration. | Built as **B-098**, after B-047, reusing the ACS from B-027 rather than waiting for the delinquency engine. Both transitions are audit-logged with the triggering invoice and day count, and both render on the tenant profile in plain English ("Access suspended, 12 days past due, 2026-07-18"). **Does not reopen D-1** — the configurable timeline, notices, lien and auction pipeline all stay Phase 2; only the single threshold moves forward, and B-057/B-058 inherit this rule rather than rebuilding it. **Closes PRD 02 §10 Q2** and **PRD 01 OQ-11**: paying restores access without staff action, and the portal may say so. Rationale for balance-to-zero over any partial rule: a partial-payment threshold teaches a tenant to pay the minimum that reopens the gate, every month, which converts a collections tool into a subscription for the delinquent. Emails do not collect rent; access does. |
| D-17 | Lapsed proof of insurance (operator review, 2026-07-31) | **Auto-enrol, per facility policy.** Notice at 30 days before the recorded proof expires, then enrolment into the facility's default protection tier when it lapses — configurable per facility, and applied regardless of what the lease captured at signing. | Written into PRD 02 US-44; the 30-day scan rides B-043's nightly jobs and the notices ship with B-050. **Trade-off, recorded honestly:** this charges a tenant for a term the lease may not have explicitly captured, which is precisely the exposure the narrower "flag and task only" option existed to close. The owner has weighed it and chosen the broader behaviour. Per D-10 all such text is a draft and not legal advice, and **this specific behaviour warrants an attorney pass before it runs against a real tenant** — the notice copy, the lease clause that authorises it, and whether the facility policy may apply retroactively to leases signed before the setting existed. |
| D-18 | **Amends D-4** — vendor stub adapters (operator review, 2026-07-31) | **One vendor stub, not two.** B-080 ships the adapter contract-test suite, the manual adapter, and **one** stub vendor adapter in a vendor-emulation profile. | D-4's "stubs only, no real driver" position stands unchanged; only the count changes. One stub proves the port/adapter contract survives a differently-shaped third-party API, which is the whole learning payload. A second stub proves nothing the first did not, at full cost, and becomes code nobody deletes because it looks like progress. A real vendor driver remains Phase 3 and contingent on a partner agreement (B-085). Recorded as an amendment rather than an edit to D-4 so the original decision and its revision both stay legible. |
| D-19 | Lighthouse performance gate vs the Core Web Vitals target (raised by B-016, 2026-07-31) | **They are two different numbers with two different jobs.** The **field** target stays LCP ≤2.5s (master §7.3, PRD 01 FR-9.1) — that is the Core Web Vitals threshold real users are measured against. The **lab** gate in CI warns at 2500ms and errors at 3000ms, with run aggregation pinned. | Lighthouse's mobile lab run applies simulated 4× CPU throttling and reports pessimistically **by design**; B-016's facility page reproduced 2612/2613/2613ms on a text-only LCP element with no image and no third party left to defer, and passed only because the default aggregation picked the most favourable run — a gate that is flaky rather than green. Warn-at-2500 keeps the signal; error-at-3000 keeps the gate meaningful. **Do not "tighten" the lab error threshold back to 2500 to match the field target** — they are not the same measurement, and a later session doing so would be reintroducing a known-flaky gate. The `lighthouserc.json` change is the owner's; this log holds the reasoning. |
| D-20 | Where autopay lives, and what re-auth guards (raised during B-036, 2026-08-04) | **Autopay is a per-lease flag (`Lease.autopayEnabled`); the card it charges is a per-tenant fact (`Tenant.stripeDefaultPaymentMethodId`). Both are required — either one missing means nothing is charged.** Re-auth (US-701) gates starting or redirecting money — adding a card, changing which card is charged, removing a card, switching autopay **on** — and deliberately **not** switching autopay **off**. | Autopay had no storage at all before this: "on" was inferred from the tenant merely having a saved card, which was wrong in both directions — a renter who opted out at checkout still read as On, and one who opted in read as Off, because `provisionMoveIn` never read the choice the checkout session had recorded. Per-lease because billing runs per lease (B-045), the dashboard already reports it per unit, and a tenant with two units can reasonably want one enrolled and one not. **B-045 must read both halves**, and must treat `autopayEnabled` with no default method as a configuration error to surface, not a charge to skip silently — the portal already warns the tenant about exactly that state. The re-auth asymmetry is the part most likely to be "tidied up" later by someone making the gate uniform: requiring a fresh login to **stop** a recurring charge is the one direction where the gate does harm, because a tenant locked out of their own account would keep being billed. Enabling is gated; disabling never is. |
| D-21 | The address of record, and what the `Tenant` address columns are now (raised during B-037, 2026-08-05) | **`TenantAddress` is append-only and authoritative; `Tenant.addressLine1`/`city`/`state`/`postalCode`/`country` are a cache of the newest row, written in the same transaction.** Every address change anywhere goes through `recordAddressChange()` — portal now, counter at B-038, returned mail after that. Each row stores the address as of that moment plus source and actor; the previous row IS the old value. | US-13 requires the current address to be "derived from the latest row, never overwritten", and the reason is evidentiary: on day 40 of a lien cycle "which address did the notice go to" has to be answerable from records. A pure derivation would have meant rewriting every existing reader (lease template, comms context, admin screens) in this item, so the columns stay as a cache instead — **but a path that writes them directly leaves the history with a gap exactly where a dispute looks.** Storing an old/new pair per row was rejected: the previous row already is the old value, and holding it twice creates two things that can disagree about one fact. Existing tenants were backfilled with an `import` row in the migration, so "newest row" is true from the first read rather than only for tenants who later edit. B-038 and B-061 both depend on this being complete. |
| D-22 | How receipt numbers are made gapless (raised during B-039, 2026-08-05) | **A `ReceiptCounter` row per facility, incremented by `INSERT … ON CONFLICT DO UPDATE … RETURNING` inside the same transaction that writes the payment.** Not a Postgres sequence, not `@default(autoincrement())`, not `MAX(receiptNumber) + 1`. Only counter-taken payments get a number; the online Stripe path leaves it null. | US-32 requires receipt numbering to be **gapless**, which is a strictly stronger guarantee than unique. A sequence does not roll back — an aborted transaction burns its number and leaves a hole no one can explain to an auditor, which is the exact failure the requirement exists to prevent. The `UPDATE` takes a row lock that serialises concurrent counter staff, and because the increment lives in the payment's own transaction, a rollback returns the number to the pool. `MAX + 1` was rejected for the opposite reason: it is not safe under concurrency without an explicit table lock, which is heavier and easier to get wrong. **Do not "simplify" this into a sequence or an autoincrement column** — both look tidier and both break the only property that matters. Tests assert all three behaviours (starts at 1 per facility, no hole after a rollback, five concurrent payments get consecutive numbers). Online card payments are deliberately excluded rather than numbered: a receipt book with holes where the web rentals were is not gapless, just confusing. |
| D-23 | Building B-095 out of its backlog position, and what the Task entity looks like (raised 2026-08-05) | **B-095 was built now, ahead of B-041, rather than left as the "left behind" gap several already-shipped items (B-026 among them) had been carrying it as.** `Task` is one table: `facilityId`, a loose `type` string resolved against a catalog (`packages/core/tasks`) for its label/proof requirements/audit sensitivity, `entityType`/`entityId` (mirroring `Document.subjectType`/`subjectId`), `businessDate`, `priority`, nullable `assigneeStaffId`, `status`, a `proof` JSON blob, and completion attribution. Idempotency key is `(type, entityId, businessDate)` — a redelivered event that would create the same task on the same facility-local day is a no-op. | The backlog itself orders B-095 (row 25a) before B-026 (row 26), which already depends on it; it was skipped when B-026 shipped and stayed skipped through a dozen later items, each noting the same gap in its own "left behind." B-041 is the first item whose own description requires "a Task view" outright, not just "an event with no consumer" — so building around it again would have meant a third bespoke queue, exactly what "one task queue, not seven" exists to prevent. The loose `type` string (not an enum) matches `Document.type`'s own precedent for the identical reason: a new task type is a catalog entry, not a migration. Two real consumers were wired immediately rather than shipping the table empty: move-in provisioning failures (`requestDownstream`, closing the gap B-026's own comment named) and returned-mail review (closing PRD 02 US-13's own AC, already reachable from a shipped B-038 button). **`missingProofFields` fails closed for an unregistered type** — it falls back to requiring the universal `note` field rather than requiring nothing, after a test caught the first version doing the opposite (an empty required-list for an unknown type meant nothing was ever "missing"). Permission-wise this reuses `tenants:view`/`tenants:edit` rather than minting `tasks:*` — matching `nav.ts`'s own pre-existing comment ("gated the same as Tenants for now") rather than deciding a new permission shape now, ahead of the six more consumers still to come. |
| D-24 | How a pending portal move-out request is represented, and why the portal enforces notice while staff don't (raised during B-041, 2026-08-05) | **No new column, no new model.** A pending request is `Lease.status = 'active'` with `moveOutDate`/`moveOutReason`/`noticeGivenAt` already set — the exact same three columns B-040 built for the *finalized* case. "Pending" versus "finalized" needs no flag: `status` is the discriminator (`ended` = finalized, `active` with a `moveOutDate` = pending). **The portal enforces the facility's notice-days policy as a hard floor on the date picker; the staff screen (B-040) still only reports a shortfall.** | Adding a `MoveOutRequest` table or a `pending` lease status would have meant either duplicating the columns B-040 already carries or teaching every reader of `moveOutDate` about a new source of truth — reusing the columns costs nothing extra and cancelling (clearing them back to null) is symmetric with never having asked. The enforcement asymmetry is deliberate, not an oversight: staff route around real-world urgency constantly (a tenant already gone, an abandonment with no notice at all) and B-040's own AC treats a shortfall as informational for exactly that reason — but a tenant scheduling ahead through self-service has no such urgency and no legitimate reason to need an exception, so the portal's date input simply will not let them pick a date the policy forbids. **If a "staff can move the date forward on the tenant's behalf" flow is ever built, it must go through B-040's advisory path, not a relaxed version of this one** — the two screens are allowed to disagree about enforcement because they answer different questions ("what does this tenant's account say" vs. "what actually happened at the property"). |
| D-25 | The metric definitions themselves, settled once (raised during B-042, 2026-08-05) | **`packages/core/metrics` owns every figure, and no screen, tile or export computes one inline.** The judgement calls, stated rather than inferred: **rentable excludes `unrentable` and INCLUDES `maintenance`**; **`overlocked` counts as occupied**; **economic occupancy is collected ÷ gross potential at street**, measured across every *rentable* unit rather than only occupied ones; **`daysPastDue` anchors to the OLDEST unpaid invoice's ORIGINAL due date**; AR buckets are 0–10 / 11–30 / 31–60 / 61–90 / 90+ with 90 landing in `d61to90`; **roll-ups sum the components and recompute the ratio, never average the ratios.** | §4.11's AC demanded these be a module with written formulas and tests, and its stated reason is the one that matters: "The first time the owner dashboard says 91% and the rent roll says 88%, he stops trusting both." Each call has a failure it prevents. Excluding maintenance from rentable would flatter occupancy by exactly the units an operator is slowest to turn around. Forgetting `overlocked` means occupancy silently *drops* the moment B-057 lands, reading as units emptying when nobody moved out. Measuring economic occupancy against occupied units only lets a site be "97% economically occupied" with a third of tenants not paying. Anchoring `daysPastDue` to a retry attempt (US-20 retries on +1/+3/+5) resets the clock on every retry, so a 40-day delinquency reports as 2 days and never reaches the day-6 access suspension or any lien step — the tenant simply stops being visible to the system meant to escalate. Averaging ratios makes a 100%-occupied 4-unit site and a 50%-occupied 400-unit site read as 75% instead of the true 50.5%. **All six are asserted in `tests/metrics.test.ts`, including the roll-up-equals-sum property the AC required be a test rather than a sentence.** |
| D-26 | Defaulting D-17's auto-enrolment to OFF per facility (raised during B-043, 2026-08-05) | **The behaviour is built exactly as D-17 specifies; the per-facility switch that turns it on ships `false`, and the tier it enrols into ships unset.** A lapsed proof therefore raises a high-priority staff task and charges nothing until a facility deliberately turns it on in Settings. | **This does not re-open D-17** — the owner chose auto-enrolment and it is implemented, tested and one dropdown away. It defers only the moment it first bills someone. D-17's own text is the reason: it records that this charges a tenant for a term the lease may not have captured, and that the behaviour "warrants an attorney pass before it runs against a real tenant." A default of `true` would have started charging the instant a facility was seeded, before that pass could happen; a default of `false` makes turning it on a deliberate, audited act by a named person at a named facility. Two guardrails ship with it: the settings form refuses to enable auto-enrolment with no tier chosen, and the scan refuses to enrol when the configured tier is no longer on sale rather than guessing a premium — in both cases the task is still raised, so a person decides. **Owner action:** flip this on per facility once the attorney pass on the notice copy and the lease clause is done. |
| D-27 | Anniversary billing as the default, and what the billing day is anchored to (raised during B-044, 2026-08-05; owner chose anniversary) | **`billingPolicy` is per facility and ships `anniversary`.** A lease's billing day is the **facility-local calendar day it started**, clamped to 28. The move-in payment therefore buys a **whole period starting that day**, so nothing is prorated on the way in and the tenant's due date never moves. `first_of_month` is built and selectable; `invoiceLeadDays` (default 5) and `prorateOnMoveIn` are per-facility alongside it. | The alternative — everyone bills on the 1st — forces a prorated partial period on every single move-in, which is the most error-prone invoice a system produces and the one tenants query most. Anniversary makes the ordinary case a full month with no division at all. **Three things this settled that were previously wrong or hardcoded, each now fixed:** `billingDay` was hardcoded to `1` at move-in, so a renter who paid a full month on the 20th would have been invoiced again on the 1st — a double-charge eleven days later. The day is now derived from the **facility-local** date, not `new Date()`'s UTC day, because at 10pm in Texas the UTC date is already tomorrow and that renter would have carried a wrong anniversary for the life of the lease. And the **lease document** stated "if you move in part-way through a month, that first payment is prorated" — under anniversary that is false, and it is a signed term the operator would have had to honour; the sentence is now a merge field that states what the facility's policy actually does. **Not settled here:** under `first_of_month` with `prorateOnMoveIn`, the checkout still charges a full first month — the nightly engine handles that policy correctly but the checkout side of it is unbuilt, and is only reachable by a facility that switches away from the default. |
| D-28 | How an autopay charge is tied to the invoice it pays, and when that link is written (raised during B-045, 2026-08-05) | **A charge that settles exactly one invoice names it, and its `PaymentAllocation` is written in the same transaction as the pending `Payment` row — BEFORE Stripe is called.** The webhook then recomputes `Invoice.amountPaidCents` as the sum of allocations on **succeeded** payments and sets the status from it. `Invoice.amountPaidCents` is never incremented. | The whole "autopay never double-charges" guarantee rests on the invoice reading as paid afterwards — without settlement the charge would succeed, post to the ledger, and leave the invoice open for the next night's run to charge again. **Writing the allocation before the Stripe call is the load-bearing part:** if Stripe charges the card and the process dies before the webhook lands, the allocation already exists, so the next run sees an attempt in flight and skips rather than charging a second time. A pending allocation counts toward nothing, because the paid total sums succeeded payments only, so it cannot make an unpaid invoice read as paid. **Recomputing rather than incrementing** is what makes Stripe's at-least-once redelivery harmless — an increment applied twice is exactly the bug redelivery causes. **B-048 extends this, it does not replace it:** partial payments spread one payment across several invoices in a configurable order, which is more allocations written by a different rule; the settle-from-allocations direction and the recompute stay. A payment that names no invoice still gets no allocation invented for it. |
| D-29 | When a failed payment reaches a person, and what the tenant hears (owner decision, 2026-08-06) | **Flag the site manager on the SECOND decline, and message the tenant once a day for three days from the FIRST.** The manager flag is the existing `failed_payment` task, raised at high priority; the retry schedule keeps running after it. The tenant cadence is three messages on three consecutive days, not one per retry attempt. | Waiting for the whole +1/+3/+5 schedule to finish means nobody looks at a failing card for six days, by which time the tenant is most of the way to a late fee over something a phone call usually fixes — so the flag moves to two declines while the automatic path continues; a person and a retry are not alternatives. The tenant cadence is deliberately decoupled from the attempts: retries land on days 1, 3 and 5, so attempt-driven messages would go silent on days 2 and 4, and the day after a tenant first hears — when they are most likely to act — would say nothing. Three days is the whole cadence and it stops there whether or not the retries have. **Channel caveat, stated rather than assumed:** the owner asked for a text, and MVP comms is email-only (PRD 05 FR-4) — the Twilio channel, quiet hours and STOP/HELP handling are **B-074**. This ships as an event (`payment.retry_reminder`) routed through the existing comms service, so it is an email today and becomes a text when B-074 configures the channel, with no code change at the emit site. **Until B-050 writes the rule and template there is no message at all** — the event fires and no rule consumes it. | 
| D-30 | What a pay-now link actually grants (raised during B-051, 2026-08-06) | **A pay link is not a session.** It grants exactly one screen — `/pay/<token>` — for exactly one lease, and nothing else in the application knows the token exists. It is a `PayLink` row with a SHA-256 of a 256-bit token, multi-use for 7 days, revocable, and revoked on move-out. It does NOT sign anyone into `/portal/*`. | CN-4 asks for a link that authenticates a tenant "into the tenant portal's payment screen ... without a password", and the literal reading — mint a portal session — is the dangerous one. `/portal/*` reveals gate codes (PRD 03 SR-2), lets a tenant change their address of record, and lets them detach the card autopay runs on. These links go in emails: forwarded to a family member, sitting in a shared inbox, read over a shoulder. Scoping by session flag would mean every portal page had to remember to check the scope — fail-OPEN, and wrong the first time someone adds a page. A separate route is fail-closed by construction: every other route sees an anonymous visitor. The cost is that the pay screen is its own page rather than the portal's, which is a deliberate deviation from CN-4's wording. **Deliberately multi-use, unlike `AuthToken`:** CN-4 wants the balance "as of page load, not as of send", so a tenant who opens the link, gets distracted and returns an hour later must not find it dead — that is the exact failure the item exists to remove. Safe because the link is not a session: the worst a stale one does is show a balance and offer to pay it. **The update-card link stays password-gated**, because changing what autopay charges from then on is more than this grants. |
| D-31 | Adding `halt_autopay` to the hold effects US-42 listed (raised during B-096, 2026-08-06) | **A sixth effect beyond US-42's five: stop charging the saved card.** Carried by `military_scra`, `bankruptcy`, `deceased` and nothing else. | US-42 lists halt dunning, halt late fees, halt access suspension, block auction and suppress marketing — all of which stop us *asking*. None of them stops us *taking*. An automatic stay under Chapter 7 makes a charge against a debtor a violation, and charging a dead person's card is its own problem; halting the chasing while continuing to help ourselves to the balance on the same lease would be the worst of both, and would look deliberate. Deliberately NOT carried by `dispute` — a disagreement about one charge is not a reason to stop taking a payment the tenant chose to make — nor by `do_not_contact`, which is a channel instruction rather than forbearance. **Subject to the same attorney pass US-42's own note demands** (D-10): the mechanism is built now because the exposure is uncapped while it does not exist, and which effects each type must carry in Texas is a legal question this project has not answered. |
| D-32 | Which URL a facility page lives at, where two PRDs disagree (raised during B-066, 2026-08-07) | **`/storage/{state}/{city}/{facility-slug}` stays.** PRD 04 US-1 AC1's `/storage-units/…` is superseded by this entry. | PRD 01 US-103 and PRD 04 US-1 AC1 specify different paths for the same page, and neither argues for its choice. `/storage/…` is what B-016 shipped, what the search page links, what the sitemap lists and what has been indexable since. Changing it would mean a site-wide 301 and a re-crawl to buy nothing a renter or a crawler can perceive — and the shorter form reads better in a result. **The 301 map built in B-066 exists for facilities that move, not for this**; if the scheme ever does need to change, that map is what makes it survivable rather than a traffic event. |
| D-33 | Whether manually transcribed reviews may power the `aggregateRating` JSON-LD (PRD 04 Open Question Q3, raised during B-071, 2026-08-10) | **No. `aggregateRating` is withheld until every review behind it is `google_api`-sourced (FR-REV-4, Phase 3).** Manual entry (`manual_google`/`manual_other`) still populates the on-page average and the visible review list (US-6 AC1/AC2) — the gate applies only to the structured-data markup that tells Google's crawler the rating is genuine and independently sourced. | Q3 asked whether staff-transcribed Google reviews "qualify" for review-snippet structured data or whether the site should "hold `aggregateRating` until API ingestion." They do not qualify: Google's structured-data policies for review snippets require the ratings be independently verifiable as collected by the site marking them up, and a transcription of a rating authored and verified on a DIFFERENT platform (Google's own) is republished third-party rating data, not this site's own collection of it — exactly what those policies exist to catch. The stakes are asymmetric with the upside: a rich-result star rating on one facility page is worth little against a manual action that can suppress rich-result eligibility **sitewide**. `qualifiesForSchemaMarkup` (`packages/core/reviews/aggregate.ts`) encodes the rule as `every review source === 'google_api'` rather than a feature flag someone could flip early, and it was already anticipated at B-066 — `selfStorageJsonLd`'s `aggregateRating` parameter has carried the comment "passing a fabricated one is the fastest way to a manual action against the whole domain" since before this entity existed. **Revisit only when FR-REV-4 ships** (Google Places/Business Profile API ingestion) and only for the facilities whose reviews are entirely API-sourced by then. |


| D-34 | Whether B-071's `review_request` is retroactively gated on the `marketing_email` consent this item introduces (raised during B-072, 2026-08-10) | **No. `review_request` (B-071) keeps its existing, CAN-SPAM-legal opt-out posture — a live unsubscribe link plus suppression, no prior-consent gate. The lead drip (this item, three NEW templates) is the one gated on "no consent, no sequence."** Both share the new FR-MSG-5 quiet-hours/daily-cap floor and the new one-click unsubscribe link, applied centrally to every `marketing`-classified send regardless of which item shipped it. | US-13 AC1's opt-in language reads as a blanket policy, and FR-MSG-4 lists "lead drip, abandoned reservation, review request" together as the three sequences the rule governs — a literal reading would gate all three. It was rejected for `review_request` specifically because tenants have no consent-capture point yet: this item's checkbox lives on the anonymous lead-capture form (US-8), and a tenant who already has a lease reaches that form only as a stranger, before any relationship exists. Gating retroactively would have meant EVERY review request stops firing the day this item ships, for every existing tenant, with no way for an operator to turn it back on — silently breaking a feature shipped one item ago to satisfy a consent flag nobody had a chance to grant. Applying the parts that do not require a missing capture point (the unsubscribe link, quiet hours, the daily cap) costs nothing and closes a real gap; applying the parts that do (prior opt-in) was worse than the inconsistency it would have removed. **Revisit when tenant-side marketing consent capture exists** — the natural home is alongside B-074's SMS consent center, or checkout — at which point `review_request` can gate on it the same way the drip does. |
| D-35 | What FR-LEAD-4's "reservation-started event" maps to, and whether `CheckoutStatus.abandoned` gets used (raised during B-073, 2026-08-10) | **`CheckoutSession` is the entity, not the free-hold `Reservation`. Its existing `abandonmentSequenceStep Int` counter (0–3) tracks the sequence; `CheckoutStatus.abandoned` is never written.** | US-9 AC1 describes "unit selected, contact info captured" — checkout step 1 — which only `CheckoutSession` models; `Reservation` is D-7's free, no-form hold and has nothing to abandon mid-fill. A dedicated counter (the same shape `Lead.dripStep`, B-072, uses) was chosen over writing the `abandoned` status because the first follow-up cannot fire before 60 minutes have passed, by which point the 30-minute lock has ordinarily already lapsed to `expired` on its own — mutating `status` again here would risk clobbering a still-genuinely-active session mid-step, and the checkout page's own `relock` recovery UI already treats a lapsed lock as recoverable regardless of how it got that way. |
| D-36 | Which "reminder/dunning" templates get an SMS variant, where the backlog line and PRD 05 CN-13 disagree (raised during B-074, 2026-08-10) | **`dunning_step` (the delinquency-stage ladder, D-16/CN-3) and both lien-notice supplements stay email-only, permanently — never an SMS variant, never texted as a fallback either.** SMS variants ship for `invoice_due_soon`, `invoice_due_today`, `payment_retry_reminder`, `payment_method_expiring` and `access_suspended` instead. | B-074's backlog line reads "SMS variants of all reminder/dunning templates," which taken literally includes `dunning_step` — PRD 05's own CN-13 AC says the opposite: "legally significant messages (delinquency stages, lien supplements, rate increases) are email-mandatory and cannot be toggled off." `dunning_step` (fired from `delinquency.day_reached`) IS CN-13's "delinquency stage," so the backlog's plain-English phrasing and the PRD's explicit legal carve-out point in different directions for the one template that matters most. The PRD wins: CN-13's reasoning is a real TCPA/consumer-protection concern (a text is easier to lose than an email, and a collections-adjacent notice is exactly the one that should not depend on a channel with no delivery record), and a backlog one-liner compressed for scanning is not the place a legal carve-out gets silently overridden. `access.suspended` is SMS-eligible despite being non-payment-triggered, because it is a consequence of non-payment rather than a delinquency STAGE itself — CN-13's own parenthetical names dunning/lien/rate-increase, not an access consequence — and a locked gate is exactly the kind of thing a tenant wants to know about the moment it happens, which a same-day email does not guarantee. |

| D-37 | Where a scheduled rate increase lives, and what CN-9's "also queued to postal mail" means for this item (raised during B-076, 2026-08-10) | **A new `TenantRateIncrease` model carries the workflow; `LeaseRateChange` stays append-only history and gains only a nullable `rateIncreaseId` back-reference. The notice is EMAIL ONLY — the postal/`Notice`-document path is deliberately not built.** An applied increase writes its `LeaseRateChange` row through one shared `applyRateChange()`, which is also the write-through US-11's schema AC demands for `Lease.monthlyRateCents`. | US-11 needs a row with a lifecycle (pending → approved → notice sent → applied, plus cancelled), and `LeaseRateChange` is the table a billing dispute reads to answer "what rate applied on this date". Putting a mutable status column on it would make that history editable, and a cancelled increase would leave a history row for a change that never happened. PRD 02 §5's own data-model line already names `TenantRateIncrease` as a separate entity from `RateChange`, so this follows the PRD rather than inventing a split. **On the notice channel:** CN-9 scopes itself explicitly — "PRD 02 owns the mail queue and the legal-sufficiency call — this module only sends the electronic copy and records it" — and the `Notice`/`NoticeTemplate` document machinery it would need is driven by a per-state "requires written notice" flag that D-10's attorney pass has not settled (PROGRESS.md still lists the rate-increase notice period itself as open). Building a statutory-document path against an unsettled legal requirement would produce a served notice nobody has verified is sufficient, which is worse than an email plus a documented gap. **Revisit when the attorney pass lands** — `NoticeType.rate_change` already exists in the enum for exactly that. |

| D-38 | Move-out proration divided by the wrong denominator, found while building the transfer (raised during B-077, 2026-08-10) | **Fixed, not documented around. `proratedCredit` now delegates to B-044's `unusedRemainder`, so the denominator is the BILLING PERIOD and the arithmetic reconciles exactly. Refunds on anniversary-billed leases change — they get slightly larger.** `settleMoveOut` gained a required `period` argument; both callers (admin and portal) now pass `billingPeriodFor(...)`. | US-18's AC states the formula: *"daily rate = monthly rate / days in billing period"*. `proratedCredit` divided by the days in the move-out date's CALENDAR MONTH, which under D-27's default (anniversary billing) is a different number — a lease billing on the 20th has a 20 Aug–20 Sep period of 31 days while `daysInMonthOf(5 Sep)` is 30. Worse, its day-count and the charge side did not tile: for a 1 Aug–1 Sep period with a 15 Aug move-out it refunded 16 days while the charge covered 14, so one day of every prorated move-out was billed to nobody and quietly kept. B-044's `unusedRemainder` exists precisely to make `charged + refunded === the full period` hold to the penny, and a regression test now asserts that across all 31 days. **Found rather than sought:** B-077's transfer is "a prorated move-out and a prorated move-in on the same day" (the proration module's own header), so shipping it against the old function would have produced a transfer whose two halves used different arithmetic — the inconsistency was unavoidable, and fixing the older side was the smaller change. US-14 line 186 and US-18 line 194 both already claimed this math was "built once"; it now is. |

| D-39 | Whether a merchandise sale is an invoice line, and where its revenue is reported (raised during B-078, 2026-08-10) | **Neither. A merchandise sale is its own `MerchandiseSale` entity paid at the point of sale, and `REVENUE_CATEGORIES` stays the four it has always been. US-34's "simple COGS report" is where merchandise money is reported.** | A merchandise sale is paid in full the moment it happens: it never ages, never dunns, never enters the allocation order and never appears on a statement. `REVENUE_CATEGORIES`' own comment explains why it is deliberately the same four the allocation order uses — "'how much rent did we collect' and 'where did this payment go' have to be the same question or the report cannot be reconciled against a tenant's ledger." Adding a fifth category for merchandise would either break that identity or force a fifth entry into the facility-configurable allocation order, where it would be meaningless (nothing allocates a payment to merchandise). B-055's own note said the category "arrives with retail POS" — this item is that, and the honest answer turned out to be that it arrives as its own report rather than as a fifth column. The existing test asserting a `merchandise` line item contributes 0 to `billedByCategory` therefore stays true and unchanged, because nothing creates such a line item. |

| D-40 | Whether staff MFA is a per-org toggle, and what happens to the staff magic-link path (raised during B-079, 2026-08-10) | **MFA is mandatory for every staff account, with no toggle and no per-role exemption. Enforcement is at the ADMIN SURFACE, not at sign-in: an unenrolled staff member authenticates normally and then reaches `/mfa` and nothing else. Staff magic links are refused at both ends — `requestMagicLink` will not mint one, and the credentials provider will not spend one.** | Master PRD §7.1 states it flatly — "Staff auth requires MFA (TOTP) from Phase 2" — as a property of the system, not a setting. A toggle would be a column whose only correct value is `true`, plus a screen to set it, plus a code path for `false` that exists to be a hole; and the first time an operator turned it off "just for the new hire" it would stay off. **On enforcement placement:** blocking the sign-in of an unenrolled account would leave a new hire with no way to ever enrol, and would break the `system` integration accounts that authenticate but never browse the admin. Putting the gate in the admin layout also means it is re-read from the database on every request, so an administrator's reset takes effect immediately rather than after the remaining 30 days of a JWT. **On magic links:** a link that signs somebody in on possession of their inbox IS a second factor, and offering it beside a mandatory TOTP prompt would mean every enrolment could be walked around with one click. §7.1's own sentence assigns the two mechanisms separately — magic-link fallback is named for tenants. Staff who forget a password still have the reset flow, which lands them at a sign-in that asks for the code. |
| D-41 | Whether an org-level default is resolved at runtime or pushed, and how "overridden" is recorded (raised during B-079, 2026-08-10) | **Pushed, never resolved. A push writes ordinary effective-dated rows into the receiving facility's own tables, so every downstream reader keeps reading exactly one place. "Overridden" is therefore a COMPUTED comparison against the live values, not a stored flag.** Notice templates are the deliberate exception and get no push at all — `MessageTemplate` already resolves org-level → facility override at render time, so the org default is already live everywhere that has not diverged. | US-4 says "push them to selected facilities," and a runtime fallback is not a push: it would change what a facility charges the moment somebody edited a central record, with no facility-level event, no effective date and nothing in that site's own history saying what happened. Every one of `FeeSchedule`, `LateFeeRule` and `DelinquencyTimeline` is already effective-dated or versioned precisely so a price change is reconstructable, and a silent central override would defeat all three. **On the flag:** an `isOverridden` boolean would be a second source of truth that goes stale the first time anyone edits a facility fee directly — which is the ordinary way fees get edited — whereas comparing the live values cannot drift because there is nothing to keep in sync. The comparison also names *what* diverges rather than merely that something does, because "Overridden" alone sends an owner to inspect twelve sites, which is the work the screen exists to save. **On templates:** pushing there would write a per-facility copy of a template the facility was already inheriting, turning every site into an override of the thing it was tracking — the exact opposite of the intent. What was genuinely missing was the visibility half, which is what shipped. |

| D-42 | Whether the gate adapter port gains a read side, and what "no drift" is allowed to mean (raised during B-080, 2026-08-10) | **The port gains `snapshot()`, and it is REQUIRED of every adapter rather than optional.** An adapter that genuinely cannot enumerate returns `{ verifiable: false, reason }`, which is recorded as a run and rendered as "nothing here has been verified" — never as zero drift. | FR-9's expected-vs-actual diff is impossible against a write-only port: B-027's outbox can prove a command was accepted and can never prove the controller still agrees a month later, which is the exact failure mode that matters (a dead-lettered revoke, a vendor-portal edit, a controller restored from backup — none of them raise an error, the former tenant's code simply keeps working). Making the method **required** rather than optional is the load-bearing part: an optional one forces every caller to ask "does this adapter support reading back?", and the honest answer for the manual adapter — "no, so nothing at this site is verified" — is precisely what a reconciliation report must be able to say out loud. A facility that could not be checked is therefore still recorded, because a site that silently drops out of the report is one nobody notices has been unverified for six months. |
| D-43 | What the one vendor stub is actually for, and what it found (raised during B-080, 2026-08-10) | **`PtiCloudAdapter` (our driver) plus `pti-emulator.ts` (a fake vendor), split deliberately. The emulator speaks a shape that is NOT ours** — its own user ids, one call that sets PIN + status + window together, numbered "time zones" instead of schedules, HTTP-ish status codes the driver must classify itself, and a paged list endpoint. | D-18 kept exactly one stub because "one proves the port/adapter contract survives a differently-shaped third-party API, which is the whole learning payload". A stub shaped like our own port would prove nothing, so the emulator was written to be inconvenient in the ways real vendor APIs are. **What it found:** the port survived three of the four frictions unchanged, and the fourth is a genuine limitation worth writing down — `set_time_window` accepts an arbitrary weekly schedule, and this vendor can only express "always" or "the site hours configured in the vendor portal". The adapter degrades honestly (the window fingerprint reports what the gate is *actually* enforcing rather than what we asked for) and reconciliation compares like with like per adapter, so a PTI site does not report drift on every credential forever. A future driver author should expect this rather than discover it. The other friction worth noting: because the vendor sets PIN and status in one call, the driver has to read grant state before pushing a code — otherwise rotating a suspended tenant's code silently restores their access. The contract suite now asserts that for every adapter. |

| D-44 | Splitting B-081 rather than building it whole (raised during B-081, 2026-08-10) | **Split into B-102–B-107, one feature each.** The map view (B-107) is explicitly **blocked** until §10 OQ-6 has a maps vendor, and is ordered last for that reason. The dropped size-estimator quiz stays dropped and is carried into none of them. | Backlog note 8 always intended this — "Phase 2/3 items are bundled... split them when they're reached" — and B-081 is the first bundle where splitting is clearly right rather than merely allowed. B-078 (POS depth) and B-080 (gate hardening) were each built whole because their parts shared a schema and a screen: a drawer session and a merchandise sale are one counter, and reconciliation, health and rotation are one integration. B-081's six parts share nothing — a mapping vendor, a payment method, a statements read model, an insurance workflow, a gate-access surface and a rewrite of the checkout core touch six different PRD sections and six different tables. Building them in one session would produce a commit nobody can review and a PROGRESS entry that cannot say what was decided. **B-106 is called out as the invasive one**: it is the only part that changes checkout's core assumptions (one unit, a near-term start), which is the substantive reason it is not bundled with the four cheap portal surfaces. |

| D-45 | What an accepted-but-unsettled bank debit is allowed to do (raised during B-103, 2026-08-11) | **A new `PaymentStatus.processing` that posts NOTHING to the ledger and settles no invoice — but suppresses late fees, dunning and gate suspension, and provisions a move-in without waiting.** Bank debit at checkout is per-facility and **off by default** (`achAtCheckoutEnabled`); in the portal and for autopay it is always available. | ACH is accepted now and can fail four business days later, which is a state the card path never had: a card is approved or declined in seconds. Three things had to be decided separately. **(1) The ledger does not move.** An invoice that reads paid on money that has not arrived is a lie the moment the bank reverses it, and it would let a delinquent tenant's gate access auto-restore (D-16) on a debit that later bounces. It also means a bounce needs no reversing entry — there is nothing to reverse — which removes the whole class of correction bugs the alternative would have introduced. **(2) The tenant is left alone anyway.** The money has left their account and nothing they can do makes it arrive faster; charging a late fee or opening a dunning ladder for those four days is the most avoidable way to make somebody who paid on time feel cheated. Suppression is scoped by tenant-at-facility rather than by invoice, because a portal payment does not always name an invoice and an allocation-based lookup would miss the commonest case — the cost is that a tenant with two units at one site gets both left alone for a few days, an error in their favour and bounded by the settlement window. **(3) The move-in does not wait.** Making a renter wait four business days for a unit they have paid for is not a product. That is a real risk — a unit and a gate code handed over against reversible money — which is exactly why it is a per-facility switch that is off until an operator turns it on, and why a late bounce raises its own high-priority task rather than only a ledger row. §3 already scoped ACH as "portal payments and autopay; **optional** at checkout"; this is that option, made explicit. |

| D-46 | Maps vendor for map *rendering* — the half of PRD 01 §10 OQ-6 that D-14 left open (owner decision, 2026-08-14) | **Google Maps Platform.** OQ-6 is now closed: D-14 settled geocoding with a bundled dataset and no vendor, and this settles rendering. **D-14's geocoding stays exactly as it is** — this decision buys map tiles and markers, not a replacement for `zipcodes`, and nothing on the search hot path gains a network call. | The owner picked it; both candidates would have worked and neither is a technical mistake, so the tie is broken by whose account already exists rather than by a feature table. **What it does not change:** search still resolves a zip locally and offline, tests still assert real coordinates without mocking a provider, and a map that fails to load must leave the results intact — PRD 01 §6.8 requires "map views have list equivalents", and B-107 makes the list the primary surface rather than a fallback, so the map is decoration over a working page. **What it now costs:** a billed API key and a browser-side script on any page that renders a map, which is why the map is confined to B-107's view and not added to facility pages by default. The key is public by necessity (it ships to the browser), so it must be referrer-restricted and scoped to the Maps JavaScript API alone — an unrestricted Maps key is a billable resource anybody can copy out of the page source. |

| D-47 | Which audience an email address belongs to, and who wins when it is both (hit in production on 2026-08-13 bootstrapping the first real owner account, then raised independently as the top finding of BOTH the UX and accessibility reviews, 2026-08-14) | **The audience is a fact about the account, resolved from the address. `?from=` is a preference, tried first, never a verdict. When an address is both an active staff account and an active tenant account and there is no hint, STAFF wins.** | `audienceFor(undefined)` returned `'tenant'`, so a manager who typed the site address and clicked *Pay bill* — the only sign-in entry point in the public header — was looked up in the `Tenant` table, where they do not exist. Password reset then said "if that email has an account, a link is on its way" and sent nothing; sign-in said "Incorrect email or password" about a password that was correct. Four symptoms, one cause, and nothing on screen for the user to correct. Fixed at the root in `resolveAudience` (`lib/auth/accounts.ts`) rather than per screen, because the four callers would otherwise each have had to remember. **Enumeration posture is unchanged and must stay unchanged**: every caller responds identically whether an account was found or not, the dummy KDF still runs, and the throttle key is still computed for a miss. **Disabled accounts are skipped rather than claimed**, so a deactivated staff member who also rents a unit resolves to their tenant account instead of resolving to nothing. **On the tie:** an employee who rents a unit is genuinely ambiguous, and staff is the safer half — the portal's own links carry `from`, so a tenant arriving the ordinary way still resolves to tenant, whereas a locked-out staff member is the one with no other route in. **What this decision does NOT fix, and B-108 owns:** the sign-in *form* is still rendered from `from`, so the second-factor field appears only at `/login?from=/admin` and an enrolled staff member reaching a bare `/login` submits without a code and is refused. |
| D-48 | The three admin routes that fail 1.4.10 Reflow at 320px (owner decision, 2026-08-14, on the accessibility review's finding 7) | **They get fixed. No exceptions, no documented-minimum-width escape, no "admin is desktop tooling" carve-out.** `REFLOW_PENDING` in `e2e/admin.spec.ts` — `/admin/units`, `/admin/units/types`, `/admin/settings` — is **deleted, not repointed at a new item**; the test it feeds is already written and starts passing in the same commit. | The owner's reasoning, recorded because a later session will be tempted to reopen it as a cheap deferral: storage operators work at a unit door with a phone in hand — B-060's whole field-ops line assumes it — and a staff member using assistive tech or a magnified display must not be shut out of the tooling their job runs on. Low-vision desktop users at 200–400% zoom hit the identical layout, and §508 exposure attaches to employee-facing tooling as much as to customer-facing. The `test.fixme` was honest and well-placed (a failure enumerated in test output beats one buried in a document), but B-094 closed and left no successor owning it, which is how a documented failure becomes a permanent one. Built as **B-116**, together with the unit list's missing `take` — the two are the same page and the same session, and 290 rows each carrying a select and a submit is what makes the overflow unfixable by CSS alone. **A future minimum-width note in the accessibility statement is not an acceptable substitute for this item** — that is the escape this decision exists to close. |
| D-49 | Whether the lease gets an SCRA clause, and what closes the servicemember gap (raised during B-112, owner decision 2026-08-14) | **Wire the declaration to a hold now; the clause waits for the attorney pass.** The self-declaration captured at the lease step must automatically raise a `military_scra` `LeaseHold` (B-121), so the delinquency ladder, overlock and auction pipelines refuse to run against a servicemember's unit without a manager. **No SCRA clause is added to `LEASE_TEMPLATE` yet** — it joins the named outstanding list in that file's header and gets drafted alongside the Texas Property Code ch. 59 lien and notice language, not before it. | B-112's row justified moving the active-duty declaration to the lease step "where the SCRA sentence they trigger actually appears", and no such sentence exists — the lease has ten clauses and none mentions military service. Investigating that turned up the larger gap: `military_scra` existed only as a hold type staff raise **by hand**, and nothing read `Tenant.activeDutyMilitary`, so a renter who declared at checkout was overlocked, dunned and auctioned exactly like anyone else. **The clause is not the protection.** 50 U.S.C. §3958 bars enforcing a storage lien against a servicemember's property during service without a court order; that binds the operator whether or not the lease prints it. What protects the tenant is the pipeline refusing to run. Asking the question and ignoring the answer is worse than not asking, because it leaves a record showing we knew. The wording is deferred because SCRA modifies lien enforcement, which is already the named outstanding item — a clause drafted now would be redrafted against the same section — and because an overreaching draft (implying a termination right SCRA does not give for storage) becomes a contractual obligation beyond the statute, signed. Correct behaviour with a silent lease beats confident wrong text in a signed one. |

| D-50 | Reservation hold window: D-7's 7-day default vs. the day-after-move-in rule B-018 actually shipped (found during B-118, 2026-08-14) | **The shipped mechanism stands; the reserve page states what it actually does, not what D-7 said it would.** B-018 built `holdExpiryFor`: a hold expires at end of day AFTER the renter's own chosen move-in date — not a fixed 7 days from booking, and not configurable per facility at all. A renter picking a move-in date 14 days out (US-401's own maximum) holds a unit up to 15 days; one moving in today holds it 2. | B-118 needed the reserve page to state the hold window before the renter hands over their details — the row's example text was "Free to hold for 7 days". Implementing that literally would have shipped a false claim for most renters, on exactly the question the trust line exists to answer honestly. Whether the product should build the 7-day, per-facility-configurable window D-7 describes, or D-7 should be corrected to describe the rule that actually shipped and has been correct behaviour since B-018, is a product decision for the owner — not one to make unilaterally while fixing a copy line. **B-126** owns it; every customer-facing description of the hold window states the real mechanism until then. **Resolved 2026-08-15 by B-126 (owner decision):** D-7 corrected to match US-401 and the code, and the configurable half US-401 already promised was built — `Facility.reservationHoldGraceDays`, default 1, with its own settings control. The reserve page's trust line is now generated from that value rather than stating a fixed rule, so an operator who changes it does not leave a stale promise on the page. |
| D-51 | Marketing SMS: build the lane, or record that promotional SMS is out of scope (raised by B-123, owner decision 2026-08-15) | **Build the lane.** `marketing_sms` gets its own consent capture with its own TCPA disclosure and version, its own suppression, its own portal control, and SMS variants of the marketing templates — separate from `account_sms` at every step. **It ships unable to send, deliberately**, and the two things that unblock it are not code: Q5's disclosure copy needs legal review, and A2P 10DLC requires a MARKETING campaign registration distinct from the transactional one (PRD 05 §6.3). Until both land the lane is built, gated and dark. | The row's own framing was build-or-decide, and the trap it names is the middle: a promo sent down the transactional lane because the marketing lane does not exist. That trap was real and structural rather than hypothetical — `smsConsentGranted` checked `account_sms` for EVERY classification, so the first marketing rule anyone wired to SMS would have sent under transactional consent with nothing to stop it. Building the lane closes it by construction: the gate now asks which lane the message belongs to and a marketing SMS with no `marketing_sms` consent is refused. Choosing to build rather than defer also means the consent records, the disclosure versioning and the audit trail exist BEFORE the first campaign, which is the same reasoning PRD 05 CN-15 already applied to `account_sms` ("consent capture ships now, so consent exists before SMS does"). The known cost, accepted: working infrastructure sits dark until legal and 10DLC clear, and no seeded rule sends marketing SMS in the meantime. |
| D-52 | Protection on a multi-unit checkout: one plan for the basket, one per unit, or a tier chosen per unit (raised by B-106 part 3, owner decision 2026-08-16) | **One plan per unit.** The tier the renter chooses applies to each unit in the basket — N premiums, N coverage limits, one plan recorded against each lease. The price summary and the protection step state the multiplied total BEFORE payment, itemised per unit. | Each plan promises to cover "up to $X of your things", and a unit is the thing being covered: three units of contents behind one $5,000 limit is under-cover the renter would discover at claim time, which is the worst possible moment to find out. The alternative of recording one premium against the first lease also leaves the other leases showing no protection at all on their own screens, so the record would disagree with what was sold. A tier chosen per unit is more accurate still and was rejected for now: it adds a decision to the step B-112 deliberately shortened, for a case no renter can reach until the basket UI ships, and it can be added later without re-deciding this. The cost, accepted: renting three units costs three premiums, so §6.4's rule that a total never moves without a stated cause makes the multiplied figure a disclosure requirement rather than a nicety. |

| D-53 | What a renter signs when one checkout rents several units (raised by B-106 parts 3–4, owner decision 2026-08-16) | **One lease document per unit, all signed by a single action.** A three-unit basket produces three `Document` rows and three `Signature` rows, stamped with one `signedAt` from one signing step. Not one combined agreement, and not three separate signing steps. | The enforceable object in this codebase is the `Lease` row, and it is per unit: delinquency, overlock, lien notices and auction all key on a lease. A single combined agreement would mean auctioning one unit cites a document that also covers the two the tenant still rents — the operator's own evidence in a lien proceeding would describe property they are not entitled to sell, which is the kind of defect that surfaces at the worst possible moment. Rejected in the other direction, three separate signing steps: it puts three signature ceremonies at the end of a checkout that §6.4 already caps at seven fields a step, and it admits a partial state where two agreements are signed and one is not, on units the renter has already paid for. Signing once against N documents keeps the legal object per-unit without charging the renter for the modelling. **What it costs:** `existingLeaseDocument` is keyed by `CheckoutSession` and returns one row, so the lease step, the signature write and the resume path each move from one document to a list. |
| D-54 | How many gate codes a multi-unit renter is issued (found during B-106 part 5, owner decision 2026-08-16) | **One code per tenant per facility.** `provisionAccessForLease` reuses the active credential already on the tenant's grant instead of minting a second. B-106 part 4's behaviour — one credential per lease — is corrected, not kept. | `AccessGrant` has been keyed on `(facilityId, tenantId)` since PRD 03: `ensureGrant` takes a tenant, not a lease. The credential hanging off it is a PIN, and the grant is what the controller enforces — so three credentials on one grant are three different PINs that open the same gate with identical permissions and identical hours. Handing a renter three codes for one door is not a smaller version of a real feature, it is a defect that reads as one. It also triples what a tenant can leak and what staff must revoke on a move-out of any single unit. Per-unit codes are defensible only against a per-door controller, and PRD 03's simulated adapter models a gate, not a door per unit — if that hardware ever lands, this is re-openable, and the credential still carries its `leaseId` so the record does not lose which lease first caused it to exist. **This makes part 5 smaller than part 4:** the fix is a reuse check, and it removes code rather than adding it. |
| D-55 | Whether a multi-unit move-in sends one welcome message or N (raised by B-106 part 3, owner decision 2026-08-16) | **One message naming every unit.** One `lease.moved_in` event for the checkout, carrying all the unit numbers as a merge field, rather than one event per lease. | The renter did one thing, and three near-identical emails inside one minute reads as a system malfunction rather than as thoroughness — it is also how a sender earns a spam complaint from somebody who just paid. The event is the natural place to resolve it because `processCommsEvent` already resolves ONE recipient per event and runs every matching rule against it: N events would mean N deliveries to the same person with no existing mechanism to collapse them. **What it costs:** a new merge field for the unit list, which `checkPublishable`'s build-time gate will require every `lease.moved_in` template to satisfy — so the templates change in the same item, not later. |
| D-56 | Which marker API the search map uses, and the second public value it costs (settled while building B-107, 2026-08-17) | **`AdvancedMarkerElement`, which requires a `NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID` alongside the key.** The map ID is created in the same Google Cloud console, is not a secret, and ships to the browser exactly as the key does. With either value unset the search page renders no map at all and the results list is untouched — that is the intended degraded state, not an error, and it is the state production is in until the key exists. | The alternative was the classic `google.maps.Marker`, which needs no map ID and is one env var cheaper. It is also deprecated, and its `label` is the vendor's own text box: a price bubble drawn that way cannot be a link, cannot be given a contrast ratio we choose, and cannot enter the tab order. B-107's row requires all three — pins meeting 3:1 against the tiles (1.4.11), no interaction depending on hover, and a keyboard route to every facility. `AdvancedMarkerElement`'s content is ordinary DOM, so the bubble is simply an `<a>` and each of those requirements is satisfied by the platform rather than by a workaround. **What it costs:** one more value in `.env.example`, and a map that is absent rather than degraded when only half the configuration is present — deliberately, because a map ID without a key produces a vendor error watermark, which is worse than no map on a page whose list already works. |
| D-57 | Which touch gets credit for a move-in when the lead and the checkout disagree (settled while building B-082 part 1, 2026-08-17) | **The LEAD's channel wins over the checkout's own attribution cookie.** A renter who arrives from SpareFoot, enquires, and comes back a week later through a Google search is credited to `aggregator`, not `organic`. Where there is no lead — a renter who went straight to checkout — the cookie is used, and where there is neither the channel is **null**, which reports as `unknown`. | Last-touch is the usual default and it is wrong here for a reason specific to this industry: aggregators charge **per completed move-in**, and they invoice on their own record of having delivered the renter, not on ours. Crediting last touch would mean paying SpareFoot out of a budget line that says SpareFoot produced nothing — the report and the invoice would disagree every month, and the report would lose. **What it does not change:** `acquisitionSource` still records how the deal was TAKEN (`web`/`phone`/`walk_in`) and is untouched; this is a second, orthogonal column, because a marketplace rental is genuinely both `web` and `aggregator` and one column cannot say so. **What it costs:** a first-touch bias for the one case where a renter genuinely re-discovers us through a different channel weeks later. Accepted deliberately — the money follows the aggregator contract, so the report has to as well. Null rather than a default is the other half: `organic` would be a fabricated answer, and fabricating credit for the channel being evaluated is the failure the whole item exists to prevent. |

| D-58 | Where a city page's "unique intro copy per city" comes from, given there is no city record (raised while building B-082 part 2, 2026-08-17) | **Generated from the facilities in the city, not typed into a `City` model.** The count, the names, the price floor and the amenity set are all read from the facility registry, so every sentence on a city page is derived from something a person already maintains. There is no editable city description, no `City` table and no admin screen for one. | PRD 04 US-4 AC1 asks for "unique intro copy per city", and the obvious reading is a text field. The house rule cuts the other way and has already been paid for twice: the facility page's FAQs and its JSON-LD are both generated *because* copy typed once drifts from the hours, the prices and the amenities it describes, and a page contradicting its own list is worse than one with no prose. Derived copy cannot drift. **What it costs:** the wording is templated — two cities with the same number of facilities get the same sentence shapes with different nouns — so this is thin-content protection at the floor, not a marketing asset. **Measured follow-up, 2026-08-17 (B-082 part 6):** the site's own duplicate-content check now scores the generated city intros at **0.82–0.85** against each other, above this codebase's own 0.8 threshold — so by our own measure these pages are duplicate content, and the report says so on every open. That was a second trigger, and it is now **settled by D-62** (B-128, 2026-08-17): a `City` row holds authored copy where somebody writes it, and everything below stays exactly as this decision describes wherever nobody has. **This decision is completed rather than overturned** — the generated intro is still what a city with no authored copy renders, which is every city on the day D-62 shipped, and it is still generated for the reason given here. **The original trigger for revisiting** is an operator actually wanting to say something about a city that is not true of its facilities; at that point it earns a model, a form and a permission in one item, per the repo's own rule that a field configuring behaviour ships with its control. Adding a nullable `cityCopy` column reachable only from a database client is specifically what this decision refuses. |
| D-59 | What replaces "distance" on a city page, which has no point to measure from (raised while building B-082 part 2, 2026-08-17) | **Nothing is printed, and the search form is offered instead.** US-4 AC1 lists distance beside starting price and rating; a city page is reached without anyone typing a location, so there is no origin. Rather than measure from a city centroid, the page carries the same `FacilitySearchForm` the homepage and results page use, prefilled with the city. | A distance from an invented origin is a number that looks measured and is not — "1.2 mi" on a page a renter reached from Google means 1.2 miles from a centroid they have never heard of, and it is wrong for every one of them by a different amount. The list is ordered cheapest-first instead, which is the question somebody who has already chosen the city is actually asking. The search page is where distance is honest, because there the renter supplied the point; handing them one click to it satisfies what AC1 wanted without printing a figure nobody can act on. |
| D-60 | Whether the size guide moves into `/guides` to complete the hub's launch set (raised while building B-082 part 3, 2026-08-17) | **It stays at `/storage/size-guide` and the hub links to it.** The launch set is five guides; four are MDX under `/guides` and the fifth is linked where it already lives. Nothing is copied. | PRD 04 US-4 AC2 lists "unit size guide (with visual comparisons)" in the hub's launch set, and that page has existed since B-016. The two ways to make the set look uniform are both worse than the asymmetry. **Copying its text** creates two indexable pages with the same content, which is precisely the duplicate-content problem this same backlog row's part 6 exists to detect — self-inflicted, on the one page in the set most likely to rank. **Moving it** means a 301 on a URL that is in the sitemap and linked from every facility page, every search result and every city page, to buy a tidier URL a renter will never notice — the identical trade D-32 already refused for facility pages. The cost is that one of five hub cards points outside `/guides`; an e2e assertion pins that href so a later session cannot "fix" the inconsistency by copying the text. |

| D-61 | Which source/medium a session belongs to when its events disagree (settled while building B-082 part 4, 2026-08-17) | **Its FIRST event in the reporting range.** Every session is attributed once, from the earliest event it fired inside the window, and every funnel step then classifies that session by the same answer. | A session can fire a tagged page view and later an untagged one — the tags are on the landing URL, not on every request — so a per-event split double-counts: the same person appears under `google/cpc` in the sessions row and under untagged in the move-ins row, and the breakdown stops adding up to the funnel above it. A breakdown that does not foot is not a breakdown, it is a second set of numbers to reconcile, and an owner deciding budget from it will quietly trust whichever half is larger. First touch is chosen over last for the same reason **D-57** chose the lead's channel over the checkout's cookie: the campaign that paid to bring somebody in is the one being evaluated. **Scope:** this decides SESSION attribution inside a report window only. It does not touch `Lease.acquisitionChannel`, which D-57 owns and which is stamped once at move-in. **What it costs:** a session that starts untagged and later clicks a tagged ad within the same window is credited to untagged. Accepted — the alternative reintroduces double-counting to fix a case that is rarer than the one it breaks. |

| D-62 | Whether city page intro copy stays generated, or becomes something a person writes (raised by B-082 part 6's own duplicate-content report; owner decision, 2026-08-17; **B-128**) | **Both — a `City` row holds authored copy, and the generated intro is the fallback whenever none is written.** A city page renders whatever somebody wrote for it; a city nobody has written for renders exactly what it rendered before, which is every city on the day this ships. The row carries prose and nothing else — no name, no state, no coordinates — and is keyed on the URL slug, so it can never become a second opinion about where a city is or how it is spelled. Editing is gated on a new `marketing:city_copy` permission asked for with a **null facility**, the same shape as `org:defaults`: a city page lists every facility in the city, so `facility:settings` at one site would let a manager edit the page listing their neighbours' sites too. | **D-58 is not overturned, it is completed.** Its reasoning — derived copy cannot drift, and a page contradicting its own list is worse than one with no prose — still holds and is exactly why the generated intro remains the default rather than being replaced by an empty box. What changed is a measurement D-58 did not have: this site's own duplicate-content check scores the three generated city intros at **0.82–0.85** against each other, above the codebase's own 0.8 threshold, so by our own measure these pages were duplicate content with **no field anybody could edit to fix it**. A landing page whose purpose is to rank cannot be left in that state. **What was rejected, and why:** raising the 0.8 threshold would silence the finding for authored copy too, which is the case the check exists for; excluding generated-vs-generated pairs from the report would concede that city pages are permanently a thin-content floor; and widening what the generator varies on fixes the number without fixing the category, since two genuinely similar cities still collide. **What it costs:** a nullable column that most cities will never fill, and a screen an operator has to visit — accepted because the screen ships in the same item as the column, per this repo's rule that a field configuring behaviour never lands reachable only from a database client. The report's advice for a generated pair now links to that screen instead of describing a product gap. |

| D-63 | Whether B-083's two integrations ship together, and what an unconfigured install does (raised while building B-083; owner decision, 2026-08-18) | **Certified mail ships now with honest degradation and no simulator; the auction-platform listing is split out as B-129, blocked.** Unconfigured means the send button is not offered, the screen names `CERTIFIED_MAIL_API_KEY`, and staff post the notice and type the tracking number exactly as they did before — the state every dev, CI and preview environment is in. The provider key is refused in both directions: a `live_` key outside production, and a `test_` key inside it. The auction half is deferred because master PRD §11 open question 9 (live on-site versus online auctions) has never been answered and a marketplace listing needs a partner agreement; the same no-simulator rule is recorded against it in advance. | **The repo had already decided this twice, in opposite directions, and the distinction is what settles it.** A simulated gate is fine (D-4) — a simulated gate is a device we control. A simulated Search Console verdict was refused in B-082 part 5 — it is a claim about what a third party did, on a screen an operator acts on. Certified mail is the second case and worse: a fabricated tracking number is a claim about what the POSTAL SERVICE did, written into the evidence chain that defends a lien sale under Texas Property Code ch. 59. It would not be a bad screen; it would be a document produced in litigation. **The both-directions key rule is not symmetry for its own sake.** Physical mail has no equivalent of FR-20's sandbox inbox, because a collected letter cannot be recalled — so a live key outside production is refused absolutely rather than redirected. And a test key in production is the quieter, worse failure: the provider accepts the letter, returns a well-formed tracking number, mails nothing, and the system writes that number down as proof of service. That is fabricated evidence produced by a configuration mistake, which is the same outcome the no-simulator rule exists to prevent, reached through the back door. **On the auction half:** writing a driver against a marketplace API would encode an answer to OQ-9 by building for it, and no spec is published without an account — so the work would be guesswork gated on a business decision nobody has taken. B-129 records the block, and records the no-simulator rule in advance so the next session does not re-derive it. |

| D-64 | Whether US-40's "management summary pack (monthly PDF)" ships as a PDF (raised while building B-084 part 1, 2026-08-18) | **No — semantic HTML, like every other generated document in this product.** US-40 names a PDF; B-023 settled in the opposite direction for lease documents and notices, and nothing has changed to reopen it. Recorded as a D-number now because this is the SECOND place PRD text asks for a PDF, and a precedent living only in a PROGRESS entry is one the next session re-derives from scratch. | B-023's finding stands: no JavaScript PDF library available in this runtime emits **tagged** PDFs — `pdf-lib`, `pdfkit` and `@react-pdf/renderer` all produce untagged output, and tagging is a structure tree none of them expose. An untagged PDF is a screen-reader dead end, which is disqualifying for a document this product generates on somebody's behalf. HTML carries the same hash and the same evidence chain, and is accessible by default. **What it costs:** "send me the monthly pack" produces a page rather than an attachment, and an operator who wants a file prints it. That is a worse handoff than a PDF and it is the trade B-023 already made deliberately. Reopen only when a tagged-PDF encoder exists in this runtime, not because somebody asks for a file. |

| D-65 | What "frozen month-end snapshot" freezes, given that some figures can be recomputed later and some cannot (settled while building B-084 part 1, 2026-08-18) | **Both are frozen; only the recomputable half is drift-checked.** Point-in-time figures — unit occupancy, square-foot occupancy, AR aging — are stored as the sole record and never compared against a later run. Period-derived figures — billed, collected, discounts, write-offs, refunds, unapplied, economic occupancy, move counts — are stored AND re-run on view, with any difference reported as drift. | §8 asks for "frozen month-end snapshots" and does not distinguish the two, and the distinction is the whole feature. **Unit occupancy reads `Unit.status`, of which this system keeps no history, and `delinquencyReport` takes no date parameter at all** — both answer "as of now" whatever range they are handed, so a July figure is observable only in July and the frozen copy is the only record that will ever exist. Recomputing one later does not reproduce it; it answers a different question with the same name. Period-derived figures come from rows carrying their own dates, so a later disagreement means somebody genuinely changed the past — a voided invoice, a backdated adjustment, a corrected move-out — which is exactly what an accountant needs flagged. **Conflating them fails in both directions:** comparing occupancy would flag every closed month forever over changes that have nothing to do with that month, and not comparing revenue would hide the post-close edits the check exists for. Enforced by types rather than by discipline — `periodDrift` takes `PeriodDerivedFigures`, so a point-in-time field cannot be passed to it. Ratios compare with a 1e-9 tolerance, because a drift report that fires on floating-point noise is one nobody reads. |

| D-66 | What a QuickBooks journal posts to, and what it deliberately leaves out (settled while building B-084 part 2, 2026-08-18) | **A per-facility chart of accounts, configured on the close screen, with a conventional chart as the per-field fallback — and NO refund line.** Ten named accounts, matched by NAME because that is what QuickBooks Online's journal import matches on. Any field left blank falls back to the conventional name, so an operator who never opens the form still gets a file that imports and one who has renamed two accounts only says so twice. Refunds are informational and are not journaled. Referral rewards post to their own account, taken OUT of the discount figure rather than added beside it. | **PRD 02 §2 says we are not the accounting system of record**, which makes exactly one property non-negotiable: the entry balances. `buildJournal` asserts its own balance before returning and throws otherwise, the same posture as B-062's proceeds waterfall — unreachable by construction and checked anyway, because an unbalanced journal reaching a real general ledger is invisible from here. **The refund omission is the trap this module was most likely to fall into.** `refundsCents` is documented as informational because a refund unwinds its original payment's allocation rows, so `collectedCents` is already net of it — a refund entry would take the money out twice **and balance while doing it**, which is precisely the error a trial balance cannot catch. **Referral rewards are a SUBSET of discounts**, not a sibling: same invoice line type, split by description. Posting both in full would double the reduction and also balance. Both are asserted by name in the tests, because the correct behaviour here looks like an omission and a later reader would reasonably try to "fix" it. **Per facility rather than org-wide** because separate books per site is ordinary in this industry, and the chart ships with its form in the same item per this repo's own rule. **A snapshot filed before the category split existed is refused, not guessed** — exporting it would mean inventing which part of a single total was rent and which was tax, and posting that invention to a real ledger; the refusal tells the operator to reopen and re-close, and says the figures will not change. |

| D-67 | How a scheduled report knows it has already gone out, and whose authority it runs under (settled while building B-084 part 3, 2026-08-18) | **No `lastSentAt` column — `Message.idempotencyKey` is the record — and the report functions gained facility-explicit variants rather than the job inventing an actor.** The send is keyed `report:{subscriptionId}:{period}:{recipientIndex}`, which `sendDirectEmail` already refuses to repeat. Authorization happens when a permitted staff member SUBSCRIBES (`reports:financial` at that facility); the job then executes a standing order against a facility it knows explicitly, through `occupancyForFacility`, `facilityRevenue`, `agingForFacility` and `movesForFacility`. | **On the missing column:** a second place recording "has this period been sent" is a second thing that can disagree with the first, and the one that disagrees silently is always the one nobody checks. The message log already has to be right about this — it is what an operator looks at when an email did not arrive — so a column beside it earns nothing and can rot. **On the actor:** the actor-scoped report functions funnel through `facilityAccess`, which correctly gives the **system actor nothing** ("the system actor is not a superuser" — the RBAC catalog's own words). The two ways to make them work from a job were both worse than the refactor: widening `facilityAccess` for system would make every facility-scoped query in the product permissive for background work, and fabricating a staff-shaped actor would put a person's identity on something nobody did. Extracting the per-facility functions also removed real waste this repo had already noticed — B-084 part 1's own comment records that its close computed every facility the actor could see and threw most of it away. **What it costs:** four more exported functions in the reporting layer, and a rule to keep — an actor-scoped report is for a screen, a facility-explicit one is for a job that has already been authorized. |
| D-68 | How unit status becomes historical, and whether that reopens D-65's freeze (settled while building B-131, 2026-08-19) | **A database trigger writes `UnitStatusHistory`, and D-65's freeze stays exactly as it is.** Every insert into `unit` and every change to its derived `status` column appends a row, from Postgres, not from application code. A period ending before the earliest row is **not** answered from current statuses under a past month's heading — `occupancyForFacility` returns the current figures with `followsPeriod: false` and a `reason`, and every surface prints one shared sentence saying which instant the figure describes. The accounting close is untouched: point-in-time figures remain frozen and never drift-checked. | **On the trigger:** `Unit.status` is derived and has two application writers today (`recomputeUnitStatus`, and the bulk status operation's pre-evaluated `to`), plus the seed, plus the demo seed. A history that depends on each of them remembering to append is a history with holes, and a hole here is **indistinguishable from "the status did not change"** — the read cannot tell them apart, so a partial history is not a weaker version of this feature, it is a wrong one. A trigger makes the guarantee structural: psql, a future migration and a caller nobody has written yet are all covered, and the check is one test that inserts a unit and asserts a row appeared without the application asking. The cost is real and stated: the trigger is invisible in `schema.prisma`, so the model carries a comment naming the migration. **On the honest gap:** history begins at the migration, which backfilled one row per existing unit. `audit_log` was considered as a backfill source and rejected — it carries only the HUMAN status changes (`unit.status_overridden`, `unit.updated`), never the derived recompute that moves a unit to `occupied` on move-in, so a reconstruction from it would be confidently partial. A wrong past presented as a recorded one is worse than a stated gap, because nothing downstream can detect it. **On D-65 staying shut:** unit occupancy is now reproducible for periods after the history begins, which is exactly the condition D-65 said did not hold — so the door is genuinely open. It stays closed here anyway, for one reason: drift-checking a filed month means comparing a stored figure against a recomputed one, and for every month already filed the recomputation would run against a history that does not cover it and report drift on every one of them, forever. Moving occupancy into the drift-checked half is a decision for a later item that can also decide what to do about the months filed before this one; doing it as a side effect of building the history table would break the close's existing output on the way past. |

| D-69 | How a request carries its impersonation session, given PRD 09 §6.1 names a JWT claim (settled while building B-091 part 2, 2026-08-19) | **Its own httpOnly cookie holding the session id, not a claim on the Auth.js JWT.** `storage.impersonation` carries the id and nothing else. Every request loads the row, re-runs the escalation guard, and then checks that the row's `impersonatorStaffId` is the staff user the JWT already authenticated — a cookie naming somebody else's session resolves to nothing. | §6.1's own argument is that **the token is not the authority**: "a JWT cannot be revoked, and FR-9/FR-18 both require server-side termination mid-session", so the row is re-read and re-validated on every request either way. Given that, the claim buys nothing and costs a token re-mint at both ends of the session — Auth.js can only add a claim by re-issuing the JWT (`unstable_update`), so *ending* a session would need a second one, and a failed re-mint on the way out would leave a claim the row no longer backs. **The security property that makes a cookie safe here is asymmetry, and it is worth stating because it is what a reviewer will check.** A forged or stolen cookie fails the binding check, so it impersonates nobody — while its mere presence trips the write block (D-70). It can therefore only ever *reduce* the holder's access, never widen it, which is why the edge layer is allowed to act on a cookie it cannot verify. **What it costs:** an attacker who can set a cookie on a staff member's own browser can block that person's writes until it is cleared, a nuisance-grade denial of service against one user. Accepted: the cookie is httpOnly and `SameSite=Lax` from our own origin, and both shells clear a cookie that resolves to nothing on the next page load. |

| D-70 | How read-only is enforced, and whether FR-12's permanent hard-block list ships as a list (settled while building B-091 part 2, 2026-08-19) | **Enforced by HTTP method at the edge — every non-GET request is refused while a session cookie is present, with two exempt paths — and the hard-block list is NOT built as a list, because with `impersonation:write` unshipped the rule is simply "block everything".** The two exemptions are `/api/impersonation/` (the way out) and `/api/auth/` (sign-out). The one member of FR-12's list that is **not** a mutation — revealing an unmasked gate code — IS built, in the portal, because nothing else would have caught it. | FR-13 says the check is the control and hiding is a courtesy, and SR-2 asks that a page which forgets to hide a button still be safe. A per-action classification satisfies neither for long: this app has **53 files of server actions** and every one of them POSTs to the page that rendered it, so a list of blocked actions is a thing a new screen can be missing from, silently, with the failure being that the write goes through. Refusing by method needs no list and covers screens nobody has written yet. **Why the list is not built anyway:** FR-12 is a ceiling on a *write* mode, and `impersonation:write` is in neither part of B-091 — PRD 09 §8 calls it "later / optional" and OQ-2 asks whether it should ship at all. A classification of blocked-versus-allowed mutations, in a build where every mutation is blocked, is code that cannot be exercised and therefore cannot be trusted when it finally is. Whoever builds write mode builds the list, against FR-12, as the thing that narrows this rule. **The gate code is the exception that proves the shape,** and it is why the list was read rather than deferred wholesale: PRD 03 SR-2 makes revealing a code a separate audited permission, so an impersonated portal rendering it would launder exactly that permission — and no write block catches it, because reading a portal page is a GET. The code is dropped from the data rather than hidden in the markup, since `GateCodePanel` is a client component and a hidden code would still be serialised into the HTML. **What it costs, and it is a real cost:** a staff member who clicks a save button anyway gets a 403 rather than a message beside the field, because a server action answered with anything but its own payload fails in the client as a generic error. Two graceful alternatives were rejected on evidence — a global `<fieldset disabled>` around the shell would also disable the portal's and the admin's **GET** filter forms (`/portal/pay` and `/portal/move-out` both have one), breaking the read-only browsing the feature exists for; and per-form disabling is the 53-file list this decision refuses. The banner states read-only in the first line a screen reader reaches, which is the mitigation; a graceful per-form refusal belongs to whoever next has reason to open those files. |

| D-71 | Whether `impersonation:write` ships at all (PRD 09 OQ-2, asked at Phase A and answered at Phase B; owner decision, 2026-08-19; **B-092**) | **Deleted, not deferred.** The permission is gone from `packages/db/rbac-catalog.ts` and pruned from every seeded database. The `ImpersonationMode` enum keeps `read_write`; the schema does not change. | §10 said the honest move, if no concrete need had appeared by the time Phase B landed, was to delete it rather than carry an unused write path — and none appeared: every troubleshooting case this feature was built for is satisfied by looking. What made deferring worse than deleting is that the permission was **grantable**. It appeared in the catalog, an owner could see it on a role, and granting it did precisely nothing, because B-091 ships no code that sets `read_write` and D-70 deliberately did not build FR-12's hard-block list. A permission that can be granted and has no effect is not a placeholder, it is a promise the product does not keep — and the one person likely to grant it is somebody who has just read §5.3 and believes it will be enforced. **The enum value stays deliberately.** Dropping it is a migration that erases the record of a decision, and every session row still has to STATE its mode rather than leave a reader to infer it from the absence of an alternative. **Re-introducing write mode is therefore a seed change plus D-70's hard-block list, never a schema change** — which is the same shape §4 already promises for widening `impersonation:tenant` to a regional. **What it cost, and it is the useful part:** this is the first permission this repo has ever retired, and the seed could not do it. It upserted catalog permissions and pruned stale ROLE GRANTS — its own comment says "removing a permission from the catalog actually revokes it" — but never deleted the `Permission` row, so `rbac-db.test.ts`'s catalog comparison failed against every database seeded before the removal. The seed now prunes retired permissions after the role loop, and says how many it removed. |

| D-72 | What "filterable by facility" means for a session row that deliberately has no facility (raised while building B-092, 2026-08-19) | **The SUBJECT's current facilities, not the impersonator's `facilityScopeSnapshot`** — a tenant's leases, or a staff user's assignments — resolved in bulk and applied after the query. The screen states in words that the filter uses where each account is *today*. | `ImpersonationSession` has no `facilityId` and B-091 part 1 gives the reason: a session is about a subject, and a subject spans facilities — a tenant with leases at two sites, or an all-facilities staff user, belongs to no single one. FR-19 nonetheless asks for a facility filter, so it has to resolve to something, and the two candidates answer different questions. The owner's question is "who looked at accounts at my Dallas site", which is about **whose account was opened**. The snapshot answers "what could this impersonator have reached at the time", which is evidence about the guard rather than about the visit — it stays on the row for the investigation that does ask it, and it is the wrong filter for this screen. **The same resolution does double duty as the facility SCOPING** (`facilityScope`'s fail-closed contract), which matters because §4's whole design is that widening `impersonation:oversee` to a `regional` is a seed change: a regional must then see the sessions that touched their sites and no others, and that is asserted rather than assumed even though owner-only seeding makes it a no-op today. **What it costs:** the filter is "as at now", so a tenant who has since moved out no longer counts at the site they were at — the same limitation B-131 fixed for occupancy with a history table, and not worth a second one for a filter. Stated on the screen rather than left for a reader to assume it is historical. |

| D-73 | Whether FR-20's frequency threshold is configurable, given the PRD says it is (settled while building B-092, 2026-08-19) | **A named constant — `FREQUENCY_FLAG_DISTINCT_SUBJECTS = 5` — not a column and not a form field.** More than five DISTINCT subjects reached by one impersonator in one UTC day puts a line on the report. Nothing is blocked, ever. | FR-20 says "configurable" and OQ-3 says the threshold "needs a real N, which needs observed usage. Ship Phase B with a conservative default and tune." There is no observed usage: the permission is owner-only at seed (D-13b) and starts are throttled to ten an hour (SR-7). Tuning a number against no data by putting a box on a screen is not configurability, it is a guess with a form field — and this repo's own rule is that a field configuring behaviour ships with its control or does not ship, which B-091 part 1 already applied to the session TTL for the same reason. A constant is not a column, so the rule is satisfied rather than dodged. **Distinct subjects rather than session count** is the substantive half of the decision: five sessions against one tenant across a morning is somebody debugging one problem, which is the feature working; five different tenants is a pattern, and the pattern is what a flag is for. **The flag is computed from the rows already on screen**, so it can never disagree with the table beneath it, and a filtered report flags what the reader can actually see. **Reopen when there is usage to tune against** — at which point it earns a column and a control together, not one of them. |

| D-74 | What a street-rate suggestion is allowed to propose, and what stops "one-click apply" becoming a ratchet (settled while building B-088 part 1, 2026-08-19) | **Increases only, never a cut — and three guards before any suggestion fires: a 5-unit minimum, a 90-day cooldown, and silence when a change is already scheduled.** US-12's example threshold (92%) becomes a two-band ladder (≥92% → +4%, ≥95% → +8%), rounded UP to the whole dollar. A soft type gets no suggestion and a pointer to promotions. | **On increases only:** a proposed price CUT is a far larger commitment than it looks — it reprices the type for every future tenant indefinitely — and this product already has the right lever for soft demand, because B-070 built a promotions engine that discounts temporarily and expires on its own. A rule that offers "lower the street rate" as a one-click action beside "raise it" makes two very different decisions look symmetrical. **On the cooldown, which is the guard that matters most:** occupancy does not fall the day a rate rises, so a rule that re-suggests on every visit would have an operator applying +8% weekly and compounding a 50% rise inside a quarter, each click individually defensible. "One-click apply" is precisely what invites that, so the suggestion goes quiet for 90 days after a change and says why. **On the 5-unit floor:** three units at 100% is one move-out from 67%; it is the smallest sample that can look like a signal. **On the scheduled-change guard:** `/admin/units/types` can queue a future rate, and suggesting on top of one means the operator applies this, the queued row lands next week, and the price moved twice for a single decision. **On rounding up to the DOLLAR, not $5:** the first draft rounded to $5 and the tests caught it — that turns +4% on a $100 rate into +5%, and on a $37 rate into +8%, so the rounding was rewriting the band the operator was reasoning about. At $1 the distortion is under a dollar on any rate, and up rather than nearest so a raise can never round back to the current price. **What it costs:** an operator who genuinely wants to cut a street rate does it on the Types tab, unassisted — deliberately, because that is a decision worth making deliberately. The web rate moves by the same proportion rather than to the same number, so an online discount survives an increase instead of being silently retired. |

| D-75 | Where an owner KPI trend gets its history from (settled while building B-088 part 2, 2026-08-19) | **The FILED month-end snapshots, never a recomputation** — one query against `AccountingPeriod.snapshot`, with the current unclosed month simply absent. A month no facility has closed is NAMED as not closed, never plotted as zero, and a month where only some sites filed says how many. | **Correctness first, speed second, and both point the same way.** D-65 already settled that point-in-time figures — unit occupancy, square-foot occupancy, AR aging — are frozen at close because they cannot be observed again: `Unit.status` had no history before B-131, `delinquencyReport` still takes no date parameter at all, and D-68 deliberately left occupancy out of the drift-checked half. Re-deriving a past month therefore answers a *different question under the same name*, which is exactly what B-131 was built to stop a report doing. The close already froze the right answer. The speed argument is the same shape: twelve months across a portfolio would mean a dozen `occupancyForFacility` calls per facility, each with its own `DISTINCT ON` over the status history — well over a hundred round trips to a remote database for one page — against one query here. **A gap is never a zero.** A missing month drawn at zero is a collapse that did not happen, and it is the single most dangerous thing a trend can show the person who decides on it; the same reasoning covers a month where one site of three has closed, which otherwise looks identical to the portfolio losing two thirds of its revenue. **It deliberately does NOT refuse an old snapshot the way `buildJournal` does.** That refusal is right there and wrong here: a journal without v2's category split cannot be posted at all, whereas occupancy, collected, receivables and moves mean the same thing in v1 and v2 — refusing by version would blank a year of history to guard a field nothing on this page reads. What it refuses instead is a missing NUMBER, per field, because a zero and an absent value are indistinguishable once rendered. **What it costs:** an owner who has never closed a month sees an empty page with an explanation and a link to the close, rather than a trend assembled from figures nobody filed. Accepted deliberately — that is the honest state, and D-65's whole argument is that the unfiled past is not recoverable. |
| D-76 | What "IndexNow/sitemap ping automation" can actually mean, given half of it no longer exists (settled while building B-087 part 1, 2026-08-20) | **IndexNow only, and Google is not in it.** One POST per night to the shared `api.indexnow.org` endpoint, carrying only the URLs whose `lastModified` falls on or after that business date. Google's `/ping?sitemap=` endpoint was retired in 2023 and Bing retired its own in favour of this, so there is no "sitemap ping" left to automate — the PRD's phrase describes two mechanisms of which one is dead. What reaches Google stays what already reached it: the sitemap, and B-082 part 5's indexation report to see what came of it. | **The reduction is written down rather than papered over, because the phrase in the PRD promises a reach this does not have.** IndexNow is read by Bing, Yandex, Seznam and Naver; for a Texas self-storage operator that is a minority of the search traffic that matters, and somebody reading "ping automation" in the roadmap would reasonably assume Google was the point of it. Building it anyway is still right — it is one POST, it is free, and a same-day re-crawl on Bing is worth having — but it must not be recorded as though the Google half shipped. **Only changed URLs are submitted, and `static` pages are excluded by `pageKind`**, because `sitemap.ts` stamps them with the request time: they would qualify as "changed" every single night while never having changed, which is the exact pattern that gets a host throttled by a protocol whose entire premise is that a submission means something happened. **The submitter also refuses to run without `NEXT_PUBLIC_SITE_URL`** — the same `hasCanonicalDomain` gate the canonical tag uses — because the worst outcome available here is asking four search engines to crawl the `.vercel.app` twin of the real site. **What it costs:** a price-only change does not trigger a submission. `lastModified` for a facility is `Facility.updatedAt`, which does not move when a rate on one of its unit types changes; that is `sitemap.ts`'s existing limitation rather than one introduced here, and the fix belongs with the sitemap — a derived last-changed date taking the newest of the facility row and its rate records — not with a second date column maintained beside it. |
| D-77 | Whether generated per-city/size landing pages may be indexed, given this codebase has twice refused to mass-produce templated pages (settled while building B-089, 2026-08-20) | **They are generated, but the duplicate detector runs as a GATE before publication rather than as a report after it.** Each page's intro is scored against every sibling SIZE's intro in the same city at render time; a page at or over the existing 0.8 threshold is served `noindex, follow` and is left out of the sitemap. Nothing about it is visible to a renter — the page still renders and still lists real inventory. What makes the pages clear the gate is that each carries two sentences about its own size from the shared guide catalogue, so a 10×10 page and a 10×15 page differ by more than a number. | **B-082 part 6 built the detector as a screen somebody reads afterwards, and that was right for pages a person wrote** — a human pasted, a human un-pastes. It is not sufficient for pages the product generates by the dozen: by the time the report is read the thin pages are indexed, and the damage from thin content is to the DOMAIN rather than to the page, which is the same asymmetry that makes `qualifiesForSchemaMarkup` refuse `aggregateRating` by construction. **The measurement is the argument, and it was taken rather than assumed.** The seven standard sizes, all describing one facility at one price in one city, score **0.738** against each other at their worst pair (10×15 vs 10×30) — clear of 0.8 with real margin, because the comparison and "typical" sentences differ. Two sizes the guide catalogue does NOT cover score **0.940**, because an unlisted size gets the measurement sentence and nothing else. Both numbers are pinned as tests. **Compared against sibling sizes only, never against the city page** — `findDuplicatePairs`' own rule, pairwise within a kind, because a two-paragraph size intro scored against a differently-shaped city intro would pass for reasons of length rather than content, and a gate that passes for the wrong reason is worse than no gate. **`follow` rather than `nofollow`** on a gated page, because it links to the facilities and those links are worth crawling whatever this page is worth. **The sitemap honours the gate**, since a sitemap entry carrying `noindex` asks a crawler to fetch a page and then tells it the fetch was pointless. **What it costs, and it is a real cost:** a portfolio with non-standard unit types — an 8×12 and an 8×16 — gets pages that exist, serve visitors and are never indexed, with no way for an operator to fix it, because there is no authored override for a size page the way B-128 added one for a city. That is **B-134**, and this is deliberately not it: an authored field is a column, a migration and an editor, and building it before a real portfolio has tripped the gate would be guessing at a form nobody has needed yet. The duplicate-content report lists every size intro so the gated ones are at least visible. |
| D-78 | Whether B-090's PWA push layer and two-way SMS inbox get built, given both are conditional in their PRDs rather than committed (owner, 2026-08-20, while splitting B-090) | **Neither becomes a build row. Both are answered here.** **PWA push: no.** PRD 00 §6 gates it on "*if metrics justify*", and there is no portal-engagement metric in this product to justify it with — nothing counts logins, repeat visits or notification opt-ins, so the condition cannot be evaluated, let alone met. **Two-way SMS inbox: no, and the real defect underneath it gets fixed instead.** PRD 05 lists it as "(evaluate)". The evaluation: at 2–10 facilities, inbound texts arrive at a volume one person handles from a phone, and an inbox is a second queue beside `Task` that has to be watched to be worth having. What is NOT acceptable is the current behaviour — `sms-webhook/route.ts` handles STOP/HELP/START/YES and **silently drops every other inbound message**, so a tenant replying "I paid yesterday" is answered by nothing at all. That is **B-135**: route an unrecognised inbound SMS to the existing `Task` queue. | Same shape as the kiosk (PRD 03 §8, default answer "no") and the marketplace channel evaluation (B-089): a PRD that says "if justified" or "evaluate" is asking for a decision, and building the thing instead is how a conditional silently becomes a commitment. **The PWA is the clearer no of the two** — it is a platform layer, not a feature, and its cost is permanent (a service worker is a cache that outlives your deploys and is a genuinely hard thing to debug when it goes wrong). Revisit only with a number: repeat portal visits per tenant per month, which nothing currently records. **The inbox answer is a redirect rather than a refusal, and that is the point.** The evaluation found a real bug wearing a feature's clothes: the argument for an inbox is "a tenant texted us and nobody saw it", and that argument is satisfied by making the text become a task in the queue staff already work, without a second inbox to staff, a threading model, or a `direction` column on `Message`. If B-135 ships and the task volume proves an inbox is warranted, that is a number to reopen this with — which is exactly the evidence PRD 05 asked for and nobody had. |
| D-79 | Whether a waitlist entry is a `Lead` (settled while building B-090 part 1, 2026-08-20) | **Its own `WaitlistEntry` model, not a `Lead` row with a new status.** No tenant, no lead, no reuse of `LeadStatus`. | It looks like a lead — a prospect, an email, a unit type they want — and reusing `Lead` breaks two things that already work correctly. **`raiseLeadFollowUps` raises a task for every `new` lead nobody has contacted inside the facility's window**, and a waitlist entry is precisely the case where there is nothing to call about: the whole design is that nobody touches these rows until a unit frees up and the sweep writes to them. Every entry would become staff work that cannot be actioned, on the ONE list a part-timer checks on a Saturday (US-41) — which is how a queue stops meaning anything. **And B-082 part 4's funnel counts leads as its denominator**, so every waitlist entry would silently dilute the conversion rate an operator prices against. That is the same class of defect B-131 fixed: a report answering a different question under the same name, with nothing on screen to say it had changed. The cost of a separate model is one table and one migration; the cost of the alternative is two working features quietly degrading, neither of which would announce it. |
| D-80 | How a waitlist availability email is classified, given the recipient is not a tenant and has no consent record (settled while building B-090 part 1, 2026-08-20) | **Transactional**, sent through `sendDirectEmail` — the path B-073's abandonment mail and B-020's resume links already use — and carrying a cancel link regardless. Email only; no SMS. | **The recipient asked to be told about one named unit type at one named facility, and this is the single message that answers that request.** That is the primary-purpose test, and it is a genuinely different case from B-073's abandonment mail, which this repo classified conservatively as marketing: abandonment is US contacting THEM about something they walked away from, while this is them asking to be contacted about one specific thing. Marking it marketing would put it behind marketing suppression and the FR-MSG-5 quiet-hours window, which would mean a prospect who asked to be told about a free unit is not told for nine hours while somebody else rents it — the exact loss the feature exists to prevent. **The cancel link is not a concession to that argument, it is independent:** an outbound address we hold has to have a way out whatever the classification, and it is the only credential a non-tenant has for their own record. **No SMS, and that is a consent decision rather than a scope one:** a phone number typed into a "notify me" box is not TCPA consent to text it, so the column exists for staff to CALL somebody and nothing sends to it. Adding an SMS channel means an explicit opt-in with disclosure at capture, the shape PRD 04 US-13 already specifies for marketing. |
| D-81 | Whether a tenant-initiated transfer commits, or only asks (settled while building B-090 part 2, 2026-08-20) | **It only asks.** The portal records the request and holds the target unit; B-077's staff wizard is still the only thing that calls `completeTransfer`. Not a caution about money — the arithmetic is identical either way, and it is shown to the tenant before they ask. It is that a transfer moves PHYSICAL GOODS between two units, and the instant the old lease closes its unit derives to `available` and is rentable to a walk-in while the tenant's things are still in it. Only a person on site can say the old unit is empty. This is the same argument US-707 settled for move-out, and it is deliberately settled the same way rather than differently. **The consequence a later session must not reverse:** a "complete this from the portal" button is not a missing convenience, it is the removal of the only check that the unit was emptied |
| D-82 | What holds the requested unit, given B-090's own audit said only business accounts needed a schema change (settled while building B-090 part 2, 2026-08-20) | **A `Reservation` with `source = 'transfer'` — no new table.** A hold is exactly what `Reservation` is, it already derives the unit to `reserved`, it already has an expiry sweep, and its partial unique index (`reservation_one_held_per_unit`) is already the real guarantee against two people being promised one unit. Three consequences were bought deliberately with it: (1) `previewTransfer`/`transferTargets` gain ONE exception — a unit that is `reserved` for the same tenant reads as available TO THAT TENANT, and to nobody else, or the request would block the transfer it exists to set up; (2) `reserveUnit`'s duplicate guard is scoped to non-transfer holds, so a tenant reserving the same size on the public site cannot silently repoint their own transfer request into a move-in checkout; (3) the `tokenHash` is minted, hashed and discarded, so unlike a prospect's hold no URL resolves to it |
| D-83 | Where an inbound text is kept, given D-78 refused the two-way inbox that would have given it a table (settled while building B-135, 2026-08-20) | **In the domain event, with the task pointing at it.** Not `Message`, which is outbound by construction — `eventId`, `ruleId`, `templateKey` and an idempotency key over all three are required columns and an inbound text has none of them; adding a `direction` column is the inbox D-78 declined, arriving one column at a time. Not `Task.proof`, which is completion evidence: the body sitting there would satisfy the type's own note requirement and let a staffer close the task without reading it. `sms.inbound_received` carries the words, and the task is `entityType: 'DomainEvent'` with `sourceEventId` set — the relationship `Task` has had since B-095, and the shape `gate_manual_action` already uses. **One task per MESSAGE, never per tenant per day**, which is the part a later session must not 'tidy': `createTask` dedupes on `(type, entityId, businessDate)`, so keying it on the tenant would make two questions in one afternoon a single task and the second person's words would be nowhere at all |
| D-84 | Whether a transfer quote a tenant was given binds the completion, given `completeTransfer` re-read the street rate (B-136, 2026-08-20) | **The hold locks the rate, flat, in both directions, for exactly as long as the hold lives.** `previewTransferFor` honours the hold's `quotedRateCents` when the hold is on the unit being previewed, and falls back to the current street rate otherwise. Three consequences, taken deliberately. (1) **Flat, not `min(quoted, street)`** — a rate that FALLS between the ask and the completion still settles at the quote, which is what a `CheckoutSession` and a prospect's `Reservation` have both always done, and inventing a floor here would give a transfer a pricing rule nothing else in the codebase has. (2) **The window is the hold's own expiry**, not a new one: `transferHoldFor` already filters on `expiresAt`, so an expired hold is no hold and the rate re-quotes — which reuses D-7/B-126's answer (anchored to the move-in date, plus `reservationHoldGraceDays`) rather than adding a second, competing clock. A tenant who reschedules cancels and re-asks, and is re-quoted, because `requestTransfer` refuses a second live request. (3) **A staff-initiated transfer is unchanged** — no hold, so it re-reads the street rate, which is correct for it: nobody was quoted anything. The locked figure is stated on both screens (the tenant's pending-request page and the staff wizard's "the tenant asked for this" note) rather than only applied, because a settlement quietly below street rate reads to staff as a bug. |

| D-85 | Whether a lease in the lien pipeline may transfer at all, and under whose authority (raised by the 2026-08-21 operator review — **RESOLVED 2026-08-21**) | **Recommendation, not a decision — the owner still has to pick.** The confirmed defect is narrow: `OCCUPYING_LEASE_STATUSES` includes `pending_auction` and the tenant-facing transfer scopes on it, so a tenant can self-serve out of the pipeline preparing their goods for sale. Every option below refuses that, and **B-137 builds only that refusal**. What is genuinely the owner's call is the *staff* side. **Option A (recommended): staff may transfer a delinquent or lien-pipeline lease with manager-and-above approval, a reason code and an audit entry, and the lien clock does not reset.** An operator very often *wants* to let a delinquent tenant downsize into something they can afford — "block the transfer" is the wrong default, because refusing costs the operator both the tenant and the balance. **Option B: no transfer past a configured delinquency stage, full stop.** Simpler to defend in a lien dispute: the evidence chain never changes units mid-cycle, and there is no argument that the served notice named a unit the tenant no longer occupied. **Option C: configurable per facility, the stage being the setting.** Honest if both answers are right somewhere, and one more per-facility switch nobody sets. **The trade-off is revenue against evidentiary tidiness**, and the exposure is real in both directions — a served lien notice names a unit, and moving the goods after service is a fact somebody's attorney will ask about. Per D-10 this needs the same attorney pass every lien-adjacent behaviour in this project carries; all text remains draft-only and is not legal advice. **Resolved 2026-08-21 (owner): Option A.** Staff may transfer a delinquent or lien-pipeline lease with manager-and-above approval, a reason code and an audit entry, and the lien clock does NOT reset. Reasoning recorded so it is not re-argued: refusing outright costs the operator the tenant *and* the balance, and letting somebody downsize into a unit they can afford is a collections tool rather than a concession. The tenant-facing self-service refusal stands unchanged — B-137 builds it, and the portal never offers a lien-pipeline transfer at all. The evidentiary cost Option B was protecting against is carried instead by the reason code and the unreset clock, so a served notice naming a unit can still be reconciled against the move. **Build split, recorded 2026-08-21 at B-137:** B-137 shipped the tenant-facing half — the portal refuses a lien-pipeline transfer outright. The staff-side approval, reason code and unreset-clock assertion this option actually chose are **B-157**. **B-157 shipped 2026-08-24, completing this decision.** All three controls are built: an explicit manager-and-above rank check (`leases:transfer` satisfied the level only by accident and would have stopped doing so the moment the permission was granted to counter staff); a closed `LIEN_TRANSFER_REASONS` vocabulary written to its own audit action `lease.transferred_in_lien_pipeline`, whose `requiresReason: true` makes `recordAudit` itself refuse an entry without one; and the unreset clock, which turned out to be **a live defect rather than an assertion to add**. D-86 re-points the unpaid invoices at the new lease, so the old lease's ledger nets to zero — and `AuctionCase`, pinned to that lease, therefore read `outstandingCents: 0` and raised a `balance_settled` blocker, *"This lease owes nothing. There is no lien to enforce."*, against a tenant who still owed the entire claim. The case now reads the balance and the `block_auction` holds forward across the transfer chain while staying pinned to the lease and unit the served notice named. |
| D-86 | Whether arrears and the delinquency clock follow the tenant through a transfer, or stay on the lease that ended (raised by the 2026-08-21 operator review — **RESOLVED 2026-08-21**) | **Recommendation, not a decision: they follow the tenant.** Confirmed today: the balance and invoices stay on the old lease, the transfer sets it to `ended`, the engine halts it as `moved_out`, and the new lease has no invoices and 0 days past due — so **collections stop entirely on a live tenant who owes money and never left**. Something must change; which thing is the owner's. **Option A (recommended): re-point the unpaid invoices at the new lease**, so one lease holds one tenant's money and every existing reader — ledger, aging, ladder, statements — keeps working unchanged. Cost: a transfer edits invoice history, and "which lease was this invoice raised against" stops being answerable from the row alone unless the move is recorded. **Option B: leave the invoices where they are and teach the engine that an `ended` lease with a transfer link is not `moved_out`.** The record stays literally true; the cost is that every reader of `Lease.status` and every AR query has to learn about the link, and the first one that does not is a silent hole exactly where a dispute looks (D-21's warning, in reverse). **Option C: settle the old lease at transfer and re-charge the arrears as an opening balance on the new one.** Clean ledgers, but it resets `daysPastDue` (D-25) to zero and hands any tenant a way to age out of the ladder by asking for a swap — the defect this decision exists to close. **Recommended shape is A with B's audit trail bolted on:** re-point, and write the move as an append-only record naming both leases, so the history question stays answerable without every reader changing. **B-138 is written to whichever answer this takes.** **Resolved 2026-08-21 (owner): Option A.** Unpaid invoices are re-pointed at the new lease, with an append-only record of the move so "which lease was this raised against" stays answerable. Reasoning: every existing reader — ledger, aging, ladder, statements — keeps working unchanged, and `daysPastDue` stays continuous, which is the whole defect being closed. Option B was refused because it makes every reader of `Lease.status` learn about a transfer link, and the first one that does not is a silent hole exactly where a dispute looks. Option C was refused outright: it resets `daysPastDue` and re-creates the amnesty. B-138 builds this. **Built 2026-08-21 (B-138), with one refinement worth recording:** the append-only record is a pair of `adjustment` ledger entries PER INVOICE, each carrying that invoice's id, rather than one lump sum — a lump sum has no invoice behind it, so `leaseLedger`'s reconciliation would count it as an uninvoiced charge and report a discrepancy equal to the arrears on every transferred lease. And Option B's link (`Lease.transferredFromLeaseId`) was built after all, for the two pieces of state that are NOT invoices and must not be rewritten: the delinquency step runs (evidence that a notice was served against a lease naming a unit) and a paid late-fee invoice (proof a ladder step was already charged). Those two readers walk the link; every money reader was left unchanged, which is what Option A was chosen for. |
| D-87 | Whether a waitlist notification holds the unit for its claim window (raised by the 2026-08-21 operator review — **RESOLVED 2026-08-21**) | **Recommendation, not a decision: no hold, but say so on the page.** B-090a's sweep notifies as many people as there are free units, oldest first, and holds each *claim* for 72 hours — it does not hold the *unit*, so the person we emailed races the public site and can lose. **Option A (recommended): keep it a race and make the copy honest** — "first to complete a rental gets it". Costs nothing, keeps inventory on sale, and is what the sweep already does; the failure mode is a prospect told they were next who was not. **Option B: hold the unit for the claim window** through the existing `Reservation` mechanism (D-82's precedent — a hold is exactly what `Reservation` is), which honours the promise the notification implies and takes a rentable unit off sale for up to 72 hours per notification, at a facility whose entire problem is that it has none. **Option C: a short hold of a few hours**, a compromise nobody has measured. **The trade-off is a broken implicit promise against real inventory held off-sale** — operator economics, not a technical question. Whichever way it goes the notification copy must change, because the current copy implies a hold that does not exist. **B-154 carries the copy half of whichever answer this takes.** **Resolved 2026-08-21 (owner): Option A.** No unit hold — the notification stays a race, and the copy says so plainly ("first to complete a rental gets it"). Reasoning: a hold costs real inventory off-sale at a site whose entire problem is having none spare, and the sweep already behaves this way, so only the copy is dishonest. B-154 carries the copy change. |
| D-88 | What happens to a scheduled rate increase whose notice provably did not arrive (raised by the 2026-08-21 operator review — **RESOLVED 2026-08-21**) | **Recommendation, not a decision: hold the increase and raise a high-priority task.** Reported and still needing confirmation at build time: the status flips to `notice_sent` before the event fires, and nothing reconciles it against `Message.status`, the suppression list or a hard bounce — so an increase applies 30 days after a notice that bounced. PRD 02 US-11 blocks an effective date that violates the minimum notice period, which is a guarantee about *delivery* that the code currently makes about *intent*. **Option A (recommended): a hard bounce, a suppression hit or no send record blocks the increase and raises a task**; the operator re-notices from a good address and the clock restarts. Cost: revenue slips a month per bad address, and a tenant who lets their inbox rot is cheaper to keep at the old rate. **Option B: apply anyway and raise the task.** No revenue slip, and the operator is charging more on a notice they can prove did not arrive — the one fact that makes the increase disputable. **Option C: fall back to postal**, which **D-37 deliberately declined to build** and which needs the per-state "requires written notice" flag D-10's attorney pass has not settled; choosing C reopens D-37 rather than extending it. **The trade-off is a month of revenue against an increase that cannot be defended.** D-10's framing applies directly: this is exactly the kind of behaviour the attorney pass exists for, and nothing here is legal advice. **Resolved 2026-08-21 (owner): Option A.** A hard bounce, a suppression hit or no send record blocks the increase and raises a task; the operator re-notices from a good address and the clock restarts. Reasoning: US-11's minimum-notice guarantee is about delivery, and an increase applied on a notice that provably did not arrive is unwinnable if disputed — a month of slipped revenue is the cheaper side of that trade. Option C stays closed: postal fallback reopens D-37 rather than extending it. B-152 builds this. |
| D-89 | Whether a UNIT TRANSFER triggers promotional recapture (settled while building B-145, 2026-08-21) | **No — a transfer recovers nothing.** The `MoveOutReason` enum's own comment already settles the principle: "a transferred tenant is still a tenant", which is why B-077 gave `transfer` its own reason value rather than counting it as a departure. A move between units at the same site does not break the promise the minimum stay was given for, and billing a recapture on one would charge somebody for staying. Built as a guard on `previewMoveOut`'s optional `reason`, so the screen (which previews before a reason is chosen) assumes a real departure and only `completeMoveOut` — which knows why — can suppress it. **The cost is named rather than mitigated: this is an avoidable-charge path.** A tenant inside a minimum stay can transfer to the cheapest unit on site and walk out of the next lease owing nothing, because the redemption stays attached to the ended lease and the new one carries no promotion. Closing it means deciding whether a promotion FOLLOWS a tenant through a transfer — which is a larger question than recapture (it also governs whether the remaining discounted periods survive the move) and is not answered here. B-157 already owns the transfer wizard's controls and is the natural home if an operator ever reports it. |
| D-90 | Whether a failing accessibility scan should BLOCK a deploy, or the public statement should stop claiming it does (raised by the 2026-08-24 accessibility review, **RESOLVED 2026-08-24**) | **Correct the sentence; do not build the gate.** The statement said the axe scans "block a release if they fail" and they never have: every scan is in the `e2e` lane of `.github/workflows/ci.yml`, gated on `main` or a non-draft PR, while `vercel.json` carries no `ignoreCommand` and there is no deploy workflow — so Vercel's Git integration builds and ships a push to `main` in parallel with Actions and independently of its result. **Option A (chosen): rewrite it to "run on every push to our main branch and on every open pull request" and say in as many words that they are not a release gate.** Free, true today, and it claims less — which is this page's standing rule. **Option B: build a Vercel `ignoreCommand` or a deploy workflow so a red scan genuinely stops a ship, and keep the stronger sentence.** Costs the full e2e lane (~30 min of Actions) on every merge to `main` rather than only on ready-for-review PRs, and hands a flaky scan the power to block a deploy. **Resolved 2026-08-24 (owner): Option A.** Reasoning: the page is a live public claim and the overstatement is live NOW, so the free correction lands today and a gate that takes a session to build does not; and a release gate is a CI-cost and release-velocity decision that should be made on its own merits, not smuggled in as the price of making one sentence true. Option B is not closed — it stays available, and taking it later is a one-line edit to this page plus the gate. B-159 builds Option A. |
| D-91 | Whether moving a `pending_auction` tenant's goods to another unit under D-85 requires RE-SERVING the lien notice (raised while building B-160, 2026-08-24, **RESOLVED 2026-08-24**) | **Yes — re-serve, naming the unit the goods are in now.** D-85 lets staff move a tenant who is already in the lien pipeline; B-157 built the approval, the reason code and the unreset arrears clock. What nobody had answered is what happens to the NOTICE, and a Texas Property Code Ch. 59 notice names the space — so once the goods are somewhere else, the notice on file describes a unit they are not in, and the advertisement generated from it would name a unit that may since have been rented to somebody else. **Option A (chosen): a transfer out of the pipeline blocks the sale until a lien notice is served naming the new unit.** The notice period runs again from that service; **the arrears clock does not reset** — that is D-85's rule and this does not touch it. Cost: the full notice period every time goods are moved, which is also the deterrent against moving them casually. **Option B: the audit record of the move suffices** — the case renders both facts and B-157's audit context carries the reason and the approver, so the sale proceeds on the original clock. Faster, and defensible only if a court reads the move as continuity of the same claim rather than a new one. **Option C: re-serve only when the unit TYPE changes** — a like-for-like shuffle keeps the clock, a resize re-serves. Rejected: it puts a legal distinction inside a size comparison, and it produces the worst outcome of the three if a court disagrees about which kind a given move was. **Resolved 2026-08-24 (owner): Option A.** Reasoning: an advertisement naming a unit the goods were never in is the commonest wrongful-sale claim after an unserved notice, and the two most expensive failures this pipeline can produce are both notice failures. D-10 applies unchanged — Texas default, built per-state configurable, draft-only and not legal advice. **Built as a read, not a rule**: `auctionCase` looks for the served notice against the CURRENT lease rather than the pinned one, so a moved case simply does not find one, and a distinct `notice_names_another_unit` blocker says why — a manager who served the notice themselves must not be told that no notice was served. B-160 builds this. |

| D-92 | How far a reversal-re-opened arrear may be pushed down the delinquency ladder, and from where (raised while building **B-161**, 2026-08-24, **RESOLVED 2026-08-24**) | **Ten days of grace, then RESUME at the stage already reached.** Per-facility configurable both ways (D-10) on the delinquency timeline itself — `reversalGraceDays` (0–90, default 10) and `reversalResumes` (default true) — because the version that governed a lease is the record a lien file is defended from, and these two numbers decide when the next notice goes out. **And, separately from the policy: at most ONE step per lease per nightly run, ever.** | A returned ACH, a bounced cheque or a chargeback re-opens the invoice at its **original** due date (D-25, and correct — the money never arrived), so the tenant is instantly back at full age. Before this, `cure()` had superseded the whole step history, so the ladder restarted from an empty record at day 90 and every step fired in one pass: four dunning letters in the same post, access cut, and the auction case opened by a bank reversal overnight with no human in it. **Resume, not restart**, because restarting re-serves ninety days of notices and hands any tenant a way to defer an auction indefinitely by paying and reversing — and resuming is defensible precisely because every earlier notice really was served and is still in `DelinquencyStepRun`. **Ten days** is ordinary NSF re-notice practice and enough for a bank-side error to be put right by somebody holding a receipt and a cure confirmation. The grace runs from the reversal, and the ladder is held for as long as the `settling_payment_failed` task stays open on top of that — while staff are working the bounce there is a person in the loop, which is the point of that queue. **The one-step-per-run cap is not part of the policy and is not configurable**: a ladder served all at once is not a ladder, and a lien file whose ninety days of notices all bear one date is not a lien file. Catching up N missed steps now takes N nights, deliberately. |

| D-93 | What rate a UNIT TRANSFER opens the new lease at, and whether a promotion follows the tenant through one (raised while building **B-162**, 2026-08-24, **RESOLVED 2026-08-24**; D-89 named this and handed it to B-157, which shipped without it) | **Three answers.** (1) **The tenant keeps their discount off street**, scaled to the new unit: `new street × (in-place ÷ old street)`, capped at street. Per-facility configurable to `street` or `in_place` (`Facility.transferRatePolicy`, default `preserve_discount`), and staff can type a different figure on the transfer preview either way. (2) **The promotion follows the tenant** — the remaining discounted periods, the minimum stay and the recapture obligation all move to the new lease. (3) **An in-flight rate increase is cancelled by the transfer**, with an audit record, and named on the preview before staff confirm. | (1) `street` is what shipped and it made a like-for-like move between two 10×10s a **rent increase served with no notice period, no approval and no `TenantRateIncrease` record** — every protection US-11 puts around raising a rent, bypassed by moving the tenant sideways through a screen for a service request they asked for. `in_place` avoids that and prices a downsize absurdly: 10×20 money for a 5×10, which can exceed that unit's street rate. Preserving the discount is the only rule that is right on a lateral move, a downsize AND an upsize, because it carries the thing the tenant actually holds — their position relative to street — rather than a dollar figure that only means anything against the unit they are leaving. **An upsize still costs more and the preview says so**, in an amber note naming both figures: a transfer is not a rate increase, so there is no notice period behind that number and the tenant has to be told before it is confirmed. (2) D-89 settled that a transfer recaptures nothing — "a transferred tenant is still a tenant" — and named the hole it left open in as many words: the redemption stayed attached to the ended lease, so a tenant inside a minimum stay could transfer to the cheapest unit on site and walk out of the next lease owing nothing, **and** lost the discounted months they had been promised. Both halves move together; carrying the obligation without the benefit would take something from the tenant for making a move the operator wanted. The redemption is **re-pointed**, not copied — `PromoRedemption.leaseId` is unique, one redemption is one promise — and its `appliedPeriods` are untouched because the period index is now counted across the whole transfer chain rather than from the new lease's own start. (3) The transfer sets a new rate, so an increase approved against the old one is a delta from a figure that no longer exists; `applyDueIncreases` would refuse it at apply time anyway, except that it never got there — the lease had ended, so the nightly run reported `ok: true, "skipped — the lease has ended"` every night while an approved, NOTICED increase evaporated. Refusing the transfer instead was rejected: it blocks a retention action for a reason the tenant cannot fix, and staff would cancel the increase and retry for the same outcome. **Re-pointing it was rejected too**, and this is the useful part — the approver signed off on a delta from a specific figure, so a re-pointed increase either has to be re-based (applying an approval nobody gave) or is refused at apply time regardless. |
| D-94 | **What a rule-based ECRI batch raises a tenant TO, and what a newly created facility's step is** (raised by **B-165**, 2026-08-24; the row could not be finished without it) | **A per-facility percentage step of the in-place rate, bounded by a dollar floor and ceiling, rounded to the dollar, capped at street.** `targetRateFor` computes `min(street, round(inPlace + clamp(inPlace × pct, minStep, maxStep)))`. The seeded default for a new facility is **10%, floor $5, ceiling $30, capped at street**, with US-11's eligibility example (≥ 9 months since the last change, ≥ $15 below street) becoming two more columns rather than staying a module constant. Every figure is per-facility (D-10) and every one has a control on `/admin/settings` in the same item. | The previous rule was `return candidate.streetRateCents` — the entire function — so a tenant at $89 against a $145 street rate received **one letter raising them 63%**, and nothing in the product could express anything else. The percentage is taken against the **in-place** rate rather than the gap because that is the number a tenant judges the letter by; the dollar **ceiling** is what actually bounds it, since a percentage alone still compounds on a high rate. The street cap is applied **last**, after the clamp and the rounding, so nothing downstream can carry a sitting tenant past what a walk-in would pay today — a position that cannot be defended on a retention call. **On the default:** a conservative number protects tenure and leaves money on the table, an aggressive one is the old behaviour with a nicer form. 10%/$30 is the common industry step and closes a large gap over two or three cycles instead of one; the owner chose it explicitly rather than letting the code's silence choose. Keeping the old behaviour as the seeded default was rejected for the reason the row was raised: a facility nobody has configured must not send the letter that loses the cohort. **What this does not do:** it does not change one-off increases (an operator typing a figure is already an explicit decision, still gated by the notice period and the regional approval), and a policy configured down to no step at all now drops the lease from the worklist rather than scheduling a notice announcing that nothing changed. |

| D-95 | **What order the tenant profile takes, and what may be hidden behind a disclosure** (raised by **B-181**, 2026-08-25; the row carried its reviewer's MEDIUM confidence and asked for an operator pass before the order was committed to) | **The read stack first, in one uninterrupted run, then a single `Actions` region holding every occasional write, all closed.** Banners unchanged; then Leases, Outstanding fees, Contact, Address of record, Referrals, Notes, Documents, Recent gate activity, Communication history; then `Actions` — support session, place a hold, charge a fee, refund, record a returned payment, military service, 24-hour gate access. Contact and address render as **text** with an Edit disclosure beside them. The two long logs show five and put the rest behind a named disclosure. No new data and no new queries. | The screen is what a staffer reads with somebody on the phone, and the units, rates and balances were the **ninth** section — behind a support-session form, a contact form and an address form, none of which is touched more than a few times a month. Two other orders were on the table. **Money near the top** (fees, charge, refund, record a return directly under Leases) buys a shorter reach for the counter's commonest write and costs a longer read stack before the contact details; it was refused because the read stack is the thing being defended, and `Outstanding fees` — which is a *reading* of what is owed, with its waive control attached — already sits there. **Disclosure in place, no `Actions` region** is the smaller diff and was refused for the opposite reason: it fixes how deep the leases are without fixing that there are seventeen headings to pass, and it leaves the writes scattered so there is no one place to look for a control. **What is deliberately NOT hidden:** the banner stack (the reviewer named it as working, and every control that can act on the account still sits below it); `Outstanding fees`, because a figure owed is a reading, not an act; and **Flag as returned mail**, which is one click, is the counterpart to the returned-mail alert directly above it, and filed under "Edit address of record" would be filed under the wrong verb. **The accessibility position, which is what makes disclosure legitimate here:** every one is a native `<details>`/`<summary>` — the collapsed state is exposed without script (4.1.2) and each summary names what it opens rather than the act (2.4.6, so "Edit contact details" and "Show 12 older messages", never "Edit" or "Show more"). And the axe pass now runs **with disclosures open**, per FR-25 (1): everything behind a closed `<details>` is invisible to axe, so without that this row would have moved half the page's controls out of the audit rather than into it. |
| D-96 | **What payment satisfies a payment-plan installment** (raised by **B-188**, 2026-08-25; **RESOLVED 2026-08-26**) | **Rent first, then the plan. An installment is satisfied only by money that settles the ARREARS the plan was agreed over** — the invoices already past due at the moment it was created, frozen on the plan as `PaymentPlan.invoiceIds`. Current rent stays due on its own date on top of the installments, and paying it credits nothing to the plan. Progress is `totalCents` less what is still outstanding on those frozen invoices, clamped at zero. **Not per-facility configurable**, deliberately: a rule a tenant can be told only after looking up their site is a rule staff will state wrongly. | Both alternatives were on the table and both were refused. **"Any payment counts"** — what shipped — meant a tenant on a plan for $1,800 of arrears who paid only next month's $150 rent had installment 1 marked `paid` while dunning, late fees and access suspension all stayed halted; the plan could **complete having collected nothing toward the arrears**, and the tenant sat permanently behind a hold owing more each month than when they started. "You kept to the plan" has to mean the debt came down or it means nothing. **"Any payment, minus the month's rent"** reaches the same answer from cash rather than from invoices, and costs a per-month rent lookup while getting none of the netting for free. **The invoice framing is what makes the defect fixes fall out rather than be built:** a returned ACH, a chargeback and a refund each unwind their allocations and `recomputeInvoices` re-opens the invoice, so progress goes backwards without the arithmetic knowing any of those things happened — which is why a plan can no longer complete itself and lift its own hold on money the bank took back. It also gives `validateSchedule`'s total a real referent: the installments are now checked against **what is owed** rather than against their own sum, so the branch that refuses a $50 plan against an $1,800 arrear is reachable from the only production caller for the first time. **What this costs is the harder collections case** — a tenant who must find rent AND an installment in the same month is the profile most likely to break — and the mitigation is that the rule is stated in customer language on `/portal/payment-plan` and on the staff form **before** a plan is agreed, never discovered afterwards from a missed installment. B-191's CN-24 messages carry the same sentence. **The covered invoices are frozen as a list rather than re-derived** from `dueDate <= createdAt`: a fee invoice raised later the same day carries that day's due date and would silently join the covered set, making progress fall for a charge the plan never promised to clear. |
| D-97 | **What autopay does for a lease on an active payment plan, in both directions** (raised by **B-189**, 2026-08-25; **RESOLVED 2026-08-26**) | **Both halves fixed, and option (A) chosen with an opt-out: installments ARE auto-collected by default, and the staffer can untick it at agreement.** **The arrears half.** The `payment_plan` hold still does not declare `halt_autopay`, and deliberately so — that effect is all-or-nothing per lease, so it would stop collecting current rent too and turn forbearance on what is owed into a rent holiday nobody agreed to. Instead `runAutopay` skips exactly the invoices the plan froze into `invoiceIds` at creation (D-96) and charges everything else on its own due date. **The installment half.** On an installment's due date the shortfall is charged from the saved method as ONE ledger-driven `PaymentIntent` (D-6 — not Stripe Billing, not a subscription, not a schedule object), carrying the plan id so the allocation is narrowed to the covered invoices and can never wander onto rent the plan never deferred. **Why (A) with an opt-out rather than (A) flat or (B).** Auto-collection collects more and is kinder to the tenant who wants it handled, which is most of them; but (B)'s argument is real — "you kept to the plan" should be able to be a statement about the tenant rather than about their card — and a checkbox at agreement costs one column and one field, which is cheaper than being wrong for every tenant in either direction. The default is ticked because the default is what most plans want. **Three separate ways a plan is manual-pay in fact**, and screens must not conflate them: the box was unticked, the lease has autopay off, or the tenant has no saved card. **A failed installment is a decline, not a breach (CN-6).** It goes onto US-20's +1/+3/+5 ladder anchored on the installment's own due date, and the hour-4 breach job leaves the plan standing while that ladder is still running — bounded by the facility's last retry offset, and ended immediately by a terminal decline code. A manual-pay plan has no ladder, so a missed installment breaks it the same night it always did. **What this does NOT settle:** what a plan may commit to in the first place (D-98), and telling the tenant any of it — the agreement, the installment-due reminder, the break — which is B-191's CN-24 and is not built. |
| D-98 | **What a payment plan may commit to, and how many a lease may have** (raised by **B-190**, 2026-08-25; **RESOLVED 2026-08-27**; the operator review's grace-days question is folded in here rather than given a row) | **Five numbers, and one of them is a new column rather than a reused one.** **(1) Authority is `maxPlanDeferralCents`, its own per-role limit — $0 counter and bookkeeper, $2,000 manager, $10,000 regional, unlimited owner — checked by `checkMonetaryAuthority(actor, 'payment_plan', arrears, facilityId)` on the WHOLE amount deferred, escalating by naming the next role exactly as a fee waiver does. **Reusing `maxFeeWaiverCents` was the obvious answer and it does not work**: a manager's waiver limit is $50 and a typical arrear is $1,800, so every real plan on the property would have escalated to the owner and the gate would have been turned off within a week. B-167's symmetric rule is right about a FEE, where a departure from the schedule in either direction is a gift; a plan gives nothing away — the money is still owed and the tenant is still on the hook for all of it. **(2) `planMaxDays`, default 90.** `MAX_INSTALLMENTS = 6` caps the count and on its own caps nothing that matters: six installments spaced sixty days apart halt dunning, late fees and access suspension for a year while the lien clock does not run. Whichever of the two binds first. **(3) `planMaxPerRollingYear`, default 2, and the SECOND one is agreed a rank up.** Cancelled and broken plans count, because the defect was that a plan broken last night was replaceable this morning, for ever — **refined by D-108 (2026-08-31): a plan cancelled the same facility-local day it was agreed, having collected nothing while it stood, is a correction and does not count.** The escalation floor is read from the role table (`lowestRankWith('delinquency:execute_step')`) rather than compared against a hardcoded `manager`, so a new role slots into the ladder without a code change — the rule `nextApproverRole` already follows. The plan past the limit is refused outright with the count named. **(4) `planGraceDays`, default 3.** A plan that breaks at 00:01 over money handed across the counter that afternoon is technically right and commercially wrong. Three rather than D-92's ten, deliberately: a returned payment is a bank error done to the tenant, while a plan is a date the tenant chose. Zero is legitimate and restores the break-that-night behaviour. **It is a separate window from B-189's retry ladder and must never be netted against it** — the ladder asks whether a charge is still coming, the grace asks whether the tenant still has time to pay some other way, so a terminal decline ends the ladder immediately and still gets its three days. **(5) Carry-forward needed no column and no arithmetic.** D-96 already measures progress as the frozen arrears less what is still outstanding on the covered invoices, so a replacement plan is agreed over what was LEFT and its own progress correctly restarts at zero — nothing was ever being discarded except the record. What was missing was legibility: `paymentPlanForLease` was a `findFirst` ordered `createdAt desc`, so a lease on its fifth plan read on every screen as a lease on one. `paymentPlansForLease` returns the chain, each plan carrying `collectedCents`, and the tenant profile states the count and the total collected in a sentence. **All three facility numbers are per-facility configurable (D-10) and have controls on `/admin/settings` in the same item.** `maxPlanDeferralCents` does not — it joins three sibling role limits that have never had an editing surface, which B-004's own scope claimed and never delivered; that gap is now **B-197** rather than absorbed silently. |
| D-99 | **Whether a short-notice move-out is charged, and on what basis** (raised by **B-194**, 2026-08-25; **B-186 named this and deliberately did not settle it**) | **OPEN — owner decision, with a legal read.** The admin move-out screen states "the lease's remedy is a charge, not a refusal" and `settleMoveOut` posts nothing — no shortfall input, no ledger entry, no fee. B-194 corrects the copy either way, because a screen naming a consequence the product never applies is worse than one naming none. The decision is what replaces it: **(A) no charge** — the shortfall figure stays informational, the copy says so plainly, and the notice date exists as the compliance record B-186 built it to be; **(B) charge the shortfall**, posted as a fee invoice through B-167's existing primitive so autopay collects it and `waiveFee` can reach it — which needs a new `FeeType` (B-167 explicitly shipped "no new fee types"), a basis (per-day of shortfall, a flat fee, or rent-through-the-notice-period), and a lease clause that says so. **This is a lease-terms and per-state question, not a UI one** (D-10: Texas default, per-state configurable, all notice and lease text draft-only and **not legal advice**) — charging a fee the lease does not provide for is the kind of small line item that reappears as a class-wide claim. The build is small once the basis is settled; the basis is not the build session's to choose. **B-194 shipped the copy correction on 2026-08-27, written to be true under (A):** the screen now says the shortfall is recorded, not blocked, that *nothing is charged for it*, and that a charge — if the lease provides for one — is posted by hand through B-167's "Charge a fee for this unit" control already on that screen (naming the missing permission instead, for a staffer without `fees:charge`). The comment above that paragraph in `move-out/page.tsx` points here, so choosing (B) replaces the sentence rather than leaving two claims in the product. **Still OPEN — the copy being true today is not the decision.** |
| D-100 | **Whether a per-person shared-access window may WIDEN the facility's own gate hours** (settled while building **B-086 part 1**, 2026-08-25) | **No — it narrows only.** `narrowSchedule` intersects the two: a day is open where both are, closed if either is, and a person's window that does not overlap the facility's at all is closed rather than an inverted range. | `AuthorizedAccessPerson.accessHours` had existed since B-029 and been read by **nothing** — written by the service, rendered by no screen, never pushed to a controller — so the question had never had to be answered. Making it real forced it, and the naive answer (push the person's schedule instead of the facility's) is a hole: `extendedHours` is 24-hour access that the facility **sells as a paid add-on** (US-4 AC3), and a per-person schedule that replaced rather than narrowed would hand it out free to anyone who typed 00:00–23:59 into a portal form — with nothing on any screen distinguishing them from a tenant who paid. Intersection also happens to be the only rule that is right in both directions: a tenant granting shared access is **narrowing their own access**, never issuing a wider one. The preset schedules are deliberately full days (00:00–23:59) for exactly this reason — the facility's hours are the outer bound and are applied on top, so a preset that named tighter times would silently override a site's own 24-hour access for one guest, invisibly. |
| D-101 | **How a tenant expresses a shared-access schedule** (settled while building **B-086 part 1**, 2026-08-25) | **Three presets — "Any time the gate is open", "Weekdays only", "Weekends only" — not a weekly-schedule editor.** The column underneath stays a full `WeeklySchedule` and a value matching no preset reads back as "Limited hours — call the office" rather than being mislabelled as one of the three. | The storage is already general, so nothing is foreclosed: a manager who eventually needs an arbitrary window gets a control without a migration. What is refused is the **UI** for one — a seven-row open/close grid in a tenant portal, to express a choice that in practice is "whenever I can get in", "while I'm at work", or "at the weekend". The reverse-lookup is by **value**, not a stored enum beside the JSON, so the label and the schedule cannot drift apart; `custom` exists because a preset's definition could change under rows already written, and telling a tenant their guest can get in on a day they cannot is worse than telling them to ring. |
| D-102 | **Whether a template has a second, HTML body** (settled while building **B-198**, 2026-08-28) | **No. One body per template, and the HTML part is that body rendered as markup.** `MessageTemplate.bodyHtml` is dropped. It existed from the comms schema onward, was null on all 40 rows that ever existed, and CN-16's editor wrote it null unconditionally on every save — so nothing in the product's life ever rendered from it, and seeding one would have been erased by the first staff edit with the operator told nothing. **The criterion that made it look necessary was CN-24's `<th scope>` on the payment-plan schedule, and a second body was the wrong place to solve it.** A schedule is not a body, it is one merge FIELD whose value is tabular; the fix is a structured merge value (`MergeValue = string | { text, html }`) built once from the installments, so the numbered list in the text part and the captioned table in the HTML part are two renderings of one array and cannot disagree about a date. **What this rules out:** hand-authored HTML per template, and with it the drift FR-9a names — two halves of one message saying different things — plus the escaping hole the old code carried (a `bodyHtml` was interpolated RAW, so a facility called "Bob & Sons" would have emitted broken markup from the very body an operator was told was safer). A template that genuinely needs new structure gets it in `htmlBody` or as a structured value, both of which every template inherits at once. **Reversing this is a migration**, and the case for it would have to be a template whose markup cannot be derived from its own text — which is not what CN-24 asked for | PRD 05 CN-16, CN-24, FR-9a; B-053, B-191, B-198 |
| D-103 | What the duplicate-content report lists first, now that B-089's city/size pairs outscore the city pairs (raised by B-200, 2026-08-28 — **RESOLVED 2026-08-28**) | **Resolved (owner): the sort does not change; the advice does.** `findDuplicatePairs` keeps authored collisions first and then most-alike-first, which is the ordering B-082 part 6 chose and D-58 reasoned about — a pair at 94% is the more urgent fact whether or not anybody can act on it. What was wrong is what the row SAID. The report treated `bothGenerated` as one case, so a **City/size page intros** pair carried "Write copy for one of these cities" pointing at `/admin/settings/marketing/cities` — and that link cannot fix it: `citySizeIntro` takes no authored override (B-134 is where one goes), so following it edits the city page and leaves the size pair exactly where it was. An operator does the work and watches nothing happen, which is worse than being told there is no fix. A city/size row now states what D-77 actually did about it — the page is served `noindex` rather than advertised — and offers no fix link at all. **The rejected option was sorting actionable pairs above unactionable ones.** It would have made the tests' positional `.first()` correct again for free, and it is the wrong trade: it hides the most-duplicated page in the portfolio below less-duplicated ones, on the one screen whose job is to say how alike things are. The tests were pinned to their rowheader instead, which is what they always meant | PRD 04 §3.2 US-4, §7 Phase 2; D-58; D-77; B-089; B-128; B-134 |
| D-104 | **Whether the auction lot sheet may be built while OQ-9 is open** (raised by B-129, 2026-08-29; **owner decision**) | **Yes — the structured lot sheet ships now; the marketplace driver stays blocked.** B-129's own row names it: "the cheap first move is a structured listing export (lot, photos, address, sale date, terms) an operator uploads themselves — it is useful whichever way OQ-9 goes and fabricates nothing." That is what shipped, and nothing else. There is no marketplace API call, no vendor driver and no `AuctionAdvertisement` row written by exporting: downloading a sheet is not evidence that an advertisement ran, and that row stays typed by the person who placed the ad (B-062). **OQ-9 remains open and is not answered by this**, which is the condition the decision rests on — a CSV an operator hands to an auctioneer, reads to a newspaper, or uploads to a marketplace is neutral between live and online, where a driver written against one vendor's API would not be. | **The selection rule is why this is worth a decision rather than a chore.** The row is about advertising, and "an advertisement that never ran is the commonest wrongful-sale claim after an unserved notice" — but the mirror image is an advertisement that RAN for a sale that could not lawfully happen. A lot goes up days before the sale, and in that window the tenant may pay, an SCRA or bankruptcy hold may land, or somebody may record a titled vehicle. Every one of those leaves `AuctionCase.status` at `scheduled`. So the sheet is computed from live `auctionReadiness` — the same function the case screen refuses on — and never from the stored status, and the cases it drops are named on screen with their blockers rather than being silently short in the file. **Terms of sale are a new facility column with its control in the same item**, per this repo's own rule, and null stays a real state: the Terms column is blank and the screen says so, which is D-63's honest-degradation posture rather than a plausible default becoming a term nobody chose. | PRD 02 §4.6 US-30; master PRD §11 OQ-9; D-63; B-062 |
| D-105 | **Where a payment nobody narrowed goes, when the payer is on a payment plan** (settled while building **B-203**, 2026-08-29) | **The plan's frozen arrears are paid first, and the rest spills onto the current month. Deferred, not restricted.** A counter, portal or pay-link payment from a tenant with an `active` plan ranks that plan's `invoiceIds` ahead of every other invoice on the SAME lease, then allocates whatever is left over the rest in the facility's ordinary order. Other leases the tenant holds at the facility are untouched and keep their ordinary rank. | **Restricting was the obvious fix and it is the wrong one.** `restrictToInvoiceIds` exists (B-189) and autopay's installment charge uses it, but autopay is charging a figure WE raised: the installment, to the cent. A counter payment is whatever the tenant chose to hand over. Restricting a $500 payment to $300 of plan arrears would strand $200 as unapplied credit — a second surprise on top of the one being fixed, and the tenant plainly meant the rest for this month. Deferring gets both cases right with one rule. **Why it is per-lease:** a tenant can hold two leases here, one on a plan and one not, and money handed over for the second must not be dragged onto the first's arrears. **What stays open:** whether a staffer should ever be able to send a counter payment to current rent instead. B-203's row already named the shape of that — "a second control on the POS screen, not the default" — and no acceptance criterion asks for it yet, so it is not built. `explicitInvoiceId` remains the escape hatch that outranks everything. | PRD 02 US-22, US-25; D-96; D-65; B-189; B-203 |

## How to use this log

When starting a Claude Code session on a backlog item, include this file alongside the relevant PRD section. If an item's PRD text contradicts a decision here (e.g., PRD 02 saying lien pipeline is MVP), the decision wins. New conflicts discovered during the build should be appended here with a new D-number rather than resolved silently.
| D-106 | **Whether a lot missing a required advertisement element is refused from the lot sheet or exported blank** (settled while building **B-205**, 2026-08-30) | **The refusal list is about whether the SALE may be advertised at all; a missing advertisement element is a blank column and a named gap on screen, never a refusal.** A lot is dropped only for the reasons D-104 already established — not scheduled, no sale date, live readiness lapsed — plus the new `no_unit_type`, which is a lot the sheet cannot describe rather than a sale that may not happen. Tenant name, time of sale, terms and the general description of the goods all export blank when unset. | The alternative was tempting and wrong: refusing a lot with no goods description would push it off the sheet on the day it most needs to be on one, and an operator who cannot advertise at all is worse off than one told which sentence to write. The gaps are named where the operator is standing — facility-wide ones (time, terms) as one note linking to delinquency settings, per-lot ones (description) as a note naming each unit with a link to its case. `goodsDescription` is a new `AuctionCase` column with its control on the case screen, in the same item, per this repo's rule. It is **not** derived from the itemised inventory and does not reopen D-104's photograph reasoning: that reasoning refused a second copy of hashed primary evidence, and this is a phrase written for publication days BEFORE the lock is cut, when no inventory exists to copy from. |
| D-107 | **How long rent the plan never deferred may go unpaid before the plan breaks** (settled while building **B-208**, 2026-08-31) | **The facility's own first late-fee step — `LateFeeRule` step 1's `daysPastDue` — and no new setting.** A plan's hold stands late fees, dunning and access suspension down on the whole LEASE, so rent charged after the plan was agreed was protected by it too: a manual-pay tenant could keep every installment, pay no rent at all, and for up to ninety days take no fee, no notice, no suspension and no lien clock, twice a year at `planMaxPerRollingYear`. The nightly breach job now also breaks a plan when a rent invoice outside the plan's frozen `invoiceIds` is that many days past due. Falls back to the delinquency timeline's first step for a facility that charges no late fees but runs a lien clock, and does nothing where there is neither. | **The number is one this facility has already chosen.** "When is rent late here" is answered by the late-fee ladder, effective-dated, with a control an operator has used; a `planCurrentRentGraceDays` beside it would be a second column configuring the same intent, which is the accumulation this repo's first rule is about — and a lenient ladder now buys a lenient plan rule automatically, which is the right coupling. A flat constant was rejected for the same reason D-10 makes compliance per-state configurable. **It gets its own message, not `payment_plan_broken`.** That template opens "A payment on your plan was not made", and this tenant made every payment the schedule asked for; telling somebody who kept to a plan that they did not, in the same email that restarts fees, notices and gate suspension, is the kind of false customer-facing sentence this repo treats as load-bearing. `payment_plan.broken_unpaid_rent` carries the `invoiceId` for the same reason B-206 made `installmentId` required. The `payment_plan_agreed` email gained the clause too, so the rule is stated before it is enforced. **This does not reopen D-96**, which already says current rent stays due on its own date; it is what makes that sentence true of a tenant who does not pay it. |
| D-108 | **Whether a plan cancelled the day it was agreed spends one of the lease's two for the year** (settled while building **B-209**, 2026-08-31) | **No, where nothing was collected while it stood — that one case is a correction, not an arrangement, and D-98 (3) is refined rather than reopened.** A plan whose `cancelledAt` falls on the same FACILITY-LOCAL day as its `createdAt`, with no `PaymentAllocation` to any of its covered invoices between those two instants, is not counted by the rolling-year cap or by the second-plan escalation. Everything else D-98 (3) says stands: broken plans count, cancelled-the-next-day plans count, and a plan that collected anything counts however it ended. | **There is no amend path anywhere in the product**, so a manager who mistypes an installment date, spots it, cancels and re-enters had spent one of the lease's two plans for the year — on a Saturday, at the counter, with the tenant standing there, and the second one then needing a regional. **It cannot buy a free plan, on two counts.** A plan cancelled the day it was agreed provided no forbearance worth having: its hold is lifted with it, and any plan surviving past local midnight counts however it ends — so the cycle D-98 was built to stop (a plan broken last night replaced this morning, for ever, while the lien clock never runs) needs plans that last days, which this excludes none of. And a payment landing on the covered arrears while it stood makes it count regardless. **The money test reads ALLOCATIONS inside the plan's own few minutes, not its progress**, and that is not a detail: progress is a live figure off the invoices (D-96), so the successor plan's payments — or next spring's — would silently un-correct a mistype somebody fixed in March, and a rule about what happened on one Saturday must not change its mind afterwards. **Facility-local, not UTC**, for the same reason every other calendar-day rule here is: a correction made at 7pm Central is the same day's work as the plan it corrects. | PRD 02 §4.6 US-25; D-96; D-98; B-190; B-209 |
