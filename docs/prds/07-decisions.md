# Decision Log — Self-Storage Business Application

**Status:** All 11 flagged gaps/conflicts from `06-backlog.md` resolved by the owner on 2026-07-30. D-12 (superuser) and D-13a–e (support impersonation) were raised and resolved later the same day, during the build.
**Precedence:** This document amends the PRDs. Where a PRD conflicts with a decision below, this document wins. When building from the backlog, treat each decision as settled — do not re-open unless the owner amends this log.

| # | Flag (from 06-backlog.md) | Decision | What it means for the build |
|---|---------------------------|----------|------------------------------|
| D-1 | Delinquency/lien + POS/reports MVP scope (PRD 02 vs master) | **Master's leaner MVP wins.** | MVP = online move-in, billing, reminders, basic late-fee schedule, occupancy & rent-roll reports. Full lien/auction pipeline, cash-drawer sessions, remaining reports, and field ops stay in Phase 2 (Milestones 5/7). Backlog phasing is correct as written. |
| D-2 | Marketing Phase 1 scope (PRD 04 vs master) | **Master wins.** | MVP marketing = SEO location pages, GBP checklist, basic lead capture. Reviews, drips, abandoned-reservation follow-ups, promo codes in checkout = Phase 2 labels, built together in Milestone 6 per backlog ordering. Promotions engine stays MVP (also required by PRD 02 US-10 / B-070). |
| D-3 | Kiosk mode (master P2 vs PRD 03 P3) | **PRD 03 wins: Phase 3, default no.** | B-085 remains an evaluation item only. No kiosk work before Phase 3. |
| D-4 | First real gate-vendor driver (master P2 vs PRD 03 stubs) | **PRD 03 wins: stubs only.** | Learning build ships the simulated adapter + manual adapter + stub vendor adapters (B-080). A real vendor driver (B-086) is Phase 3 and contingent on a partner agreement. |
| D-5 | Scheduled tenant rate increases (PRD 02 MVP vs master P2) | **Master wins: Phase 2.** | Stays at B-076. MVP has street-rate management only; existing-tenant increases with notice letters come in Phase 2. |
| D-6 | Stripe Billing vs ledger-driven charges | **Ledger-driven PaymentIntents.** | Our invoice ledger is the source of truth; it creates Stripe PaymentIntents on due dates and autopay runs. Do NOT use Stripe Billing subscriptions. PRD 05's CN-6 "Stripe Billing retry config" reference is void — retry schedule (+1/+3/+5) is implemented in our billing engine per PRD 02. Unblocks B-043–B-046. |
| D-7 | Reservation deposit policy | **Free hold, no card.** | Reservations require no card and no deposit; holds expire (default 7 days, per-facility configurable). PRD 02 US-14's "optional deposit" is dropped from scope; master OQ-3 closed. B-018 builds free-hold only. |
| D-8 | Consent store ownership | **Shared schema package owns it.** | One `consent` table/package in the core schema (built at B-002, extended B-032/B-072). PRD 04 (marketing consent) and PRD 05 (SMS/transactional consent) both read/write through it; neither module owns the table. |
| D-9 | PRD cross-reference numbering | **Fixed editorially.** | Master §3 amended to five modules including `05-communications-prd.md`; PRD 02 sibling references corrected to actual filenames. No build impact. |
| D-10 | Operating state for legal defaults | **Texas, built configurable.** | Compliance config is per-state by design; Texas (Property Code Ch. 59 self-storage lien statute) is the seeded default for lien timelines, notice templates, late-fee behavior, lease clauses, and SMS disclosure copy. All legal artifacts are drafts requiring attorney review before any real-world use — this is a learning project, not legal advice. Unblocks Milestone 5 (B-056, B-061) and B-074. |
| D-11a | Autopay pre-charge reminder default | **On by default.** | Autopay tenants get a pre-charge notice (e.g., "card ending 4242 will be charged $X on {date}") a few days before the charge, plus receipts. Per-tenant opt-out. Applies to B-050 templates; closes PRD 05 Q1. |
| D-11b | Minor phasing splits | **Backlog's treatment stands.** | Spanish language: Phase 3 (bundled B-090). City landing pages: Phase 2 (B-082); generated size-in-city pages: Phase 3 (B-089). |
| D-12 | Superuser account (raised during B-004, 2026-07-30) | **No permission bypass. The `owner` role with an all-facilities assignment is the only super user.** | Never add an `isSuperuser` flag or any short-circuit in `can()` / `facilityScope()` / `checkMonetaryAuthority()`. Unrestricted access must always be an ordinary `StaffFacilityAssignment` row with `roleId = owner` and `facilityId = null` — grantable, revocable, and visible to the audit log (B-005) and the lien evidence chain. Bootstrapping the first such account is a `db:create-owner` script, scoped to **B-007**, which needs a staff login to demo role-gated routes anyway. **Not contradicted by D-13** — see PRD 09 §3 for why impersonation is not a bypass. |
| D-13a | Tenant notification on staff impersonation (PRD 09 OQ-1, 2026-07-30) | **Do not notify.** | No email, SMS, or in-portal alert fires when staff view a tenant's account. The `ImpersonationSession` row and dual-attributed audit entries are still written in full — this governs what the tenant is *pushed*, never what is *kept* (PRD 09 FR-16/FR-17). Consequence: oversight reporting (B-092) becomes the only channel where misuse surfaces, which is why D-13b is deliberately tight. Re-open if operating outside Texas — several state privacy regimes treat access disclosure differently (D-10). |
| D-13b | Default roles for impersonation permissions (PRD 09 OQ-2) | **Owner only, all four permissions.** | `impersonation:tenant`, `:staff`, `:write`, `:oversee` all seed to `owner` alone — tighter than the drafted `owner + regional`, because D-13a removed the tenant-facing signal. Widening to `regional`/`manager` is a seed change, not a migration; do it once B-092's reporting is live and usage is observable. |
| D-13c | `ImpersonationSession` retention (PRD 09 OQ-3) | **≥7 years, matching the audit log.** | Rows are a few hundred bytes and carry the accountability trail given D-13a. A shorter window would strand audit entries that reference them. Explicitly excluded from any shorter former-tenant PII purge; the rows hold no tenant PII beyond an id reference. |
| D-13d | Impersonated portal render target (PRD 09 OQ-6) | **The real portal at real URLs.** | Not embedded in the admin shell. Goal G1 is reproducing the user's exact view, and layout/responsive bugs are what people call about. URL segregation is therefore **not** a security control here — the banner, the service-layer write blocks, and the escalation guard are (PRD 09 FR-22/FR-23). |
| D-13e | Tenant-initiated support access (PRD 09 OQ-4) | **Deferred, not rejected.** | A portal-side "grant support access for 1 hour" button implies tenant awareness, which sits oddly with D-13a. If D-13a is ever reconsidered, reconsider this at the same time — they are a pair. |
| D-14 | Geocoding provider (PRD 01 OQ-6, raised during B-015, 2026-07-31) | **No vendor. A bundled US zip/city dataset (`zipcodes`, BSD).** | Follows D-4's precedent for undecided vendors: ship a real local implementation, slot a driver in later. Both OQ-6 candidates (Google Maps Platform, Mapbox) need a billed API key for what is public data that changes about once a year, and a network call on the hot search path. Offline and deterministic, so tests assert real coordinates instead of mocking a provider. **OQ-6 is not closed — it narrows to maps rendering (B-016, FR-1.3) and address autocomplete**, which do still need a vendor; typeahead at B-015 is served from our own facility list, not a gazetteer. Revisit if search must handle street addresses or non-US locations, neither of which a zip/city dataset covers. |
| D-15 | Customer-facing lexicon (raised by the UX review, 2026-07-31) | **One word per concept on every customer surface.** Recorded in full as PRD 01 §6.10. | **size** (not "unit type"), **unit**, **online price** / **in-store price** (not "web rate" / "street rate"), **gate hours**, **office hours**. The industry's words — unit type, street rate, web rate, lease, delinquent — stay on admin screens. No internal identifier (backlog ID, entity name, status enum) may render on a customer-reachable route, `/login` included, and a test asserts it. One money formatter for every customer-facing price (`formatRate()`, shipped in B-016): `$129/mo`, no trailing zeros. Binds every public-site and portal item from B-017 onward; the already-shipped copy is corrected in B-093. This is a product-owner call, not an owner escalation — recorded here because three surfaces had already drifted apart and PRD prose alone would not bind later items. |
| D-16 | Gate access suspension for non-payment, and what payment restores it (operator review, 2026-07-31) | **Accepted into MVP in full.** A single per-facility threshold suspends gate access at N days past due (Texas practice ≈ day 6, configurable), and access restores automatically within ~2 minutes of a **qualifying payment**, which is defined as **the balance going to zero** — rent, late fees, everything — stored as a per-facility setting so it can be relaxed later without a migration. | Built as **B-098**, after B-047, reusing the ACS from B-027 rather than waiting for the delinquency engine. Both transitions are audit-logged with the triggering invoice and day count, and both render on the tenant profile in plain English ("Access suspended, 12 days past due, 2026-07-18"). **Does not reopen D-1** — the configurable timeline, notices, lien and auction pipeline all stay Phase 2; only the single threshold moves forward, and B-057/B-058 inherit this rule rather than rebuilding it. **Closes PRD 02 §10 Q2** and **PRD 01 OQ-11**: paying restores access without staff action, and the portal may say so. Rationale for balance-to-zero over any partial rule: a partial-payment threshold teaches a tenant to pay the minimum that reopens the gate, every month, which converts a collections tool into a subscription for the delinquent. Emails do not collect rent; access does. |
| D-17 | Lapsed proof of insurance (operator review, 2026-07-31) | **Auto-enrol, per facility policy.** Notice at 30 days before the recorded proof expires, then enrolment into the facility's default protection tier when it lapses — configurable per facility, and applied regardless of what the lease captured at signing. | Written into PRD 02 US-44; the 30-day scan rides B-043's nightly jobs and the notices ship with B-050. **Trade-off, recorded honestly:** this charges a tenant for a term the lease may not have explicitly captured, which is precisely the exposure the narrower "flag and task only" option existed to close. The owner has weighed it and chosen the broader behaviour. Per D-10 all such text is a draft and not legal advice, and **this specific behaviour warrants an attorney pass before it runs against a real tenant** — the notice copy, the lease clause that authorises it, and whether the facility policy may apply retroactively to leases signed before the setting existed. |
| D-18 | **Amends D-4** — vendor stub adapters (operator review, 2026-07-31) | **One vendor stub, not two.** B-080 ships the adapter contract-test suite, the manual adapter, and **one** stub vendor adapter in a vendor-emulation profile. | D-4's "stubs only, no real driver" position stands unchanged; only the count changes. One stub proves the port/adapter contract survives a differently-shaped third-party API, which is the whole learning payload. A second stub proves nothing the first did not, at full cost, and becomes code nobody deletes because it looks like progress. A real vendor driver remains Phase 3 and contingent on a partner agreement (B-085). Recorded as an amendment rather than an edit to D-4 so the original decision and its revision both stay legible. |
| D-19 | Lighthouse performance gate vs the Core Web Vitals target (raised by B-016, 2026-07-31) | **They are two different numbers with two different jobs.** The **field** target stays LCP ≤2.5s (master §7.3, PRD 01 FR-9.1) — that is the Core Web Vitals threshold real users are measured against. The **lab** gate in CI warns at 2500ms and errors at 3000ms, with run aggregation pinned. | Lighthouse's mobile lab run applies simulated 4× CPU throttling and reports pessimistically **by design**; B-016's facility page reproduced 2612/2613/2613ms on a text-only LCP element with no image and no third party left to defer, and passed only because the default aggregation picked the most favourable run — a gate that is flaky rather than green. Warn-at-2500 keeps the signal; error-at-3000 keeps the gate meaningful. **Do not "tighten" the lab error threshold back to 2500 to match the field target** — they are not the same measurement, and a later session doing so would be reintroducing a known-flaky gate. The `lighthouserc.json` change is the owner's; this log holds the reasoning. |
| D-20 | Where autopay lives, and what re-auth guards (raised during B-036, 2026-08-04) | **Autopay is a per-lease flag (`Lease.autopayEnabled`); the card it charges is a per-tenant fact (`Tenant.stripeDefaultPaymentMethodId`). Both are required — either one missing means nothing is charged.** Re-auth (US-701) gates starting or redirecting money — adding a card, changing which card is charged, removing a card, switching autopay **on** — and deliberately **not** switching autopay **off**. | Autopay had no storage at all before this: "on" was inferred from the tenant merely having a saved card, which was wrong in both directions — a renter who opted out at checkout still read as On, and one who opted in read as Off, because `provisionMoveIn` never read the choice the checkout session had recorded. Per-lease because billing runs per lease (B-045), the dashboard already reports it per unit, and a tenant with two units can reasonably want one enrolled and one not. **B-045 must read both halves**, and must treat `autopayEnabled` with no default method as a configuration error to surface, not a charge to skip silently — the portal already warns the tenant about exactly that state. The re-auth asymmetry is the part most likely to be "tidied up" later by someone making the gate uniform: requiring a fresh login to **stop** a recurring charge is the one direction where the gate does harm, because a tenant locked out of their own account would keep being billed. Enabling is gated; disabling never is. |
| D-21 | The address of record, and what the `Tenant` address columns are now (raised during B-037, 2026-08-05) | **`TenantAddress` is append-only and authoritative; `Tenant.addressLine1`/`city`/`state`/`postalCode`/`country` are a cache of the newest row, written in the same transaction.** Every address change anywhere goes through `recordAddressChange()` — portal now, counter at B-038, returned mail after that. Each row stores the address as of that moment plus source and actor; the previous row IS the old value. | US-13 requires the current address to be "derived from the latest row, never overwritten", and the reason is evidentiary: on day 40 of a lien cycle "which address did the notice go to" has to be answerable from records. A pure derivation would have meant rewriting every existing reader (lease template, comms context, admin screens) in this item, so the columns stay as a cache instead — **but a path that writes them directly leaves the history with a gap exactly where a dispute looks.** Storing an old/new pair per row was rejected: the previous row already is the old value, and holding it twice creates two things that can disagree about one fact. Existing tenants were backfilled with an `import` row in the migration, so "newest row" is true from the first read rather than only for tenants who later edit. B-038 and B-061 both depend on this being complete. |
| D-22 | How receipt numbers are made gapless (raised during B-039, 2026-08-05) | **A `ReceiptCounter` row per facility, incremented by `INSERT … ON CONFLICT DO UPDATE … RETURNING` inside the same transaction that writes the payment.** Not a Postgres sequence, not `@default(autoincrement())`, not `MAX(receiptNumber) + 1`. Only counter-taken payments get a number; the online Stripe path leaves it null. | US-32 requires receipt numbering to be **gapless**, which is a strictly stronger guarantee than unique. A sequence does not roll back — an aborted transaction burns its number and leaves a hole no one can explain to an auditor, which is the exact failure the requirement exists to prevent. The `UPDATE` takes a row lock that serialises concurrent counter staff, and because the increment lives in the payment's own transaction, a rollback returns the number to the pool. `MAX + 1` was rejected for the opposite reason: it is not safe under concurrency without an explicit table lock, which is heavier and easier to get wrong. **Do not "simplify" this into a sequence or an autoincrement column** — both look tidier and both break the only property that matters. Tests assert all three behaviours (starts at 1 per facility, no hole after a rollback, five concurrent payments get consecutive numbers). Online card payments are deliberately excluded rather than numbered: a receipt book with holes where the web rentals were is not gapless, just confusing. |
| D-23 | Building B-095 out of its backlog position, and what the Task entity looks like (raised 2026-08-05) | **B-095 was built now, ahead of B-041, rather than left as the "left behind" gap several already-shipped items (B-026 among them) had been carrying it as.** `Task` is one table: `facilityId`, a loose `type` string resolved against a catalog (`packages/core/tasks`) for its label/proof requirements/audit sensitivity, `entityType`/`entityId` (mirroring `Document.subjectType`/`subjectId`), `businessDate`, `priority`, nullable `assigneeStaffId`, `status`, a `proof` JSON blob, and completion attribution. Idempotency key is `(type, entityId, businessDate)` — a redelivered event that would create the same task on the same facility-local day is a no-op. | The backlog itself orders B-095 (row 25a) before B-026 (row 26), which already depends on it; it was skipped when B-026 shipped and stayed skipped through a dozen later items, each noting the same gap in its own "left behind." B-041 is the first item whose own description requires "a Task view" outright, not just "an event with no consumer" — so building around it again would have meant a third bespoke queue, exactly what "one task queue, not seven" exists to prevent. The loose `type` string (not an enum) matches `Document.type`'s own precedent for the identical reason: a new task type is a catalog entry, not a migration. Two real consumers were wired immediately rather than shipping the table empty: move-in provisioning failures (`requestDownstream`, closing the gap B-026's own comment named) and returned-mail review (closing PRD 02 US-13's own AC, already reachable from a shipped B-038 button). **`missingProofFields` fails closed for an unregistered type** — it falls back to requiring the universal `note` field rather than requiring nothing, after a test caught the first version doing the opposite (an empty required-list for an unknown type meant nothing was ever "missing"). Permission-wise this reuses `tenants:view`/`tenants:edit` rather than minting `tasks:*` — matching `nav.ts`'s own pre-existing comment ("gated the same as Tenants for now") rather than deciding a new permission shape now, ahead of the six more consumers still to come. |
| D-24 | How a pending portal move-out request is represented, and why the portal enforces notice while staff don't (raised during B-041, 2026-08-05) | **No new column, no new model.** A pending request is `Lease.status = 'active'` with `moveOutDate`/`moveOutReason`/`noticeGivenAt` already set — the exact same three columns B-040 built for the *finalized* case. "Pending" versus "finalized" needs no flag: `status` is the discriminator (`ended` = finalized, `active` with a `moveOutDate` = pending). **The portal enforces the facility's notice-days policy as a hard floor on the date picker; the staff screen (B-040) still only reports a shortfall.** | Adding a `MoveOutRequest` table or a `pending` lease status would have meant either duplicating the columns B-040 already carries or teaching every reader of `moveOutDate` about a new source of truth — reusing the columns costs nothing extra and cancelling (clearing them back to null) is symmetric with never having asked. The enforcement asymmetry is deliberate, not an oversight: staff route around real-world urgency constantly (a tenant already gone, an abandonment with no notice at all) and B-040's own AC treats a shortfall as informational for exactly that reason — but a tenant scheduling ahead through self-service has no such urgency and no legitimate reason to need an exception, so the portal's date input simply will not let them pick a date the policy forbids. **If a "staff can move the date forward on the tenant's behalf" flow is ever built, it must go through B-040's advisory path, not a relaxed version of this one** — the two screens are allowed to disagree about enforcement because they answer different questions ("what does this tenant's account say" vs. "what actually happened at the property"). |
| D-25 | The metric definitions themselves, settled once (raised during B-042, 2026-08-05) | **`packages/core/metrics` owns every figure, and no screen, tile or export computes one inline.** The judgement calls, stated rather than inferred: **rentable excludes `unrentable` and INCLUDES `maintenance`**; **`overlocked` counts as occupied**; **economic occupancy is collected ÷ gross potential at street**, measured across every *rentable* unit rather than only occupied ones; **`daysPastDue` anchors to the OLDEST unpaid invoice's ORIGINAL due date**; AR buckets are 0–10 / 11–30 / 31–60 / 61–90 / 90+ with 90 landing in `d61to90`; **roll-ups sum the components and recompute the ratio, never average the ratios.** | §4.11's AC demanded these be a module with written formulas and tests, and its stated reason is the one that matters: "The first time the owner dashboard says 91% and the rent roll says 88%, he stops trusting both." Each call has a failure it prevents. Excluding maintenance from rentable would flatter occupancy by exactly the units an operator is slowest to turn around. Forgetting `overlocked` means occupancy silently *drops* the moment B-057 lands, reading as units emptying when nobody moved out. Measuring economic occupancy against occupied units only lets a site be "97% economically occupied" with a third of tenants not paying. Anchoring `daysPastDue` to a retry attempt (US-20 retries on +1/+3/+5) resets the clock on every retry, so a 40-day delinquency reports as 2 days and never reaches the day-6 access suspension or any lien step — the tenant simply stops being visible to the system meant to escalate. Averaging ratios makes a 100%-occupied 4-unit site and a 50%-occupied 400-unit site read as 75% instead of the true 50.5%. **All six are asserted in `tests/metrics.test.ts`, including the roll-up-equals-sum property the AC required be a test rather than a sentence.** |
| D-26 | Defaulting D-17's auto-enrolment to OFF per facility (raised during B-043, 2026-08-05) | **The behaviour is built exactly as D-17 specifies; the per-facility switch that turns it on ships `false`, and the tier it enrols into ships unset.** A lapsed proof therefore raises a high-priority staff task and charges nothing until a facility deliberately turns it on in Settings. | **This does not re-open D-17** — the owner chose auto-enrolment and it is implemented, tested and one dropdown away. It defers only the moment it first bills someone. D-17's own text is the reason: it records that this charges a tenant for a term the lease may not have captured, and that the behaviour "warrants an attorney pass before it runs against a real tenant." A default of `true` would have started charging the instant a facility was seeded, before that pass could happen; a default of `false` makes turning it on a deliberate, audited act by a named person at a named facility. Two guardrails ship with it: the settings form refuses to enable auto-enrolment with no tier chosen, and the scan refuses to enrol when the configured tier is no longer on sale rather than guessing a premium — in both cases the task is still raised, so a person decides. **Owner action:** flip this on per facility once the attorney pass on the notice copy and the lease clause is done. |
| D-27 | Anniversary billing as the default, and what the billing day is anchored to (raised during B-044, 2026-08-05; owner chose anniversary) | **`billingPolicy` is per facility and ships `anniversary`.** A lease's billing day is the **facility-local calendar day it started**, clamped to 28. The move-in payment therefore buys a **whole period starting that day**, so nothing is prorated on the way in and the tenant's due date never moves. `first_of_month` is built and selectable; `invoiceLeadDays` (default 5) and `prorateOnMoveIn` are per-facility alongside it. | The alternative — everyone bills on the 1st — forces a prorated partial period on every single move-in, which is the most error-prone invoice a system produces and the one tenants query most. Anniversary makes the ordinary case a full month with no division at all. **Three things this settled that were previously wrong or hardcoded, each now fixed:** `billingDay` was hardcoded to `1` at move-in, so a renter who paid a full month on the 20th would have been invoiced again on the 1st — a double-charge eleven days later. The day is now derived from the **facility-local** date, not `new Date()`'s UTC day, because at 10pm in Texas the UTC date is already tomorrow and that renter would have carried a wrong anniversary for the life of the lease. And the **lease document** stated "if you move in part-way through a month, that first payment is prorated" — under anniversary that is false, and it is a signed term the operator would have had to honour; the sentence is now a merge field that states what the facility's policy actually does. **Not settled here:** under `first_of_month` with `prorateOnMoveIn`, the checkout still charges a full first month — the nightly engine handles that policy correctly but the checkout side of it is unbuilt, and is only reachable by a facility that switches away from the default. |
| D-28 | How an autopay charge is tied to the invoice it pays, and when that link is written (raised during B-045, 2026-08-05) | **A charge that settles exactly one invoice names it, and its `PaymentAllocation` is written in the same transaction as the pending `Payment` row — BEFORE Stripe is called.** The webhook then recomputes `Invoice.amountPaidCents` as the sum of allocations on **succeeded** payments and sets the status from it. `Invoice.amountPaidCents` is never incremented. | The whole "autopay never double-charges" guarantee rests on the invoice reading as paid afterwards — without settlement the charge would succeed, post to the ledger, and leave the invoice open for the next night's run to charge again. **Writing the allocation before the Stripe call is the load-bearing part:** if Stripe charges the card and the process dies before the webhook lands, the allocation already exists, so the next run sees an attempt in flight and skips rather than charging a second time. A pending allocation counts toward nothing, because the paid total sums succeeded payments only, so it cannot make an unpaid invoice read as paid. **Recomputing rather than incrementing** is what makes Stripe's at-least-once redelivery harmless — an increment applied twice is exactly the bug redelivery causes. **B-048 extends this, it does not replace it:** partial payments spread one payment across several invoices in a configurable order, which is more allocations written by a different rule; the settle-from-allocations direction and the recompute stay. A payment that names no invoice still gets no allocation invented for it. |

## How to use this log

When starting a Claude Code session on a backlog item, include this file alongside the relevant PRD section. If an item's PRD text contradicts a decision here (e.g., PRD 02 saying lien pipeline is MVP), the decision wins. New conflicts discovered during the build should be appended here with a new D-number rather than resolved silently.
