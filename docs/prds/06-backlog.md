# 06 — Prioritized Product Backlog

**Product:** Multi-facility self-storage business application
**Status:** v1.0 — 2026-07-30
**Sources:** `00-master-prd.md`, `01-customer-website-prd.md`, `02-admin-dashboard-prd.md`, `03-hardware-integrations-prd.md`, `04-marketing-seo-prd.md`, `05-communications-prd.md`

This is the single, strictly-ordered build backlog for the whole platform. Items are sequenced so that every item is buildable when reached (all dependencies appear earlier), foundation comes first, and the shortest path runs to the two golden paths: **(1) a renter completes an online move-in with payment and receives a simulated gate code** (end of Milestone 2) and **(2) the billing engine invoices tenants and the comms module sends payment/past-due reminders** (end of Milestone 4). Numbering is global and continuous across milestones.

Sizes: **S** ≈ one short session, **M** ≈ one focused session, **L** ≈ 2–3 sessions or one long one, **XL** ≈ should be split during sprint planning. Phase = the phase the feature belongs to per the PRDs (master PRD wins on conflicts — see Flagged gaps/conflicts). A ✅ on the ID means the item is built and committed.

**Numbering.** B-numbers are global, continuous and permanent — never renumbered, never reused. The **#** column is positional. Rows added after the original sequencing carry a letter (`16a` = inserted between 16 and 17) so that inserting an item does not renumber seventy rows below it. The same rule applies recursively where a lettered position is itself full: `81da` = inserted after `81d`, before `81e`. B-093 onward were added on 2026-07-31 from the operator, UX and accessibility reviews; B-109–B-119 on 2026-08-12 from the second UX and accessibility reviews (of everything shipped through B-105); B-120–B-123 on 2026-08-14, out of building B-110 and B-112 and out of reviewing what the promotions engine actually reaches, rather than out of a review; B-124 and B-125 on 2026-08-14 out of building B-114; B-126 on 2026-08-14 out of building B-118; B-128 on 2026-08-17 out of B-082 part 6's own duplicate-content report; B-129 on 2026-08-18, split out of B-083 rather than raised — it is half of US-30, blocked on an open question older than the row it came from; B-130 on 2026-08-18, found by B-083's own e2e when no demo lease could generate a notice to send; B-131 on 2026-08-18, found while building B-084 part 1 — the occupancy report's date range does not apply to the occupancy figure; B-132 on 2026-08-18, found while fixing B-130 — three demo leases carry a delinquency label with no money behind it. B-133 on 2026-08-20, split out of B-087 rather than raised — it is the half of that row that needs Google API access and a policy read, and it is blocked in the same way B-129 and B-085 are. B-134 on 2026-08-20, raised by B-089's own measurement rather than by a review — D-77's gate correctly refuses the size pages a real portfolio would most want, and nothing in the product can currently unblock them. B-090a and B-135 on 2026-08-20 out of splitting B-090: the first is that row's part 1, the second is a defect its two-way-inbox evaluation turned up. B-090b on 2026-08-20 is that row's part 2, and B-136 on 2026-08-20 is a defect part 2 found in its own quote — the figure the tenant agrees to is not the figure the commit re-reads. **B-137–B-156 on 2026-08-21** from the third review block (operator, UX and accessibility, over B-120–B-136), inserted after B-130 so the whole block precedes every row that was open when it was written; each row states whether its finding was verified against the code or still needs confirmation at build time. Each row names the reviewer, or the item, that raised it. **B-157 on 2026-08-21** was raised while building B-137, not by a review: D-85 settled on Option A, B-137 built only the half every option agreed on, and the staff-side approval, reason code and unreset lien clock that Option A actually chose are still unbuilt. **B-158 on 2026-08-22** was diagnosed while building B-146, from a test failure the previous block's reason field made attributable. **B-159–B-184 on 2026-08-24** from the fourth review block (operator, UX and accessibility, over B-137–B-158), inserted after B-158 so the whole block again precedes every row that was open when it was written; positions continue the lettering under `83bv` (`83bva`–`83bvz`) so nothing below renumbers. Twenty-six rows from twenty-eight findings: near-identical findings were merged rather than numbered, and the merges are named in note 13. **B-185 on 2026-08-24** was measured while building B-161 rather than reviewed — the unit test database had accumulated 13,106 facilities and the sweep's failures had stopped being about the code; position `83bwa` continues the lettering past `83bvz`.

---

## Milestone 1: Foundation

Everything else builds on this: repo, schema, auth, RBAC, jobs/events, and the facility/unit inventory that both the website and billing consume.

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 1 | B-001 ✅ | Monorepo & app scaffold: Next.js (App Router) + TypeScript, Tailwind/shadcn, Prisma + Postgres, Vitest/Playwright, CI (incl. axe + Lighthouse hooks), env/secret handling | PRD 00 §5 Tech Stack, §7.2–7.4 | M | — | MVP |
| 2 | B-002 ✅ | Core data model & migrations: Facility, UnitType, Unit, Tenant, Lease, Invoice/LineItem, Payment, LedgerEntry, AccessCredential/AccessGrant, Reservation, Lead, Promotion, Notice, AuditLog, StaffUser — money as integer cents, facility_id everywhere | PRD 00 §7.5–7.6 Data Model; PRD 02 §6.1 | L | B-001 | MVP |
| 3 | B-003 ✅ | Auth foundation: Auth.js sessions for customers (email/password + magic link) and staff; rate-limited login, password reset | PRD 00 §7.1 Auth; PRD 01 FR-5 | M | B-002 | MVP |
| 4 | B-004 ✅ | RBAC roles-as-data (tenant/counter/manager/regional/owner/system) + server-side facility scoping on every endpoint; monetary authority limits config | PRD 00 §7.1; PRD 02 §3 Roles & Permissions | M | B-003 | MVP |
| 5 | B-005 ✅ | Append-only audit log service (actor, timestamp, entity, before/after, reason codes) wired as a shared package | PRD 00 §7.1; PRD 02 §4.10 US-38 | M | B-002 | MVP |
| 6 | B-006 ✅ | Background jobs + event bus foundation: job runner (Inngest/Trigger.dev or Vercel Cron), domain-event outbox, idempotent consumers | PRD 00 §5; PRD 02 FR-4/FR-7 | M | B-001 | MVP |
| 7 | B-007 ✅ | Admin shell: left nav, global header, facility switcher with "All facilities" context, universal search stub, role-gated routes — **plus `db:create-owner` bootstrap script** (creates the first StaffUser with an all-facilities `owner` assignment and a single-use password-setup link; refuses a second owner without `--force`). Nothing can create a staff account before this, per D-12 | PRD 02 §4.1 US-1, FR-1–3 | M | B-004 | MVP |
| 8 | B-008 ✅ | Facility settings CRUD: address/geo, timezone, office + gate hours, effective-dated tax components & fee schedule, state field | PRD 02 §4.1 US-3 | M | B-007, B-005 | MVP |
| 9 | B-009 ✅ | Unit type management: dimensions, attributes (climate, drive-up, floor, power), clonable across facilities | PRD 02 §4.2 US-6 | S | B-008 | MVP |
| 10 | B-010 ✅ | Unit inventory: list + grid view (JSON layout import), derived statuses (available/reserved/occupied/overlocked/maintenance/unrentable), bulk edit with preview. **Do not build:** the deferred drag-to-place layout editor is dropped, not pending (operator review — a site layout is drawn once and never again; JSON import plus the grid is the answer). Closes PRD 02 §10 Q4 | PRD 02 §4.2 US-5/US-7/US-8 | M | B-009 | MVP |
| 11 | B-011 ✅ | Street rate management: web vs in-store rates per unit type, effective-dated, rate history, exposed via API | PRD 02 §4.3 US-9 | S | B-009 | MVP |
| 12 | B-012 ✅ | Seed & demo data script: ≥2 facilities, unit types/units, tenants in every lifecycle state | PRD 02 §7 (demo scenarios); PRD 03 US-7 AC4 | S | B-010, B-011 | MVP |

## Milestone 2: First online move-in (Golden Path 1)

Search → facility page → unit → reserve or rent → e-sign → Stripe payment → provisioned lease → simulated gate code → confirmation email.

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 13 | B-013 ✅ | Public site shell: mobile-first layout, persistent header, homepage with search hero, static/legal pages (FAQ, terms, privacy, accessibility statement) | PRD 01 §6.1–6.2, FR-8 | M | B-001 | MVP |
| 14 | B-014 ✅ | Inventory & pricing read API with quote tokens (price seen = price charged), availability cache TTL + live checkout checks | PRD 01 FR-2 §Inventory & pricing | M | B-010, B-011 | MVP |
| 15 | B-015 ✅ | Location search: geocode zip/city, distance-ranked results with "units from $X/mo", shareable URLs, zero-results fallback | PRD 01 §4.1 US-101, FR-1 | M | B-013, B-008, B-014 | MVP |
| 16 | B-016 ✅ | Facility detail page: photos, office vs gate hours, amenities, click-to-call, maps deep link, live unit list, crawlable URL scheme | PRD 01 §4.1 US-103 | M | B-015 | MVP |
| 16a | B-093 ✅ | **Public-site remediation of shipped code** (accessibility + UX + accessibility-statement reviews): raise `--ring` and split out an operable-control border token so both clear 3:1, with an explicit `:focus-visible` rule and a unit test on the token pairs; drop `inputMode="numeric"` from the mixed zip/city search field (iOS renders a digits-only keypad, so the documented "Austin, TX" input is impossible on an iPhone); rewrite `/login` and the size-help copy in customer language per D-15 — no backlog ID may render on a customer-reachable route — and add the missing size answer to the FAQ; scope the accessibility statement's claims to what CI and `PROGRESS.md` actually record, with a dated "last reviewed" line. Test additions that make these stick: loop the 320px reflow assertion over the whole public route list rather than the homepage alone, add a 200%-zoom pass and a text-spacing pass, assert `[role=status]` is attached on load, and run one manual focus-indicator sweep (Chrome/Firefox/Safari) plus one VoiceOver pass over the facility page — **recorded in `PROGRESS.md`**, because a statement's credibility rests on the record, not the intention | PRD 01 §6.8/§6.8.1, §6.10; D-15 | S | B-016 | MVP |
| 16b | B-094 ✅ | **Admin shell remediation of shipped code** (accessibility + UX reviews): skip link as the first focusable element with `<main id="main" tabIndex={-1}>`; facility switcher gets a visible Switch button and stops navigating on `change`, with the new context announced; server actions return field errors instead of throwing — `aria-invalid`, `aria-describedby`, a focused error summary, and one persistent `role="status"` region per form for success; confirm-and-echo step plus a server-side range check before an append-only tax or fee row is published; `<th scope="row">` and row-naming on the hours and unit tables; dashboard tiles get time windows in their labels, an "Available now" tile, and drill-through; `/admin/*` joins the axe run on the existing staff session, with scans after an invalid submit and after a bulk-edit preview opens, and a two-line assertion that changing the facility select does not navigate | PRD 02 §5.5 FR-16–FR-24, §4.1 US-2 | M | B-093 | MVP |
| 17 | B-017 ✅ | Unit browsing + transparent pricing: filters/sort, availability counts (truthful scarcity), web vs in-store rate display with no strike-through when the two prices are equal, **"What you'd pay today" itemized expander** footing to total due today + then $X/mo, **search context carried into the facility page** (back-to-results affordance + distance), lexicon pass per D-15; static size guide page | PRD 01 §4.2–4.3 US-201/US-202/US-301, §4.1 US-103, §6.8.1, §6.10 | M | B-016, B-093 | MVP |
| 18 | B-018 ✅ | Free reservation service: no-card hold with expiry, atomic availability decrement, signed-token complete/cancel links (cancel lands on a confirmation page — no irreversible action from a `GET`), expiry stated as an absolute local date and time rather than a countdown, duplicate guard, expiry job | PRD 01 §4.4 US-401, FR-3, §6.8.1; PRD 02 US-14 (reserve) | M | B-017, B-006 | MVP |
| 19 | B-019 ✅ | Stripe foundation: Customers, PaymentIntents/SetupIntents, signed webhooks, idempotency keys, reconciliation of events into the ledger | PRD 01 §7.3; PRD 00 §7.4 PCI scope | M | B-002, B-006 | MVP |
| 20 | B-020 ✅ | Checkout session state machine: server-side resumable stepper, 30-min unit lock with heartbeat, unit-lost fallback offer. **The persistent price summary is this item's chrome, not B-025's** — every step renders due-today + then-$X/mo from the same shared calculation as B-017, collapsing to one sticky line on mobile; T-5-min lock warning with a one-activation extend; step transitions move focus and announce | PRD 01 FR-4.1, §6.4, §6.8.1 | L | B-017, B-018 | MVP |
| 21 | B-021 ✅ | Checkout steps 1–2: renter details with WCAG `autocomplete` tokens **and** address autocomplete, `inputmode` matching the data (numeric only on a zip-only field), implicit account creation (guest-style), unit assignment & confirmation; field errors identified, suggested, tied by `aria-describedby`, and summarised in a focused block | PRD 01 §4.5 US-501 steps 1–2, FR-5, §6.8.1 | M | B-020, B-003 | MVP |
| 22 | B-022 ✅ | Protection plan: admin-configured **effective-dated** plan catalog with per-facility required-vs-optional policy, choose-or-waive checkout step with attestation and a **structured waiver record** (carrier, policy number, expiry, declaration page, or audited manager override), premium added to recurring billing and prorated like rent; radios in a `<fieldset>`, Continue never disabled. Ships as an **in-house catalog** — PRD 02 §10 Q5 stays open and does not block it (decided 2026-07-31); a provider program, if ever adopted, replaces the catalog behind the same lease-facing behaviour | PRD 01 US-501 step 3, FR-4.3, §6.8.1; PRD 02 §4.3 US-44 | M | B-021 | MVP |
| 23 | B-023 ✅ | Document generation service: templated PDFs with merge-field validation (fails loudly on missing fields) — shared by leases, receipts, notices. PDFs are **tagged**: heading structure, reading order, table headers, document `lang`, `/Title`, real text not raster | PRD 02 FR-6; PRD 01 §6.8.1 | M | B-002 | MVP |
| 24 | B-024 ✅ | Lease template + e-sign step: merged lease render (same heading structure on screen, keyboard-scrollable, summary as page content not a tooltip), typed signature + consent (IP, timestamp, SHA-256 hash), review-before-commit step, immutable stored PDF | PRD 01 US-501 step 4, FR-4.2, §6.8.1; PRD 02 US-15 | M | B-023, B-022 | MVP |
| 25 | B-025 ✅ | Payment step: Stripe Payment Element + wallets (`appearance` theme meeting 4.5:1 text and 3:1 borders/focus; Element errors mirrored into a page-level live region), itemized total due today as a `<table>`/`<dl>`, autopay default-on enrollment with the §4.6 disclosure adjacent to a one-activation opt-out (SetupIntent). Manual keyboard + screen-reader pass and Stripe's VPAT on file before merge | PRD 01 US-501 step 5, §4.6 US-601, §6.8.1 | M | B-019, B-024 | MVP |
| 25a | B-095 ✅ | **One task queue, not seven** (operator review): a single `Task` entity — facility, type, subject entity, facility-local business date, priority, assignee (nullable = anyone), status, typed proof fields, completion actor, source event id — with idempotent creation on (type, entityId, businessDate), a mobile-first per-facility "my day" list with overdue escalation and regional roll-up, proof-gated completion, and audit on sensitive types. Placed here because B-026 is the first consumer; B-041, B-046, B-054, B-059, B-060, B-065, B-097 then all read and write this instead of each inventing a queue | PRD 02 §4.9 US-41 | M | B-006, B-005 | MVP |
| 26 | B-026 ✅ | Move-in provisioning + rollback: webhook-driven finalization, unit → occupied, lease + ledger created, `lease.moved_in` emitted, downstream failures become `Task` rows never dead ends. **Writes the lease's first `LeaseRateChange` row** (reason `move_in`) — the rate-increase clock cannot be started retroactively | PRD 01 FR-4.4–4.6; PRD 02 US-14 (move-in), US-11 | M | B-025, B-095 | MVP |
| 27 | B-027 ✅ | Access Control Service: AccessGrant state machine (pending→active⇄suspended→revoked), **one grant per credential holder × facility** (the tenant is one holder; authorized persons added at B-029 are others), persistent command outbox with idempotency/retry/dead-letter, per-facility adapter config | PRD 03 §4.1, FR-1–FR-3 | L | B-006, B-002 | MVP |
| 28 | B-028 ✅ | SimulatedAdapter + mock gate controller + virtual keypad dev page + fault injection (offline/latency/webhook failure) | PRD 03 FR-8, US-7 Simulation | M | B-027, B-012 | MVP |
| 29 | B-029 ✅ | Gate code issuance on move-in + confirmation screen: unique code per code policy, async issuance with retry ("code texted within 15 min" fallback), confirmation page with code/address/hours. **Plus the authorized-access list per lease** (operator review): named people with their own individually-revocable credentials, suspended with the lease, gate events attributed to the holder — staff-managed at MVP, per-facility cap default 3, portal self-service deferred to Phase 2 | PRD 03 US-1, **US-9**; PRD 01 US-501 step 7 | M | B-026, B-028 | MVP |
| 30 | B-030 ✅ | Comms core: event-driven notification service — rules as data, consent/suppression check, template render, Resend email with domain auth, idempotent sends (hard invariant), append-only message log, kill switch + sandbox redirect | PRD 05 FR-1–FR-4, FR-16–FR-21 | L | B-006 | MVP |
| 31 | B-031 ✅ | Move-in path transactional emails: reservation confirmation + expiry reminder, move-in welcome (gate code after credential active, masked in logs), lease PDF + first receipt, **and the checkout resume link** — sent when the email is captured at step 1, carrying the signed session token, restoring the step the renter left. Without it, FR-4.1's "resumable" only works while the tab stays open | PRD 05 CN-7, **CN-22**; PRD 01 §4.8 US-801, FR-4.1 | M | B-030, B-029, B-018 | MVP |
| 32 | B-032 ✅ | SMS consent capture at move-in (unchecked-by-default checkbox, disclosure versioning, stored consent record) **plus a distinct `notice_email` consent value** — consent to receive *notices* by email is not `account_email` and not marketing consent, and Texas permits electronic notice only where the tenant agreed to it; this migration already touches consent — + kick off A2P 10DLC brand/campaign registration (long external lead time) | PRD 05 CN-15, §6.3; PRD 02 §4.4 US-13; PRD 04 FR-MSG-1 | S | B-021 | MVP |

## Milestone 3: Tenant portal & admin operations essentials

Tenants can self-serve; staff can run the counter. Also unlocks saved payment methods, which the autopay run needs.

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 33 | B-033 ✅ | Portal login: email/password + magic link, forgot password, 30-day sessions with re-auth for sensitive actions | PRD 01 §4.7 US-701 | M | B-003 | MVP |
| 34 | B-034 ✅ | Portal dashboard: balance/due date, autopay status, gate code behind "show" tap with copy (reveal is a `<button aria-expanded>`, code announced character by character, "Copied" announced from a persistent region). **Plus the past-due state** — problem/consequence/action banner above everything else, pay-now ≤2 taps, and the gate-code panel saying access is suspended rather than showing a code that will fail at the gate. Display-only: it renders whatever delinquency signal the ledger exposes and never computes delinquency. If no signal exists by this item, record that as an explicit dependency rather than pulling Phase-2 delinquency forward | PRD 01 US-702/US-708, §6.5, §6.8.1 | M | B-033, B-029 | MVP |
| 35 | B-035 ✅ | Portal one-time payment: pay balance in ≤3 taps, saved or new method, instant receipt | PRD 01 US-703 | M | B-034, B-019 | MVP |
| 36 | B-036 ✅ | Payment methods & autopay management: toggle autopay, add/remove/update cards, default method, next scheduled charge | PRD 01 US-704 | M | B-035 | MVP |
| 37 | B-037 ✅ | Portal documents & contact info: lease PDF, receipts, statements list; edit phone/email/address/alt contact with dual-email confirmation, syncs to admin. **Address changes write a history row** (old/new, source `portal`, actor, timestamp) — the current address is derived from the latest row, never overwritten | PRD 01 US-705/US-706; PRD 02 §4.4 US-13 | M | B-034, B-023 | MVP |
| 38 | B-038 ✅ | Admin tenant profile: fast search, leases, balance, delinquency status, immutable notes, typed document uploads (**one document store, reused by notices, walkthrough photos and auction evidence**), communication history shell. Counter address edits write the same history rows as B-037, with a returned-mail flag that makes contact info visibly stale and creates a task | PRD 02 §4.4 US-13/US-16 | M | B-026 | MVP |
| 38a | B-096 ✅ | **Lease holds** (operator review): `LeaseHold` with type (`military_scra`, `bankruptcy`, `deceased`, `litigation`, `dispute`, `do_not_contact`, `payment_plan`), effective from/to, reason, supporting document, placed-by actor; effects declared per type as configuration — halt dunning, halt late fees, halt access suspension, hard-block auction eligibility, suppress marketing; persistent banner on the tenant profile and on every notice-generation and auction-approval screen; audited placement, manager-or-above to lift SCRA/bankruptcy. Placed here because B-038 is where it renders and B-052 (which already says the ladder halts on "hold") is the first consumer of the behaviour | PRD 02 §4.4 US-42 | S | B-038 | MVP |
| 39 | B-039 ✅ | Walk-in (POS) move-in + manual payments: same move-in wizard at the counter, card/cash (tendered/change)/check payments, print/email receipt. **Plus cash accountability** — `Payment.receivedByStaffId` set from the session actor and required for cash/check/money order, a per-facility per-business-day payments summary printable as a deposit slip, manager approval over a configurable cash threshold, gapless per-facility receipt numbering. This is a read over `Payment`; it is not a drawer session and does not pre-empt B-078 (D-1 stands) | PRD 02 §4.8 US-32 | M | B-026, B-038 | MVP |
| 39a | B-097 ✅ | **Phone & counter inquiry capture** (operator review): one-click "new inquiry" from any admin screen writing a `Lead` with source (`phone`, `walk_in`, `referral`, `drive_by`), then one click to quote (both prices + applicable promotion) and one click to a free hold through B-018's reservation service — no card, no account, under 60 seconds; follow-up task on any lead not contacted within the facility's window; source carried through reservation → move-in so conversion reporting can split phone from web. B-068 builds the *web* forms much later; roughly half of real rentals start on the phone, and today there is nowhere to put one | PRD 02 §4.8 US-43 | M | B-039, B-018, B-095 | MVP |
| 40 | B-040 ✅ | Admin move-out: final balance per proration policy, write-off threshold with override, `lease.moved_out` → access revoked **on the move-out date at facility close** (not midnight UTC), move-out confirmation email. **Unit releases to `maintenance`, not `available`** — a staff "verified empty and clean" confirmation with optional photo is what makes it rentable, and skipping it needs a manager override; `moveOutDate` / `paidThroughDate` / `noticeGivenAt` recorded separately; facility settings for prorate-out and required notice days (Texas defaults, labelled as configuration); staff-initiated abandonment path dated to last occupancy evidence that does not forgive the balance; ended leases with a balance land on a former-tenant AR list with write-off or collections disposition | PRD 02 US-14 (move-out); PRD 03 US-2; PRD 05 CN-8 | M | B-038, B-027, B-030 | MVP |
| 41 | B-041 ✅ | Portal move-out request: date picker with notice-rule validation, owed/prorated preview, staff verification queue **as a `Task` view**, cancellable | PRD 01 US-707; PRD 02 US-41 | M | B-040, B-034, B-095 | MVP |
| 42 | B-042 ✅ | MVP reporting: portfolio dashboard (occupancy, revenue MTD, delinquent AR, today's moves per facility, drill-through on every tile, no lifetime counters) + occupancy & rent-roll report with CSV export. **Plus the two things nothing else owns:** a shared `metrics` module in core holding every definition with a written formula and unit tests (occupancy — state that "rentable" excludes `unrentable` and includes `maintenance` —, sq-ft occupancy, economic occupancy, rate variance, AR buckets, days-past-due, move counts), with no screen computing any of them inline and roll-up-equals-sum asserted in a test; **and US-39(3), the move-in/move-out report** — counts and net by facility and date range, by source, with reservation conversion and average days to move-in. Report 3 was orphaned: B-042 took US-39(1–2), B-055 (4–5), B-078 (6) | PRD 02 US-2, §4.11 US-39(1–3), §8; PRD 00 §6 MVP | L | B-038 | MVP |

## Milestone 4: Billing engine & payment reminders (Golden Path 2)

Recurring invoices, autopay, retries, fees — and the comms centerpiece: due-soon/due-date reminders and the past-due dunning ladder with pay-now magic links.

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 43 | B-043 ✅ | Billing scheduler: nightly per-facility jobs (facility-local time), idempotent + re-runnable, Billing Runs screen with per-item outcomes, catch-up after downtime. Hosts the two pre-emptive scans: **cards expiring within 30 days** (retrigger at 7, suppressed on replacement) and **proof of insurance expiring within 30 days**, the second of which enrols the lease into the facility's default protection tier when the proof lapses without replacement, per facility policy (**D-17**), notifying the tenant of the enrolment and its cost | PRD 02 FR-4, §4.3 US-44; PRD 05 CN-10a | M | B-006, B-026, B-022 | MVP |
| 44 | B-044 ✅ | Recurring invoice generation + proration: anniversary/first-of-month policies, gapless sequential numbering, tax treatment, deterministic unit-tested proration math **built once in core, in both directions, so B-077's transfer wizard is a screen over existing math rather than a second implementation**, `invoice.created/due_soon/due_today` events. Plus the rate-variance query B-042's report and the Phase-2 rate-increase worklist both read: in-place vs current street rate per occupied unit with months since last change | PRD 02 §4.5 US-17/US-18, §4.11 US-39(2), §4.3 US-11 | L | B-043 | MVP |
| 45 | B-045 ✅ | Autopay run: nightly idempotent charge run against saved methods, succeeded/failed/skipped visibility | PRD 02 US-19 | M | B-044, B-036 | MVP |
| 46 | B-046 ✅ | Failed-payment retry: configurable schedule (+1/+3/+5), card-expired short-circuit, failed-payments staff queue **as a `Task` view scoped to unresolved items**, and the single shared `daysPastDue(lease)` definition — computed from the original invoice due date, never the last retry — living in core and used by every later consumer | PRD 02 US-20, §4.11 US-39; PRD 02 US-41 | M | B-045, B-095 | MVP |
| 47 | B-047 ✅ | Late fee schedule: per-facility rules, caps, automatic assessment, waive with permission + reason code (audited). **Plus the fee types a real facility charges** — `lock_cut`, `cleaning`, `damage`, `transfer`, `certified_mail`, `auction_cost` alongside the original four, all effective-dated per facility | PRD 02 US-21 | S | B-044 | MVP |
| 47a | B-098 ✅ | **Gate access suspension & restore on non-payment** (operator review; decided as **D-16**): one per-facility versioned threshold — suspend the `AccessGrant` at N days past due (default 6, configurable) and restore automatically within ~2 minutes once the balance reaches zero, driven by B-046's shared `daysPastDue()` and executed through B-027's ACS; both transitions audit-logged with the triggering invoice and day count, both rendered in plain English on the tenant profile, both notified to the tenant; blocked outright by a `LeaseHold` whose effects halt suspension. Qualifying payment = balance to zero, held as a per-facility setting beside the partial-payment allocation policy. **Takes a minimal slice of B-057/B-058 forward — those items inherit this rule as their access step rather than rebuilding it — and does not reopen D-1**, which keeps the timeline, notices, lien and auction pipeline in Phase 2 | PRD 02 §4.6 US-45, US-22; PRD 03 US-3; PRD 05 CN-11 | M | B-047, B-046, B-027, B-030, B-096 | MVP |
| 48 | B-048 ✅ | Partial payments (configurable allocation order, displayed at payment time) + refunds (card via Stripe, cash/check payable, reason codes, RBAC limits) | PRD 02 US-22/US-23 | M | B-044, B-039 | MVP |
| 49 | B-049 ✅ | Tenant ledger screen: chronological charges/payments/credits/refunds with running balance, reconciles to invoices/AR, CSV/PDF export | PRD 02 US-24 | M | B-044 | MVP |
| 50 | B-050 ✅ | Payment lifecycle notices: upcoming-due (default 5 days) + due-date reminders with autopay skip logic, payment receipts, payment-failed notice with fix path ≤5 min, **and the card-expiring-soon notice** driven by B-043's scan — worth 2–4 points of autopay success against PRD 02 §7's ≥92% target, and it keeps a three-year on-time tenant out of the dunning ladder over something visible a month ahead | PRD 05 §3.1 CN-1/CN-2/CN-6, CN-10a; PRD 01 §4.8 | M | B-043, B-044, B-046, B-030 | MVP |
| 51 | B-051 ✅ | Pay-now magic links: ≥128-bit single-purpose tokens, limited portal session scoped to pay, expiry/revocation, click + payment attribution back to the send record | PRD 05 CN-4, FR-12/FR-13 | M | B-035, B-030 | MVP |
| 52 | B-052 ✅ | Past-due dunning ladder: configurable day 1/5/10/30 steps driven by billing-engine events (never a comms-side calendar), at-most-once per invoice per step, instant halt on qualifying payment/move-out/**hold — the hold being B-096's `LeaseHold`, evaluated by its declared effects rather than a per-screen check** | PRD 05 CN-3/CN-5; PRD 02 US-42 | M | B-050, B-051, B-096 | MVP |
| 53 | B-053 ✅ | Template editor + per-facility sender identity: merge-field picker, preview + test-send, versioned saves, publish blocked on unknown fields; per-facility From address/reply-to, auto postal-address footer | PRD 05 CN-16/CN-17 | M | B-030 | MVP |
| 54 | B-054 ✅ | Message history on tenant record + shared suppression list + failure queue (hard bounce → tenant flag + **`Task`**, not a private queue table) | PRD 05 CN-18/CN-19/CN-20, FR-14/FR-15; PRD 02 US-41 | M | B-030, B-038, B-095 | MVP |
| 55 | B-055 ✅ | Revenue + delinquency-aging reports (billed vs collected by category; AR aging buckets with tenant detail, **including ended leases still carrying a balance** — former-tenant AR never disappears from the aging view), CSV export, all definitions read from B-042's metrics module | PRD 02 US-39(4–5), §8 | M | B-042, B-044 | MVP |

## Milestone 5: Delinquency pipeline, access sync & field ops

The full late → overlock → pre-lien → lien → auction state machine, wired to gate suspension and comms. (Full pipeline is Phase 2 per the master roadmap; PRD 02 marks it MVP — see Flagged conflicts.)

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 56 | B-056 ✅ | Delinquency timeline configuration: per-facility ordered steps keyed to days-past-due, versioned, per-state disclaimer guardrails ("example configuration" labeling) | PRD 02 §4.6 US-25/US-29 | M | B-047 | Phase 2 |
| 57 | B-057 ✅ | Delinquency engine: nightly evaluation against timeline version, automated actions + queued staff tasks, `delinquency.day_reached` / `stage_changed` events, cure halts pipeline and restores access. **Inherits B-098's access rule as its access step** — the threshold becomes one configured step and the suspend/restore behaviour is not reimplemented | PRD 02 FR-5, US-45 | L | B-056, B-043, B-098 | Phase 2 |
| 58 | B-058 ✅ | Gate access suspend/restore on delinquency/cure: idempotent stage-event handling, restore SLA ≤2 min (simulated), overlock task creation. **Mostly built at B-098**; what remains here is driving the same ACS calls from timeline stage events rather than the single threshold, plus overlock task creation | PRD 03 US-3 | S | B-057, B-027, B-098 | Phase 2 |
| 59 | B-059 ✅ | Delinquency queue: **a filtered view of B-095's `Task` list**, today's due steps grouped by type, required proof fields (tracking #, photo) that gate completion, visual escalation of overdue items carrying text or icon-plus-text — never colour alone (1.4.1) | PRD 02 US-26, US-41; PRD 02 §5.5 FR-22/FR-23 | M | B-057, B-095 | Phase 2 |
| 60 | B-060 ✅ | Field ops: overlock should-be vs confirmed list with mismatch flags, daily mobile walkthrough checklist, maintenance tickets (blocking unit availability) — all as `Task` types and views, not new queues | PRD 02 §4.9 US-35/US-36/US-37, US-41 | M | B-059, B-095 | Phase 2 |
| 61 | B-061 ✅ | Pre-lien/lien notice generation: templated notices with itemized ledger-reconciled claim, immutable PDFs with delivery proofs, **the address of record it actually rendered stored on the `Notice` row** beside the document hash, and the `notice_email` consent checked before electronic delivery. A corrected notice is a new document with a new date, never a regenerated PDF at the same URL | PRD 02 US-27, §4.4 US-13 | M | B-057, B-023 | Phase 2 |
| 62 | B-062 ✅ | Auction pipeline: eligibility flags, hard-blocked approval when any step lacks proof, advertising record fields, sale outcome + surplus recording, cancel-on-payment. **Plus the four things that actually appear in a wrongful-sale complaint** — lock-cut record with itemized inventory and hashed timestamped photos; buyer record (ID reference, resale certificate, cleanout deadline and forfeit terms); a system-calculated proceeds waterfall posted as ledger entries (sale costs → lien balance → surplus), never typed in as a total; surplus treated as a liability with a statutory life, notified to the address of record and dispositioned. **Vehicles, boats and trailers hard-block the standard path** with "requires separate vehicle lien process" | PRD 02 US-28 | L | B-061 | Phase 2 |
| 63 | B-063 ✅ | Comms delinquency-stage notices + pre-lien/lien supplements (courtesy only, never claims to be the statutory notice; linked to PRD 02 evidence chain). **The access-suspended and access-restored pair ships earlier, with B-098** (D-16); this item covers the remaining stage notices | PRD 05 §3.3 CN-11/CN-12 | M | B-057, B-030, B-098 | Phase 2 |
| 64 | B-064 ✅ | Gate hours enforcement (per-facility weekly schedule, timezone/DST-safe, per-grant overrides) + access event pipeline and admin event log with anomaly flags (after-hours, repeated denials, unknown code) | PRD 03 US-4/US-5, FR-4/FR-5 | L | B-028 | MVP |
| 65 | B-065 ✅ | ManualAdapter work queue: commands become **`Task` rows** (B-095) with exact keypad actions, escalation on overdue, adapter switching preserves grants | PRD 03 US-6; PRD 02 US-41 | M | B-027, B-095 | MVP |

## Milestone 6: Marketing, SEO & lead capture

The demand engine. SEO location pages and lead capture are master-MVP; reviews, drips, and abandoned-cart follow-ups are master-Phase 2 (PRD 04 lists them Phase 1 — see Flagged conflicts).

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 66 | B-066 ✅ | SEO infrastructure: SSR/SSG marketing routes, `SelfStorage` JSON-LD (+`FAQPage`), canonical/URL policy with 301 map, sitemap.xml + robots.txt, meta/OG templates, NAP formatting utility, CWV/Lighthouse CI gate | PRD 04 §3.1 US-1/US-3, FR-SEO-1–7 | L | B-016 | MVP |
| 67 | B-067 ✅ | Facility marketing profile editor: SEO title/meta with character guidance, hero/long copy, FAQs, photo management with required alt text, 5-min revalidation; GBP manual checklist card. **Launch gate: at least one exterior photo per active facility** — with no photos, no CTA and one price, the facility page is currently less persuasive than the search card that links to it | PRD 04 US-2/US-5, FR-CMS-1/2; PRD 01 US-103 | M | B-008, B-066 | MVP |
| 68 | B-068 ✅ | Lead capture & attribution: quote/callback forms, lead entity with dedup, first/last-touch UTM cookie (90-day), spam controls, `lead.created` hand-off + manager notification, lead status lifecycle in admin | PRD 04 §3.5 US-8/US-10, FR-LEAD-1–3 | M | B-016, B-002 | MVP |
| 69 | B-069 ✅ | Analytics: `track()` wrapper, server-side event log as funnel source of truth, standard event set incl. server-fired `move_in_completed`, per-facility funnel report, cookie consent banner (**keyboard-dismissible with focus moved in and returned, no trap, does not obscure content at 320px or 200% zoom, Reject as reachable and prominent as Accept** — consent banners are the most common shipped keyboard trap) | PRD 04 §3.8 US-15, FR-AN-1–4; PRD 01 §6.8.1 | M | B-013, B-026 | MVP |
| 70 | B-070 ✅ | Promotions engine end-to-end: promo/promo-code entities + admin CRUD, eligibility service, site badges with plain-language terms, checkout carry-through, structured discount hand-off to billing invoices, redemption tracking with atomic caps | PRD 02 US-10; PRD 04 §3.6 US-11/US-12, FR-PROMO-1–5 | L | B-011, B-020, B-044 | MVP |
| 70a | B-100 ✅ | **Referral program core** (PRD 10): `ReferralInvite` (one code, one use) + `Referral` entities, `/r/{code}` attribution surviving the canonical redirect, `referral_tenant` channel, qualification on move-in **plus cleared payment**, both $50 discounts through B-070's structured-discount path, and every fraud rule in §5.4 refusing with a readable reason — **stated as an acceptance criterion, not softened at build time into a generic "not eligible"** (accessibility review 2026-08-12): a refusal that does not say what would have qualified is a 3.3.3 failure and a support call. **No field anywhere accepts a third party's email** — §5.2 is a permanent non-goal, not a simplification | PRD 10 §5.1/§5.3/§5.4/§5.5, §6 | L | B-070, B-068, B-044 | Phase 2 |
| 70b | B-101 ✅ | **Referral visibility**: portal list (first name + initial only, state, credit date) — **a real `<table>` with `<th scope>` rather than a `<div>` grid, and referral state (invited / moved in / qualified / credited) carried in words, never a coloured pill alone** (1.4.1, 1.3.1; accessibility review 2026-08-12, same wording B-059's row already carries) — staff view on both tenant profiles with the refusing rule named, `referral_tenant` in the funnel report, and the revenue report splitting referral rewards from promotional discounts — one is acquisition cost, the other a price decision. Plus PRD 10 §6.3's four transactional templates | PRD 10 §5.6/§5.7/§6.3 | M | B-100, B-069, B-055 | Phase 2 |
| 71 | B-071 ✅ | Reviews: manual entry + facility-page display (gated `aggregateRating` decision), post-move-in review-request email (max 1/tenancy, tracked) | PRD 04 §3.4 US-6/US-7, FR-REV-1–3 | M | B-067, B-030, B-026 | Phase 2 |
| 72 | B-072 ✅ | Marketing consent + lead drip: `marketing_email`/`marketing_sms` consent capture, CAN-SPAM unsubscribe, shared suppression enforcement, declarative sequence engine, 3-step lead drip | PRD 04 §3.7 US-13/US-14, FR-MSG-1–5 | M | B-054, B-068 | Phase 2 |
| 73 | B-073 ✅ | Abandoned-reservation follow-up: 60-min abandonment detection, +1h/+24h/+72h resume-checkout sequence (consent-gated), recovery attribution | PRD 04 US-9, FR-LEAD-4 | M | B-072, B-020 | Phase 2 |

## Milestone 7: Phase 2 hardening & reach

SMS goes live (gated on 10DLC), operational depth in admin, hardware hardening, and website/marketing fast-follows.

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 74 | B-074 ✅ | SMS channel live: Twilio Messaging Service per facility, STOP/HELP/START keywords (provider + app level), quiet hours 8am–9pm, SMS→email fallback with paired idempotency, SMS variants of all reminder/dunning templates; tenant notification preference center | PRD 05 CN-13/CN-14, FR-5/FR-7/FR-8; §6.2–6.4 | L | B-032, B-052 | Phase 2 |
| 75 | B-075 ✅ | Delivery dashboard + alerting: sends/delivery/bounce/opt-out rates per facility/template, failure queue with follow-up tasks, silent-failure detectors and dead-letter surface | PRD 05 CN-19, FR-19 | M | B-054 | Phase 2 |
| 76 | B-076 ✅ | Tenant rate increases: one-off + rule-based scheduling, minimum-notice enforcement, approval review screen, templated notice generation and delivery, auto-apply to first invoice on/after effective date | PRD 02 §4.3 US-11; PRD 05 CN-9 | M | B-044, B-053 | Phase 2 |
| 77 | B-077 ✅ | Unit transfer wizard: close old lease / open new, two-sided proration **reusing B-044's core math rather than rebuilding it**, atomic status updates, unified tenant history, `LeaseRateChange` written with reason `transfer`. *Operator review argued for pulling this forward to just after B-040, because a downsize transfer is the cheapest retention save there is and the manual workaround (move-out + move-in) double-charges the admin fee, splits tenant history and resets the rate-increase clock. Kept in Phase 2: the MVP exit criteria do not include transfers and the learning build has no tenants to retain — but the proration math moved to B-044 so pulling it forward later is a UI item, not a rebuild* | PRD 02 US-14 (transfer) | M | B-040, B-044 | Phase 2 |
| 78 | B-078 ✅ | POS depth: drawer sessions (float, close-out, over/short), deposits reconciliation report, merchandise sales (locks/boxes SKUs, stock, COGS) | PRD 02 US-33/US-34, US-39(6) | L | B-039, B-048 | Phase 2 |
| 79 | B-079 ✅ | Staff & org hardening: TOTP MFA for staff, org-level defaults push (fees, templates, timelines) with visible per-facility overrides | PRD 00 §7.1; PRD 02 US-4 | M | B-004, B-053 | Phase 2 |
| 79a | B-108 ✅ | **Staff MFA enrolment and sign-in: QR code, a save path for the recovery codes, and the second-factor field on a bare `/login`.** (1) `/mfa` shows only a 32-character base32 key to type by hand. The `otpauth://` URI it needs already exists and is already offered as a link — but that link only helps when the enrolling device *is* the phone, and the ordinary case is a key on a laptop screen and an authenticator in a pocket. Render the same URI as a QR, generated server-side and inlined (data URI or inline SVG) — the QR encodes the shared secret, so it must never be a fetchable URL, never be logged, and must die with the pending enrolment. **The typed key stays visible beside it, not behind a toggle**, and the accessibility review of 2026-08-12 corrected three things about how this row stated that: the criterion is **1.1.1 Non-text Content, Level A** — a stricter obligation than the AA this row claimed, and this row gets quoted in the accessibility statement; the image therefore takes **`alt=""`** because the adjacent key IS the text equivalent (`alt="QR code"` announces information it does not carry, and `alt={otpauthUri}` puts the shared secret into the accessibility tree, into AT logs and into extension dumps — the exact surface the rest of this row keeps it off); and the typed key is not yet an adequate equivalent, because `formatSecretForDisplay` groups base32 into pronounceable four-character blocks that VoiceOver reads as words, and `I`/`1`, `O`/`0`, `S`/`5` are indistinguishable by ear — it gains a character-separated `sr-only` reading matching `gate-code-panel.tsx`. Without that, the QR helps sighted staff and nobody else, inverting the item's own justification. (2) **Recovery codes get a way to save them** (UX review, finding 15): the copy says "this is the only time they are shown" and the screen offers no copy-all, no download, no print and no acknowledgement gate, while the codes live in a client component's `useActionState` — a refresh, a Back or a stray click loses them permanently, and the recovery path from there is an administrator reset on a product with one owner account. Copy-all and download-as-text beside the list with "Copied" announced from a persistent region (the `GateCodePanel` pattern), plus an "I have saved these codes" checkbox that must be ticked before the page will navigate on; identical treatment on regenerate; codes never logged. (3) **The sign-in form still infers the audience from `?from=`** even though D-47 fixed the lookup: the second-factor field renders only at `/login?from=/admin`, so an enrolled staff member reaching a bare `/login` submits without a code and is refused — the same "correct password rejected" symptom D-47 exists to kill. The field must be reachable for a staff account arriving with no hint, **without the form differing by whether an address exists** (no enumeration; the two-step "email, then code" shape is one way, always rendering it with its existing "leave blank if you have not set up two-factor authentication yet" hint is another — the item picks one and says which). In the same pass, the magic-link disclosure states as a general fact that staff sign in with a password and a code (D-40), since it is offered at a bare `/login` today and a staff member who uses it is told a link is on its way that flows.ts will never mint — a general sentence leaks nothing about any particular address | PRD 00 §7.1; PRD 01 §6.8.1 (portal dashboard, gate code); D-40, D-47 | M | B-079 | Phase 2 |
| 80 | B-080 ✅ | Hardware hardening: nightly expected-vs-actual reconciliation job, adapter contract-test suite, **one** stub vendor adapter in a vendor-emulation profile (**D-18 amends D-4: one stub, not two** — one proves the port/adapter contract survives a differently-shaped third-party API, which is the whole learning payload; a second proves nothing the first did not, at full cost, and becomes code nobody deletes because it looks like progress), adapter health dashboards, camera link management, webhook secret rotation | PRD 03 §8 Phase 2, FR-9/FR-10 | L | B-027, B-064 | Phase 2 |
> **B-081 was split on 2026-08-10 (D-44), per note 8 below.** It bundled six unrelated features behind one number — a map view, a payment method, a statements centre, an insurance flow, an access-list surface and a checkout rewrite — sharing no schema, no screen and no PRD section. The rows below replace it. The dropped size-estimator quiz stays dropped; it is not carried into any of them.

| 81a | B-102 ✅ | **Monthly statements centre** (PRD 01 US-705, §6.1/§6.3): a per-month statement per lease — opening balance, every charge and payment in period, closing balance — rendered through B-042's semantic-HTML document pipeline (there is still no PDF encoder, by the same standing decision), listed in the portal alongside receipts and the lease, and reconciling to the ledger to the cent | PRD 01 US-705, FR-6.1/6.3 | M | B-044, B-042 | Phase 2 |
| 81b | B-103 ✅ | **ACH bank debit + Stripe Link** at checkout and in the portal (§3: "cards, Apple Pay, Google Pay, Link, and ACH bank debit"). ACH settles days later and can fail after acceptance, so this needs the pending-payment state the card path never required: a move-in must not wait on it, and a `payment_intent.processing` → `succeeded`/`failed` webhook path has to reach the ledger | PRD 01 §3, §9 Phase 2 | M | B-036, B-019 | Phase 2 |
| 81c | B-104 ✅ | **Insurance tier change + proof-of-insurance upload** in the portal (US-705: "option to change tier (takes effect next billing cycle) or submit proof of own insurance"). Tier changes are effective-dated to the next cycle, never mid-period; a proof upload lands in the same staff review queue D-17's auto-enrolment already reads | PRD 01 US-705 | M | B-037, B-095 | Phase 2 |
| 81d | B-105 ✅ | **Portal self-service for the authorized-access list**: a tenant adds, edits and revokes their own authorized people, each change going through B-029's existing grant path and B-027's outbox — same audit trail as the counter, no second way to open a gate | PRD 01 §9 Phase 2; PRD 03 US-8 | M | B-029 | Phase 2 |
| 81da | B-109 ✅ | **Stale copy, dead references and enum identifiers on staff screens** (UX review 2026-08-12): three sentences on shipped admin screens describe a build that no longer exists, and one of them costs money — `/admin/leads/[leadId]` tells the counter agent "no promotions engine yet (B-070), so nothing here is discounted" while the facility page prices that promotion, so a phone quote and a web quote for the same unit disagree and the tenant finds out at the counter. Delete it, confirm the quote actually reflects the live engine, and show the promotional first-period price beside due-today so the two channels agree. Same treatment for settings' "late-fee rules are configured in B-047" (link the screen that owns them) and the dashboard's "portfolio roll-up arrives with reporting (B-042)" (shipped — the roll-up itself is B-113). Plus one shared label map for unit, lease and lead status used by every admin select, badge and filter chip, so `available`/`overlocked`/`new`/`overlock_apply` stop rendering as option text and as *Filtered to "overlock\_apply"*; admin may use industry words, it may not use enum identifiers. Bulk-edit's Reason becomes a `<select>` of the audit reason catalog with free text in an adjacent note — it is a hand-typed `management_approval` on a destructive operation across up to 290 units today. Homepage size help links to `/storage/size-guide`, not `/faq`. Extend D-15's "no backlog ID renders" test from the public routes to `/admin/*` and `/portal/*` | PRD 02 §5.5 FR-19/FR-22; D-15; PRD 04 FR-PROMO-3 | S | B-070, B-094 | MVP |
| 81db | B-110 ✅ | **Checkout dynamic state** (accessibility review 2026-08-12, findings 1–6 — one machine, one item). The heading is prepared for focus and never given it, and the success message is returned into the component `revalidatePath` unmounts on the same render that would have announced it: a renter fills thirteen fields, presses Continue, and hears nothing, five times over. Move the live region above the `<section>` so it outlives the step, focus `#step` on every transition, and announce the destination ("Your unit — step 2 of 6") — same treatment for extend-lock and re-lock. Surface the T-5-minute lock warning from a client timer rather than from whether the renter happened to submit inside the last five minutes, which on the lease step means never (2.2.1). Swap `disabled` for `aria-busy` plus a re-entry guard on **both** payment submits, with a pre-mounted "Taking payment. This can take a few seconds." — `use-my-location.tsx` already documents why disabling the focused element is wrong, and both money paths do it. One `bodyOf()` in the document renderer so a **resumed** lease step stops injecting the stored document's `<h1>` (first visit and resume disagree about the heading outline today, and B-031 emails a resume link precisely to make resuming normal). `id="lease-summary"` so the dangling `aria-labelledby` resolves and the plain-language summary is a landmark again. `Field` gains checkbox and radio so `consented`, `attested` and the `tier` group carry `aria-invalid`/`aria-describedby` at the control — on the `<fieldset>` for the group — instead of only in the summary a control-by-control navigator never reaches. Tests: focus lands on `#step` after a valid step-1 submit, and a region captured before an action has non-empty text on the same handle after. **Prerequisite of B-106**, which reproduces every one of these once per unit | PRD 01 §6.8.1 (checkout stepper, payment step, protection plan), §6.8, FR-4.1 | M | B-025, B-035, B-094 | MVP |
| 81dc | B-111 ✅ | **Checkout goes both ways, and the price says what changed** (UX review 2026-08-12, findings 2 and 3). §6.4 requires "back navigation never loses data" and `lib/checkout/session.ts` already says "the renter may always go back to a step they have already completed" — nothing renders a control, so a renter who mistypes the email that receives the lease, the receipt **and the gate code** discovers it at step 4 and can only abandon or rent against an address they cannot read. Completed steps in the progress indicator become links; every step below `provisioned` carries a Back control beside Continue at the same tap size; returning rehydrates from `session.data` and going forward re-asks nothing; Back is refused once payment has succeeded, and the payment step's control says what it does — "Go back — nothing has been charged yet". Focus and announcement reuse B-110's transition. Second half: `PriceSummaryProps.changeNote` exists, renders into a pre-mounted status region, and is passed by nobody, which is why choosing a $12/mo protection tier moves both totals with no stated cause one screen before the card form; pass it whenever a step changed a total, clear it on the next step. And make the summary persistent on every viewport — `sticky bottom-0 sm:static` resolves on desktop to "last element on the page", which is not what §6.4 asks for — then check at 360×640 that the sticky bar does not cover the Payment Element's submit. **Prerequisite of B-106** | PRD 01 §6.4, §6.8.1 (payment step, 3.3.4) | M | B-020, B-110 | MVP |
| 81dd | B-112 ✅ | **Checkout step 1 down to the field cap, and consumer-sized controls** (UX review 2026-08-12, findings 8 and 13): §6.4 caps a step at ≤7 visible fields and step 1 renders 14, on a phone, immediately after "Rent now". Derive city and state from the zip using D-14's bundled dataset and show them read-only with an edit affordance for the PO-box and mismatch cases — `state` is a free-text 2-character input today, so "Texas" fails validation after submit; move the alternate contact and the active-duty declaration to the lease step, where the SCRA sentence they trigger actually appears; group the two consent checkboxes below the primary action, since marketing consent is the only field on the screen that serves us rather than the renter. Net 14 → 8, asserted in a test so the next item cannot quietly re-add. Plus tap targets: `CONTROL_CLASS` is `h-9` (36px) against §6.2's ≥44px, on every public and portal form including this one — one token with an admin density and a consumer density, and one rendered-height assertion on a public form. **Prerequisite of B-106**, which adds a unit picker and a start-date picker to this same step | PRD 01 §6.4, §6.2, §6.8.1 (renter details); D-14 | M | B-021, B-015 | MVP |
| 81de | B-113 ✅ | **Admin dashboard drill-through and an "All facilities" that rolls up** (UX review 2026-08-12, findings 5 and 6): five of seven tiles are dead ends, including the only two that mean somebody has to act — "Failed payments today: 3 · needs attention" with no destination teaches the reader to skip the row, which is the exact failure the tile's own rewrite was meant to prevent. Every tile links to the filtered list behind it. Delinquency is reported as **past-due AR in dollars** from D-25's metrics module with its window in the label, not as a count of `Lease.status = 'delinquent'` — nothing sets that status until B-057, so the tile reads 0 beside real AR — and a test asserts the tile and the report behind it agree. "All facilities" renders a per-facility roll-up on the dashboard, Units, Delinquency and Inquiries in the shape `/admin/tasks` already uses, replacing five different phrasings of "pick a single facility above" on the owner's own default context; **New inquiry** opens with a facility selector as its first field instead of refusing, because it exists so a ringing phone costs one click | PRD 02 §4.1 US-2, §4.8 US-43, §5.5 FR-23; D-12, D-25 | M | B-042, B-094, B-097 | MVP |
| 81df | B-114 ✅ | **The Tenants screen lists tenants** (UX review 2026-08-12, finding 4): it is a heading, a search box and nothing else until you type, so the screen named after them answers none of "who are my tenants", "who owes me money" or "who moved in this week", and every past-due question routes through Reports. Default list scoped to the switcher, newest lease first, paginated at 25 with "Showing 1–25 of 143"; columns name · facility and unit(s) · lease status in plain words · **balance** · days past due, the last two from the metrics module (D-25) rather than computed inline; filter chips as links so a view is shareable (All · Past due · Moved in this month · Ending soon · Former tenants); the row is a link to the profile and a past-due row carries the words, not only a colour; a truncated search says so rather than silently dropping matches past 25 | PRD 02 §4.4 US-13, §5.5 FR-22/FR-23; D-25 | M | B-038, B-042 | MVP |
| 81dg | B-115 ✅ | **Tasks and delinquency cards name and link their subject** (UX review 2026-08-12, finding 9): `TaskRow` already carries `entityType` and `entityId` and neither screen uses them, so a card reads "Fit an overlock / Lease · Aug 12 / Unassigned" — no tenant, no unit, no amount, no link — and the staffer memorises nothing, goes to Tenants, searches, and comes back. The subject line resolves to "Unit B-14 — Ada Renter" and links to the profile or the unit; delinquency cards additionally carry balance and days past due from the metrics module, which is the figure that queue exists for and the one it omits; the filter chip renders the catalog label rather than the raw key; a task whose subject has been deleted renders unlinked rather than broken. This is the screen a manager works from a phone on the property, so the card layout is checked at 320px in this item rather than left to B-116 | PRD 02 §4.9 US-41, §4.6 US-26, §5.5 FR-22 | S | B-095, B-059, B-042 | MVP |
| 81dh | B-116 ✅ | **320px reflow on the three admin routes that fail it, and the unit list's volume** (accessibility review 2026-08-12 finding 7, UX review finding 12; owner decision **D-48**): card-per-row below `sm` on the units table — the grid view is already close and reflows fine — stacked settings forms, and the same for unit types; `REFLOW_PENDING` in `e2e/admin.spec.ts` is **deleted, not repointed**, and the test it feeds starts passing in the same commit. `listUnits` has no `take`, so a single facility renders ~100 rows and the portfolio 290, each carrying a `<select>`, a submit and a link — roughly 900 tab stops between the top of the table and the bulk-edit heading below it, on a page that also scrolls sideways. Paginate at 50 with a count line, reusing the tenant list's pattern rather than inventing a second one and keeping filter state in the URL; move **Add a unit** and **Import layout** off the daily screen, since two once-per-facility setup jobs should not sit below the inventory; occupied rows show the tenant name, linked, so "who is in B-14?" is answerable from the screen that knows | PRD 02 §5.5 FR-16/FR-22, §4.2 US-5/US-7; D-48 | M | B-010, B-094 | MVP |
| 81di | B-117 ✅ | **Navigation hierarchy, admin and portal** (UX review 2026-08-12, findings 11 and 16): twenty undifferentiated admin destinations in one column — an org chart of the codebase, not a day at a facility — which becomes a horizontal scroll strip on a phone with **Walkthrough** at position 9 and **Tasks** at 14, the two screens that exist to be used phone-in-hand. Group into **Today** (Dashboard, Tasks, Inquiries) · **Property** (Units, Walkthrough, Maintenance, Overlocks, Gate Activity, Keypad Queue, Gate Health) · **Money & tenants** (Tenants, Billing, POS, Delinquency, Rate Increases, Auctions, Reports) · **Admin** (Settings, Audit Log); below `sm` collapse to Today plus a "More" disclosure rather than a strip that gives no sign there is more to the right. Remove **Leases** and **Audit Log** from the nav until they have screens — both resolve to the "built in a later backlog item" placeholder, and a nav promising two destinations it does not have reads as unfinished to the person being asked to trust it with rent; leases are already reachable from the tenant profile. Portal: order the nine links by frequency (Overview · Payment methods · Statements · Documents), group the rest under a Manage disclosure, and separate **Move out** visually — it is the one irreversible destination in the list, and at 360px the flat row wraps to four lines above the balance | PRD 02 §5.1 FR-2, §5.4 FR-12, §5.5 FR-16; PRD 01 §6.5 | S | B-007, B-034 | MVP |
| 81dj | B-118 ✅ | **Facility page: hero photo, sticky rent CTA, and the hold window stated before the form** (UX review 2026-08-12, findings 7 and 14): §6.3 fixes photos first and the built page renders them at ~line 942, after the hours table, every unit card and a paragraph of marketing copy — a renter deciding between three sites on a phone judges "clean, lit, not a dump" from an image and is given one last. One hero photo (or a 3-up strip) directly under the facility name, above the contact block, as the LCP element with explicit `width`/`height` and `fetchPriority="high"`; the rest of the gallery stays where it is and keeps `loading="lazy"`; a facility with no photos renders no placeholder and no empty frame. Delete the header comment claiming the gallery is "missing on purpose: nothing in the product stores a photo yet" — stale since B-067 and the reason nobody re-checked the position. Plus §6.2's missing sticky "Rent now" bar below `sm` once the unit list has scrolled past the fold ("From $89/mo · Rent now", one primary action, a secondary "Reserve free"), and the reserve form stating the hold window in its trust line — "Free to hold for 7 days" (D-7, per-facility) — which is the deliberate comparer's whole question and is currently answered only after they have handed over their details. **The §6.3 hours-vs-units reorder is deliberately NOT in scope**; it contradicts written PRD text and waits on the owner, per the note now in §6.3 | PRD 01 §6.3, §6.2, §4.4 US-401; D-7 | M | B-016, B-067, B-018 | MVP |
| 81dk | B-119 ✅ | **The accessibility scan contract** (accessibility review 2026-08-12, test gaps 1–7): coverage grew by accident rather than by contract. `PUBLIC_ROUTES` calls itself "the contract" and is missing `/messaging-policy`; `/login`, `/forgot-password`, `/reset-password`, `/mfa`, `/reauth` and `/unsubscribe` are in no scan at all; `/checkout` is scanned only in its "session not found" state, so five of six steps and the confirmation screen have never been seen by any tool — which is why finding 5's dangling `aria-labelledby`, a plain axe violation, survived to a source review. Add an `AxeBuilder` scan after each `advance` in the move-in walk `e2e/smoke.spec.ts` already drives; add one shared `ADMIN_ROUTES` list carrying the same "a route not in this list is a route nobody checks" comment, covering the ~40 unscanned admin routes; run the reflow, 200%-zoom and text-spacing loops over portal, auth and admin rather than public only. Two behavioural assertions the suite has never had: that focus **moves** where it should, and that a live region's text **changes** on the handle it was captured from — structural presence is all that is checked today, and it is exactly the half findings 1 and 3 break. Plus the five manual passes automation cannot replace, each recorded in `PROGRESS.md` the way B-093's was: VoiceOver + Safari over the full checkout; VoiceOver + iOS over the portal past-due state (its persistent banner is `role="alert"`, which is not what `alert` is for) and the gate-code reveal; NVDA + Firefox over the admin error summary, the tax confirm-and-echo and a facility switch (which B-094's row claims announces the new context, and no live region exists to do it); keyboard-only tab-stop count on `/admin/units` at 290 units; and a focus-indicator sweep across Chrome, Firefox and Safari, which has not been re-run since B-093 despite six months of new components | PRD 01 §6.8 (last two bullets), §6.8.1; PRD 02 §5.5 FR-24 | M | B-110, B-116, B-094 | MVP |
| 81dl | B-120 ✅ | **The e2e suite is not repeatable, and it does not notice when it is testing the wrong application** (found while building B-110). Three problems, one theme: a run that goes red tells you nothing until you have ruled the environment out by hand. (1) **A second full sweep fails without a reseed.** `admin-move-out`'s settlement preview mutates a seeded lease, `admin-tasks`' returned-mail flag finds the task it created last time, and the reservation specs exhaust the sandbox's inventory — each passes in isolation, so the failure reads as flakiness and is not. `global-setup.ts` already releases stale checkout locks and reservation holds and explains at length why that cost three diagnoses in one evening; the precedent is right and the coverage is short. Either specs provision and clean their own fixtures, or setup resets the ones they consume — pick one and say which, because doing half of each is how it got here. (2) **`baseURL` is hardcoded to port 3000 while `webServer` silently falls back to another port when 3000 is taken**, so a stray dev server from any other project makes the entire suite drive an application that is not this one. What that looks like is 300 tests failing with `ERR_CONNECTION_REFUSED` and `net::ERR_EMPTY_RESPONSE`, which reads exactly like a code regression; it happened during B-110 and cost most of an hour. Fail on the port being occupied, or read the port back from the started server — never both hardcode and fall back. (3) **The e2e database is not covered by `db:migrate:test`.** That script migrates the `storage_test` SCHEMA, which is what `vitest.config.ts` redirects to; Playwright reads `DATABASE_URL` straight and wants `public` in the same database, so a fresh machine gets `The table public.checkout_session does not exist` from global setup before a single test runs. Needs its own named script beside the others and a line in CLAUDE.md, since the existing one is worded as though it covers everything | PRD 01 §6.8; the e2e suite itself | M | — | MVP |
| 81dm | B-121 ✅ | **The active-duty declaration has to reach the delinquency pipeline** (found in B-112; owner decision **D-49**). `military_scra` exists in the holds catalog with the right restrictions — no sale, no access suspension, no collections without a manager — and it is raised **only by hand**. Nothing reads `Tenant.activeDutyMilitary`, so a renter who ticks the box at the lease step is overlocked, dunned and auctioned exactly like anyone else, and the record shows we asked and stored the answer. The declaration raises the hold automatically on move-in, and a staff member setting the flag on a tenant profile does the same; lifting it keeps US-42's manager requirement. Covers every lease the tenant holds, not just the one being signed, since the protection is about the person. **The lease clause is deliberately NOT in scope** — D-49 defers the wording to the attorney pass that already owns the Texas Property Code ch. 59 lien and notice language, because SCRA modifies exactly that and a clause drafted now would be redrafted against it. A test that a delinquent lease under an active-duty hold produces no overlock, no dunning step and no auction case | PRD 02 §4.9 US-42; PRD 01 US-501 step 4; D-49; 50 U.S.C. §3958 | M | B-096, B-112 | MVP |
| 81dn | B-122 ✅ | **A renter can actually enter a promo code** (PRD 04 US-11 AC3, FR-PROMO-2/3; found 2026-08-14 during the B-112 review). `offerFor` has taken a `code` argument since B-070 and **no surface passes one**, so a code-gated promotion can be created in admin, is correctly hidden from the badges, and can never be redeemed by anybody — `PromoCode.usesCount` has no path to increment. A code entry on the facility page and at the checkout price summary, both re-evaluated server-side (a promotion the browser can name is a discount the browser can choose), attaching through the same `PromoSnapshot` "Rent now" now uses. The refusal has to say WHICH rule failed — expired, used up, not for this unit type, not for returning tenants — because `REJECTION_MESSAGES` already distinguishes them and a generic "that code is not valid" is a support call and a 3.3.3 failure. Entering a code that is worse than the automatic promo already applied keeps the better one and says so (FR-PROMO-4's no-stacking rule, stated to the renter rather than silently applied). Announced from the price summary's live region, and the change note names it — B-111 built both | PRD 04 §3.6 US-11 AC3, §4.5 FR-PROMO-2/3 | M | B-070, B-111 | Phase 2 |
| 81do | B-123 ✅ | **Marketing SMS, or a decision not to have it** (PRD 04 §3.7 US-14, FR-MSG-1; found 2026-08-14). Every promotional message the product can send goes by email. `comms/service.ts` says it plainly — B-072 shipped `marketing_email` only, there is no `marketing_sms` capture flow, so no marketing SMS rule is seeded and none could be evaluated if it were. Every SMS today is gated on `account_sms`, which is TRANSACTIONAL consent, and sending a promo under it is the compliance failure PRD 05's two-lane split exists to prevent. Either build the lane — `marketing_sms` consent captured with its own disclosure and version, its own suppression, quiet hours already enforced by `isSmsQuietHours`, and SMS variants of the drip and promo templates — or record a decision that promotional SMS is out of scope and stop implying otherwise in PRD 04. **The trap to avoid is the middle:** a promo sent down the transactional lane because the marketing lane does not exist | PRD 04 §3.7 US-14, FR-MSG-1; PRD 05 §5.1 | M | B-072, B-074 | Phase 2 |
| 81dp | B-124 ✅ | **A validation error must not discard what was typed** (found 2026-08-14 while building B-114). **Cause, once found: React 19 resets a `<form action>` once its action resolves** — no code in this repo clears anything, and the echo-into-`defaultValue` fix this row first prescribed could not have worked, because the inputs never remount. Every server action in the product therefore re-rendered its form empty after a field error. On the lease step that is not an inconvenience, it is a trap: mistype your signature and the re-render silently clears the **e-sign consent tick and the typed name**, so the next press is refused with "Tick the box to agree to sign electronically" for a box the renter did tick, on the last screen before payment. `activeDutyMilitary`, `altContactName` and `altContactPhone` clear alongside it. B-094 got the error *reporting* right (`aria-invalid`, `aria-describedby`, focused summary) and value *retention* was never part of it. 3.3.3 is not satisfied by an error that names a field the form just emptied. Fixed in `AdminForm`, which writes the submitted values back into the form's own controls on the error and confirm branches — one place, every control, no change to any of the 156 actions that return `fieldError`, and at the form rather than in `Field` because the protection step's radios are raw `<input>`s. Passwords, hidden fields, file inputs and buttons are excluded; restoring the confirm step's named button would have disarmed it. Consent stays stored only on success; the echo is a UI convenience, never a record of consent | PRD 02 §5.5 FR-19/FR-20; PRD 01 §6.8.1; WCAG 3.3.3 | M | B-094 | MVP |
| 81dr | B-126 ✅ | **Reconcile D-7's promised reservation hold window against what shipped** (found during B-118, 2026-08-14; D-50): D-7 says holds expire on a 7-day default, per-facility configurable. B-018 shipped something else — `holdExpiryFor` expires a hold at end of day after the renter's own chosen move-in date, hardcoded, no facility setting at all. Either build the 7-day-configurable window D-7 describes (a `Facility` column, an admin settings field, migrating `holdExpiryFor` to read it), or correct D-7 to describe the day-after-move-in rule that actually shipped and has been correct product behaviour since B-018. Until decided, every customer-facing description of the hold window states the real mechanism, not the "7 days" figure — B-118's reserve-page trust line is the first of these | PRD 01 §4.4 US-401, FR-3; D-7, D-50 | S | B-018 | MVP |
| 81ds | B-127 ✅ | **Two friends redeeming the same referral invite in the same instant DEADLOCK instead of one losing** (found 2026-08-17 during B-082 part 4, in a full-suite run under parallel load). `qualifyReferral` opens a transaction that INSERTs a `referral` row — which takes a shared FK lock on the `referral_invite` it points at — and only THEN `UPDATE`s that same invite to claim it. Two concurrent qualifications each hold the shared lock and each wait for the other to release it before the update: Postgres reports `40P01 deadlock detected` and **both transactions roll back**, so neither friend gets the invite and one of the two rentals loses a $50 reward with no record of why. The race is already the tested case — `gives one invite to exactly one friend under a race` — and it passes because the losing path is exercised far more often than the deadlocking interleaving; it failed once in four full-suite runs and zero times in three isolated ones, which reads as flakiness and is not. **The fix is an ordering change, not a lock:** claim the invite FIRST (`updateMany ... WHERE redeemedAt IS NULL`), then create the referral, so the loser blocks on a row lock and finds `count === 0` rather than racing for the same lock from the other side. The refusal record the current code writes has to be preserved — the tenant is told which rule refused them (§6.3) — so the loser still writes a `refused` referral, just after losing rather than before. **Fixed during B-082 part 5, 2026-08-17, having been deferred one part earlier.** The judgement changed when it reproduced a second time: at roughly one full-suite run in three it would redden CI at random and made it impossible to report any suite honestly green, so the deferral was costing more than the fix. Claim first, create second — both transactions now take the same locks in the same order, which makes the deadlock impossible rather than unlikely. The loser is created `refused` outright instead of created `earned` and downgraded a statement later, removing a moment where a $50 referral was visible inside the transaction. **The race test was strengthened from two contenders to four**: at two it caught this bug about one run in three and never in isolation, and a guard that passes two times in three is not a guard. 0 failures in 7 full-suite runs and 12 isolated ones after the fix, against 2 in 6 before | PRD 10 §6.1; FR-REF-4 | S | B-100 | Phase 2 |
| 81dt | B-128 ✅ | **City pages need copy somebody can write, or the decision that they never will** (found 2026-08-17 by B-082 part 6's own duplicate-content report, on the copy B-082 part 2 generated). D-58 chose to generate the city intros from the facility records rather than add a `City` model, on the honest footing that it is thin-content protection at the floor and named the trigger for revisiting: an operator wanting to say something about a city that is not true of its facilities. **There is now a second, measured trigger.** The site's own check scores the three demo city intros at **0.82–0.85** against each other, over this codebase's own 0.8 duplicate threshold — so by the project's own measure the city pages are duplicate content, and the report says so every time it is opened. That is the check working, not a defect in it. The decision is the owner's: build an editable per-city description (a `City` model, an admin screen and a permission, per the rule that a field configuring behaviour ships with its control), or record that generated intros are accepted permanently and exclude generated-vs-generated pairs from the report so it stops reporting a state nobody intends to change. **Do not simply raise the threshold** — that hides the same finding for authored copy too, which is the case the check exists for. Until this is decided, the report's guidance for these pairs deliberately says "there is no city description to edit today" rather than telling somebody to rewrite a field that does not exist. **Decided 2026-08-17 (owner) — D-62: build the editable copy, keeping the generated intro as the fallback.** A `City` row holds authored prose and nothing else, keyed on the URL slug so it can never become a second opinion about where a city is or how it is spelled; `cityIntro` takes it and wins when it is there, so a city nobody has written for renders exactly what it rendered before — which is every city on the day this shipped. `/admin/settings/marketing/cities` lists every city that HAS a page (the list comes from the facilities, so copy cannot be published about a place we do not operate in), shows what each page says today rather than an empty box and a hint, and clearing the box is a real way back to generated. The permission is new — `marketing:city_copy`, asked for with a **null facility** like `org:defaults`, because a city page lists every site in the city and `facility:settings` at one of them would let a manager edit the page listing their neighbours'. **The duplicate-content report reads which one actually rendered**, so writing copy for a city moves it from `generated` to `authored` — and two authored cities that are still alike are now reported as a real copy problem instead of a product gap. The report's advice for a generated pair links to the new screen rather than describing something nobody could do. **The threshold was not touched**, per this row | PRD 04 §3.2 US-4 AC1; D-58 | M | B-082 | Phase 2 |
| 81dq | B-125 ✅ | **e2e runs against a production build, not the dev server** (found 2026-08-14 while building B-114). `playwright.config.ts` starts `npm run dev:test`, and Turbopack compiles CSS lazily: under a full parallel sweep the checkout step can be served a bundle that does not yet carry `h-(--control-h,2.75rem)`, so the zip field renders at its content height of **21px** and the §6.2 44px assertion fails. It passes in isolation, passes on a freshly restarted server, and failed intermittently across three sweeps today — which reads exactly like a broken tap-target and is not one. The value is the tell: 21px is the height rule being **absent**, not wrong. Add `build:test`/`start:test`/`e2e:server`, point `webServer.command` at the build, raise the `webServer` timeout past a cold build, and keep `E2E_DEV=1` as the escape hatch for debugging one spec. A dev server is not the artifact we deploy — it skips minification and bundle splitting and serves an error overlay in place of the real error boundaries, so every dev-only difference is a defect class this suite is structurally blind to | PRD 00 §5; house convention | S | B-001 | MVP |
| 81e | B-106 ✅ | **Future-dated move-ins beyond 14 days + multi-unit rental in one checkout.** The invasive one: today's checkout assumes one unit and a near-term start, so this touches the lock, the quote, proration and the lease document. Split from the rest of B-081 precisely because it is the only part that changes the checkout core. **Do not start it before B-110, B-111 and B-112 are done** — those three fix a checkout that cannot go backwards, never says why a total moved, asks fourteen questions on step 1, and announces nothing when the step changes; multi-unit reproduces each of those once per unit, and a multi-unit checkout on a one-way stepper with a silent total is strictly worse than today's. Its own added criteria (both reviews, 2026-08-12): the price summary itemises per unit and the change note names **which** unit changed the total; each unit in the basket is a named group (`<fieldset>`/`<legend>` or a labelled region) so three "Remove" controls do not share one accessible name (2.4.4/4.1.2); **one lock, one warning, one extension covering the whole basket** — N independent countdowns cannot satisfy 2.2.1 in any usable way; the future-dated start is an `<input type="date">` with manual text entry and a notice-rule violation carries a *suggestion*, not just a refusal (3.3.3), the shape `/portal/move-out` already solved; and every step transition still moves focus and announces from a region that outlives the step | PRD 01 §9 Phase 2, §6.4, §6.8.1 | L | B-020, B-026, B-044, B-110, B-111, B-112 | Phase 2 |
| 81f | B-107 ✅ | **Map view with pins/price bubbles + "use my location"**, with a list equivalent that is the primary surface, not a fallback (PRD 01 §6.8: "map views have list equivalents"). **Unblocked 2026-08-14: Google Maps Platform (D-46).** Needs a referrer-restricted, Maps-JavaScript-API-only key — it ships to the browser, so an unrestricted one is a billable resource anybody can copy out of the page source. D-14 is untouched: geocoding stays on the bundled dataset, so search keeps resolving offline and the map is decoration over a list that already works. **Accessibility criteria, written now because they do not depend on the vendor** (accessibility and UX reviews, 2026-08-12): the view **defaults to the list** and the map is behind a toggle or a `<details>`, matching the treatment the facility page already gives its embed; every pin has a keyboard-reachable equivalent in that list and the map is never the only route to a facility (2.1.1); pins and price bubbles meet 3:1 against the tiles (1.4.11), which is the criterion an arbitrary satellite or terrain background fails by default and therefore needs a stated tile style; the container is not a keyboard trap and does not capture scroll without an explicit opt-in gesture (2.1.2); result-count changes on pan or zoom announce from a pre-mounted live region (4.1.3), as the facility page already does; no interaction depends on hover; the canvas or iframe carries an accessible name with the text address and directions link **preceding** it; and "use my location" reuses `components/site/use-my-location.tsx` rather than being rebuilt — that component is the model (`aria-busy` not `disabled`, pre-mounted status region, graceful degradation, shareable result URL) and a second implementation will not be as good. **Shipped 2026-08-17.** The list is the view and the map is a collapsed `<details>` beneath it; the vendor script is not fetched until it is opened, so a renter who never opens it costs nothing. Price bubbles are `AdvancedMarkerElement` content, which makes each one an ordinary `<a>` — tab order, contrast and no hover dependence come from the platform rather than from a workaround (**D-56**, which is also why a second public value, `NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID`, joins the key). `use-my-location.tsx` was reused unchanged, as the row requires — it was already on this page. **Outstanding, and named rather than assumed:** nobody has watched this render against a live key, because none is configured; the e2e blocks `maps.googleapis.com` deliberately so no spec depends on a vendor being reachable, and the accessibility statement records the live assessment as owed instead of claiming it | PRD 01 FR-1.3, §6.8, §10 OQ-6 | M | B-015, B-016 | Phase 2 |
| 82 | B-082 ✅ | Marketing reach: city pages (`ItemList` schema) + markdown/MDX content hub with guide schema, funnel reporting v2 (source/medium, sequence attribution, promo ROI), Search Console integration, duplicate-content warnings. **Plus the marketplace *integration* surface** (owner, 2026-07-31): availability feed out, lead attribution in, rate parity, and channel source carried through reservation → move-in so a channel's move-ins are not credited to organic. This is the plumbing only — *whether* to list on a marketplace stays a business decision outside the backlog, and master §1.4 still treats marketplace aggregation itself as a Phase-3 consideration. **Split into parts, 2026-08-17, because this row is six deliverables and size L.** **Part 1 ✅ (marketplace attribution plumbing)**: `Lease.acquisitionChannel` + utm trio stamped at move-in (**D-57** — the lead's channel beats the checkout cookie, because aggregators invoice per completed move-in); `byChannel` beside `bySource` in the moves metric and BOTH rendered on `/admin/reports`, which previously computed `bySource` and displayed nothing while telling the operator "nothing records how a rental was acquired"; `GET /api/public/marketplace/availability`, whose rate parity is structural because it reads the same `publicInventoryForFacility` the website renders from; `POST /api/public/marketplace/leads`, where the channel is decided by WHICH partner key authenticated and never by the request body. **Part 2 ✅ (city pages)**: `/storage/{state}/{city}` — which has been a live **301 target and a 404 at the same time** since B-066, because `recordFacilityRetirement` sends every retired facility there and the facility page's breadcrumb names it, while `paths.ts` carried a comment deferring the page to "B-071" (B-071 shipped reviews). Lists every active facility in the city cheapest-first with its starting price and review average, `ItemList` + `BreadcrumbList` JSON-LD built from the same array the page renders, 404 when the city has none (AC1's indexability rule, enforced rather than `noindex`-ed), and city entries in the sitemap from the same function the page reads — so it cannot advertise a URL the page refuses. **D-58**: the intro copy is generated from the facilities, not typed into a `City` record. **D-59**: no distance, because a city page has no origin to measure from; the search form is offered instead. **Part 3 ✅ (content hub)**: `/guides` plus four MDX guides — what fits in a 10x10, a moving checklist, packing tips, a climate-control explainer — with `Article` + `BreadcrumbList` markup and `FAQPage` on the two that carry real questions. Prose lives in `content/guides/*.mdx`; everything a machine reads is typed in `lib/guides/catalog.ts`, because frontmatter lets a guide ship without the fields `Article` needs and fail silently as absent markup. **D-60**: the size guide is the fifth of AC2's launch set and stays at `/storage/size-guide`, linked rather than re-published — copying it would manufacture the duplicate content part 6 exists to detect. AC3's CTA points at the search, not a facility (there is no location on a guide page), and the filter is carried through onto whichever facility the reader picks; `GuideFilter` is typed against `SIZE_BANDS`/`FEATURE_FILTERS` so a CTA cannot name a filter the facility page ignores. **Found on the way: the search form would have dropped the filter it had just promised to carry** — a GET form replaces the whole query string, the same trap B-122's promo box documents — so `FacilitySearchForm` and `UseMyLocation` both take a `carry`. **Also corrected: part 2's claim that this app has no middleware.** It has `apps/web/proxy.ts`, Next 16's name for it, which is why grepping for `middleware.ts` finds nothing. **Part 4 ✅ (funnel reporting v2)**: the funnel is now SPLIT by source/medium as well as filtered by it — and the `utmSource`/`utmMedium` filters it had accepted since B-069 finally have controls, having been unreachable the whole time. Every session is attributed to exactly one source/medium from its first event in the range (**D-61**), which is what makes the breakdown foot to the funnel above it rather than being a second set of numbers. Sequence attribution widened from B-073's single abandonment flag to a catalog now covering the lead drip too, deliberately non-exclusive with the page saying so. Promo ROI at `/admin/reports/promotions`, reading `PromoRedemption` rather than the event log, with discount **given** and **still to give** as separate columns and a months-to-earn-back figure. **Found and fixed: a filter carried in by URL silently reset to "Every source" while the report underneath stayed filtered** — the dropdown options came from the rows in range, so any value absent from them rendered as unfiltered; true of B-069's channel filter too. **Found, not fixed: B-127**, a deadlock when two friends redeem one referral invite at the same instant. **Part 5 ✅ (Search Console)**: indexation monitoring — of the pages our sitemap advertises, which has Google actually indexed. Google's site-verification token in the root layout (omitted entirely when unset, because an empty tag is a FAILED verification rather than an absent one), a service-account client written against the REST endpoints with `node:crypto` rather than pulling in `googleapis` for two HTTP calls, and `/admin/reports/indexation` reading the exact URL list `sitemap()` publishes. **Deliberately no simulator**, unlike the gate hardware: a simulated gate is a device we control, while a fabricated index verdict is a claim about what Google did with our pages on a screen an operator makes decisions from. Unconfigured shows no verdicts and names the three missing variables. Decisions are taken from Google's `verdict` enum and never from its free-text `coverageState`, which it rewords without warning; the sentence is kept verbatim for display. **Part 6 ✅ (duplicate-content warnings)**: `/admin/reports/duplicate-content`, comparing every prose field the site publishes against every other of the same kind — facility meta descriptions, opening lines and long descriptions, city page intros, guide descriptions. B-067's check covered ONE field at edit time on the page being edited; this covers the two collisions it structurally could not see — long-form descriptions, the biggest thin-content lever on a facility page, and GENERATED copy, which no editor ever opens. Compared within a kind and never across kinds, because a 155-character meta description scores low against a 600-word page for reasons of length rather than content. Authored collisions sort above generated ones whatever the score. **It immediately caught B-082 part 2's own output**: the three demo city intros score **0.82–0.85** against each other, over this codebase's own 0.8 threshold, so by our own measure the city pages are duplicate content. That is the check working, and it is now **B-128** for the owner to decide. The guidance for those pairs deliberately says there is no city description to edit rather than telling somebody to rewrite a field that does not exist. **B-082 is complete.** | PRD 04 §3.2 US-4, §7 Phase 2 | L | B-066, B-069, B-097 | Phase 2 |
| 83 | B-083 ✅ | **Certified-mail API integration for lien notices with tracked proof.** **Scope narrowed 2026-08-18 (owner, D-63): the online auction platform listing is split out as B-129**, because it is blocked on something older than this row — master PRD §11 open question 9 (live on-site auctions versus online) has never been answered, and a StorageTreasures-class listing needs a partner agreement, the same gate B-085 sits behind. Building a driver for it now would encode an answer to OQ-9 by building for it. **Built:** `mailNoticeCertified` posts the stored notice document by USPS certified mail through Lob's documented `POST /v1/letters`, hand-rolled with `fetch` (no SDK, the same call B-082 part 5 made for Search Console), and records the returned tracking number as proof of service. **No simulator, and this is the strongest case in the repo for that rule** — a simulated gate is a device we control (D-4) and a simulated index verdict was already refused in B-082 part 5, but a fabricated tracking number is a claim about what the POSTAL SERVICE did, sitting in the evidence chain that defends a lien sale under Texas Property Code ch. 59. Unconfigured is the shipped state: the button is not offered, the screen names `CERTIFIED_MAIL_API_KEY`, and staff post the notice and type the tracking number exactly as before. **The key is refused in BOTH directions and the second one is the point:** a `live_` key outside production would put real paper in a real mailbox from a laptop and post has no sandbox inbox (a collected letter cannot be recalled); a `test_` key IN production returns a valid-looking tracking number and mails nothing, writing proof of a service that never happened into a lien file — the fabricated evidence this item refuses to build a simulator for, arrived at through a configuration mistake. **Every refusal happens before the provider is called**, so the one thing this path can never do is put paper in the post and fail to record it; the single remaining window (provider accepts, database write fails) returns the tracking number IN the refusal so it is on screen for a person rather than lost, and the request carries the notice id as an idempotency key so pressing send again returns the original letter instead of posting a second legal notice. Delivery is recorded through `recordNoticeDelivery` rather than by writing the `Notice` row, so B-061's consent and proof gates still run on this path. `deliveredAt` is the mailing date, because service by certified mail is complete on mailing — a refused or unclaimed letter does not undo it. **Found and fixed on the way:** both admin screens held two-entry notice-type label maps and printed the raw enum value (`late_notice`) for four of the six `NoticeType` members; hoisted to one total `noticeTypeLabel` | PRD 02 US-30; D-63 | M | B-061, B-062 | Phase 2 |
| 83b | B-130 ✅ | **No demo lease can generate a lien notice, so B-061's generation path has never been exercised end to end** (found 2026-08-18 while building B-083). Measured with the product's own `previewNotice` across every demo lease: **0 of 14 can generate one.** Twelve refuse with `nothing_owed` and one with a credit balance — both correct — and the fourteenth is the **delinquent** lease the notices screen belongs to, which refuses with `ledger_does_not_reconcile`: "the invoices show more owed than the ledger does". That last one is a seed defect, not the product working. The lease seeded specifically to be delinquent is the one lease that ought to be able to state a claim, and it is the only one that cannot. **What it costs is coverage, not correctness in production:** B-061's refusal is doing exactly its job (US-27 requires the claim to reconcile at generation time, and generating from disagreeing sources would bake the discrepancy into a legal document), but it means notice GENERATION, notice service, and now B-083's certified-mail send have no e2e path at all — every one of them is asserted against disposable fixtures in `tests/notices-db.test.ts` and `tests/certified-mail-db.test.ts` and by nothing a browser touches. **Not fixed in B-083 deliberately:** making the delinquent lease reconcile changes what it owes, and the past-due portal banner, the delinquency queue, the dunning specs and the auction pipeline all assert against that lease — so it is a seed change with a blast radius, which is an item rather than a tail-end edit. Fix the seed so the delinquent lease's invoices and ledger agree, then add the e2e that generates a notice and serves it. **Fixed 2026-08-18. The cause was one missing field.** B-114 added the delinquent lease's INVOICE beside an already-existing ledger charge and never linked them, so the charge carried no `invoiceId` — and `reconcile` therefore counted the same $161 twice, once as an invoice outstanding and once as an uninvoiced charge, reporting the lease as short by exactly the amount owed. `generateInvoices` links every charge it raises; an unlinked one genuinely IS a charge no invoice accounts for, so the check was right and the seed was wrong. The invoice is now created first and the ledger entry carries its id. **A second refusal was behind it:** with the arithmetic fixed the lease refused with `no_template`, B-061's other deliberate rule, so the demo now seeds pre-lien AND lien templates for the primary facility — a demo of the lien arc that cannot produce its FIRST step demonstrates the end of a process without its beginning. Measured after: **1 of 14 demo leases can generate a lien notice**, which is the one that should. **The e2e now generates a notice, checks its 64-character hash is stored, and records certified-mail service with a tracking number** — the path B-061, B-083 and this row all depended on and none had. Repeatability comes from B-120's third discipline: `global-setup.ts` clears demo notices at the start of every run, which qualifies because nothing in the suite reads a pre-existing notice and the seed deletes them on re-run — necessary because generating is deliberately NOT idempotent, a notice being a served document the product must never rewrite. **Found while verifying it: running `npx playwright test` bare skips global setup entirely** (no dotenv, so `DATABASE_URL` is unset and the setup returns at its first line) — the first repeatability check was therefore meaningless and passed anyway; now in CLAUDE.md | PRD 02 §4.6 US-27; B-061, B-083 | S | B-061 | Phase 2 |
| 83ba | B-137 ✅ | **A transfer must carry the tenant's protective state, not just their rate** (operator review 2026-08-21; **verified against the code**). `completeTransfer`'s `tx.lease.create` copies rate, autopay, protection, billing day and `paidThroughDate` and nothing else, so the new lease starts with none of the tenant's holds. `syncActiveDutyHolds` is called from exactly two places — checkout provisioning and the admin tenant screen — and **never from transfer**, so an active-duty tenant who transfers comes out with `activeDutyMilitary = true` and no `military_scra` hold; the delinquency engine's `onHold` check then passes and the ladder runs on a servicemember. Same for `bankruptcy`, `deceased` and `litigation`. Separately, `OCCUPYING_LEASE_STATUSES` includes `pending_auction` and the portal transfer scopes on it, so **a tenant can self-serve out of the lien pipeline**. This row carries every open `LeaseHold` onto the new lease, re-syncs the active-duty declaration through the same path a move-in uses, and refuses the tenant-facing transfer for a lease in `pending_auction`. **It is the protective half of a split and ships first** — B-091's rule: no commit lands the permissive half before it. Eligibility policy is *not* settled here — **D-85** (may staff transfer a lien-pipeline lease at all, and under whose authority) is an owner decision, and this row builds only the tenant-facing refusal every option under D-85 agrees on **D-85 settled 2026-08-21:** the portal never offers a lien-pipeline transfer; staff may, with manager-and-above approval, a reason code and an unreset lien clock.| PRD 02 §4.4 US-14 (transfer), US-42; D-85 | M | B-090b, B-096, B-121 | Phase 2 |
| 83bb | B-138 ✅ | **Collections must survive a transfer** (operator review 2026-08-21; **verified**). The permissive half of B-137, second for the reason B-137 states. The balance and the invoices stay on the old lease, which the transfer sets to `ended`, so the engine halts it as `moved_out`; the new lease has no invoices and 0 days past due. **Collections therefore stop entirely on a live tenant who owes money and never left the property.** `daysPastDue` must still anchor to the oldest unpaid invoice's original due date (D-25) across the transfer boundary, and the ladder must resume at the step it was on rather than restarting at day zero. **Whether the arrears follow the tenant or stay on the closed lease is D-86** and this row is built to that answer: both shapes are real work — one edits invoice history, the other teaches every reader of `Lease.status` about a link — and choosing between them silently is exactly what the decision log exists to prevent **D-86 settled 2026-08-21:** unpaid invoices are re-pointed at the new lease, with an append-only record of the move.| PRD 02 §4.4 US-14 (transfer), §4.6 US-25; D-25; D-86 | M | B-137, B-057 | Phase 2 |
| 83bc | B-139 ✅ | **The accessibility statement is overstating, and its exception list is hand-written** (accessibility review 2026-08-21; **verified**). The page names exactly one coverage exception, the checkout confirmation screen. `/portal/refer` is a real static portal page linked from the portal layout, it appears in no axe scan — `PORTAL_ROUTES` omits it and a grep for `/refer` across `e2e/` returns nothing — and it is disclaimed by nothing, so that sentence is currently **false in the overstating direction**. Four further routes are unscanned: `/portal/pay/done`, `/checkout/resume/[token]`, `/confirm-email`, `/admin/tenants/[tenantId]/transfer`. Add the static ones to the scan set, state the rest as exceptions, and **generate the exception list from the route lists the run actually uses**, so the page goes stale on an edit rather than on a merge. Acceptance: no route reachable from a shipped nav appears in neither the scan set nor the exception list, asserted by a test. Near the top because CLAUDE.md makes this page a standing obligation and it has gone false in both directions inside twelve days | PRD 01 §6.8, §6.10; PRD 02 §5.5 FR-24/FR-25 | S | B-119, B-093 | MVP |
| 83bd | B-140 ✅ | **A transfer hold emails the tenant a move-in reminder for a move-in that is not happening** (operator review 2026-08-21; **verified**). `sendExpiringSoonReminders` selects `status: 'held'` with **no `source` filter**, so a transfer hold (D-82) receives `reservation_expiring_soon` — which tells an existing tenant to "use the link from your original confirmation email to complete your move-in online", a link D-82 deliberately ensured never exists. The tenant is also never told when the hold actually expires. Scope the sweep by `source`; give a transfer hold its own expiry notice naming the absolute facility-local date and time and pointing at the office rather than a link; make every reader of `Reservation` declare which sources it serves, asserted by a test. **This is a consequence D-82 bought and did not price**: reusing `Reservation` inherited its whole mailing list | PRD 05 §3.2 CN-23; D-82; PRD 01 §6.8.1 | S | B-090b, B-030 | Phase 3 |
| 83be | B-141 ✅ | **"Complete" on `/admin/tasks` silently does nothing when the note is empty** (operator **and** accessibility reviews, independently, 2026-08-21; **verified**). The note input has no `required` while all three sibling queues have it, every task type requires a note, and `completeTaskAction` discards the `{ ok: false, missingFields }` the service returns — so the button is pressed, the page re-renders identically, and the task is still open. The accessibility reviewer found the same screen from the other side: nothing is announced on submit, and every card's controls carry identical accessible names (4.1.3, 2.4.6). One item, because it is one screen with one root cause — a refusal that never reaches the caller. Fix at the action rather than at the input: mark the fields required, return `missingFields` to the screen as `aria-invalid` + `aria-describedby` with a focused summary, announce the outcome from a `role="status"` region present **before** the submit (FR-19/FR-20), and name each card's controls after its subject | PRD 02 §4.9 US-41, §5.5 FR-19/FR-20/FR-22 | M | B-095, B-115, B-094 | MVP |
| 83bf | B-142 ✅ | **The portal transfer screen swallows every failure, states no expiry, and has no date ceiling** (UX **and** accessibility reviews independently, 2026-08-21; **needs confirmation at build time**). Reported: the page keeps only the `ok` branch of the preview, so a failed request re-renders **byte-identical** — no price, no message (3.3.1). The sibling admin wizard does it correctly with `TRANSFER_PROBLEM_COPY` in a `role="alert"`, and that dictionary is already imported into the portal's own action file. Ranked above its size on two independent reviewers, an existing correct sibling and a tiny diff. **Four findings merged in, all the same screen or feature:** no `max` on the transfer-date input and no server-side ceiling, where the public reserve page gets both right; the pending screen never names the hold expiry while the staff banner claims the unit is held "until you complete or cancel it", which is untrue because it also lapses (`holdWindowSentence` exists for exactly this); `10×10` rendered with a raw `×` inside a radio's accessible name where the two-span pattern is shipped elsewhere (1.3.1); and a pending transfer or move-out being invisible on `/portal`, two taps deep behind a "Manage" disclosure, when "did that go through" is the one question the tenant came back to answer | PRD 01 §4.7 US-709, §6.8.1; PRD 02 §4.4 US-14 | S | B-090b, B-136 | Phase 3 |
| 83bg | B-143 ✅ | **An inbound text is truncated on the card and readable nowhere else** (operator review 2026-08-21; **verified**). B-135 puts the first 80 characters on the task card, and the comment in `task-subjects.ts` claims the full message is on the tenant's record. It is not: `sms.inbound_received` has an emit site and a catalog entry and **no read site anywhere in the codebase**. A tenant's third sentence — the one with the unit number in it — is in the database and reaches no human. Render the full body where the task links, from the domain event D-83 chose as its home, and **correct the false comment in the same item**: a comment asserting a screen that does not exist is how the gap stayed invisible | PRD 05 §8; D-83; PRD 02 §4.9 US-41 | S | B-135 | Phase 3 |
| 83bh | B-144 ✅ | **`Promotion.minStayMonths` configures nothing, because nothing can set it** (operator review 2026-08-21; **verified by repo-wide grep** — zero references outside `schema.prisma`). A direct hit on this codebase's own rule that a column configuring behaviour gets its control in the same item, and the cost is concrete: **"first month free with a six-month minimum" cannot be expressed at all**, so the operator either gives the month away unconditionally or does not run the promotion. This row is the control only — the field on the promotion form, its validation, and the minimum stated on the lease and on the checkout summary, because it is a term the renter is being held to. The clawback is the larger half and is its own row | PRD 02 §4.3 US-10 | S | B-070 | MVP |
| 83bi | B-145 ✅ | **Recapture when a promoted lease ends before its minimum stay** (operator review 2026-08-21; the larger half of B-144, split out rather than bundled). US-10's own parenthetical says "min stay implied by **recapture rules**" and there are none — a column recording a term nothing enforces is worse than no column, because it reads as enforced. Charge back the discount already given as a line item on the final statement, at the facility's configured recapture policy (full / prorated / none, **default none**, labelled as configuration per D-10), and surface the amount and the reason on the move-out preview before the tenant agrees to it. A recapture a tenant first sees on a final invoice is a chargeback | PRD 02 §4.3 US-10, §4.4 US-14 (move-out); PRD 01 US-707 | M | B-144, B-040 | MVP |
| 83bj | B-146 ✅ | **There is no returned-payment path at all** (operator review 2026-08-21; **verified**). `feeType: 'nsf'` and `'lien'` are configurable per facility and **read by nothing**; `LedgerEntry.reversalOfId` exists with a schema comment citing PRD 02 FR-8 and is **written by no code**. A bounced check or an ACH return therefore leaves the money recorded as collected, forever. `refundPayment` is the wrong tool and must not be reached for: for cash and check it pulls from the open drawer, so recording a bounce that way makes the till short by money that never left it. This row builds the reversal primitive — an append-only entry through `reversalOfId`, allocations unwound so the invoices re-open and `daysPastDue` sees the arrears again (D-25), the configured `nsf` fee assessed and waivable like any other fee (US-21), the whole thing audit-logged — and raises the **existing** `failed_payment` task rather than a new queue, per US-41's rule | PRD 02 §4.5 US-46, US-21, US-23; §5.3 FR-8; D-22 | M | B-048, B-049, B-047 | MVP |
| 83bk | B-147 ✅ | **Card disputes reach nothing** (operator review 2026-08-21; **verified**). The reconciler's `HANDLED_EVENTS` covers five Stripe events and no `charge.dispute.*`, so a chargeback is something the operator learns about from a bank statement. Rides B-146's reversal primitive rather than inventing a second one: `charge.dispute.created` posts the reversal and raises the task, `charge.dispute.closed` posts the win or leaves the loss standing, both under the same idempotency contract as the five events already handled. Second because a dispute handler with nowhere to write its reversal is the same defect one layer up | PRD 02 §4.5 US-46; PRD 01 §7.3; D-6 | M | B-146, B-019 | MVP |
| 83bl | B-148 ✅ | **Waitlist and lead forms announce success to nobody** (accessibility review 2026-08-21; **needs confirmation at build time**). Reported: both forms **insert** their `role="status"` on submit instead of mutating one mounted at page load, so nothing is announced (4.1.3), and focus falls to `<body>` when the submit button unmounts (2.4.3). This is the exact failure `AdminForm` documents and B-111 fixed product-wide; these two marketing surfaces are where it never reached. One diff for both | PRD 01 §6.8.1 (waitlist & lead capture), §6.8; PRD 04 US-8 | S | B-090a, B-068 | MVP |
| 83bm | B-149 ✅ | **Checkout's unit-lost branch is still a dead end** (UX review 2026-08-21; **needs confirmation at build time**). Reported: the branch says "call us" with **no phone number**, no alternatives and no waitlist, while B-090a shipped `WaitlistForm` only on the lower-intent facility page. This is the missing half of B-090a — the highest-intent moment in the entire funnel is the one place the capture is absent. Ship the facility's own phone number, the waitlist form for the size that was lost, and the other available sizes at that facility. **Deliberately out of scope: a recommender.** Ranking alternative sizes or nearby facilities is a feature nobody asked for; the list the facility page already renders is enough | PRD 01 FR-4.1, §6.7, §9 Phase 3; D-79 | M | B-090a, B-020 | MVP |
| 83bn | B-150 ✅ | **AR aging sits under a month picker and always answers "as of today"** (operator review 2026-08-21; **needs confirmation at build time**). Reported: the identical defect B-131/D-68 fixed for unit occupancy, one section below the fix on the same page — so a month-end AR figure and the aging table beneath it disagree with nothing on screen explaining why, and the fix for one did not generalise to the other. Either answer for the date asked for, or name the instant the section does answer for, in facility-local time. **Merged in because it is the same file and the same size:** the occupancy table's facility cell is a `<td>` where FR-22 requires `<th scope="row">` (1.3.1) | PRD 02 §4.11 US-39.4, §5.5 FR-22; D-68 | S | B-055, B-131 | MVP |
| 83bo | B-151 ✅ | **An overlock outlives the lease it was applied to** (operator review 2026-08-21; **needs confirmation at build time**). Reported: after a transfer or a move-out with a balance the engine halts the lease as `moved_out` rather than `cured`, so the release task never fires. The unit then reads `overlocked` indefinitely, the reconciliation screen shows **no mismatch** — system and physical state agree, and both are wrong — and the occupancy metric counts it as occupied, so the site's rentable inventory quietly shrinks by one unit per event with nothing reporting it. Release on lease end regardless of cure state, raise the physical-removal task, and assert the unit returns to the rentable denominator | PRD 02 §4.9 US-36, §4.6 US-25, §4.11 US-39.1; D-25 | M | B-058, B-040 | Phase 2 |
| 83bp | B-152 ✅ | **A rate-increase notice is recorded as sent with no delivery check** (operator review 2026-08-21; **needs confirmation at build time**). Reported: the status flips to `notice_sent` **before** the event fires, and nothing afterwards reconciles it against `Message.status`, the suppression list or a hard bounce — the increase then applies 30 days later regardless. US-11 blocks an effective date that violates the minimum notice period, which is a guarantee about delivery that the code makes about intent. Set the status from the send result, reconcile against the message record before the increase applies, and block on a bounce or suppression hit with a high-priority task. **What then happens to the increase is D-88**, not this row's call **D-88 settled 2026-08-21:** a hard bounce, suppression hit or missing send record blocks the increase and raises a task.| PRD 02 §4.3 US-11; PRD 05 §3.2 CN-9; D-37; D-88 | M | B-076, B-054 | Phase 2 |
| 83bq | B-153 ✅ | **There is no way to lower an existing tenant's rate** (operator review 2026-08-21; **a genuine feature gap, not a defect**). B-076 built the increase workflow and D-37 gave it a model; the retention save that ECRI itself creates demand for has no path, so a manager keeping a good tenant edits a lease rate directly or does nothing. It is the same workflow in the other direction: a `TenantRateIncrease` row with a negative delta, approval through the **existing** monetary-authority limits (manager-and-above by default, reusing `checkMonetaryAuthority` rather than minting a new threshold), a `LeaseRateChange` written through `applyRateChange()` with reason `retention`, and no notice period, since nothing statutory governs charging a tenant less. **Deliberately given no D-number:** the approval level is a default the RBAC already models and is reversible in a seed | PRD 02 §4.3 US-11, §3 Roles & Permissions; D-37 | M | B-076 | Phase 2 |
| 83br | B-154 ✅ | **The waitlist cannot be taken at the counter, and the demand report withholds the phone numbers it stores** (operator review 2026-08-21; **needs confirmation at build time**). Reported: the notify-me form exists only on the public facility page, so the identical ninety-second call about a size we do **not** have captures nothing — US-43 exists for precisely the case where we do. And `/admin/reports/waitlist` renders demand without the contact details the `WaitlistEntry` row already holds, which makes it a list of people who wanted to give us money. Also reported and **deliberately left to D-87**: the sweep emails without holding the unit, so the person we notified races the public site for it; whichever way that goes, the notification copy must match **D-87 settled 2026-08-21:** no unit hold — the notification stays a race and the copy says so.| PRD 02 §4.8 US-43, §4.11; PRD 01 §9 Phase 3; D-79; D-87 | S | B-090a, B-097 | Phase 3 |
| 83bs | B-155 ✅ | **Protection attach rate is not measured anywhere** (operator review 2026-08-21; **verified as an unbuilt AC** — US-44 already requires it and no row owns it). Dollars are reported and the ratio is not, so there is no per-facility, per-month or per-staff figure and nothing to coach against, though the data exists. Build it in the metrics module like every other definition (D-25 — no screen computes it inline): plan-enrolled leases ÷ new move-ins as a percentage, per facility, per month, per staff member who completed the move-in, plus a split by channel (web / counter / phone per US-43), which is the reviewer's addition to US-44's own wording | PRD 02 §4.3 US-44, §4.11 US-39; D-25 | S | B-042, B-022 | MVP |
| 83bt | B-156 ✅ | **The scan contract II: post-interaction states, parameterised routes, and a control that does nothing** (accessibility review 2026-08-21; **needs confirmation at build time**). B-119 made the scans cover the routes they claimed to; the reviewers found three structural gaps that survive it, and all three are why `/admin/tasks` could ship a dead button green. (1) **Post-interaction states are scanned almost nowhere** — a scan of a page at rest never sees an invalid submit, an opened disclosure or a step change, and B-122's promo box lives in a collapsed `<details>` the checkout scan never opens, so its refusal state and its contrast have **never** been checked. (2) `ADMIN_ROUTES`/`PORTAL_ROUTES` are **static-only by construction**, so every `[param]` route is covered only where somebody remembered a click-through — the admin transfer wizard is in no scan at all. (3) **Nothing in CI can fail when a control does nothing silently**; the reviewers' cheapest catch is asserting that after a submit, a live region **already attached before** the submit has non-empty text. Last in the block for B-119's own reason: it asserts what B-141 and B-142 fix, so running it earlier only produces a suite red for known reasons | PRD 02 §5.5 FR-25, FR-24; PRD 01 §6.8 | M | B-119, B-120, B-141, B-142 | MVP |
| 83bv | B-158 ✅ | **A gate command can be skipped by clock skew between the app and the database** (diagnosed 2026-08-22 while building B-146, from the reason field B-137's test was given for exactly this purpose). `GateCommand.nextAttemptAt` defaults to Postgres `now()`; `drainGateCommands` selects `nextAttemptAt: { lte: now }` where `now` is a **Node-side `new Date()`**. The two clocks are independent — the database is remote — so a command inserted microseconds before a drain can carry a timestamp LATER than the drain's cutoff and be passed over. **This is not test-only.** `gate-simulator-db.test.ts`'s offline case is where it surfaces (intermittently, only under a full parallel sweep, passing in isolation — recorded as an unexplained failure on 2026-08-19 and diagnosed on 2026-08-22 when the assertion's `reason` came back **`unknown_code`**, which rules out the `outside_hours` wall-clock theory the comment had hedged on: the code never reached the simulated vendor at all). In production the same skip happens and self-heals on the next drain, which for a move-in gate code means **a tenant standing at a keypad that does not work** for however long until the next cron tick. The cheap fix is to stop comparing two clocks: drain on `nextAttemptAt <= NOW()` evaluated **in the database**, or default the column from the application clock that will later read it. Wants one test that a command inserted and drained in the same breath is always picked up | PRD 03 FR-3; PRD 03 US-6 | S | B-080 | Phase 2 |
| 83bu | B-157 ✅ | **The staff side of D-85: a lien-pipeline transfer needs approval, a reason code and an unreset clock** (raised 2026-08-21 while building B-137, out of D-85's own settlement rather than a review). B-137 built the half every option under D-85 agreed on — the portal refuses. The half the owner actually chose is unbuilt: staff may transfer a `pending_auction` lease **with manager-and-above approval, a reason code, and a lien clock that does not reset**, and today the wizard asks for none of the three. `leases:transfer` is manager-and-above by role, which satisfies the authority level by accident and records no reason, and nothing asserts the clock survives — the transfer sets the old lease to `ended`, which is exactly the boundary B-138 is being built to teach the engine about. **The evidentiary cost Option B was protecting against is carried entirely by these three controls**, so until they exist the decision is only half-honoured: a served notice names a unit, and moving the goods after service with no reason code recorded is the fact the tenant's attorney asks about. Wants the reason code on the same `TransferReasonCode`-shaped enum the rest of this repo uses for monetary overrides, and one test that the lien clock reads the same value either side of the move | PRD 02 §4.4 US-14 (transfer eligibility); D-85; D-10 | M | B-137, B-138 | Phase 2 |
| 83bva | B-159 ✅ | **The public accessibility statement is overstating again, in two sentences** (accessibility review 2026-08-24; **verified against the code and against `docs/PROGRESS.md`**). **First in the block, ahead of every defect in it**, for the reason CLAUDE.md makes this page a standing obligation: it is a live public claim about this codebase rather than a defect a visitor has to reach, and it has now gone false in the overstating direction twice inside a fortnight. Four claims, two of them plainly untrue. (1) **"they block a release if they fail" is false** — every axe scan is in the `e2e` lane of `.github/workflows/ci.yml:105-110`, gated on `main` or a non-draft PR, while `vercel.json` carries no `ignoreCommand` and there is no deploy workflow, so Vercel's Git integration builds and ships a push to `main` in parallel with Actions and independently of its result; compounding it, CLAUDE.md's own always-open-as-a-draft rule means the lane does not run at all on the PR where the code is written. (2) **"We test by keyboard by hand" is unrecorded** — PRD 02 §5.5 FR-24 defines "recorded" as a line in `docs/PROGRESS.md` and there is none for any item; the screen-reader sentence beside it IS correctly disclaimed, the keyboard one is not. (3) **"These are the pages outside that run" cannot be a complete account of gaps** — B-139's generated list filters a route-keyed array, so no post-interaction STATE can ever appear in it. (4) **"They also fail on checks the tool could not decide" holds only for the public route loop** (`e2e/a11y.spec.ts:59-71`); `smoke.spec.ts:1278`, `portal.spec.ts`, `admin.spec.ts`, `admin-transfer.spec.ts`, `admin-tenants.spec.ts` destructure `violations` only, so every checkout step — the money path — is scanned without the `incomplete` assertion the page claims. **This row corrects the claims and does not build the gate.** Claims (2), (3) and (4) are prose: fold keyboard into the disclaimed sentence unless a pass is recorded in `PROGRESS.md` with its date, scope the `incomplete` sentence to the public routes, and add one sentence after the generated list saying it names pages and that post-interaction states are not all covered — the enumerated state list is B-184 and this row must not wait for it. Claim (1) has two honest endings and choosing between them is a release-velocity and Actions-cost trade-off: **OWNER DECISION, and it must produce a new D-number before this row can be finished** — rewrite the sentence to "run on every push to our main branch and on every open pull request" (free, true today, claims less), or build a Vercel `ignoreCommand`/deploy gate so a failing scan genuinely blocks a ship (~30 min of Actions on every merge, and a red scan stops a deploy) and keep the stronger sentence. **`LAST_REVIEWED` does not move** — these are claim corrections and do not license the date; the date moves when the "where we fall short" list is re-verified | PRD 01 §6.10, §9 (statement claims only what is true today); PRD 02 §5.5 FR-24 | S | B-139, B-119 | MVP |
| 83bvb | B-160 ✅ | **A lien transfer leaves the auction file pointing at the wrong unit and posts the sale proceeds to the wrong lease** (operator review 2026-08-24; **verified**). The largest single item in the block and deliberately merged into nothing: D-85 lets staff move a `pending_auction` tenant's goods, B-157 built the approval and the unreset clock, and every downstream reader of the case still names the unit the notice was served on. `apps/web/lib/auctions/service.ts:250` pins `row.unit.number`, so the lock-cut instruction, the inventory and the advertisement all name unit 214 while the goods sit in 108 — **staff cut the lock on a re-rented unit**. `service.ts:~659` posts proceeds to `view.leaseId`, which B-138 zeroed when it re-pointed the invoices, while summing `outstandingCents` across the whole chain — so after a completed sale the live lease still shows full arrears and the ladder keeps running on a tenant whose goods have already been sold. `apps/web/lib/admin/transfer.ts:~684` returns the old unit to `available` with no auction guard. Builds: the case view renders both facts ("notice named 214, goods now in 108") from `lienPipelineNoticeUnitNumber`; `recordLockCut`, the inventory and the advertisement name the **current** unit; sale postings go to the tail lease resolved through `leaseSuccessorIds`; `recomputeUnitStatus` refuses `available` while a live case names the unit. **POLICY, and this row cannot be finished until it produces a new D-number:** does a lien transfer require **re-serving** the §59 notice for the new unit, or does the audit record of the move suffice? It changes whether the readiness blockers reset and whether the sale can proceed on the original clock — a Texas Property Code Ch. 59 question with real legal exposure (D-10: Texas default, per-state configurable, draft-only and not legal advice). Do not answer it in code or in PRD prose | PRD 02 §4.6 US-27/US-30, §4.4 US-14 (transfer); D-85; D-86; D-10 | M | B-157, B-138, B-062 | Phase 2 |
| 83bvc | B-161 ✅ | **A bounced check or a chargeback replays the entire delinquency ladder in one night** (operator review 2026-08-24; **verified**; both B-146 and B-147 named it unclosed). `packages/core/delinquency/engine.ts:55-57` returns every unexecuted step at or below `daysPastDue` and `apps/web/lib/delinquency/engine.ts:~190` executes all of them in one run; `engine.ts:448-456` `cure()` supersedes every step instance, so a re-opened arrear starts with an empty history **at full age**. `apps/web/lib/billing/reversals.ts` restores the original due date — correct per D-25, and exactly what makes the replay maximal. The result of one returned ACH on a 90-day-old invoice: four dunning letters in the same post, late fees re-assessed on charges a superseded run already billed, access cut, and `engine.ts:353` `openAuctionCase` fired — the lien pipeline opened by a bank reversal, overnight, with no human in it. Builds: **at most one step per lease per run**; the ladder gated while an open `settling_payment_failed` task exists; resumption from the reached stage rather than from an empty history; and no re-assessment of a late fee a superseded run already charged. **POLICY — ANSWERED 2026-08-24 as D-92: ten days of grace, then RESUME at the stage already reached**, both per-facility configurable on the timeline (`reversalGraceDays` 0–90 default 10, `reversalResumes` default true) with the controls on the delinquency settings screen. Resume closes the pay-then-reverse loop that otherwise defers an auction indefinitely, and is defensible because every earlier notice really was served and is still in `DelinquencyStepRun`. The one-step-per-run cap turned out to be the larger half and is NOT configurable. The fourth bullet needed no code: `assessLateFees` reads which steps it has charged from the fee invoices, which a reversal does not void — asserted rather than assumed | PRD 02 §4.5 US-46, §4.6 US-25; §5.3 FR-8; D-25 | M | B-147, B-146, B-052 | Phase 2 |
| 83bvd | B-162 ✅ | **A transfer re-prices to street, drops any in-flight rate increase, and resets the ECRI clock to zero** (operator review 2026-08-24; **verified**). Three defects with one cause — the new lease is built as if the tenant were new. `apps/web/lib/admin/transfer.ts:242-248` `currentRateFor` returns `streetRateCents` and `:312-315` uses it as `newRateCents` with no override, so a like-for-like move between two 10×10s can raise the rent with no notice period and no rate-increase record. `apps/web/lib/pricing/tenant-rate-increases.ts:825-829` records a due increase against an ended lease as `ok: true, "skipped — the lease has ended"` and `transfer.ts` never mentions `tenantRateIncrease` at all, so an approved, noticed increase evaporates **silently and reports success**. `tenant-rate-increases.ts:~407` reads batch eligibility from `rateChanges[0].effectiveFrom ?? lease.startDate` on the NEW lease, and the transfer writes a `reason: 'transfer'` change dated today — so months-since-last-increase resets to zero and the tenant is exempt from ECRI for another nine months. Builds: a settable rate on the transfer preview defaulting to the facility's policy; a live `TenantRateIncrease` either re-points to the new lease or the transfer **refuses with a named problem** — the silent skip must go; ECRI tenure read through `transferredFromLeaseId`. **POLICY — ANSWERED 2026-08-24 as D-93.** The rate **keeps the tenant's discount off street**, scaled to the new unit and capped at street (`Facility.transferRatePolicy`, default `preserve_discount`, with `street` and `in_place` configurable and a settable figure on the preview) — the only rule that is right on a lateral move, a downsize AND an upsize. The promotion **does follow the tenant**, benefit and obligation together, which closes the hole D-89 named. The in-flight increase is **cancelled and named on the preview**, not re-pointed: the approver signed off on a delta from a figure the transfer replaces. Two consequences the row did not anticipate had to be built with it — the promo schedule's period index is now counted across the transfer chain, and the minimum stay is measured from the tenancy's origin, or the carried promotion lands months late and bills a recapture on somebody who already served it | PRD 02 §4.3 US-11, §4.4 US-14 (transfer); D-37; D-89; D-5 | M | B-157, B-153, B-076 | Phase 2 |
| 83bve | B-163 ✅ | **Proof of insurance stops being monitored the moment a tenant transfers units** (operator review 2026-08-24; **verified**; the leak underneath the number B-155 built). `apps/web/lib/admin/transfer.ts:470-497` copies `protectionPlanName` and `protectionCents` and handles **no `protectionWaiver`**, while `apps/web/lib/billing/scans.ts:162-175` `scanExpiringProtectionProofs` reads waivers by `leaseId` and then filters to `OCCUPYING_LEASE_STATUSES` — so a waiver naming the ended lease is dropped permanently, not merely missed once. The tenant's certificate expires, nothing notices, `insurance_proof_lapsed` never fires, and US-44's required-protection policy is unenforced for exactly the tenants who moved. The same erasure hits the `deceased` and `litigation` waivers the scan reads. Built as: `completeTransfer` re-points the waiver in the same transaction carrying `expiresAt` — **including an already-expired one**, or a unit swap becomes a way to shed a lapse that has already happened; the scan reads forward via `leaseSuccessorIds` and **re-points what it finds**, which is the backstop and the repair in one (B-151's reasoning — a nightly job that fixes what it finds cannot be forgotten the way a one-off script can), so no separate repair shipped; and `/admin/reports/protection` lists occupying leases with neither a plan nor an unexpired waiver, longest-uncovered first, distinguishing a lapsed certificate from one that was never recorded. **The `deceased` and `litigation` half of this row was already false when it was written**: those are `LeaseHold` types, B-137 already carries every open hold through a transfer, and `litigation` and `bankruptcy` were already asserted in `transfer-db.test.ts` — no scan reads holds by `leaseId`, so nothing was stranded. Checked rather than assumed. **Leaves behind:** no coverage verification against a carrier — a waiver is still a typed record, as at B-022 | PRD 02 §4.3 US-44, §4.4 US-14 (transfer); PRD 01 US-501 step 3 | M | B-155, B-137, B-022 | MVP |
| 83bvf | B-164 ✅ | **A tenant whose goods are in the lien pipeline can still schedule a move-out from the portal** (operator review 2026-08-24; **verified**). B-137 closed this on the transfer screen and left the identical hole one screen over: `apps/web/lib/portal/move-out.ts:59` `tenantMoveOutLeases` filters on `OCCUPYING_LEASE_STATUSES`, which includes `pending_auction`, and `:~110` `previewTenantMoveOut` uses the same wide list — where `PORTAL_TRANSFERABLE_STATUSES` in `apps/web/lib/portal/transfer.ts` was narrowed for exactly this reason. Same defect class, same fix, different file. Builds: the lease is **listed but not actionable**, using B-137's refusal-panel pattern — name the lien process in customer language (D-15), give the facility's own phone number via `phoneFor`, and never return `not_found`, because a tenant told their lease does not exist is a tenant who calls angry. Staff-side move-out stays allowed and takes B-157's reason code. **Accessibility:** the refusal panel is a `role="alert"` mounted with the page rather than inserted on submit (4.1.3 AA), and its state is not conveyed by colour alone (1.4.1 A); the disabled path states why in text rather than presenting a control that silently refuses (3.3.1 A). One test. **Built with the rule moved rather than copied:** `PORTAL_SELF_SERVICE_STATUSES` now lives in `packages/core/inventory` beside the `OCCUPYING_LEASE_STATUSES` it is carved out of, so the third self-service screen does not have to rediscover it and a reviewer can see which of the two lists a query used; `PORTAL_TRANSFERABLE_STATUSES` stays as the transfer screen's own vocabulary, defined from it. The phone number comes from `phoneFor`/`CallLink` rather than being interpolated into the copy, so a facility with no number of its own falls back to the org line instead of rendering "ring them" with nothing to ring — the transfer screen B-137 built still has that gap. Cancelling a request made before the pipeline opened stays allowed: trapping a tenant with a move-out they cannot withdraw is worse than the hole this row closes, and cancelling moves nobody's goods | PRD 01 §4.7 US-707, §6.8.1; PRD 02 §4.4 US-14 (move-out); D-85; D-15 | S | B-137, B-041, B-157 | Phase 2 |
| 83bvg | B-165 ✅ | **Batch ECRI raises every eligible tenant straight to full street rate, with no cap and no configurable rule** (operator review 2026-08-24; **verified**). `packages/core/pricing/rate-increase.ts:123-125` `targetRateFor` returns `candidate.streetRateCents` — that is the entire function — and `:89-92` `DEFAULT_ELIGIBILITY` (9 months, $15 gap) is a hardcoded module constant that `apps/web/app/admin/rate-increases/actions.ts:59-74` never overrides because no form field exists to override it with. A tenant at $89 against a $145 street rate receives one letter raising them 63% in a single step, which is how an operator loses a whole cohort in a month. This is this codebase's own rule — a column that configures behaviour gets its control in the same item — failing in the harder direction: **there is no column.** Builds a per-facility ECRI policy (percent step, minimum and maximum dollar step, cap-at-street, minimum months since the last change, minimum gap to street), `targetRateFor` computing `min(street, inPlace × (1 + pct))` bounded by the min/max and rounded to whole dollars, the controls on `/admin/settings` in the same item, and a batch preview that states **the rule and the percentage**, not only the dollar delta. **POLICY, and this row cannot be finished until it produces a new D-number:** the default step and cap for a newly created facility. A conservative default protects tenure and leaves money on the table; an aggressive one is the current behaviour with a nicer form. Per-facility configurable regardless (D-10) — the seeded default is the owner's number and must not be guessed | PRD 02 §4.3 US-11; D-37; D-5; D-10 | M | B-153, B-076, B-008 | Phase 2 |
| 83bvh | B-166 ✅ | **A rate increase held for an undelivered notice has no way back — the only control is Cancel** (operator review 2026-08-24; **verified**; B-152's own left-behind, which nothing picked up). D-88 settled that a bounce, a suppression hit or a missing send record blocks the increase and raises a task; `apps/web/app/admin/rate-increases/page.tsx:155-174` then renders help text and the shared cancel form on a `notice_failed` row and nothing else, and the copy says "Cancel it and schedule it again" — which means re-typing a Lease ID into B-153's free-text form (fixed by B-177) and re-deriving the delta by hand, per row, with no batch action for a bounced-domain event that held forty of them. Builds: a **Re-notice** action cloning the increase at the same delta with a recomputed `noticeDate` and `effectiveDate`, cancelling the old row and linking the two so the history reads as one attempt; a refusal to re-notice while the contact record still carries the address that failed, naming that address; a batch "re-notice all held" scoped to a facility; and `rate_increase_notice_undelivered` completing **only** on an actual re-notice or cancel, never on a free-text note — a proof field satisfied by typing is not proof. **Accessibility:** the new control is named after its subject rather than repeating "Re-notice" down the list (2.4.6 AA), and the outcome is announced from a region mounted before the submit (4.1.3 AA). **Built as specified, with three things the row did not price.** The clone carries the **original approval** rather than queueing for a second one — same lease, same current rate, same new rate, so the regional's sign-off still describes what happens; the mirror of B-162's reasoning, which is also why a re-notice is refused if the lease rate has moved since. "The clock restarts" turned out to be a statement about the **period**, not the date: `noticeDate = effectiveDate − noticeDays ≥ today` by construction, so an increase with runway left keeps the date the tenant would have been told about and only one whose runway the hold consumed slides forward. And the fourth bullet needed a mechanism rather than a special case — a new `resolvedByAction` on the task catalog, so `completeTask` refuses the type outright (the action is reachable with any task id, not only through the card that no longer renders a note box) and the two things that move the record complete it themselves. **Leaves behind:** no attempt counter on the chain; the refusal checks that the address CHANGED, not that the new one is healthy, so a correction to a different suppressed address holds again one night later; no e2e spec, the same gap B-152 left | PRD 02 §4.3 US-11; PRD 05 §3.2 CN-9; D-88; D-37 | M | B-152, B-076 | Phase 2 |
| 83bvi | B-167 ✅ | **Six configurable fee types can be configured and cannot be charged, and no ad-hoc charge exists anywhere** (operator review 2026-08-24; **verified**). `packages/db/prisma/schema.prisma:411-422` defines `lock_cut`, `cleaning`, `damage`, `lien`, `certified_mail` and `auction_cost`, and `apps/web/app/admin/settings/page.tsx:50-65` gives every one of them an editable schedule — while repo-wide only `admin`, `late`, `transfer` and `nsf` are ever charged. `apps/web/app/admin/tenants/[tenantId]/actions.ts` exports thirteen actions and **not one of them posts a charge**. So a unit left full of rubbish, a lock cut at the operator's cost and a damaged door are all settings screens with no consequence, and the certified-mail and lien costs the operator is legally allowed to recover never appear in a cure quote — the tenant pays less than the statute permits and the operator eats the postage. Builds one charge control, reached from the move-out screen and the tenant profile: fee type, pre-filled overridable amount from the facility schedule, required note, posted through the **existing** `raiseFeeInvoice` shape so autopay collects it and `waiveFee` can reach it, gated by `checkMonetaryAuthority` on the override amount, audited, and visible to the tenant on their own documents screen. `lien` and `certified_mail` costs post as incurred from the notice pipeline so a cure quote is arithmetically complete. **No new fee types and no free-text amounts outside the authority ladder** — a charge nobody can waive is the defect one layer down. **Built as specified, and it settled three things the row did not.** Posting a fee needed **its own permission** — `fees:charge`, counter-staff and up — because neither `tenants:edit` nor `payments:take` describes putting money onto an account. The authority gate is measured against the **departure from the schedule in EITHER direction** against the existing fee-waiver limit: under-charging is a waiver by another name, over-charging is the direction that hurts the tenant, and one symmetric rule needs no new column — a counter staffer's $0 limit means "the facility's price, exactly", and an unpriced fee type is a departure from zero so all of it needs waiver authority. And it uncovered a **real, latent bug that had to be fixed first**: `(Invoice.leaseId, periodStart)` was unique across every invoice KIND, so a lease could carry only one fee invoice per calendar day — a move-out posting cleaning and damage on the same afternoon lost the second silently, and an NSF fee on a day that already had a late fee threw. Now a partial unique scoped to `kind: 'rent'`; late fees keep `chargedSteps`, which was always the real guard. Three copies of the fee-invoice shape became one primitive. **Leaves behind:** a fee on an ENDED lease is not autopay-collected (correctly — the settlement collects it); no approval queue for an over-limit override, same posture as `waiveFeeInvoice`; `auction_cost` still has no automatic poster; the recapture B-168 owns is still a raw ledger entry | PRD 02 §4.5 US-21/US-23, §4.4 US-14 (move-out), §4.1 US-3; D-10 | M | B-146, B-040, B-004 | MVP |
| 83bvj | B-168 ✅ | **A promotional recapture cannot be waived, argued down, or partially forgiven at the counter** (operator review 2026-08-24; **verified**; the collections half of B-145). `apps/web/lib/admin/move-out.ts:249-255` posts the recapture as a raw `ledgerEntry` of `type: 'charge'` rather than as a fee invoice, so `waivableFees` and `waiveFee` cannot see it, it does not age, it does not dun, and autopay does not collect it. The only lever `CompleteMoveOutInput` (`move-out.ts:167-177`) offers is `writeOff?: boolean` — all or nothing across the **entire** residual, so forgiving a disputed $60 recapture also forgives $400 of genuine arrears, in one unaudited boolean. Builds: post the recapture as a `kind: 'fee'` invoice on the NSF shape B-146 established, so every existing tool reaches it; a reduce-or-waive override carrying a reason code and gated by `checkMonetaryAuthority` **on the amount forgiven**, not on the amount charged; the recapture rendered as its own settleable line on the final statement beside arrears rather than folded into a residual; and recapture-collected versus recapture-waived reportable per facility, which is the number that says whether the minimum stay is doing anything. Keeps D-89 intact — a transfer still recovers nothing. **Built as specified.** The reduction flows through the SETTLEMENT rather than being subtracted after it, so `amountDueCents`, the write-off threshold and the manager-override decision all read one number — subtracting afterwards would leave the screen demanding a manager for a debt that no longer exists. The override is **clamped to the ruled figure**: an operator may forgive a recapture, never invent one, and over-charging needs no reason because nothing was forgiven. `applyRecaptureOverride` rewrites the reason sentence to say what was forgiven and that sentence stays on the LEDGER row, keeping B-145's rule that the tenant's statement must not use different words from the screen that got their consent. Reportability is three columns, not one — charged and waived on `PromoRedemption`, collected read through to the invoice's own `amountPaidCents`, all null while the lease is live so "not decided yet" never reads as "recovered nothing" — and its own audit action `promo.recapture_reduced`, because filed under `fee.waived` the question "is the minimum stay real" cannot be asked of it. **Leaves behind:** a write-off still leaves the recapture invoice `open`, the same pre-existing shape rent invoices have had since B-047; the move-out screen does not re-preview as you type (server component), so the total above the fieldset does not move until submit; the report's range still filters on when the promotion was REDEEMED, not when the recapture happened | PRD 02 §4.3 US-10, §4.4 US-14 (move-out), §4.5 US-21; D-89 | M | B-145, B-146, B-167 | MVP |
| 83bvk | B-169 ✅ | **Nothing finds the overlocks already stuck, and the removal task tells staff the tenant has paid when they have not** (operator review 2026-08-24; **verified**; B-151's own named tail, plus a defect B-151 introduced). `packages/core/tasks/catalog.ts:254-257` still labels the task "Take the overlock off — the tenant has paid", unchanged by B-151, which now also raises `overlock_remove` after an auction sale and after an abandonment — so the card asserts a payment that did not happen, on the two paths where the tenant most certainly did not pay. And `apps/web/lib/delinquency/engine.ts:64-76` returns early with `skippedNoTimeline` **before** reaching B-151's backstop at ~170, so a facility with no delinquency timeline configured never releases an overlock at all: the units B-151 was built to free are exactly the ones it cannot reach. Builds: a label stating the fact with the reason carried in the card body (cured / lease ended / auction / abandonment); a "locks on units with no live lease" list so the stuck ones are findable rather than inferred; a one-off repair that runs **regardless of timeline configuration**; and the unit board showing an overlocked unit with no live lease as blocked inventory with its removal task linked, since today the reconciliation screen agrees with itself and both halves are wrong. **Built as specified, with the repair shaped differently.** The row asked for a one-off repair; it shipped as an unconditional nightly job step (`delinquency.stuck-overlocks`) instead, because B-151's own reasoning is that a sweep which fixes what it finds cannot be forgotten the way a script can — its first run IS the repair. The backstop was MOVED out of `runDelinquencyTimeline` rather than duplicated, so one rule has one home. The reason needed somewhere to live: a new generic `Task.detail` carries it per card (the type cannot know which of five paths raised it, the caller always does), and `UnitOverlock.removalReason` records it on the lock itself. `stuck_no_lease` is flagged **immediately** rather than after 24 hours — every other mismatch on that screen is somebody not having got there yet, this one is inventory nobody is chasing. The units board needed no new query: `overlocked` with no occupant is the state. **Found and fixed:** `tests/overlock-db.test.ts` shares one lease and never reset its status between tests, so the first tests to end a lease made a sibling pass or fail on run order. **Leaves behind:** `deriveUnitStatus` still returns `overlocked` for a lease-less unit — changing it is an occupancy-math change well beyond this row, so the screens name the state instead; `Task.detail` has one writer, and several other task types have creators who know more than their label does | PRD 02 §4.9 US-36, §4.6 US-25, §4.11 US-39.1; D-25 | S | B-151, B-058 | Phase 2 |
| 83bvl | B-170 ✅ | **The task-completion form asks for two of the four proof fields, and announces nothing when it works** (UX **and** accessibility reviews independently, 2026-08-24; **verified**; SC 4.1.3 AA, 2.4.3 A, 3.3.1 A, 3.3.2 A). One screen, one session, three findings merged. (1) `components/admin/task-complete-form.tsx:38-53` renders `note` and conditionally `photo_reference` and nothing else, `apps/web/app/admin/tasks/actions.ts:24-36` forwards only those two, and `packages/core/delinquency/timeline.ts:52` `PROOF_FIELDS` also contains `tracking_number` and `delivered_on` — which `admin/settings/delinquency/page.tsx:238` lets an operator require. Such a task **can never be completed from that screen**, and the refusal is the raw enum key (`` `${field} is required.` ``). (2) `completeTaskAction` (`actions.ts:42`) returns `success('Task completed.')` and revalidates in the same commit, so React writes the message into `AdminForm`'s `role="status"` and unmounts the `<li>` containing it simultaneously — the text never exists long enough to be an observable mutation, and focus, which was on the submit inside that `<li>`, falls to `<body>` so the next Tab starts from the sidebar. The same pattern breaks "Make live" on `admin/settings/promotions/page.tsx:133-158` and "Record the return" on `admin/tenants/[tenantId]/page.tsx:923-984`, because in each the row reporting the outcome is the row the outcome removes. This is the failure B-148 fixed for the two public marketing forms and never reached the admin side. (3) The note's only visible label is the placeholder "What did you do?" (3.3.2). Builds: an input for **every** field in `task.requiredProofFields` driven from one label map, with human labels (`tracking_number` → "Tracking number", `delivered_on` → "Date delivered", `type="date"`) and a real `<label>` on each — no per-field booleans, and an unmapped key is a **build-time** failure rather than a raw key shown to staff; plus an opt-in `announceOutside` region rendered by the PAGE above the list, so the message survives the row it reports on — the structural move `components/marketing/form-result.tsx:48-50` already makes — with focus moved to it via `tabIndex={-1}`. The list heading is the natural target ("Tasks (11)" → "Tasks (10)"), which answers "did it work" with a countable fact. Acceptance: after a successful completion `expect(page.getByRole('status')).toContainText('Task completed')` **and** `expect(page.locator('body')).not.toBeFocused()`, asserted on all four queues that render this component (`admin/tasks`, `admin/delinquency`, `admin/walkthrough`, `admin/access/queue`) | PRD 02 §4.9 US-41, §5.5 FR-19/FR-20/FR-22/FR-24 | M | B-141, B-095, B-094 | MVP |
| 83bvm | B-171 ✅ | **Both public marketing forms are silent on the error path** (accessibility review 2026-08-24; **verified**; SC 3.3.1 A, 4.1.3 AA, 1.4.1 A). `components/marketing/form-result.tsx:59-67` renders `{success ? state.message : ''}`, so on `status: 'error'` the live region stays **empty** and the summary ("There is a problem with one field.") is discarded, while focus stays on the submit. The only evidence of a refusal is a `<span>` reachable by swiping backwards to the email field, so a screen-reader user cannot distinguish "we refused this" from "we accepted it and said nothing" — and `joinWaitlistAction` always sets `fieldErrors.email` while `lead-actions.ts:71` always returns a `fieldError`, so **every** error path on both forms is affected. B-148 fixed the success branch only, and `e2e/smoke.spec.ts:1553-1604` tests only success, which is why it survived. Builds: write `state.message` into the same mounted `role="status"` on error and move focus to it — `FormResult` already holds the ref, the `tabIndex={-1}` and the effect and only branches on `success` — and give error text a distinct class, because the region is hardcoded `text-green-700` and renders a red-meaning message green (1.4.1). Acceptance: submit `not-an-email` on each form, then `expectAnnounced(status, /problem/)` and `expect(status).toBeFocused()`, and run axe on the refused state | PRD 01 §6.8.1 (waitlist & lead capture), §6.8; PRD 04 US-8 | S | B-148, B-090a | MVP |
| 83bvn | B-172 ✅ | **Checkout's unit-lost branch says your answers are still here, gives no way to use them, and reads as noise to a screen reader** (UX **and** accessibility reviews 2026-08-24, same branch, merged; **verified by reading — the branch renders only for a lapsed session whose size has since gone**; SC 2.4.4 A, 2.4.6 AA). B-149 shipped the phone number, the waitlist and the alternative sizes and left the branch a **display**: `(public)/checkout/page.tsx:341-416` lists other sizes as links, `:432` `!session.lockLapsed &&` hides the step section, and `lib/checkout/session.ts:675-695` `relock` only ever re-claims `session.unitTypeId` — so the copy "everything you have entered is still here" is true and unusable, because nothing in the branch can point the session at a different size. Worse, `:633-683` renders `PriceSummary` and `PromoCodeStep` regardless of `lockLapsed`, so a sticky "Due today $187.42 … then $129/mo" keeps quoting the unit the renter has just been told they lost. The basket already supports lines of a different type (`addableTypes`, `page.tsx:250-256`). And `:387` and `:403` write the sizes as raw glyphs, so VoiceOver says *"10 prime times 10 prime Standard, link"* and *"Or wait for a 10 prime times 10 prime, heading level 3"* — the entire recovery path at the highest-intent moment in the funnel, unreadable, while `portal/transfer/page.tsx:233-238`, `admin/reports/waitlist/page.tsx:113-118` and `waitlist-form.tsx:27`'s `sizeLabel` all apply the `aria-hidden`/`sr-only` pair correctly **in this same block**, and this branch passes the spoken form to the form while writing glyphs in the heading above it. Builds: a "Move me to this size" submit per alternative that re-points the session line at that `unitTypeId`, claims a unit and returns the renter to the step they were on; each alternative naming the trade ("10×15 — 5 feet longer · $149/mo, $20 more than the unit you had"); the price summary and promo box suppressed or restated as "was" while `lockLapsed`; phone and waitlist kept as the fallback for when no size can be switched to, and **only then** may the copy stop claiming the answers are still here; the existing `aria-hidden`/`sr-only` pair at both glyph sites. Acceptance: the branch gets a scan of its own — open a session, mark the demo unit type unavailable, expire the lock, run axe plus a link-name assertion — which is the first time this branch has been scanned at all (B-149 deferred it to B-156, which deferred it too). **Deliberately out of scope: a recommender**, as B-149 already ruled | PRD 01 FR-4.1, §6.4, §6.7, §6.8.1; D-79 | M | B-149, B-106, B-020 | MVP |
| 83bvo | B-173 ✅ | **The date you typed is not the date that posts — all four move-out and transfer screens** (UX review 2026-08-24; **verified**; SC 3.3.2 A, 3.3.4 AA). The date input sits in a separate `method="GET"` form whose only submit is "Recalculate", while the action form carries a hidden field built from the URL — so changing Sep 1 to Sep 5 and pressing **Complete move-out** closes the lease on Sep 1, silently, after showing the tenant a Sep 5 figure. Nothing on any of the four screens says the picker is inert until a second button is pressed: `admin/tenants/[tenantId]/move-out/page.tsx:104-122` versus `:202` (`moveOutDate` read from `searchParams` at `:70-74`); `admin/tenants/[tenantId]/transfer/page.tsx:115-140` versus `:216`; `portal/move-out/page.tsx:155-174` versus `:234-238`; `portal/transfer/page.tsx:253-264` versus `:326`. This is a money-path 3.3.4 failure on the two screens that end a tenancy. One code shape, one session, all four screens: either the date control lives **inside** the committing form, or the commit re-reads it — one form, one truth. If the two-step preview stays, the commit refuses while the picker differs from the previewed date, naming both ("You changed the date. Press Recalculate to see what a Sep 5 move-out settles to."), and the commit button restates the date it is about to act on. **The row offered the two fixes as alternatives and it needed both.** Moving the control into the committing form alone only mirrors the defect — it then commits the typed date against figures worked out for a different one, which is the same lie with the operands swapped. So: the control IS a field of the committing form on all four screens, AND `stalePreview` refuses while it and the priced value disagree. Recalculate stayed one control rather than becoming two, because a submit button whose `formAction` is a **string** is the one case React hands back to the browser — so "Recalculate" / "Update" / "Show me what it costs" is a native `formMethod="get"` submit of the committing form. **Scope was wider than the date**, and deliberately: the same mechanism made the transfer screens commit the previously-previewed **unit**, and the admin one the previously-previewed **rate**, so all three are guarded rather than the one the row named. Free with the merge — the pricing submit is first in DOM order, so Enter in the date field re-prices instead of committing money. **Leaves behind:** the refusal is a STATE and no axe scan reaches it (B-184's route-versus-state gap; unlike B-171/B-172 this row does not close it for its own states — the guard is asserted in `e2e/admin-move-out.spec.ts` and on the portal only by `stalePreview`'s unit tests); a GET submit now serializes the whole committing form into the URL, which is ugly and harmless; and B-174's portal move-out holes (`max`, the vanishing preview) are untouched and still its | PRD 02 §4.4 US-14 (move-out, transfer); PRD 01 §4.7 US-707/US-709, §6.8.1 | S | B-157, B-090b, B-041 | MVP |
| 83bvp | B-174 ✅ | **The portal move-out money panel vanishes with no message, and the date has no ceiling** (UX review 2026-08-24; **verified**; SC 3.3.1 A, 4.1.3 AA). B-142 fixed both of these on the sibling screen and neither fix crossed one file: `portal/move-out/page.tsx:131` keeps only the `ok` branch (`previewResult.ok ? preview : null`) and `:176` has no else, so a refused preview renders **nothing** while "Request this move-out" stays live and pressable; the picker at `:159-167` carries `min` and no `max`, and `lib/portal/move-out.ts:196-217` checks a floor and no ceiling, so a tenant can request a move-out in 2031. Builds: B-142's `previewProblem` `role="alert"` block ported across; Request hidden or disabled whenever there is no preview; `max` on the input **and** a server-side ceiling, since the client's is a courtesy; and `noticeShortfallDays`, which is computed and rendered nowhere, either shown to the tenant or deleted — a figure the code works out and nobody sees is the notice rule failing quietly. **Built as specified. `noticeShortfallDays` was deleted, and the reason matters more than the deletion**: `noticeGivenAt` is written in exactly two places in the whole codebase, both in this file — stamped by `requestMoveOut`, cleared by `cancelMoveOutRequest` — and the picker only renders when there is no request outstanding, so on this screen it is **always null**. `noticeShortfallDays(null, …)` returns the FULL required notice, so the figure was a constant equal to `moveOutNoticeDays`, not a measurement, and rendering it with the admin screen's sentence would have told a tenant picking a compliant date that they were ten days short. The notice rule lives in the floor on this screen (`min` plus `date_too_soon`), and that is where it belongs. **The ceiling is 365 days, a module constant rather than a facility column** — a column that configures behaviour has to ship with an operator control in the same item, and nothing has asked for this to vary by site. **Both bounds moved into one `moveOutDateProblem` shared by the preview and the write**, because a screen refusing what the write accepts is how the two drift, and the ceiling existed in neither. The preview refused nothing at all before, which is what left the alert with nothing to say. The action's four-entry copy map was a near-duplicate of the lib's and now spreads it. Also scanned: the refused state gets an axe pass of its own in `e2e/portal-move-out.spec.ts`, one more of B-184's route-versus-state gaps closed for one state. **Real bug found, not fixed here:** since nothing outside the portal ever writes `noticeGivenAt`, the ADMIN move-out screen reports the full notice period as a shortfall for every walk-in tenant — a tenant who gave notice at the counter has none recorded, and there is no way to record it. Wants its own row. **Leaves behind:** the ceiling does not vary by facility; the walk-in `noticeGivenAt` gap above; B-182 still owns the "ring them" wording in the lien refusal this screen renders | PRD 01 §4.7 US-707, §6.8.1; PRD 02 §4.4 US-14 (move-out) | S | B-142, B-041 | MVP |
| 83bvq | B-175 ✅ | **The signed lease states the minimum stay and never states the charge** (UX review 2026-08-24; **verified**). `lib/lease/build.ts:122-131` `termSummary` is deliberately silent on consequence — B-144's choice — against `packages/core/promotions/recapture.ts:95-99`, which since B-145 actually charges it. The word "recovered" first appears on the move-out screen, at the moment the amount is already computed and the tenant is already leaving; a charge whose first mention is the final statement is a chargeback with paperwork. B-145 left the sentence alone on the grounds that a single template cannot say both, and `termSummary` is **already a per-facility computed merge field**, so it can. Builds: branch `termSummary` on `promoRecapturePolicy` as well as `minStayMonths` — `prorated`: "…we will charge back the share of the discount for the months you did not stay"; `full`: "…the whole discount you were given"; `none`: unchanged, because nothing is recovered and saying otherwise is the same defect mirrored. Acceptance: one test pins each policy's sentence against what `recaptureFor` actually produces, the way B-145 pinned the ledger description to the screen — the lease and the arithmetic move together or not at all. **Built as specified, with the wording moved rather than branched in place.** `minStayTermSummary` lives in `packages/core/promotions/recapture.ts`, beside `recaptureFor` — not in `lib/lease/build.ts` where the sentence has always been — because the acceptance criterion is that the two move together, and a test asserting a sentence and a number under the same policy is the obvious thing to write when they share a file and a thing somebody has to remember when they do not. **Two tests, not one:** the pure one pins each policy's sentence to the amount `recaptureFor` returns under it, and a DB one pins the WIRING — that the facility's own column is what reaches the signed document — because `minStayTermSummary` could be right while `buildLease` passed it a hardcoded `none`, which is the exact shape of the bug this row fixes. PRD 02 US-10 gains the AC; it had one requiring the minimum stay be stated and none requiring the consequence. **Leaves behind:** no e2e — reaching the changed sentence means a facility whose policy is not the `none` default, and flipping the shared demo facility is the unscoped mutation B-120 forbids; the DB test covers the wiring instead. The `none`-with-a-minimum-stay case is still a condition with no consequence, which is B-176's | PRD 02 §4.3 US-10, §4.4 US-14 (move-out); PRD 01 US-501 step 4; D-10 | S | B-145, B-144, B-024 | MVP |
| 83bvr | B-176 ✅ | **Minimum stay and recapture policy are two settings on two screens, neither mentioning the other** (UX review **and** accessibility review, promotions half, 2026-08-24, merged as one screen; **verified**; SC 2.4.6 AA). `admin/settings/promotions/page.tsx:240-250` hints that minimum stay "is stated on the lease and in checkout" and never says whether anything is recovered, while `admin/settings/page.tsx:547-561` defaults `promoRecapturePolicy` to `none` ("Nothing — the discount is theirs to keep"). An operator can therefore run a six-month minimum for a year in the belief it is enforced, and it is decoration. Merged from the accessibility review, same file: `:143-152` renders "Make live" / "Pause" / "End" three times per promotion so the rotor reads "Make live, Pause, End, Make live, Pause, End…" with nothing identifying which promotion — the `aria-label` on the wrapping `AdminForm` buys nothing, because no screen reader composes a form's label into its descendants' names, and `task-complete-form.tsx:22-25` states that rule in a comment on the component created to fix it. Builds: the hint reads the **current** policy and states it with a link to the setting; submitting `minStayMonths > 0` at a `none` facility returns an `AdminForm` `confirm` step echoing the consequence; the promotions list row appends "· not recovered on early move-out"; the missing line that a promotion cannot be edited after creation; `aria-label={`Make ${promotion.name} live`}` and its two siblings; and the promotion form's own label fixed while there, since `${status} ${promotion.name}` renders "active Summer Special", which is not a sentence | PRD 02 §4.3 US-10, §4.1 US-3, §5.5 FR-22; D-10 | S | B-144, B-145, B-170 | MVP |
| 83bvs | B-177 ✅ | **Lowering a tenant's rate asks for a lease ID and never says whose rate you just cut** (UX review **and** accessibility review, rate-increase half, 2026-08-24, merged as one screen; **verified**; SC 2.4.6 AA, 3.3.4 AA). `admin/rate-increases/page.tsx:275-281` and `:317-322` take a **free-text "Lease ID"** with the hint "From the tenant's lease page", while the eligible-tenants table at `:238-247` already lists every tenant, unit and current rate and offers no action at all — so the workflow is copy a cuid, paste a cuid, and a mistyped one permanently reduces a different tenant's rate with a reason code recorded against them and no confirmation between the paste and the commit. B-153 shipped this as its own named left-behind and B-166's re-notice makes it worse by requiring the same paste again. Merged from the accessibility review: `:165-175` renders "Approve" and "Cancel" per row with identical accessible names and identical styling side by side, so the rotor reads "Approve, Cancel, Approve, Cancel…" and the destructive one is one row-misread away. Builds: a picker of active leases ("Tenant — Unit 104 — $129/mo now") replacing the free text; per-row "Lower this rate" and "Schedule an increase" that prefill it; **both** money forms returning a `confirm` step echoing tenant, unit, old rate, new rate and effective date; `aria-label={`Approve increase for ${row.tenantName}`}` and the same for Cancel, with a destructive visual treatment on Cancel; the Now/New/Street/Gap columns headed with a unit; and one date format throughout (`:88` renders a raw `2026-09-23` beside `:97`/`:31`'s "Sep 23, 2026"). Also renders B-165's rule and percentage in the batch preview, which is why it sits after it | PRD 02 §4.3 US-11, §5.5 FR-22/FR-23; D-37 | M | B-165, B-166, B-153 | Phase 2 |
| 83bvt | B-178 | **"Charge the fee?" does not say how much, and the option labelled Yes carries the value `no`** (UX review 2026-08-24; **verified**). `admin/tenants/[tenantId]/page.tsx:963-976` renders a dropdown headed "Charge the fee?" whose default option reads "Yes — charge the configured fee" and whose value is `"no"`, because the underlying field is `waiveFee` — a control whose displayed answer is the **opposite** of its stored value, one careless edit away from waiving every returned-payment fee at the counter. It also never states the amount, on the one screen where the amount is the decision. Builds: the facility's configured returned-payment amount in the label; a sentence rather than a control when no fee is configured, because a dropdown offering to charge nothing is a question with no answer; and the field renamed so value matches label (`chargeFee`, yes/no). Same session, same file, same class: the refund form's "Cheque number" input is always visible under a "Cheques only" hint and should render only for a cheque payment, and the write-off reason select on `move-out/page.tsx:237-241` prints raw enum keys ("collections uneconomic") — `REASON_CODES` gets a label map, the same one B-170 establishes for proof fields. **Accessibility:** each control is named after its subject rather than the action (2.4.6 AA) and the outcome announces through B-170's page-level region, since recording a return removes the payment from `profile.returnable` and takes the message with it | PRD 02 §4.5 US-46/US-21, §4.4 US-14 (move-out), §5.5 FR-22; D-15 | S | B-146, B-170 | MVP |
| 83bvu | B-179 | **A returned payment tells the tenant to phone, on a screen that could take the money** (UX review 2026-08-24; **verified**; SC 1.4.1 A, 2.4.4 A). `portal/documents/page.tsx:117-131` reads "Returned unpaid by the bank. This amount is owed again — please call us" while the amount column still shows the full payment as though it landed, and `PayNowButton` is one tap away at `portal/page.tsx:31-40`. The product asks the tenant to queue on a phone line to do something the next screen does in three taps, and the only signal that the money is owed again is a sentence beside a figure that contradicts it. Builds: the action in place of the instruction, linking the same pay route the dashboard uses; the amount marked **in words** as well as visually ("$129.00 · returned"), so the state is not conveyed by colour or position alone; the returned-payment fee named if one was charged, since the tenant will otherwise pay the wrong total; and the **facility's** phone number via `phoneFor` kept as the secondary route rather than the org line | PRD 01 §4.7 US-705, §6.5, §6.8.1; PRD 02 §4.5 US-46; D-15 | S | B-146, B-035, B-037 | MVP |
| 83bvv | B-180 | **The lead screen's waitlist capture is labelled only by placeholder and types the email once per size** (UX review 2026-08-24; **verified**; SC 3.3.2 A, 1.3.1 A). `admin/leads/[leadId]/page.tsx:150-165` renders `label={<span className="sr-only">}` plus a placeholder "Email" **once per size row**, so a caller wanting two sizes is typed twice and a placeholder is the only visible label — which vanishes the moment anything is typed and is unavailable to anyone using speech input. The public form beside it does it correctly (`components/marketing/waitlist-form.tsx:60-62`), so the pattern exists and did not reach the counter screen. Builds: visible `<label>`s throughout, placeholders removed or turned into an example ("e.g. IMG_4471"); the email taken **once** above the size table and prefilled from the lead, since the record already holds it; and success copy naming the size and the address ("Added to the waitlist for the 10×20. We'll email ada@example.com as soon as one is free") instead of "On the waitlist", which is D-87's honest-copy obligation applied at the counter — the notification stays a race and the confirmation should not imply a reservation. The task-form half of this finding is merged into **B-170**, same defect, different screen | PRD 02 §4.8 US-43, §5.5 FR-22; PRD 01 §6.8.1; D-87 | S | B-154, B-097, B-090a | Phase 3 |
| 83bvw | B-181 | **The tenant profile is seventeen stacked sections and the third one is a support-session form** (UX review 2026-08-24; **verified**; reviewer confidence MEDIUM). `admin/tenants/[tenantId]/page.tsx` is 1,399 lines in a single scroll with no in-page navigation and nothing collapsible: impersonation at `:275-328`, the contact-edit form at `:330-378`, the **leases** at `:623-723`, comms at `:1293-1396`. The units, rates and balances the person at the counter is being asked about are the ninth thing down, behind three write forms the same person uses a few times a month. Builds ordering and disclosure only — **no new data and no new queries**: leases directly under the banner stack; the occasional write forms (impersonation, contact edit, hold, note, record a return) behind `<details>` or an Actions region closed by default, read first and write second; contact details as text with an Edit disclosure; comms log and gate history capped at five with "Show more". **The banner stack stays exactly as it is** — the reviewer names it as working, and every control that can act on the account sits below it deliberately. **Accessibility:** each disclosure is a real `<summary>` named after its content (2.4.6 AA), the collapsed state is programmatically exposed (4.1.2 A), and the page is scanned **with a disclosure open**, per FR-25 (1). **Reviewer confidence is MEDIUM and this row inherits it:** worth one pass with an operator on the order before committing to it, and it sits after B-178 because that row fixes the controls this one is about to hide | PRD 02 §4.4 US-13/US-16, §5.5 FR-22; D-13a | M | B-178, B-038, B-091 | MVP |
| 83bvx | B-182 | **Customer-facing copy asks tenants to "ring the office" and explains itself with internal reasoning** (UX review 2026-08-24; **verified**; D-15 lexicon). Three separate breaches of D-15 on shipped customer routes. (1) "Ring them on (512) 555-0100" at `portal/transfer/page.tsx:93, 126, 154, 158, 209` and `lib/portal/transfer.ts:54, 60` — British register on a Texas product; becomes "Call the office at X" on **every** customer-reachable route. (2) "cheque" throughout `admin/tenants/[tenantId]/page.tsx:806, 862, 866-867, 913` and the POS screens, which a US operator reads as a typo — one product-wide pass to "check", including the field B-178 renames. (3) `date_too_far_out` reads "Pick a date within 60 days — the same window the public site holds a unit for", which is internal reasoning a tenant has no way to use; becomes "Pick a date within the next 60 days." Same pass: the `lien_pipeline` refusal copy carries the facility's number rather than sending the tenant looking for it, and checkout's confirmation fallback routes through `phoneFor` so one checkout can never show two different phone numbers (`(public)/checkout/page.tsx:563-568` renders the org line while `:367` renders the facility's). **Leaves behind:** no lexicon lint — D-15 is still enforced by review, and a rule that fires on "ring"/"cheque" in a customer-reachable route would be its own row if it is ever worth one | PRD 01 §6.10 (customer lexicon), §6.8.1; PRD 02 §4.8; D-15 | S | B-090b, B-142, B-039 | MVP |
| 83bvy | B-183 | **AR aging sits under a month picker that does not govern it, and the fix is a paragraph** (UX review 2026-08-24; **verified**; reviewer confidence MEDIUM). B-150 answered the question and answered it in prose: `lib/admin/reports.ts:137-153` and its render on `admin/reports/page.tsx` now carry an `ar-as-at` paragraph explaining that "the July 2026 range above does not apply to them, because how old a debt is describes one instant rather than a range" — while four aging figures still sit under a July heading with three period-scoped sections above them. A reader who does not read the paragraph reads four wrong numbers, and the paragraph argues where it should state. Builds: the as-at sections visually detached under their own "As of right now" sub-heading (or on their own route), the sentence shortened to the fact with the reasoning dropped, and the **same treatment applied to the waitlist and occupancy notes**, so the product has one way of saying "this figure ignores the picker" rather than three. Same file, merged in: `/admin/reports/waitlist`'s "Been told" and "Free now" column headings are guesses at plain English and should read "Notified" and "Units free now"; a row with people waiting **and** a unit free is the single actionable case on the page and should be surfaced as such rather than left to be spotted across two columns; and the `<summary>` disclosures at `:129-131` all read "3 people", so the rotor lists them undistinguished — `aria-label={`Contacts waiting for ${w} foot by ${l} foot`}` (2.4.6 AA), the third and last part of the accessibility review's A5 | PRD 02 §4.11 US-39.4, §4.8 US-43, §5.5 FR-22; D-68 | S | B-150, B-154, B-131 | MVP |
| 83bvz | B-184 | **The scan contract III: states the route list cannot express, and the assertions that were only ever run on public routes** (accessibility review 2026-08-24; **verified**). **Last in the block, deliberately, for B-119's and B-156's reason**: every part of it asserts what the rows above fix, so running it earlier only produces a suite red for reasons already written down. Six gaps, one row. **T1 (M)** — `tests/a11y-scan-coverage.test.ts` walks `apps/web/app` for `page.tsx`, so the contract is **route-keyed by construction and a state has no directory**; now that B-139 generates the public exception list from it, the gap is invisible on the page as well as in CI. Add a second exported pair in `scan-coverage.ts` — `SCANNED_STATES` / `STATE_EXCEPTIONS`, keyed `{route, state, spec or reason}` — rendered beside the route exceptions and cross-checked by a unit test against an `// a11y-state:` comment convention in the specs. It does not achieve completeness, and nothing can; it makes "checkout's sold-out branch is not scanned" a **printed sentence** rather than a paragraph in `PROGRESS.md`, which is what B-159 needs and must not wait for. **T2 (S)** — `incomplete` is asserted on the 93 public route scans and nowhere else (`e2e/a11y.spec.ts:59-71`); `smoke.spec.ts:1278`, `portal.spec.ts`, `admin.spec.ts`, `admin-transfer.spec.ts`, `admin-tenants.spec.ts`, `admin-move-out.spec.ts`, `admin-pos.spec.ts` and `pay-link.spec.ts` destructure `violations` only, and every checkout step is in the second group. Lift the filter-and-assert block into `e2e/a11y-helpers.ts` beside `expectPreexisting` and have `assertNoAxeViolations` call it — one function, eight call sites. **T3 (S)** — no axe scan runs after an invalid submit on **any** admin or portal form, which FR-24 requires in as many words; `AdminForm`'s error summary (`role="alert"`, `text-red-700`, `aria-invalid`) has never been scanned in any state on any admin page. One `await assertNoAxeViolations(page)` after the existing `expect(alert).toBeFocused()` in `e2e/admin-tasks.spec.ts:107-137`. **T4 (S)** — nothing asserts where focus goes after a **successful** submit, which is the single line that would have caught B-170: extend `expectAnnounced` with an optional `{ focused: true }`, assert it on `/portal/contact` (`portal.spec.ts:226`) and on task completion. **T5 (S)** — `expectPreexisting` still cannot be pointed at `AdminForm`'s `status: 'confirm'` summary, because it is conditionally rendered and therefore arrives already carrying its message; the confirm-and-echo step on `/admin/settings`'s four tax and fee actions (`admin/settings/actions.ts:172,231,311,662`) is **the only place in the product where 3.3.4 error prevention is implemented**, and its echo panel arrives populated and silent. Pre-mount the confirm region alone — B-156's visual-cost objection applies to the bordered error box, not to an empty `role="status"` paragraph. **T6 — three manual passes, and they are part of this row's definition of done**, recorded in `docs/PROGRESS.md` per FR-24, not merely performed: a **VoiceOver pass on the money path** (checkout 1→4, `/portal/pay`, the tenant profile's returned-payment and refund forms), owned by whoever takes **B-170**, in that session, because only a person can hear that its fix is success rather than silence; a **keyboard pass over the four task queues and the promotions list**, specifically where focus lands after each submit, same owner; and the **live-map pass** owed since `PROGRESS.md:4249`, owned by the first session with a billed vendor key. A pass not written down did not happen — that is the rule B-159 is correcting the public page against | PRD 02 §5.5 FR-25, FR-24; PRD 01 §6.8, §6.10 | M | B-156, B-119, B-120, B-170, B-171, B-172 | MVP |
| 83bwa | B-185 | **The unit test database accumulates fixtures until the sweep stops being trustworthy** (found while building B-161, 2026-08-24; **measured**). `storage_test` held **13,106 facilities and 8,276 units** — every one of them created in August 2026, none by design. Only 32 of ~120 database suites call `facility.deleteMany`, so the rest leave their fixtures behind on every run, for ever. Two failures fall out of it and BOTH read as product bugs: `tests/marketplace-db.test.ts` calls `marketplaceFeed()`, which scans the whole facility table with unit counts, and under parallel load it holds a Prisma connection past the 10s pool timeout — four tests fail with *"Timed out fetching a new connection from the connection pool"*, and the same file passes 10/10 alone; and `admin-tenants-db.test.ts`'s `searchTenants` case fails because `searchTenants` caps at 25 results and hundreds of leftover "Ada Renter" rows now fill them. A clean-tree sweep on 2026-08-24 was **4 failed / 3,583 passed**; dropping and re-migrating the schema is what made it green again. Builds: a per-suite cleanup discipline that a new suite cannot silently skip (a shared `afterAll` helper keyed to the suite's own `suffix`, or a truncate-between-files hook), and — separately — whichever of the two failing assertions should be scoped to its own fixtures rather than to the whole table regardless. **The reset is a local command today and nobody is told to run it**, which is the part that makes this a row rather than a note: the failure looks exactly like a broken feed and a broken tenant search | PRD 08; CLAUDE.md "Test isolation is not free on a shared database" | M | — | MVP |
| 83bwb | B-186 | **Every walk-in tenant is recorded as having given no notice at all** (found while building B-174, 2026-08-25; **verified by grep — `noticeGivenAt` has exactly two writers in the codebase**). `Lease.noticeGivenAt` is written in `lib/portal/move-out.ts` and nowhere else: `requestMoveOut` stamps it, `cancelMoveOutRequest` clears it. Nothing on the admin side ever sets it — not `completeMoveOut`, not the profile, not the counter. So `noticeShortfallDays(null, …)` returns the full required notice by design (`packages/core/move-out/settlement.ts:159`), and `admin/tenants/[tenantId]/move-out/page.tsx:185-195` tells staff "This is 10 days short of the notice the lease asks for" for **every tenant who did not use the portal** — including one who gave thirty days at the counter and was thanked for it. The figure is not a measurement, it is a constant that reads like a finding, on the screen staff treat as the compliance record of how a tenancy ended. B-174 deleted the same field from the PORTAL preview for this reason and deliberately left the admin side to its own row. **Two things, and the row should not pretend they are one.** (1) **There is no way to record notice given off-platform**, so the fix is a control: a "Notice given on" date on the move-out screen (and on the profile, since notice usually arrives before anyone opens a move-out), defaulting to blank rather than today — a default of today is the same lie in the opposite direction, and a field nobody filled in must stay distinguishable from one filled in with today. Writing it recomputes the shortfall from a real date. (2) **The remedy the copy names does not exist.** "The lease's remedy is a charge, not a refusal" is what the screen says, and nothing charges: `settleMoveOut` takes no shortfall input and no ledger entry is ever posted for it. So either the sentence stops promising a charge, or the charge gets built — which is a lease-terms and per-state question (D-10) rather than a UI one, and is why this row surfaces it rather than settling it. **Accessibility:** the new date field is a real `<label>` and, on the move-out screen, a field of the committing form per B-173's rule, so it cannot be typed and then not posted | PRD 02 §4.4 US-14 (move-out), §5.5 FR-22; PRD 01 §4.7 US-707; D-10 | S | B-174, B-173, B-041, B-038 | MVP |
| 83a | B-129 | **Online auction platform listing from the auction pipeline** (split from B-083, 2026-08-18, D-63). B-062 built the auction pipeline and records advertising as `AuctionAdvertisement` rows a person types — publication, run date, tear-sheet reference. Listing a lot on a marketplace from that pipeline is the missing half of US-30. **Blocked on two things, neither of them code.** (1) **Master PRD §11 open question 9 has never been answered** — live on-site auctions versus online — and building a marketplace driver silently answers it. (2) A StorageTreasures-class listing needs a **partner agreement**; no API is published without an account, so there is no spec to write against, which is exactly why B-085's gate-vendor driver is gated the same way. **The rule B-083 settled applies here too (D-63): no simulator.** A fabricated "listed on 3 August" record is a claim about a public advertisement that Texas requires before a sale, and an advertisement that never ran is the commonest wrongful-sale claim after an unserved notice. When it is unblocked, the cheap first move is a structured listing export (lot, photos, address, sale date, terms) an operator uploads themselves — it is useful whichever way OQ-9 goes and fabricates nothing | PRD 02 US-30; master PRD §11 OQ-9; D-63 | M | B-062, B-083 | Phase 2 |
| 84 | B-084 ✅ | **Split into four parts, 2026-08-18** (owner), because this row is four deliverables and two of them are correctness-critical — and because a management pack or an accounting export cut from LIVE data silently disagrees with itself between runs, so the frozen close has to land first and the other two read from it. **Part 1 ✅ (monthly close + frozen snapshots)**: `AccountingPeriod` per facility per month, filed through `/admin/reports/close` under a new `accounting:close` permission held at regional and above — closing fixes the figures a site is measured on, which is why it stops short of `manager` for the same reason `auctions:approve` does. **D-65 settles what "frozen" means**, and it is the whole feature: unit occupancy reads `Unit.status` which this system keeps no history of, and `delinquencyReport` takes no date parameter at all, so both answer "as of now" whatever range they are handed — a July figure is observable only in July and the filed copy is the only record there will ever be. Those are frozen and never drift-checked. Billed, collected, discounts, write-offs, refunds, unapplied, economic occupancy and move counts come from dated rows, so they are frozen AND re-run on view, with any difference reported as drift — a voided invoice, a backdated adjustment, a corrected move-out. Conflating the two fails both ways and the split is enforced by types, not discipline. Month bounds come from the same `monthBounds` a tenant statement uses, so a statement and a close for one month cover exactly the same days; the resolved window is STORED, so a later timezone correction cannot re-slice a filed month. A month cannot be closed before it has ended in the facility's own timezone. Reopening requires a reason, clears the snapshot rather than leaving withdrawn figures on display, and writes them to the audit log, which is the only place they survive. **D-64: the management pack is HTML, not the PDF US-40 names** — B-023's finding that no library here emits *tagged* PDFs still stands, and this is the second place PRD text asks for one. **Part 2 ✅ (QuickBooks journal export)**: `/admin/reports/journal.csv` cuts a balanced general-journal entry from a CLOSED month's frozen figures — refusing an open one, which is the ordering argument from the split made enforceable. **D-66**: ten named accounts configured per facility on the close screen, matched by NAME because that is what QuickBooks Online's import matches on, with a conventional chart as the per-field fallback so an operator who never opens the form still gets a file that imports. `buildJournal` asserts its own balance and throws otherwise — unreachable by construction, checked anyway, because an unbalanced journal reaching a real general ledger is invisible from here; a swept test covers 100+ combinations. **Two omissions that look like bugs and are the point:** refunds are NOT journaled, because a refund unwinds its original payment's allocations so collected is already net of it and a second entry would take the money out twice **while balancing**; and referral rewards are taken OUT of the discount figure rather than posted beside it, because they are a subset of it — the same invoice line type split by description. Both are asserted by name so a later reader does not "fix" them. The snapshot gained a category split (version 2), because a journal credits rental income, fee income and sales tax payable to different accounts and a single total cannot be taken apart afterwards; a version-1 snapshot is **refused with an instruction to reopen and re-close**, never exported with a guessed split. Drift now covers the category leaves too, so a rent-to-fee reclassification that leaves the total unchanged is reported. **Part 3 ✅ (scheduled report emails)**: `ReportSubscription` per facility — a report, a cadence and a recipient list — sent by a `reports.email` job at 6am facility-local, after the overnight billing and delinquency sweeps so the figures already include last night's invoices and late fees. **This is the first generated email in the product, so it is the first to meet PRD 05 FR-9a, and every clause of it is now a test rather than a review note**: `lang` on the root, a real `<h1>` then `<h2>`s in order with nothing skipped, `<th scope>` on columns AND rows, a caption on every table, no images at all (which settles the alt-text and text-as-image clauses together), link text that names its destination, no colour anywhere to carry meaning, and a text part built from the same document rather than by stripping tags — asserted by checking it contains no markup and still carries every figure. **D-67 settles two things.** There is **no `lastSentAt` column**: the send is keyed `report:{subscription}:{period}:{recipient}` and `sendDirectEmail` already refuses a repeat, so a second record of the same fact is a second thing that can disagree with it. And the job does **not** invent an actor — `facilityAccess` correctly gives the system actor nothing, so the report layer gained facility-explicit variants (`occupancyForFacility`, `facilityRevenue`, `agingForFacility`, `movesForFacility`) called after authorization already happened at subscribe time; widening `facilityAccess` for system would have made every facility-scoped query permissive for background work. That extraction also removed the waste part 1's own comment recorded. **A monthly report says whether its month has been closed**, which is the link that makes part 1 pay for itself: an email showing numbers that quietly differ from the filed ones is exactly the confusion the close exists to remove. A bad recipient address is refused rather than dropped. **Found the hard way, and now guarded:** the scheduled job was never registered — every other piece was built and tested and none of it could have run, and both a full unit suite and a green e2e sweep passed over it. A test now asserts the registration itself. **Part 4 ✅ (management summary pack)**: `/admin/reports/pack` — the whole month on one page, in the five questions an owner actually asks (how full it was, what it earned, what it gave away or lost, what was owed, who came and went). **HTML, not the monthly PDF US-40 names** (D-64). **Built as an `EmailDocument`**, the structure part 3 defined, which is reuse with a reason rather than convenience: a pack an owner OPENS and a pack an owner is SENT must not be able to say different things about one month, so there is one document and two renderings — and it is subscribable through part 3, monthly only, because a weekly pack would have nothing filed to read and would quietly send live figures. **It reads the FILED figures when the month is closed**, which is the payoff of the whole four-part ordering: a pack cut live changes between the day it is read and the day it is quoted. **The provenance sentence leads the page**, never trails it, and has three genuinely different forms — not closed and can still change; closed and settled; closed but N named figures no longer match, which it calls a restatement to explain rather than an error. Those numbers get quoted in a board meeting and "can this change?" is what decides whether they should be. Promotional discounts are separated from referral rewards (the same reasoning as D-66 and the revenue report), tax is labelled as held for the state rather than as income, refunds are labelled as already deducted so nobody subtracts them twice, and the over-90 bucket carries the words "needs attention" rather than a colour. **Finished converting `figuresFor` to be facility-explicit**, which is where part 1's own comment about computing every facility and throwing most of it away stops being true. **B-084 is complete.** Reporting depth: scheduled report emails, monthly close with frozen snapshots, management summary pack, QuickBooks-compatible journal export. **This is the first item that generates email HTML nobody wrote by hand, so it is the first that has to meet PRD 05 FR-9a** (added 2026-08-12 from the accessibility review, which found no email criteria anywhere in PRD 05): a real text alternative part, `lang`, actual headings in order, `<th scope>` on every tabular figure, nothing carried by colour alone, no text rendered as an image, and link text that names its destination. Cheap here, expensive once templates exist, are versioned, and have rendered snapshots stored against thousands of message rows | PRD 02 US-40, §8; PRD 05 FR-9a | M | B-055, B-042 | Phase 2 |

## Milestone 8: Phase 3 — grow & extend

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 83c | B-132 ✅ | **The demo leases that were delinquent in name only now owe real money** (found 2026-08-18 while fixing B-130; built 2026-08-19). Three of the four leases carrying a delinquency label had **no invoices and no ledger entries whatsoever** — only the primary facility's `delinquent` lease, the one B-114 and B-130 had already worked on, had any financial state. So every multi-facility aging figure was one lease at one site wearing three hats, and a `pending_auction` lease that owes nothing can never have a served lien notice behind it, which is the auction pipeline's own scheduling precondition (B-062) — the auction arc could not be walked end to end, for exactly the reason the notice arc could not before B-130. **Fixed in B-130's shape, extracted as `seedUnpaidRent`:** the INVOICE first, the ledger charge carrying its `invoiceId`, because `reconcile` counts invoices-outstanding PLUS charges no invoice accounts for and an unlinked charge is therefore counted twice. One month for each `delinquent` lease, **three** for each `pending_auction` one, oldest first and a month apart, since every aging figure reads `daysPastDue` from the oldest unpaid invoice's original due date (D-25). **Measured with the product's own `previewNotice`, not a re-derivation:** lien-notice eligibility went **1 of 14 → 2 of 14**, being Austin's `delinquent` and `pending_auction` leases. **Dallas's two now refuse with `no_template` rather than `nothing_owed`, and that is left as it is on purpose** — B-061's rule is that a facility which has not written its notice text generates nothing instead of mailing the unedited example, so the demo now carries one configured facility and one that is not, which demonstrates the guard better than two identical sites would. **The auction CASE is seeded too**, through the product's own `openAuctionCase` (which writes no audit entry, so the script's contract holds): `flag_auction_eligible` opens one on the way to `pending_auction`, so that status with no case behind it is state the product itself cannot reach. `AuctionCase.lease` and `.unit` are `onDelete: Restrict`, so the teardown gained a delete or the NEXT re-seed would fail on a foreign key rather than reset. **Deliberately NOT seeded: a generated or served notice, and any advertising record** — both are claims about documents served and advertisements run, which is what D-63 refused for this exact arc; the demo leaves them walkable through the product. **Left behind:** `/admin/auctions/{id}` still has no axe scan, and there is now a real case id to scan against for the first time; and nothing walks the auction arc end to end yet, because generating and serving a notice against shared demo data needs B-120's scoped-fixture discipline rather than a tail-end edit | PRD 02 §4.6 US-28; B-062, B-130 | S | B-130 | Phase 2 |
| 84a | B-131 ✅ | **Unit occupancy is now historical, and says which instant it answers for when it cannot be** (found 2026-08-18 while building B-084 part 1; built 2026-08-19 — **D-68**). `occupancyForFacility` took `periodStart`/`periodEnd` and used them for the collected figure and the street rate while reading `unit.status` with **no date filter at all**, so asking for July's occupancy in December returned December's unit statuses under July's heading — on a screen with a date picker on it. **`UnitStatusHistory`, written by a database trigger, not by application code.** `Unit.status` is derived and has two application writers (`recomputeUnitStatus`, and the bulk status operation's pre-evaluated `to`) plus two seeds; a history that depends on each of them remembering is a history with holes, and a hole here is **indistinguishable from "the status did not change"** — the read cannot tell them apart, so a partial history is not a weaker version of this, it is a wrong one. The trigger covers psql, future migrations and callers nobody has written; one test inserts a unit and asserts a row appeared without the application asking. It fires on `AFTER UPDATE OF status` and returns early on a same-value write, or every unrelated unit edit touching the column would log a change that did not happen. **The gap is stated, never papered over.** History begins at the migration, which backfilled one row per existing unit. `audit_log` was considered as a backfill source and rejected: it carries only the HUMAN status changes, never the derived recompute that moves a unit to `occupied` on move-in, so a reconstruction would be confidently partial — and a wrong past presented as recorded is worse than an honest gap, because nothing downstream can detect it. So a period older than the history returns TODAY'S figures flagged `followsPeriod: false` with a `reason`, and every surface prints **one shared sentence** — the screen, the scheduled email, and the CSV as an `as at` date column — because a reader who sees two wordings of the same caveat trusts neither. Four reasons are carried rather than re-derived from a bare false (`as-at-period-end`, `period-not-ended`, `before-history`, `no-units`); "the month is older than the history" and "the month has not finished" are different sentences to a reader. A unit absent from the as-at map did not exist yet and drops out of **both** numerator and denominator, so a building opened in August is correctly not in July's denominator; economic occupancy is measured over the same unit set, because the metrics module exists to stop two figures on one page disagreeing about which units count. The portfolio total makes the **weakest** claim any facility makes, and facilities with no units are dropped rather than allowed to drag it false forever. **D-65's freeze is deliberately NOT reopened** — occupancy is reproducible only for months after the history begins, so drift-checking filed months would report drift on every one filed before it, forever; that is a later item's decision, not a side effect of this one. **Left behind:** unit TYPE is not historised, so a retyped or resized unit carries today's square footage into a past month; and the management pack still reads filed point-in-time figures, so its provenance sentence is unchanged (adding an as-at field there is a snapshot version bump, which D-66 already makes expensive) | PRD 02 US-39.1, §8; D-65; D-68; B-084 | M | B-042 | Phase 2 |
| 85 | B-085 | First real gate-vendor driver (OpenTech CIA or PTI StorLogix Cloud — needs partner agreement, PRD 03 OQ-5) + kiosk-mode evaluation/decision. **Accessibility acceptance criteria added 2026-08-24** (accessibility review — an at-risk row rather than a new one, because the row exists and its criteria do not): a kiosk is a fixed-viewport touch surface with a session timeout, which is **SC 2.2.1 Timing Adjustable (A)** in its purest form, and PRD 03 defaults kiosk mode to "no" with no accessibility wording either way. **If the evaluation lands on yes**, the kiosk surface ships with a session timeout that warns at 20 seconds remaining and offers a one-press extension (2.2.1), a rental flow completable end to end with an external keyboard (2.1.1 A) including a visible focus indicator at the kiosk's actual contrast, reflow at the kiosk's real pixel width with no horizontal scrolling (**1.4.10 AA**), and error suggestion on every refused field (**3.3.2 A**, **3.3.3 AA**) since a walk-up user has no support channel on the screen. **If it lands on no**, the evaluation records "no" with its reasoning and the row ships **no kiosk surface at all** — a half-built kiosk is the one shape these criteria cannot be retrofitted onto. Depends on the OQ-5/D-3 decision either way | PRD 00 §6 Phase 2 (hardware); PRD 03 §8 Phase 2–3; PRD 01 §6.8.1; D-3 | L | B-080 | Phase 3 |
| 86 | B-086 | Smart-entry shared access (Nokē-class, simulated unless partnered): secondary time-boxed credentials, individual revocation, native-app/PWA spike for Bluetooth unlock. **Accessibility acceptance criteria added 2026-08-24** (accessibility review — an at-risk row rather than a new one; the default build of a Bluetooth unlock is an icon button that changes colour plus a toast, which fails four SCs at once): the unlock control exposes its state programmatically via `aria-pressed`/`aria-busy` (**4.1.2 A**) and announces the outcome from a live region **present before the press** (**4.1.3 AA**), never by colour or icon alone (**1.4.1 A**); a time-boxed credential states its remaining validity as an **absolute facility-local time**, per the standing rule at PRD 01 §6.8.1 that B-142 applied to the transfer hold, never as a countdown a screen-reader user has to poll (**2.2.1 A**); and a failed unlock names the facility's phone number in text rather than presenting a retry icon, because the failure state is where a tenant is standing outside a gate. Applies to the simulated build too — the criteria are about the control, not the vendor | PRD 03 US-8, §8 Phase 3; PRD 01 §6.8.1 | L | B-080 | Phase 3 |
| 87 | B-087 ✅ | **Split into two parts, 2026-08-20** (owner), per note 8's rule that bundled Phase 3 rows are split when they are reached — and this one splits along a blocker rather than along a screen: two of its four features need Google API access this build does not have, and two need nothing at all. **Part 1 ✅ (built 2026-08-20 — D-76): IndexNow submission + structured-data monitoring.** Nightly global jobs, a report at `/admin/reports/structured-data`, and the key file at `/indexnow/{key}.txt`. D-76 records the reduction in what the row promised: **Google retired its sitemap-ping endpoint in 2023**, so "IndexNow/sitemap ping" is IndexNow alone, reaching Bing, Yandex, Seznam and Naver. **Part 2 → B-133**, below | PRD 04 §7 Phase 3; D-76 | M | B-071, B-067 | Phase 3 |
| 87a | B-133 | **Google reviews ingestion + GBP API sync** (split out of B-087, 2026-08-20). The half of B-087 that needs credentials nobody has yet, and it is blocked on two separate things. (1) **GBP write access needs an approved application** — hours/NAP push and the discrepancy alerts that replace the manual checklist all go through the Google Business Profile API, which is not open to a project that has not been granted it, so there is no spec to build against. That is the same partner gate B-085 carries and it gets the same treatment. (2) **Reviews ingestion silently answers PRD 04 OQ-3**, which is explicitly marked *"needs a policy read before launch"*: `qualifiesForSchemaMarkup` returns false today because no review carries `source = google_api`, and the moment one does, this site starts emitting `aggregateRating` — a claim to Google that it collected the ratings itself. The cost of getting that wrong is a manual action against the rich-result eligibility of the whole domain, not one page, which is why the gate was built to wait on a value rather than a flag. **When it is unblocked**, ingestion comes first (it is what retires manual entry, PRD 04 FR-REV-4) and the GBP sync follows; the existing manual checklist at `/admin/settings/marketing` stays until the API replaces it, not before | PRD 04 FR-REV-4, §7 Phase 3, OQ-3 | M | B-087 | Phase 3 |
| 88 | B-088 ✅ | **Split into two parts, 2026-08-19**, per note 8's rule that bundled Phase 2/3 rows are split when they are reached: the two halves share no screen, no data and no PRD section. **Part 1 ✅ — revenue-management aids** (built 2026-08-19 — **D-74**). `/admin/units/rates`, a Rates tab beside Types: per-unit-type occupancy, a two-band suggestion ladder from US-12's own 92% example, and one-click apply that is **pre-filled but editable** — the rule proposes and the person prices. The rule is pure and lives in `packages/core/pricing` beside `rate-increase.ts`, because the rate a NEW tenant is quoted and the rate an EXISTING tenant is raised to are different prices with different consequences and must never be confused; nothing here reads or writes `Lease.monthlyRateCents`, and the screen says so in its first sentence with a link to `/admin/rate-increases`. Occupancy comes from the metrics module per unit type rather than being recomputed (D-25), which a test pins by exercising the two judgement calls — `maintenance` is rentable, `unrentable` is not. **Increases only, never a cut, plus three guards (D-74):** a 5-unit floor, a 90-day cooldown, and silence when a change is already scheduled. The cooldown is the one that matters — occupancy does not fall the day a rate rises, so without it "one-click apply" compounds +8% weekly into a 50% rise in a quarter. Applying reuses `publishUnitTypeRate`, so the permission, the audit entry and the effective-dating are the existing ones rather than a second write path. **Part 2 ✅ — owner KPI dashboard** (built 2026-08-19 — **D-75**). `/admin/reports/kpi`: the last 12 months of unit occupancy, economic occupancy, collected, receivables and net moves, portfolio-wide. **What it adds over the two existing surfaces is TIME** — `/admin` answers "how are we doing now" and every report answers "how did we do in that period"; neither answers "is it getting better", which is the only question an owner not running the counter asks. **Built from the FILED month-end snapshots and never from a recomputation (D-75)**, which is correctness before it is speed: D-65 settled that point-in-time figures cannot be observed again, so re-deriving a past month answers a different question under the same name — and it is also one query instead of the ~150 round trips twelve months across a portfolio would otherwise cost. Ratios are recomputed from components, never averaged (the `sumOccupancy` trap, asserted in both places it hides, including economic occupancy where the snapshot stores only a ratio and the type system cannot catch it). **A month nobody closed is named, never drawn as zero**, and a month where only some sites filed says so — one site not closing its books otherwise looks exactly like the portfolio falling off a cliff. Unlike `buildJournal` it does NOT refuse a v1 snapshot: the category split was v2's only addition and no KPI here reads it, so refusing would blank a year of history to guard a field nothing on the page uses. **Superseded scoping note:** the open question was what an owner view adds beyond B-042's dashboard and B-084's reports; the answer was trend, and the snapshots were already the right source | PRD 02 US-12; PRD 00 §6 Phase 3 | M | B-042, B-055 | Phase 3 |
| 89 | B-089 ✅ | **Per-city/size landing pages** (built 2026-08-20 — **D-77**). The row was a bundle of four and three of them were already settled when it was reached: **A/B testing** stays "do not build" (below), **marketplace channel evaluation** is not a backlog item (below), and the **referral program shipped as B-100/B-101** under PRD 10 — so this row's live scope was per-city/size pages alone, and it is an M rather than the L it was sized as. `/storage/{state}/{city}/size/{dimension}` (`/storage/tx/austin/size/10x10`), generated from real priced inventory and 404ing where there is none. The literal `size` segment keeps a dimension out of the facility page's slug namespace. **D-77 is the substance:** this is the page type that manufactures thin content, and B-082 part 6's duplicate detector now runs as a GATE before publication rather than a report after it — a page scoring ≥0.8 against a sibling size gets `noindex, follow` and is left out of the sitemap. Measured, not assumed: the seven standard sizes clear at 0.738, two sizes outside the guide catalogue score 0.940 and are both gated. **Do not build: offer A/B testing** — at 2–10 facilities no offer test reaches significance inside a quarter, so the result is noise that prices get changed on; set a rate, watch move-ins for 60 days, adjust. **Marketplace (SpareFoot-class) channel evaluation is not a backlog item** — it trades a chunk of first-year revenue for a tenant who was already searching for you, and it is a decision for a specific site that will not fill. Confirmed by the owner 2026-07-31, with the *integration* surface (feed out, attribution in, rate parity, channel source) carved out to B-082 so the plumbing exists if that decision is ever taken | PRD 00 §6 Phase 3; PRD 04 §7 Phase 3; D-77 | M | B-082 | Phase 3 |
| 89a | B-134 | **Authored copy for a size page, when a real portfolio needs it** (raised while building B-089, 2026-08-20, out of D-77's own measurement). A size the guide catalogue does not cover gets one sentence of generated copy — the measurement — so two of them score **0.940** against each other and D-77's gate refuses both. They render, they serve visitors, they list real inventory, and they are never offered to an index, with nothing an operator can do about it. The fix is the shape B-128 already built for cities: an authored intro keyed on state + city slug + dimension, edited under the existing `marketing:city_copy` permission, winning over the generated one when present — at which point the gate passes because the words are somebody's rather than a template's. **Deliberately not built with B-089**, and the reason is the same rule that governs the rest of this repo: an authored field is a column, a migration and a form, and building it before a portfolio has actually tripped the gate is guessing at a control nobody has needed. **The trigger is concrete** — a non-standard unit type appearing in a real facility, which `/admin/reports/duplicate-content` shows as a "City/size page intros" pair. The cheaper half-fix, worth considering first, is adding the missing sizes to `UNIT_SIZES` in `packages/core/marketing/unit-sizes.ts`: that costs three sentences and no schema, and it is the right answer whenever the size is a standard one the catalogue simply lacks | PRD 04 §3.2 US-4 AC1; D-77; D-62 | S | B-089 | Phase 3 |
| 90 | B-090 | **Split into six parts, 2026-08-20** (owner), and two of the seven things it listed were answered rather than built (**D-78**). The audit that preceded the split found **none of the seven built** — the only shipped slice was a staff-placed `payment_plan` hold, a forbearance switch rather than a plan — and three shipped rows (B-074, B-077, B-102) had each explicitly deferred their half here and said so in-line, so the row was accurate rather than stale. **Not built, answered instead (D-78):** PWA push (PRD 00 gates it on "*if metrics justify*" and no portal-engagement metric exists to justify it) and the two-way SMS inbox (PRD 05 says "(evaluate)"; the evaluation found the real defect underneath it, now **B-135**). **Part 1 ✅ waitlists** — below. **Parts 2–6, in no fixed order:** tenant self-service transfer from the portal (M, a screen over `lib/admin/transfer.ts`, deferred here by B-077); delinquency payment plans and self-cure (L, correctness-critical — money, the dunning ladder and lien exposure at once, and today's `payment_plan` hold has no schedule, no amounts, no breach detection and nothing tenant-facing); broadcast sends (M — **PRD 05 CN-21 [P2]**, a firm commitment no row owned until this split, reusing `MessageTemplate` and `sendDirectEmail`); business accounts with consolidated billing (L/XL, the only one needing a schema change — a payer ABOVE lease, where `Lease` is one tenant + one unit and `Invoice` is per-lease today); and Spanish/multilingual (L, touches every customer-facing string, and note it is **PRD 01 Phase 2** rather than Phase 3 — a phase earlier than this row implied). **Do not build yet: live chat / AI assistant** — at this operator size chat is a phone that rings on the manager's desk with worse latency and no ability to close. B-097 (phone lead capture) is the fix; revisit chat only if phone volume is proven to exceed what one person can answer | PRD 01 §9 Phase 2–3; PRD 05 §8 Phase 3; PRD 00 §6 Phase 3; D-78 | XL | B-074, B-081 | Phase 3 |
| 90a | B-090a ✅ | **Waitlists with notify-me** (B-090 part 1, built 2026-08-20 — **D-79**, **D-80**). A sold-out size dead-ended in a phone number: `checkout/actions.ts` told the renter to call and captured nothing, so demand at the moment of highest intent was lost and never counted. A `WaitlistEntry` — **its own model, not a `Lead` (D-79)**, because reusing `Lead` would have put every entry into `raiseLeadFollowUps`' task queue as uncallable work AND into B-082 part 4's funnel denominator, quietly diluting the conversion rate an operator prices against. Form behind a `<details>` on each full size; a cron-tick sweep that notifies **as many people as there are free units, oldest first**, holds each claim 72 hours and then gives the next person a turn; a cancel link that is the only credential a non-tenant has; and `/admin/reports/waitlist`, which exists because a waitlist is the only demand signal this product has for inventory it does **not** have. Mail is transactional and email-only (**D-80**) | PRD 01 §9 Phase 3; D-79; D-80 | M | B-090 | Phase 3 |
| 90b | B-135 ✅ | **An inbound text nobody sees — now a task somebody works** (raised 2026-08-20 by D-78's evaluation of the two-way SMS inbox; built 2026-08-20 — **D-83**). `sms-webhook/route.ts` handled STOP, HELP, START and YES and answered everything else with `<Response/>`: a tenant writing *"the gate won't open"* got no reply, no record, and nobody told — from a number we had texted them from first. An unrecognised inbound now emits `sms.inbound_received` carrying the words, raises an `inbound_sms_review` task at the tenant's own facility (**`high` priority** — the only type in this queue where a person is standing somewhere waiting), and answers the sender that a human has it, with **their own site's phone number** rather than the shared SMS line. The message is on the task card, truncated, so nobody opens a task to triage it (B-115's rule). **Where the words live is D-83**: the domain event, because `Message` is outbound by construction and `Task.proof` is completion evidence — and **one task per MESSAGE, not per tenant per day**, or `createTask`'s `(type, entityId, businessDate)` key would swallow a second question the same afternoon. **Still deliberately not an inbox** — no threading, no `direction` column, no second queue to staff. **Left behind:** a text from a number matching no current tenant records the event but raises no task, because `Task` requires a facility and this SMS number is site-wide, so `To` cannot supply one; the sender is given a number to call | PRD 05 §8 Phase 3; D-78; D-83; PRD 02 US-41 | S | B-074, B-095 | Phase 3 |
| 90c | B-090b ✅ | **Tenant self-service transfer, as a request** (B-090 part 2, built 2026-08-20 — **D-81**, **D-82**). A tenant wanting a bigger unit had to phone: `lib/admin/transfer.ts` (B-077) has done the whole job since it shipped, and nothing customer-facing reached it. `/portal/transfer` now lists every available unit at their site with its rate and the monthly difference, prices the swap through **the admin module's own arithmetic** (`previewTransferFor`, split out of `previewTransfer` so there is not a second proration for the tenant that nobody reconciles), and records the ask. **It does not commit** — D-81: a transfer moves physical goods, and the moment the old lease closes its unit is rentable to a walk-in with the tenant's things still in it, so staff finish it in B-077's wizard, unchanged. The target unit is **held with a `Reservation` marked `source = 'transfer'`** (D-82, no schema change), which bought exactly one exception in `previewTransfer`/`transferTargets` — a unit `reserved` for the same tenant reads available to THAT tenant and nobody else, or the request would block the transfer it exists to set up. Also shipped: the request task deep-links nothing, so the **wizard itself** shows staff what was asked for and prefills it; `lease.transfer_requested` with a confirmation naming both units and **no dollar figure** (the total depends on the day staff actually complete it); the expiry sweep cancels the task when a hold lapses. **Left behind:** the quoted rate is not honoured at commit — **B-136** | PRD 01 §9; PRD 02 §4.3 US-14; D-81; D-82 | M | B-077, B-095 | Phase 3 |
| 90d | B-136 ✅ | **A transfer quote that nothing honours** — built 2026-08-20 (**D-84**). The hold locks the rate for as long as it lives; an expired hold re-quotes; a staff-initiated transfer with no hold is unchanged. (Found 2026-08-20 while building B-090 part 2, not by a review.) The portal shows a tenant what the swap costs, holds the unit, and stores the figure on the hold as `quotedRateCents` — and then `completeTransfer` **re-reads the current street rate** when staff commit, which is correct for a staff-initiated transfer and wrong for one the tenant was quoted. A rate rise between the ask and the completion charges them something other than the number they agreed to, silently, and the number they agreed to is sitting on the row. **Deliberately not fixed inside B-090 part 2**, because the fix is a policy question rather than a patch: does a request lock a rate, for how long, and does the lock survive the tenant rescheduling? Those are the same three questions a reservation's own quote raises, and D-7/B-126 answered them for reservations by anchoring expiry to the move-in date — the cheap first move here is probably the same shape, honouring `quotedRateCents` for as long as the hold lives and re-quoting after. **The trigger is a rate change landing between an ask and its completion**, which at a 2–10 facility operator is weeks apart at most, so this is small and real rather than theoretical | PRD 02 §4.3 US-14; D-82; D-7 | S | B-090b | Phase 3 |

## Appended after v1.0 — internal tooling

Added 2026-07-30, after the original 90 were sequenced. Appended rather than inserted so the global numbering above stays stable. **These are not in top-to-bottom build order** — their dependencies (B-003/B-004/B-005) are already built, so they are buildable at any point; the Phase column is the recommendation, not a gate.

| # | ID | Item | PRD/Feature | Size | Depends on | Phase |
|---|---|---|---|---|---|---|
| 91 | B-091 ✅ | **Split into two parts, 2026-08-19** (owner), and the split has one constraint behind it: **no commit may ship “a session can start” before “writes are blocked and the banner shows”**, so part 1 deliberately builds no actor swap and nothing renders as the subject. **Part 1 ✅ — the guard and the record.** `ImpersonationSession` + two nullable `AuditLog` columns (one migration, purely additive so the append-only triggers are untouched, FR-26), the four permissions owner-only (D-13b), the escalation guard as ONE function with its adversarial suite (SR-1), the session lifecycle — start with a required reason, throttled, end, server-enforced expiry, and FR-9 re-validation that ends a session when the guard stops agreeing — and dual attribution plumbed through `recordAudit`. **Part 2 ✅ — enforcement, banner, UI** (built 2026-08-19 — **D-69**, **D-70**). The actor swap lives in ONE function, `currentActor()`, because every screen and service in the app already resolves who is asking through it — so an impersonated request runs as the subject everywhere at once rather than in the places somebody remembered. Read-only is enforced **by HTTP method at the edge**, not by a list of blocked actions: every non-GET is refused while the session cookie is present, with two exempt paths (the way out, and sign-out). That is what makes SR-2 hold for the 53 files of server actions in this repo and for screens nobody has written yet, and it is why **FR-12's hard-block list is deliberately NOT built as a list** — with `impersonation:write` unshipped the rule is "block everything", and a classification that cannot be exercised cannot be trusted when it finally is (D-70). **The one member of that list which is not a mutation IS built:** an impersonated portal renders no gate code, dropped from the data rather than hidden in the markup, because PRD 03 SR-2 makes revealing one a separate audited permission and no write block catches a GET. The session id travels in its own httpOnly cookie rather than as a JWT claim (**D-69**) — the row is the authority either way, and a forged cookie fails the binding check while still tripping the write block, so it can only ever reduce access. Banner first in the document ahead of the skip link (FR-14/FR-15), start controls with required reason on the tenant profile and the staff-security screen, "Return to my account" as a route handler (a server action POSTs to its own page and so has no path to exempt), and **PRD 09 OQ-1's disclosure drafted into the public privacy notice**, saying in as many words that we do not notify you — the half that makes D-13a defensible rather than merely undisclosed. `impersonation:write` is in neither part — PRD 09 §8 lists it as "later / optional" and OQ-2 asks whether it should ship at all. **Left behind:** a blocked submit returns a 403 rather than a message beside the field (D-70 records the two graceful alternatives and why both were refused), and B-092 owns the oversight half that FR-21 calls load-bearing | PRD 09 §5–7 Phase A | L | B-003, B-004, B-005 | Phase 2 |
| 92 | B-092 ✅ | **Impersonation oversight** (built 2026-08-19 — **D-71**, **D-72**, **D-73**). `/admin/impersonation`, owner-only: sessions running right now with a one-click force-end, the filterable record with a matching CSV, and FR-20's frequency flags. **No tenant notification and no tenant-facing activity view** (D-13a) — which makes this the *only* misuse-detection channel, so it should not trail B-091 indefinitely. **The active list filters `expiresAt > now` AS WELL AS `endedAt IS NULL`**, which is the rule B-091 part 1 wrote down and told this item not to drop: expiry is enforced lazily, so a session whose impersonator closed their laptop is unexpired on paper and expired in fact, and listing it would offer a force-end button that ends nothing. It has its own test. **Force-end authorises by "you can only end what you can see"** — the id must be in `activeSessions(actor)` — so widening `impersonation:oversee` to a regional stays a seed change and confines them to their own sites without this function changing. **FR-19 names PRD 02 US-38's audit-log surface as the thing to mirror and that surface does not exist** — `findAuditEntries` has had no consumer outside tests since B-005 — so this follows the `/admin/reports` convention instead: filters in the query string, a `.csv` sibling re-reading the same string through the same function. **"Filterable by facility" resolves through the SUBJECT** (D-72), because the session row deliberately has no facility and the owner's question is whose account was opened, not what the impersonator could have reached. **FR-20's threshold is a named constant of 5 distinct subjects a day, not a column** (D-73) — OQ-3 wants a real N and there is no observed usage to tune against. **PRD 09 OQ-2 is answered: `impersonation:write` is DELETED** (D-71, owner) rather than deferred — a permission an owner can grant that changes nothing is a promise the product does not keep. That exposed a real gap: the seed pruned stale role GRANTS but never removed a retired `Permission` row, so `rbac-db.test.ts`'s catalog check failed against every database seeded before the removal; the seed now prunes retired permissions and says how many. **Left behind:** an owner cannot yet see WHAT was looked at during a session, only that it happened — read-only means no audit entries are written during one, so there is nothing to show until write mode exists (which D-71 just decided against) | PRD 09 §5.5, §8 Phase B | M | B-091 | Phase 2 |

---

## How to use this backlog with Claude Code

- **Work strictly top to bottom.** The order encodes the dependency graph; when you reach an item, everything it depends on is already built. Don't cherry-pick a later item unless its entire "Depends on" chain is done.
- **One item (or one small cluster) per session.** S/M items are a single focused session. L items should be split into 2–3 sessions at start ("build the state machine," then "build the UI on it"). B-090 (XL) must be split before starting.
- **Load the source PRD section as context.** Each item's PRD/Feature column points at the authoritative spec (e.g., "PRD 02 §4.5 US-17/US-18" → open `02-admin-dashboard-prd.md` and paste/reference that section plus the master PRD's data model §7.5). The PRD's acceptance criteria are the item's definition of done.
- **Accessibility is loaded, not remembered.** Every customer-facing item additionally loads **PRD 01 §6.8 and §6.8.1** (the per-flow acceptance criteria); every admin item additionally loads **PRD 02 §5.5**. This is not a formality — the accessibility audit of 2026-07-31 found that the shipped admin routes carried most of the blocking findings precisely because PRD 02 contained no accessibility text at all, so every admin session had built from a spec that never mentioned AA. Customer-facing items also load **PRD 01 §6.10** (the D-15 lexicon).
- **Start each session with:** the backlog row, the PRD section(s), the master PRD's cross-cutting requirements (§7 — auth, WCAG 2.1 AA, mobile-first, PCI scope, data model), and the current schema. End each session with tests passing (billing math and the rental funnel are the two places the PRDs demand tests) and the seed script updated if entities changed.
- **Two demo checkpoints:** after B-032 run the golden-path-1 demo (search → rent → pay → simulated gate code → welcome email). After B-055 run golden-path-2 (nightly run invoices a seeded lease → due-soon reminder → simulated failed payment → dunning step 1 → magic-link payment halts the ladder). ✅ Run and kept: `tests/golden-path-2-db.test.ts` drives the real `SCHEDULED_JOBS` registry night by night, so the checkpoint fails a pull request rather than being a thing that passed once.
- **Don't resolve flagged conflicts silently.** If an item touches something in the Flagged gaps/conflicts list, get an owner decision first or build to the master PRD's position and note it.

## Rationale: top ordering decisions & trade-offs

1. **Foundation before any feature (B-001–B-012).** Every PRD reads/writes the shared schema (master §7.5) and assumes facility scoping, RBAC, audit logging, and an event bus. Building these once, first, prevents each module re-inventing them — the master PRD explicitly forbids modules re-deciding cross-cutting concerns.
2. **Golden Path 1 before the tenant portal or billing.** The MVP exit criterion (master §6) is "a real prospect can find a facility, rent and pay online, and appear in the rent roll." Milestone 2 is the shortest dependency chain to that: it forces the checkout state machine, Stripe, document generation, the ACS + simulator, and the comms core into existence — the five hardest shared subsystems — while producing a demoable product early.
3. **ACS + simulator inside Milestone 2, real vendors last.** PRD 03's own conclusion: the market is partner-gated, so the simulated adapter *is* the primary adapter for this build and the contract fixture for future real drivers. Gate codes are therefore "simulated but real" from the first move-in; vendor drivers wait until Phase 3 (B-085), matching PRD 03 over the master's more optimistic Phase 2 wording (flagged below).
4. **Comms core lands in Milestone 2, not 4.** The move-in confirmation email is required for Golden Path 1 anyway, so the idempotent send pipeline, message log, and Resend integration get built early and Milestone 4 only adds rules/templates on top. SMS consent capture and 10DLC registration also start here because registration has unknown external latency (PRD 05 §6.3) — consent must exist before SMS does.
5. **Portal before the autopay run.** B-045 (autopay) needs saved payment methods and the autopay toggle (B-036); the portal milestone also gives dunning links somewhere to land (B-051 deep-links into the pay screen). Interleaving admin essentials (tenant profile, POS, move-out) here means staff can operate the facility as soon as tenants exist.
6. **Billing before delinquency, delinquency before lien.** The dunning ladder (B-052) is driven by billing-engine day events by explicit PRD 05 requirement, so invoices/late fees must exist first. The full delinquency→lien→auction pipeline follows in Milestone 5 as Phase 2 per the master roadmap, even though PRD 02 labels it MVP (flagged) — the learning value and legal risk both argue for doing it after the money loop works.
7. **Marketing/SEO after the transactional core.** SEO pages sell inventory; there must be inventory, live pricing, and a working rental funnel to sell into. Within Milestone 6, master-MVP items (SEO infrastructure, lead capture, promotions) precede master-Phase-2 items (reviews, drips, abandonment). The main trade-off: promotions (B-070) sit late despite touching checkout — acceptable because checkout works without discounts, and B-070 depends on billing (B-044) to apply them to invoices correctly.
8. **Deliberate bundling at the tail.** Phase 2/3 items are bundled (B-078, B-080–B-082, B-089–B-090) to keep the list at ~90; split them when they're reached. MVP items are granular because they'll actually be built next.
9. **The five items added on 2026-07-31, and why they sit where they sit.**
   - **B-093 and B-094 (remediation) go next, before B-017.** They are fixes to already-shipped code — two design tokens, a keyboard-hostile input, customer-facing copy showing an internal ticket number, an accessibility statement making four claims the build cannot support, a missing skip link, a form layer that surfaces no errors at all. None of it is large. All of it gets multiplied by the next item: B-017 alone adds filters, a size guide and a cost summary to the public site, and every one of them inherits the same tokens and the same status-region pattern. Work of this shape never gets done unless it has a number, which is why it has one.
   - **B-095 (`Task`) goes before B-026** because B-026 is the first consumer — "downstream failures become admin tasks, never dead ends" needs somewhere to put them — and because five later items each specify their own staff queue against a schema that already names `Task` and never builds it. Building it once, first, is the same argument that put RBAC and the audit log in Milestone 1.
   - **B-096 (lease holds) goes right after B-038** so it has a profile to render on, and comfortably before B-052, which already says the dunning ladder halts on a "hold" that nothing defines.
   - **B-098 (access suspension) goes after B-047**, once late fees exist and the shared `daysPastDue()` from B-046 is in place. It is the one place MVP acquires actual collections leverage, and it deliberately reuses the ACS built at B-027 rather than waiting for the Phase-2 delinquency engine, which then inherits it as a configured step. D-1 is untouched: only the threshold moved.
   - **B-097 (phone/counter capture) goes after B-039**, once the reservation service and counter operations both exist. It is the only MVP item that captures the demand arriving by phone; without it the conversion reporting built later will show web leads only, and look excellent.
   - **Four schema additions are folded into existing items rather than given their own** — `LeaseRateChange` into B-026, address-of-record history into B-037/B-038, `Payment.receivedByStaffId` into B-039, and the `notice_email` consent value into B-032. Each is a few columns whose entire value is that they exist *before* the data does: none can be reconstructed retroactively, and each has a workflow item downstream (B-076, B-061, B-078) that is unbuildable without the history it was supposed to have been keeping.

10. **The eleven items added on 2026-08-12 (B-109–B-119), and why they sit between B-105 and B-106.** They are the second review pass — UX and accessibility over everything shipped through B-105 — and every one is a defect in built code rather than a new feature, so they belong at the build frontier and ahead of the two remaining feature rows in that block. Three of them (**B-110, B-111, B-112**) are hard prerequisites of **B-106** and now appear in its "Depends on": B-106 rewrites the checkout core for multi-unit, and a checkout that cannot go backwards, never states why a total moved, asks fourteen questions on step 1 and announces nothing on a step change would have each of those reproduced once per unit. That is the same argument that put B-093/B-094 before B-017, and it is the reason the position is a constraint rather than a preference. **B-119 (the scan contract) is deliberately last** in the block: it asserts what B-110 fixes and loops reflow over the admin routes B-116 fixes, so running it earlier only produces a suite that is red for known reasons. **B-109 is first** because it is the cheapest and one of its three stale sentences is actively costing money — the counter is told there is no promotions engine while the website prices the promotion, so a phone quote and a web quote for the same unit disagree. Two findings were merged rather than numbered: the MFA recovery-code save path and the sign-in form's second-factor field went into **B-108**, which already opens both files.

11. **B-120 (e2e repeatability) sits with B-119 rather than with the review block.** It came out of building B-110, not out of the 2026-08-12 reviews, and it is here for the same reason B-119 is last: both are about whether a red suite means anything. B-119 makes the scans cover what they claim to; B-120 makes a failure attributable. Neither is a feature, and either one skipped leaves the next six items being verified by a suite that cannot distinguish a regression from a stale fixture or a stray dev server on port 3000.

12. **The twenty items added on 2026-08-21 (B-137–B-156), and why they sit ahead of every open row.** They are the third review pass — operator, UX and accessibility over everything shipped in **B-120 through B-136** — and, as with the 2026-08-12 block, every one is a defect in built code or an AC no row owns rather than a new feature, so they belong at the build frontier. They are inserted immediately after **B-130 (83b)**, which puts them ahead of all five rows open when the block was written (**B-129, B-085, B-086, B-133, B-134**). That position is a constraint rather than a preference: every one of those five is blocked on a partner agreement, an API credential or a real portfolio, none of which this project controls, so leaving twenty confirmed defects behind them means the frontier stops moving.
    - **B-137/B-138 first**, because the transfer defect is the only one in the block that loses an operator money it cannot get back: collections stop entirely on a live tenant, and the ladder runs on an active-duty servicemember whose hold did not survive the transfer. It is a split, and **the split carries B-091's constraint** — B-137 (holds carry over, lien-pipeline escape closed) must land before B-138 (collections resume), because resuming the ladder first runs it against exactly the tenants whose holds have not yet been carried.
    - **B-139 next**, because CLAUDE.md makes the accessibility statement a standing obligation, the page is currently false in the overstating direction, and the fix is an hour.
    - **B-140 next** as the cheapest thing actively reaching tenants — a transfer hold mailing an existing tenant a move-in link D-82 ensured never exists.
    - **B-141 and B-142 next**, the two silent failures: a queue whose Complete button is indistinguishable from a broken one, and a request screen that re-renders byte-identical on refusal. **B-142 ranks above its size** because two reviewers found it independently and a correct sibling already exists in the codebase.
    - **B-156 is deliberately last**, exactly as B-119 was in the previous block: it asserts what B-141 and B-142 fix.
    - **Eight findings were merged rather than numbered.** The transfer-date ceiling, the unstated hold expiry, the raw `×` in a radio's accessible name and the invisible pending request all went into **B-142** (one screen, one session); the occupancy table's missing row headers into **B-150** (same file, same size); the unscanned admin transfer wizard, B-122's never-opened promo disclosure and the three structural test-coverage gaps into **B-156**. The unread `lien` fee type went into **B-146** rather than becoming its own row, and **no new task type** was created for returned payments — the existing `failed_payment` queue carries them, per US-41.
    - **Four questions were not answered here** and are appended to `07-decisions.md` as **D-85** (may a lien-pipeline lease transfer at all, and under whose authority), **D-86** (do arrears follow the tenant or the lease), **D-87** (does a waitlist notification hold the unit) and **D-88** (what happens to a rate increase whose notice bounced). B-138, B-152 and B-154 are each written to whichever answer their decision takes.

13. **The twenty-six items added on 2026-08-24 (B-159–B-184), and why they too sit ahead of every open row.** They are the fourth review pass — operator, UX and accessibility over everything shipped in **B-137 through B-158** — and the criterion is the one the 2026-08-12 and 2026-08-21 blocks used: every one is a verified defect in built code or an acceptance criterion no row owns, not a new feature, so they belong at the build frontier. They are inserted immediately after **B-157/B-158 (`83bu`/`83bv`)**, which puts them ahead of all five rows still open (**B-129, B-085, B-086, B-133, B-134**) — and that position is the same constraint as last time rather than a preference: every one of those five is blocked on a partner agreement, an API credential or a real portfolio, none of which this project controls, so leaving twenty-six confirmed defects behind them means the frontier stops moving.
    - **B-159 is first, ahead of larger and more expensive defects**, because it is not a defect a user has to reach — it is a live public claim about this codebase, and CLAUDE.md makes that page a standing obligation. It has now been false in the overstating direction twice in twelve days; the correction is prose and an hour, and a page claiming that failing scans block a release while Vercel ships independently of Actions is the one item here that converts a fixable bug into an alleged misrepresentation.
    - **B-160 is the largest single item in the block and is deliberately merged into nothing.** A D-85 lien transfer leaves the auction file naming the unit the notice was served on: staff cut the lock on a re-rented unit, and after a completed sale the proceeds post to the lease B-138 zeroed while the live lease still shows full arrears and the ladder keeps running. It is money, a stranger's goods and a §59 record in one defect.
    - **B-161 next**, because a returned ACH on an old invoice replays the whole ladder in one night — four letters, re-assessed late fees, access cut and an auction case opened by a bank reversal with no human in it. Both B-146 and B-147 named it unclosed; nothing picked it up.
    - **B-162–B-164 are the rest of the transfer cluster**, in the order the data flows: pricing and the ECRI clock, then the insurance proof the transfer silently stops monitoring, then the portal move-out hole that is B-137's fix one file over. B-164 is small and late in the cluster only because it is the same fix already written down.
    - **B-165 before B-177**, because the batch preview B-177 redesigns has to have a rule to display; a screen that states the rule before the rule exists states the default hardcoded constant.
    - **B-170 sits ahead of the four rows that use its mechanism** (B-176, B-178, B-181, and B-184's manual passes). It builds the page-level announcement region that survives the row it reports on; every other admin row in this block that removes its own card depends on it existing.
    - **B-184 is deliberately last**, exactly as B-119 and B-156 were in the previous two blocks: it asserts what B-170, B-171, B-172, B-176, B-177 and B-183 fix, so running it earlier only produces a suite red for reasons already written down. **T6's three manual passes are part of its definition of done, with the owner each finding names** — the VoiceOver money-path pass and the keyboard pass over the task queues belong to whoever takes B-170, in that session, because a text-only session cannot verify that its fix is success rather than silence; the live-map pass belongs to the first session with a billed vendor key. A pass not recorded in `PROGRESS.md` did not happen, which is the rule B-159 is correcting the public page against.
    - **Two findings became acceptance criteria on existing rows rather than new rows.** The accessibility review's two at-risk items are appended in place to **B-085** (kiosk mode: 2.2.1, 1.4.10, 3.3.2 if the evaluation lands on yes; no kiosk surface at all if it lands on no) and **B-086** (Bluetooth unlock: 4.1.2, 4.1.3, 1.4.1, and an absolute facility-local expiry rather than a countdown). Both rows already existed and both are blocked on the same partner agreements; what they lacked was criteria, and a criterion written after the vendor spike is a criterion written to fit what got built.
    - **Six findings were merged rather than numbered.** The task-completion form's missing proof fields (UX), its silent success and dropped focus (accessibility) and its placeholder-only labels (UX) are one component and one session — **B-170**. The rate-increase screen's free-text Lease ID and its undistinguished Approve/Cancel pair are one screen — **B-177**. The promotions settings hint and the promotions list's repeated control names are one screen — **B-176**; the waitlist report's `<summary>` names went to **B-183**, the same file as the aging fix, which splits the accessibility review's A5 across three rows because it was three screens. Checkout's unit-lost dead end (UX) and its unspeakable size glyphs (accessibility) are one branch that nobody can reach today, so they are scanned once and fixed once — **B-172**. The lead screen's placeholder labels stayed separate as **B-180** because it is a different screen from the task form, despite arriving in the same finding.
    - **Nothing was rejected as out of scope, and one thing was declined on the reviewer's own advice:** the UX review's "deliberately not raised" list (the sticky price summary, the portal pending-transfer screen, the shared recapture sentence, the banner stack) is left alone, and **B-181 inherits its reviewer's MEDIUM confidence** — it is ordering and disclosure only, and it wants one pass with an operator before the order is committed to.
    - **Five questions are not answered here and are not in `07-decisions.md` yet.** Each is stated in the row that raises it, and **that row cannot be finished until it produces a new D-number**: whether a lien transfer requires re-serving the §59 notice for the new unit (**B-160**); how many days of grace a reversal-re-opened arrear gets, and whether the ladder resumes at the reached stage or restarts (**B-161**); the default ECRI step and cap for a new facility (**B-165**); whether a transfer re-prices to street, carries the in-place rate or takes the greater, and whether a promotion follows the tenant through a transfer — D-89's open half (**B-162**); and whether the accessibility statement's release-blocking sentence is corrected or made true with a deploy gate (**B-159**). They are the owner's calls on legal exposure, tenant retention, revenue and Actions cost respectively; none is answered in PRD prose, and the log gets its new rows when the owner decides, not before.

## Flagged gaps/conflicts

> **STATUS: ALL RESOLVED 2026-07-30.** Every item below was decided by the owner — see `07-decisions.md` (D-1 through D-11b), which supersedes the text below. The list is kept for historical context only.

1. **Delinquency/lien scope — PRD 02 vs master.** PRD 02 marks the full delinquency, lien, and auction pipeline (US-25–29), POS drawer sessions (US-33), field ops (US-35–37), and all six reports (US-39) as MVP. Master §6 Phase 1 lists only "basic late-fee schedule" and "occupancy & rent-roll report," deferring the full pipeline, drawer, and roll-up depth to Phase 2. Master wins per its own precedence rule; backlog phases follow master (Milestones 5/7 = Phase 2). Owner should confirm.
2. **Marketing Phase 1 scope — PRD 04 vs master.** PRD 04 Phase 1 includes reviews display, review-request emails, promo codes in checkout, lead drips, and abandoned-reservation sequences. Master Phase 1 marketing is only SEO location pages, GBP linkage, and basic lead capture; the rest is master Phase 2. Backlog labels follow master; ordering follows PRD 04 (built together in Milestone 6). Note promotions are also PRD 02 MVP (US-10), so B-070 is kept MVP.
3. **Kiosk mode — master vs PRD 03.** Master Phase 2 hardware includes "kiosk mode (rental flow on an on-site tablet)"; PRD 03 explicitly defers kiosks to Phase 3 with a default answer of "no." Backlog follows PRD 03 (evaluation only, B-085).
4. **First real gate-vendor driver — master vs PRD 03.** Master Phase 2 promises "first real gate integration (one vendor driver)"; PRD 03 Phase 2 ships only stub adapters because partner agreements are out of scope for the learning build. Backlog follows PRD 03 (stubs in B-080; real driver B-085, Phase 3, contingent on a partnership).
5. **Rate increases — PRD 02 MVP vs master Phase 2.** PRD 02 US-11 (scheduled tenant rate increases with notices) is tagged MVP; master puts "rate-change management with tenant notices" in Phase 2. Backlog places it at B-076 (Phase 2).
6. **Stripe Billing vs ledger-driven charges (unresolved joint decision).** PRD 01 OQ-5 recommends admin-ledger-driven PaymentIntents; PRD 05 CN-6 references "Stripe Billing retry config." B-043–B-046 assume ledger-driven per the PRD 01 recommendation — needs an explicit decision before Milestone 4.
7. **Reservation deposit ambiguity.** PRD 01 mandates free, no-card reservations; PRD 02 US-14 describes reservations "with optional deposit"; master OQ-3 leaves the policy open. B-018 builds free-hold only; deposit support is an open owner decision.
8. **Consent store ownership.** PRD 04 defines `contact_consent`; PRD 05 adds `account_sms` and proposes the shared schema own the table (its OQ-8). B-002/B-032/B-072 must implement one shared package — assign ownership at Milestone 1.
9. **PRD cross-reference numbering is inconsistent.** PRD 02's header cites siblings as `01-master`, `03-customer-website`, `04-hardware`, `05-marketing`, but actual filenames are `00-master`, `01-customer-website`, `03-hardware`, `04-marketing`; PRD 05 (communications) is not in the master PRD's four-module feature map (§3) at all. Recommend a doc pass to renumber references and amend master §3 to five modules.
10. **State selection blocks legal configuration.** Master OQ-1 (operating states) gates lien timelines, notice templates, lease clauses, late-fee caps, and SMS disclosure copy (PRD 01 OQ-1, PRD 02 OQ-3, PRD 05 Q2/Q5). Milestone 5 (B-056, B-061) and B-074 cannot ship real defaults without it — needed before Milestone 5 starts.
11. **Minor phasing splits.** Spanish language: PRD 01 Phase 2 vs master Phase 3 multilingual (bundled in B-090). City pages: PRD 04 Phase 2 vs master Phase 3 "per-city/size landing-page generation" (backlog treats city pages as Phase 2 in B-082, generated size-in-city pages as Phase 3 in B-089). Autopay-tenant reminder default (PRD 05 Q1) needs an owner call before B-050 templates.
